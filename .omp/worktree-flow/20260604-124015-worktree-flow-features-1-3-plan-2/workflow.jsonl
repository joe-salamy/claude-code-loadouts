{"timestamp": "2026-06-04T12:40:19", "event": "workflow_log_started", "log_file": "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts\\.codex\\worktree-flow\\worktree-flow-features-1-3-plan-2-20260604-124015\\workflow.jsonl"}
{"timestamp": "2026-06-04T12:40:29", "event": "harness_exec_start", "cwd": "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2", "output_file": "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\handoff\\implementation-final-response.md", "command": ["codex", "exec", "--cd", "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2", "--sandbox", "danger-full-access", "--add-dir", "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex", "--add-dir", "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts\\.git", "--output-last-message", "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\handoff\\implementation-final-response.md"]}
{"timestamp": "2026-06-04T12:52:13", "event": "harness_exec_finish", "cwd": "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2", "output_file": "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\handoff\\implementation-final-response.md", "output_file_exists": true, "returncode": 0, "stdout": "Implemented and committed the approved plan.\n\nCommit: `cb2f747 Implement plan`\n\nWhat changed:\n- Added shared `.codex/scripts/worktree-flow.py`.\n- Replaced Codex/OMP flow files with compatibility wrappers.\n- Added workflow invariant checks for implementation, audit, and pre-integration.\n- Synced loadout `.harness` script copies.\n- Added drift-protection test.\n- Updated worktree-flow docs.\n- Wrote `.codex/handoff/implementation-summary.md` and left it untracked as required.\n\nVerification passed:\n- `python -m unittest tests.test_codex_worktree_flow`\n- `python -m unittest tests.test_worktrees_loadout_sync`\n- `git diff --check -- . ':!scratchpad.md' ':!.codex/handoff/**'`\n- Combined focused unittest rerun: 54 tests passed\n\nFinal status is clean except for untracked `.codex/handoff/`.\n", "stderr": "2026-06-04T19:40:31.757551Z ERROR codex_core::session::session: failed to load skill C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\skills\\barebones-code-refactor\\SKILL.md: invalid YAML: mapping values are not allowed in this context at line 2 column 83\nOpenAI Codex v0.137.0\n--------\nworkdir: C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nmodel: gpt-5.5\nprovider: openai\napproval: never\nsandbox: danger-full-access\nreasoning effort: medium\nreasoning summaries: none\nsession id: 019e9426-c7bc-70b2-a4a3-2d4773a31276\n--------\nuser\nUse the implement-worktree skill.\n\nImplement the approved plan in `docs/plans/worktree-flow-features-1-3.md` inside this worktree.\n\nRequirements:\n- Do not create, switch, merge, delete, or rebase worktrees.\n- Keep edits scoped to the plan.\n- Run focused tests or checks appropriate to the change.\n- Commit the completed implementation.\n- Write `.codex/handoff/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n- Do not commit files under `.codex/handoff/`; they are workflow artifacts and must remain untracked.\n\n2026-06-04T19:40:45.397951Z ERROR codex_core_skills::loader: failed to read skills dir C:\\Users\\joesa\\.codex\\.tmp\\plugins\\plugins\\build-web-data-visualization\\skills\\grammar-of-graphics-and-declarative-visualization: The system cannot find the path specified. (os error 3)\n2026-06-04T19:40:45.398331Z ERROR codex_core_skills::loader: failed to read skills dir C:\\Users\\joesa\\.codex\\.tmp\\plugins\\plugins\\build-web-data-visualization\\skills\\node-link-and-diagram-layout: The system cannot find the path specified. (os error 3)\ncodex\nIâ€™ll use the `implement-worktree` skill and follow the repo AGENTS constraints, including logging the skill load before applying its workflow.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Raw .codex/skills/implement-worktree/SKILL.md' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'python .codex/scripts/skill-usage-manager.py record implement-worktree --scope repo --path .codex/skills --repo .' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 968ms:\n---\nname: implement-worktree\ndescription: Implements an approved Markdown plan inside the current script-created git worktree, verifies the result, commits the implementation, and writes a handoff summary for audit.\n---\n\n# Implement Worktree\n\nUse this skill inside a feature worktree that has already been created by the workflow script. The script owns branch creation, worktree creation, merge, and cleanup. This skill owns implementation behavior inside the current worktree.\n\n## Required Input\n\n- Path to the approved plan file.\n- Current shell location must be the feature worktree, not the primary `main` checkout.\n\nIf the plan path is missing or the current checkout is `main`, stop before editing and report the problem.\n\n## Safety Rules\n\n- Do not create, switch, merge, delete, rebase, or clean up worktrees.\n- Do not edit the primary/main checkout.\n- Do not push.\n- Do not read, write, or diff `scratchpad.md`.\n- Preserve user changes you did not make.\n- Keep edits scoped to the approved plan.\n\n## Workflow\n\n1. Confirm the current branch and worktree:\n\n```powershell\ngit worktree list\ngit branch --show-current\ngit status --short\n```\n\nStop before editing if the current branch is `main` or if the current path is the primary `main` checkout.\n\n2. Read the plan completely. Restate the implementation scope, non-goals, assumptions, and expected verification.\n\n3. Implement the plan in the current worktree only. Follow this repository's `AGENTS.md` and any relevant skills.\n\n4. Run focused tests or checks appropriate to the changed files. If a relevant check cannot be run, record the reason clearly.\n\n5. Commit all intended implementation changes:\n\n```powershell\ngit status --short\ngit add <changed-files>\ngit commit -m \"Implement plan\"\ngit status --short\n```\n\nDo not include `scratchpad.md` or workflow handoff artifacts. The workflow merge only receives committed branch changes; uncommitted or unstaged files left in the worktree will not be included in the squash/no-ff integration merge.\n\n6. Write `.<harness>/handoff/implementation-summary.md` with:\n   - Plan path\n   - Worktree path\n   - Branch name\n   - Commit SHA, or a clear note if no commit was created\n   - Changed files\n   - Behavior changes\n   - Tests/checks run and results\n   - Skipped checks and why\n   - Implementation decisions and tradeoffs\n   - Assumptions, blockers, residual risks, or follow-up work\n\n## Output Standard\n\nThe summary must be detailed enough for a context-cleared audit agent to verify the work without relying on chat history. Treat the summary as a handoff artifact, not a final response substitute.\n\n\n succeeded in 1484ms:\ncodex\nThe skill is loaded and recorded. Iâ€™m checking the current worktree/branch and status first so I donâ€™t edit the main checkout or disturb unrelated changes.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Raw .gitignore' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git status --short' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git worktree list' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git branch --show-current' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 956ms:\n# Planning docs \ndocs/\n\n# OS\nThumbs.db\n.DS_Store\n\n# Python\n__pycache__/\n.pytest_cache/\n*.py[cod]\n\n# Editor\n*.swp\n*.swo\n*~\n.vscode/\n.idea/\n\n# Blank files \nblank.*\n\n succeeded in 1474ms:\n M .codex/skill-usage.json\n?? .codex/handoff/\n\n succeeded in 1732ms:\nC:/Users/joesa/Documents/law-school-python/harness-loadouts                                                              b170398 [main]\n/mnt/c/Users/joesa/Documents/law-school-python/harness-loadouts-worktree-flow-features-1-3-plan                          b170398 [feature/worktree-flow-features-1-3-plan] prunable\nC:/Users/joesa/Documents/law-school-python/harness-loadouts-codex-and-omp-worktree-flow-split                            47a1977 [feature/codex-and-omp-worktree-flow-split]\nC:/Users/joesa/Documents/law-school-python/harness-loadouts-integrate-codex-and-omp-worktree-flow-split-20260602-173358  f6179d0 [integration/codex-and-omp-worktree-flow-split-20260602-173358]\nC:/Users/joesa/Documents/law-school-python/harness-loadouts-worktree-flow-features-1-3-plan-2                            b170398 [feature/worktree-flow-features-1-3-plan-2]\n\n succeeded in 2152ms:\nfeature/worktree-flow-features-1-3-plan-2\n\ncodex\nThe checkout is on `feature/worktree-flow-features-1-3-plan-2`, not `main`. There is already a skill-usage log update and an untracked handoff directory; Iâ€™ll keep `.codex/handoff/` out of the commit as required.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Raw docs/plans/worktree-flow-features-1-3.md' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'rg --files' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 987ms:\n# Worktree Flow Features 1-3 Plan\n\n## Requirements\n\nImplement the first three proposed improvements for the worktrees loadout scripts:\n\n1. Collapse the duplicated `worktree-flow-codex.py` and `worktree-flow-omp.py` implementations into one harness-neutral implementation.\n2. Add drift protection so active installed scripts in this repo stay synchronized with the shipped worktrees loadout scripts.\n3. Enforce post-implementation and post-audit workflow invariants so the script does not silently drop uncommitted work, while still allowing a no-op audit that creates no commit.\n\nConstraints and repo rules:\n\n- The worktrees loadout is installed in this repo; update both active scripts under `.codex/scripts/` and loadout templates under `loadouts/worktrees/.harness/scripts/`.\n- Do not read, write, or diff `scratchpad.md`.\n- When changing scripts in `.codex/scripts/`, keep the exported worktrees loadout script copy in sync. If `loadouts/worktrees/.opencode/scripts/` exists in the implementation context, update it as required by repo guidance; otherwise update the current `.harness` template copy used by `harness-init.ps1`.\n- Preserve existing behavior for Codex and OMP wrappers unless the shared implementation makes a behavior intentionally stricter.\n- For audit enforcement, do not require a new audit commit when the audit finds nothing to change.\n\n## Key Findings From Read-Only Review\n\n- `.codex/scripts/worktree-flow-codex.py` and `.codex/scripts/worktree-flow-omp.py` are full duplicated implementations. They differ only in:\n  - `HARNESS_DIR = Path(\".codex\")` vs `Path(\".omp\")`\n  - `DEFAULT_HARNESS = \"codex\"` vs `\"omp\"`\n- Matching copies exist under `loadouts/worktrees/.harness/scripts/`.\n- `.codex/scripts/worktree-flow-codex.py` currently performs the main phase flow around lines 158-193:\n  - create feature worktree\n  - run implementation\n  - require `implementation-summary.md`\n  - reject tracked handoff artifacts\n  - run audit\n  - require `audit-summary.md`\n  - reject tracked handoff artifacts\n  - finish or stop\n- The script already excludes handoff artifacts from the integration commit in `stage_integration_changes()` with `:(exclude)<handoff>/**`.\n- Existing tests are concentrated in `tests/test_codex_worktree_flow.py` and already cover wrappers/defaults, prompts, merge modes, handoff exclusions, conflict handling, skill usage consolidation, and a local git squash merge smoke path.\n- Test helpers use `FakeRunner`, so new invariant tests can be mostly command-level unit tests with targeted real-git tests only where command output semantics matter.\n\n## Design Decisions\n\n### Shared implementation plus compatibility wrappers\n\nUse one shared implementation file and keep wrapper entrypoints:\n\n- `.codex/scripts/worktree-flow.py`\n- `.codex/scripts/worktree-flow-codex.py`\n- `.codex/scripts/worktree-flow-omp.py`\n- `loadouts/worktrees/.harness/scripts/worktree-flow.py`\n- `loadouts/worktrees/.harness/scripts/worktree-flow-codex.py`\n- `loadouts/worktrees/.harness/scripts/worktree-flow-omp.py`\n\nThe shared file should contain the current flow implementation. Wrappers should be tiny and should invoke the shared module with explicit harness defaults:\n\n- Codex wrapper: default harness dir `.codex`, default CLI `codex`.\n- OMP wrapper: default harness dir `.omp`, default CLI `omp`.\n\nThis preserves current command names and tests while removing the duplicated 900-line implementation.\n\nThe shared script should also support direct execution. Recommended direct behavior:\n\n- Infer default harness directory from the script grandparent directory when installed under `.<harness>/scripts/`.\n- Derive default CLI from that directory name when possible.\n- Keep `--harness` for explicit CLI override.\n- Add `--harness-dir` only if needed to make direct execution from `.harness/scripts/` unambiguous; otherwise wrappers can supply explicit defaults and direct execution can default from parent directory.\n\n### Drift protection by normalized content comparison\n\nAdd a focused test, preferably a new `tests/test_worktrees_loadout_sync.py`, that compares active `.codex/scripts/` files to `loadouts/worktrees/.harness/scripts/` files after normalizing line endings.\n\nCompare these files at minimum:\n\n- `worktree-flow.py`\n- `worktree-flow-codex.py`\n- `worktree-flow-omp.py`\n- `skill-usage-manager.py`\n\nThe failure message should identify the stale pair and explain that active and loadout copies must be updated together.\n\nLine-ending-only differences must not fail the test.\n\n### Workflow invariant enforcement\n\nAdd explicit checks after implementation, after audit, and immediately before integration.\n\nThe implementation pass must fail unless:\n\n1. `.<harness>/handoff/implementation-summary.md` exists.\n2. No handoff artifacts are committed.\n3. The feature branch has at least one commit after the base ref.\n4. The feature branch differs from the base ref.\n5. The feature worktree has no pending tracked or untracked non-handoff files.\n\nThe audit pass must allow no-op audits:\n\n1. Capture `HEAD` before audit.\n2. Run audit.\n3. Require `.<harness>/handoff/audit-summary.md`.\n4. Reject committed handoff artifacts.\n5. Require no pending tracked or untracked non-handoff files.\n6. Capture `HEAD` after audit.\n7. Accept either:\n   - `HEAD` unchanged and clean outside handoff, meaning no findings/no fixes; or\n   - `HEAD` changed and clean outside handoff, meaning audit fixes were committed.\n\nDo not require a new audit commit when `HEAD` is unchanged and the worktree is clean outside handoff.\n\nBefore calling `finish()`, repeat the guards that protect the integration merge:\n\n- no committed handoff artifacts on the feature branch\n- no pending non-handoff files in the feature worktree\n- feature branch still differs from base\n\n## Critical Files To Modify\n\n- `.codex/scripts/worktree-flow.py` â€” new shared implementation.\n- `.codex/scripts/worktree-flow-codex.py` â€” replace duplicated body with Codex wrapper.\n- `.codex/scripts/worktree-flow-omp.py` â€” replace duplicated body with OMP wrapper.\n- `loadouts/worktrees/.harness/scripts/worktree-flow.py` â€” loadout copy of shared implementation.\n- `loadouts/worktrees/.harness/scripts/worktree-flow-codex.py` â€” loadout Codex wrapper.\n- `loadouts/worktrees/.harness/scripts/worktree-flow-omp.py` â€” loadout OMP wrapper.\n- `tests/test_codex_worktree_flow.py` â€” update imports and existing wrapper/default tests; add invariant tests.\n- `tests/test_worktrees_loadout_sync.py` â€” add script drift protection test, or add equivalent tests to `test_codex_worktree_flow.py` if keeping one file is simpler.\n- `loadouts/worktrees/.harness/docs/codex-worktree-flow.md` â€” document the shared script and compatibility wrappers.\n\n## Implementation Steps\n\n1. Create the shared flow script.\n   - Move the current implementation from `worktree-flow-codex.py` into `worktree-flow.py`.\n   - Replace module constants with values supplied through a small defaults object or `main()` parameters.\n   - Keep `HANDOFF_DIR` behavior derived from the active harness directory.\n   - Avoid global mutable harness defaults that make importing Codex and OMP variants in one test process unreliable.\n\n2. Replace the two variant scripts with wrappers.\n   - Each wrapper imports/loads the sibling shared script by file path because the filename contains hyphens and is not a normal Python module name.\n   - Each wrapper delegates to shared `main()` with explicit defaults.\n   - Ensure `if __name__ == \"__main__\"` still exits with the delegated return code.\n\n3. Update tests for the shared implementation.\n   - Load shared module from `.codex/scripts/worktree-flow.py`.\n   - Load wrapper modules for Codex and OMP wrapper default tests.\n   - Keep existing behavioral tests pointed at the shared implementation configured with Codex defaults.\n   - Add tests proving wrappers still default to the right harness CLI and harness dir.\n\n4. Add drift protection.\n   - Add normalized script comparison from `.codex/scripts/` to `loadouts/worktrees/.harness/scripts/`.\n   - Normalize CRLF/LF before comparing.\n   - Include all flow files and `skill-usage-manager.py`.\n\n5. Add invariant helpers to the shared implementation.\n   - `head_rev(worktree: Path) -> str`\n   - `commits_since_base(worktree: Path, base: str, branch: str) -> list[str]` or a count via `git rev-list --count base..branch`\n   - `require_commits_since_base(worktree, base, branch, phase_name)` for implementation only.\n   - `require_branch_changed_since_base(worktree, base, branch)` using `git diff --quiet base...branch -- .` or equivalent command semantics.\n   - `non_handoff_status(worktree) -> list[str]` based on porcelain status.\n   - `require_clean_except_handoff(worktree)` that permits only files under the active handoff directory and fails for any other tracked, staged, or untracked change.\n\n6. Insert checks in the phase flow.\n   - After implementation summary and tracked-handoff checks:\n     - require implementation commit(s) since base\n     - require branch changed since base\n     - require clean except handoff\n   - Capture audit `HEAD` before `run_audit()`.\n   - After audit summary and tracked-handoff checks:\n     - require clean except handoff\n     - capture audit `HEAD` after audit\n     - allow same or different `HEAD`; no extra audit commit requirement\n   - Before `finish()`, repeat no tracked handoff artifacts, clean except handoff, and branch changed checks.\n\n7. Copy synchronized scripts to loadout template.\n   - Ensure `.codex/scripts/` and `loadouts/worktrees/.harness/scripts/` are identical after normalizing line endings for shared script, wrappers, and skill usage manager.\n\n8. Update docs.\n   - Document the shared script as the implementation and wrappers as stable compatibility entrypoints.\n   - Keep quick start examples using existing wrapper names unless choosing to promote direct `worktree-flow.py` usage.\n\n## Tests To Add Or Update\n\nAdd focused tests for:\n\n1. Shared script default inference/direct parser behavior.\n2. Codex wrapper passes `.codex` and `codex` defaults.\n3. OMP wrapper passes `.omp` and `omp` defaults.\n4. Active scripts match loadout template scripts after line-ending normalization.\n5. Implementation fails when no commit exists after base.\n6. Implementation fails when branch has no diff from base.\n7. Implementation fails when untracked non-handoff files remain.\n8. Implementation allows untracked handoff files.\n9. Audit succeeds with no new commit when the worktree is clean outside handoff.\n10. Audit succeeds with a new commit when the worktree is clean outside handoff.\n11. Audit fails when non-handoff changes are left uncommitted.\n12. Final pre-integration guard fails if non-handoff changes appear after audit.\n\n## Verification\n\nRun focused verification after implementation:\n\n```powershell\npython -m unittest tests.test_codex_worktree_flow\npython -m unittest tests.test_worktrees_loadout_sync\n```\n\nIf the sync test is added to `tests/test_codex_worktree_flow.py` instead of a new file, run:\n\n```powershell\npython -m unittest tests.test_codex_worktree_flow\n```\n\nAlso run the existing local git smoke test as part of `tests.test_codex_worktree_flow`; it already exercises squash staging and handoff exclusion.\n\n## Remaining Todos For Execution\n\n- Decide during implementation whether direct shared-script execution needs a public `--harness-dir` option or whether wrapper-supplied defaults are sufficient. Prefer the smallest public API that keeps direct execution useful.\n- Check whether `loadouts/worktrees/.opencode/scripts/` exists in the execution context; if it does, update it per repo guidance.\n- Preserve all existing test coverage while moving imports to the shared implementation.\n\n\n succeeded in 2073ms:\nAGENTS.md\ntests\\test_skill_usage_manager.py\ntests\\test_harness_init.py\ntests\\test_codex_worktree_flow.py\nREADME.md\nexamples\\typescript\\AGENTS.md\nexamples\\python\\AGENTS.md\nharness-init.ps1\nLICENSE\nloadouts\\worktrees\\AGENTS.md\nloadouts\\python\\AGENTS.md\nloadouts\\typescript\\AGENTS.md\n\ncodex\nIâ€™ve read the approved plan. The implementation is scoped to consolidating the Codex/OMP worktree-flow scripts into a shared implementation, adding script sync tests, adding workflow invariant guards, updating the loadout docs/copies, then running the specified unittest targets and committing only the implementation files.\n  â†’ Inspect current scripts and tests\n  â€¢ Create shared flow implementation and wrappers\n  â€¢ Add invariant guards and tests\n  â€¢ Sync loadout copies and docs\n  â€¢ Run focused tests, commit, write handoff\ncodex\nNext Iâ€™m reading the current flow scripts and tests in parallel. Iâ€™m avoiding `scratchpad.md` entirely and focusing only on the files named by the plan.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Raw .codex/scripts/worktree-flow-codex.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Raw .codex/scripts/worktree-flow-omp.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-ChildItem -Force loadouts/worktrees/.harness/scripts,loadouts/worktrees/.opencode/scripts -ErrorAction SilentlyContinue | Select-Object FullName' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Raw tests/test_codex_worktree_flow.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 978ms:\n#!/usr/bin/env python3\n\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport os\nimport re\nimport shutil\nimport subprocess\nimport sys\nfrom dataclasses import dataclass\nfrom datetime import datetime\nfrom pathlib import Path\nfrom typing import Sequence\n\nHARNESS_DIR = Path(\".codex\")\nDEFAULT_HARNESS = \"codex\"\nHANDOFF_DIR = HARNESS_DIR / \"handoff\"\nEMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n\n\nclass FlowError(RuntimeError):\n    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n\n\n@dataclass(frozen=True)\nclass CommandResult:\n    args: tuple[str, ...]\n    cwd: Path\n    returncode: int\n    stdout: str = \"\"\n    stderr: str = \"\"\n\n\nclass CommandRunner:\n    def __init__(self, dry_run: bool = False) -> None:\n        self.dry_run = dry_run\n\n    def run(\n        self,\n        args: Sequence[str],\n        cwd: Path,\n        *,\n        check: bool = True,\n        capture: bool = True,\n        input_text: str | None = None,\n    ) -> CommandResult:\n        display = \" \".join(args)\n        print(f\"+ ({cwd}) {display}\")\n        if self.dry_run:\n            return CommandResult(tuple(args), cwd, 0)\n\n        executable = shutil.which(args[0])\n        if executable is None:\n            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n        resolved_args = [executable, *args[1:]]\n        try:\n            completed = subprocess.run(\n                resolved_args,\n                cwd=cwd,\n                check=False,\n                capture_output=capture,\n                text=True,\n                input=input_text,\n            )\n        except OSError as exc:\n            raise FlowError(\n                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n            ) from exc\n        result = CommandResult(\n            tuple(args),\n            cwd,\n            completed.returncode,\n            completed.stdout or \"\",\n            completed.stderr or \"\",\n        )\n        if check and result.returncode != 0:\n            raise FlowError(format_command_failure(result))\n        return result\n\n\ndef format_command_failure(result: CommandResult) -> str:\n    parts = [\n        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n        f\"cwd: {result.cwd}\",\n    ]\n    if result.stdout.strip():\n        parts.append(\"stdout:\\n\" + result.stdout.strip())\n    if result.stderr.strip():\n        parts.append(\"stderr:\\n\" + result.stderr.strip())\n    return \"\\n\".join(parts)\n\n\ndef slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n    words = [part for part in cleaned.split(\"-\") if part]\n    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n\n\ndef plan_title(plan_path: Path) -> str:\n    text = plan_path.read_text(encoding=\"utf-8\")\n    for line in text.splitlines():\n        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n        if match:\n            return match.group(1).strip()\n    return plan_path.stem\n\n\ndef derive_slug(plan_path: Path) -> str:\n    return slugify(plan_title(plan_path))\n\n\ndef is_relative_to(path: Path, parent: Path) -> bool:\n    try:\n        path.resolve().relative_to(parent.resolve())\n        return True\n    except ValueError:\n        return False\n\n\ndef ensure_dir(path: Path) -> None:\n    path.mkdir(parents=True, exist_ok=True)\n\n\ndef write_text(path: Path, text: str) -> None:\n    ensure_dir(path.parent)\n    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n\n\n@dataclass(frozen=True)\nclass Names:\n    slug: str\n    branch: str\n    worktree: Path\n    run_id: str\n\n\n@dataclass(frozen=True)\nclass FlowConfig:\n    repo: Path\n    plan: Path\n    base: str\n    model: str | None\n    harness: str\n    merge_mode: str\n    keep_worktrees: bool\n\n\nclass HarnessWorktreeFlow:\n    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n        self.config = config\n        self.runner = runner\n        self.log_file: Path | None = None\n\n    def run(self) -> None:\n        repo = self.git_root(self.config.repo.resolve())\n        plan = self.config.plan.resolve()\n        self.validate(repo, plan)\n\n        names = self.unique_feature_names(repo, derive_slug(plan))\n        self.prepare_harness_permissions(repo / HARNESS_DIR)\n        self.prepare_git_permissions(repo)\n        self.start_log(repo, names.run_id)\n        print(f\"Feature branch: {names.branch}\")\n        print(f\"Feature worktree: {names.worktree}\")\n\n        self.create_feature_worktree(repo, names)\n        plan_in_worktree = self.ensure_plan_in_worktree(\n            repo, plan, names.worktree, names.slug\n        )\n        self.snapshot_skill_usage_baseline(names.worktree, repo)\n        self.run_implementation(names.worktree, plan_in_worktree)\n        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n        self.run_audit(names.worktree, plan_in_worktree)\n        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n        archive_dir = self.archive_handoff(\n            repo, names.worktree, names.run_id, \"feature\"\n        )\n        print(f\"Handoff archive: {archive_dir}\")\n\n        if self.config.merge_mode == \"stop\":\n            print(\"Stopped before merge by request.\")\n            print(f\"Plan: {plan_in_worktree}\")\n            print(f\"Worktree: {names.worktree}\")\n            print(f\"Branch: {names.branch}\")\n            return\n\n        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n\n    def validate(self, repo: Path, plan: Path) -> None:\n        if not plan.exists():\n            raise FlowError(f\"Plan file does not exist: {plan}\")\n        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n\n    def git_root(self, start: Path) -> Path:\n        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n        root = result.stdout.strip()\n        return Path(root).resolve() if root else start\n\n    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n        repo_name = repo.name\n        parent = repo.parent\n        suffix = 1\n        while True:\n            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n            branch = f\"feature/{candidate_slug}\"\n            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n            branch_exists = self.runner.run(\n                [\"git\", \"branch\", \"--list\", branch], repo\n            ).stdout.strip()\n            if not branch_exists and not worktree.exists():\n                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n                return Names(candidate_slug, branch, worktree, run_id)\n            suffix += 1\n\n    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n        self.runner.run(\n            [\n                \"git\",\n                \"worktree\",\n                \"add\",\n                str(names.worktree),\n                \"-b\",\n                names.branch,\n                self.config.base,\n            ],\n            repo,\n        )\n        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n        self.ensure_dir(names.worktree / HANDOFF_DIR)\n        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n        self.prepare_git_permissions(names.worktree)\n\n    def ensure_plan_in_worktree(\n        self, repo: Path, plan: Path, worktree: Path, slug: str\n    ) -> Path:\n        if is_relative_to(plan, repo):\n            rel = plan.relative_to(repo)\n            target = worktree / rel\n            if target.exists():\n                return target\n        else:\n            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n            target = worktree / rel\n\n        self.ensure_dir(target.parent)\n        self.copy_file(plan, target)\n        return target\n\n    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n        prompt = f\"\"\"Use the implement-worktree skill.\n\nImplement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n\nRequirements:\n- Do not create, switch, merge, delete, or rebase worktrees.\n- Keep edits scoped to the plan.\n- Run focused tests or checks appropriate to the change.\n- Commit the completed implementation.\n- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n\"\"\"\n        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n        self.harness_exec(worktree, prompt, output)\n\n    def run_audit(\n        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n    ) -> None:\n        summary = HANDOFF_DIR / (\n            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n        )\n        audit_finish_instruction = (\n            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n            if post_conflict\n            else \"Commit audit fixes if changes are made.\"\n        )\n        prompt = f\"\"\"Use the audit-worktree skill.\n\nFresh audit pass in this worktree.\n\nRead:\n- `{self.rel(worktree, plan_path)}`\n- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n\nAudit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n{audit_finish_instruction}\nDo not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\nWrite `{summary.as_posix()}` before finishing.\n\"\"\"\n        output = (\n            worktree\n            / HANDOFF_DIR\n            / (\n                \"post-conflict-audit-final-response.md\"\n                if post_conflict\n                else \"audit-final-response.md\"\n            )\n        )\n        self.harness_exec(worktree, prompt, output)\n\n    def harness_sandbox_mode(self) -> str:\n        if os.name == \"nt\":\n            return \"danger-full-access\"\n        return \"workspace-write\"\n\n    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n        self.ensure_dir(output_file.parent)\n        args = [\n            self.config.harness,\n            \"exec\",\n            \"--cd\",\n            str(cwd),\n            \"--sandbox\",\n            self.harness_sandbox_mode(),\n        ]\n        for writable_root in self.extra_writable_roots(cwd):\n            args.extend([\"--add-dir\", str(writable_root)])\n        if self.config.model:\n            args.extend([\"--model\", self.config.model])\n        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n        self.log_event(\n            \"harness_exec_start\",\n            cwd=str(cwd),\n            output_file=str(output_file),\n            command=args[:-1],\n        )\n        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n        self.log_event(\n            \"harness_exec_finish\",\n            cwd=str(cwd),\n            output_file=str(output_file),\n            output_file_exists=output_file.exists(),\n            returncode=result.returncode,\n            stdout=result.stdout,\n            stderr=result.stderr,\n        )\n        if result.returncode != 0:\n            raise FlowError(format_command_failure(result))\n\n    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n        integration_branch = f\"integration/{names.slug}-{stamp}\"\n        integration_worktree = (\n            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n        )\n\n        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n        self.runner.run(\n            [\n                \"git\",\n                \"worktree\",\n                \"add\",\n                str(integration_worktree),\n                \"-b\",\n                integration_branch,\n                self.config.base,\n            ],\n            repo,\n        )\n        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n        self.prepare_git_permissions(integration_worktree)\n        integration_plan = self.copy_integration_context(\n            names.worktree, integration_worktree, plan_path\n        )\n        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n\n        integrated = False\n        try:\n            skill_usage_restored = False\n            if self.config.merge_mode == \"squash\":\n                merge = self.runner.run(\n                    [\"git\", \"merge\", \"--squash\", names.branch],\n                    integration_worktree,\n                    check=False,\n                )\n                if merge.returncode != 0:\n                    unmerged = self.unmerged_paths(integration_worktree)\n                    if unmerged:\n                        self.restore_integration_skill_usage_to_head(\n                            integration_worktree, repo\n                        )\n                        skill_usage_restored = True\n                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n                        self.resolve_conflict(\n                            integration_worktree, names, integration_plan\n                        )\n                    elif not unmerged:\n                        raise FlowError(format_command_failure(merge))\n            else:\n                merge = self.runner.run(\n                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n                    integration_worktree,\n                    check=False,\n                )\n                if merge.returncode != 0:\n                    unmerged = self.unmerged_paths(integration_worktree)\n                    if unmerged:\n                        self.restore_integration_skill_usage_to_head(\n                            integration_worktree, repo\n                        )\n                        skill_usage_restored = True\n                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n                        self.resolve_conflict(\n                            integration_worktree, names, integration_plan\n                        )\n                    elif not unmerged:\n                        raise FlowError(format_command_failure(merge))\n\n            if not skill_usage_restored:\n                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n            self.consolidate_skill_usage(\n                names.worktree, integration_worktree, repo, feature_baseline\n            )\n            self.stage_integration_changes(integration_worktree)\n            self.runner.run(\n                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n            )\n\n            self.archive_handoff(\n                repo, integration_worktree, names.run_id, \"integration\"\n            )\n            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n            integrated = True\n        finally:\n            if integrated and not self.config.keep_worktrees:\n                self.cleanup(repo, integration_worktree, integration_branch, names)\n\n    def resolve_conflict(\n        self, integration_worktree: Path, names: Names, plan_path: Path\n    ) -> None:\n        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n        self.write_text(\n            context_path, self.conflict_context(integration_worktree, names, plan_path)\n        )\n        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n\nResolve merge conflicts in this integration worktree.\n\nRead:\n- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n- `{self.rel(integration_worktree, plan_path)}`\n- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n\nPreserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\nDo not commit.\n\"\"\"\n        self.harness_exec(\n            integration_worktree,\n            prompt,\n            integration_worktree\n            / HANDOFF_DIR\n            / \"conflict-resolution-final-response.md\",\n        )\n        self.require_file(\n            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n        )\n        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n        self.require_file(\n            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n        )\n\n    def copy_integration_context(\n        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n    ) -> Path:\n        source_handoff = feature_worktree / HANDOFF_DIR\n        dest_handoff = integration_worktree / HANDOFF_DIR\n        self.ensure_dir(dest_handoff)\n        if source_handoff.exists():\n            for item in source_handoff.iterdir():\n                if item.is_file():\n                    self.copy_file(item, dest_handoff / item.name)\n\n        try:\n            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n        except ValueError:\n            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n        dest_plan = integration_worktree / rel_plan\n        self.ensure_dir(dest_plan.parent)\n        if plan_path.exists():\n            self.copy_file(plan_path, dest_plan)\n        return dest_plan\n\n    def archive_handoff(\n        self, repo: Path, worktree: Path, run_id: str, stage: str\n    ) -> Path:\n        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n        self.ensure_dir(archive_dir)\n        source = worktree / HANDOFF_DIR\n        if source.exists():\n            for item in source.iterdir():\n                if item.is_file():\n                    self.copy_file(item, archive_dir / item.name)\n        return archive_dir\n\n    def stage_integration_changes(self, worktree: Path) -> None:\n        self.runner.run(\n            [\n                \"git\",\n                \"add\",\n                \"-A\",\n                \"--\",\n                \".\",\n                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n            ],\n            worktree,\n        )\n\n    def skill_usage_script(self, worktree: Path) -> Path:\n        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n\n    def skill_usage_ledger(self, repo_root: Path) -> Path:\n        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n            candidate = repo_root / harness_dir\n            if candidate.exists():\n                return candidate / \"skill-usage.json\"\n        return repo_root / \".skill-usage.json\"\n\n    def skill_usage_ledger_in_worktree(\n        self, worktree: Path, reference_repo: Path\n    ) -> Path:\n        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n        return worktree / rel\n\n    def snapshot_skill_usage_baseline(\n        self, worktree: Path, reference_repo: Path | None = None\n    ) -> Path:\n        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n        ledger = (\n            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n            if reference_repo is not None\n            else self.skill_usage_ledger(worktree)\n        )\n        if self.runner.dry_run:\n            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n            return baseline\n        self.ensure_dir(baseline.parent)\n        if ledger.exists():\n            self.copy_file(ledger, baseline)\n        else:\n            baseline.write_text(\n                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n                encoding=\"utf-8\",\n                newline=\"\\n\",\n            )\n        return baseline\n\n    def restore_integration_skill_usage_to_head(\n        self, integration_worktree: Path, reference_repo: Path | None = None\n    ) -> None:\n        ledger = (\n            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n            if reference_repo is not None\n            else self.skill_usage_ledger(integration_worktree)\n        )\n        rel = ledger.relative_to(integration_worktree).as_posix()\n        exists_at_head = (\n            self.runner.run(\n                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n                integration_worktree,\n                check=False,\n            ).returncode\n            == 0\n        )\n        if exists_at_head:\n            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n        else:\n            self.runner.run(\n                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n                integration_worktree,\n                check=False,\n            )\n            if not self.runner.dry_run and ledger.exists():\n                ledger.unlink()\n\n    def consolidate_skill_usage(\n        self,\n        source_worktree: Path,\n        integration_worktree: Path,\n        target_repo: Path,\n        baseline_path: Path,\n    ) -> None:\n        self.runner.run(\n            [\n                sys.executable,\n                str(self.skill_usage_script(integration_worktree)),\n                \"consolidate\",\n                \"--source-ledger\",\n                str(\n                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n                ),\n                \"--base-ledger\",\n                str(baseline_path),\n                \"--target-ledger\",\n                str(\n                    self.skill_usage_ledger_in_worktree(\n                        integration_worktree, target_repo\n                    )\n                ),\n                \"--source-repo\",\n                str(source_worktree),\n                \"--target-repo\",\n                str(target_repo),\n                \"--target-worktree\",\n                str(integration_worktree),\n            ],\n            integration_worktree,\n        )\n\n    def has_unmerged_paths(self, worktree: Path) -> bool:\n        return bool(self.unmerged_paths(worktree))\n\n    def unmerged_paths(self, worktree: Path) -> list[str]:\n        result = self.runner.run(\n            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n            worktree,\n            check=False,\n        )\n        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n\n    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n        expected = {\n            f\"{harness_dir}/skill-usage.json\"\n            for harness_dir in (\n                HARNESS_DIR.as_posix(),\n                \".harness\",\n                \".codex\",\n                \".opencode\",\n                \".claude\",\n                \".omp\",\n                \".agents\",\n            )\n        }\n        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n\n    def require_no_tracked_handoff_artifacts(\n        self, worktree: Path, treeish: str\n    ) -> None:\n        result = self.runner.run(\n            [\n                \"git\",\n                \"ls-tree\",\n                \"-r\",\n                \"--name-only\",\n                treeish,\n                \"--\",\n                HANDOFF_DIR.as_posix(),\n            ],\n            worktree,\n            check=False,\n        )\n        tracked = result.stdout.strip()\n        if result.returncode != 0 or not tracked:\n            return\n        raise FlowError(\n            \"Workflow handoff artifacts were committed, but they must remain \"\n            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n        )\n\n    def git_common_dir(self, worktree: Path) -> Path | None:\n        result = self.runner.run(\n            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n            worktree,\n            check=False,\n        )\n        raw = result.stdout.strip()\n        if result.returncode != 0 or not raw:\n            return None\n        path = Path(raw)\n        if not path.is_absolute():\n            path = worktree / path\n        return path.resolve()\n\n    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n        roots: list[Path] = []\n        harness_dir = worktree / HARNESS_DIR\n        if harness_dir.exists():\n            roots.append(harness_dir.resolve())\n\n        common_dir = self.git_common_dir(worktree)\n        if common_dir is not None and not is_relative_to(common_dir, worktree):\n            roots.append(common_dir)\n        return roots\n\n    def prepare_git_permissions(self, worktree: Path) -> None:\n        common_dir = self.git_common_dir(worktree)\n        if common_dir is not None and not is_relative_to(common_dir, worktree):\n            self.prepare_harness_permissions(common_dir)\n\n    def ensure_dir(self, path: Path) -> None:\n        if self.runner.dry_run:\n            print(f\"+ mkdir -p {path}\")\n            return\n        ensure_dir(path)\n\n    def copy_file(self, source: Path, dest: Path) -> None:\n        if self.runner.dry_run:\n            print(f\"+ copy {source} {dest}\")\n            return\n        shutil.copy2(source, dest)\n\n    def write_text(self, path: Path, text: str) -> None:\n        if self.runner.dry_run:\n            print(f\"+ write {path}\")\n            return\n        write_text(path, text)\n\n    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n            return\n        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n        if shell is None:\n            print(\n                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n                \"PowerShell was not found.\",\n                file=sys.stderr,\n            )\n            return\n        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n        script = r\"\"\"\n$Root = $env:CODEX_PERMISSION_ROOT\n$Group = $env:CODEX_PERMISSION_GROUP\n$ErrorActionPreference = 'Stop'\n$identity = New-Object System.Security.Principal.NTAccount($Group)\n$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n$propagate = [System.Security.AccessControl.PropagationFlags]::None\n$items = @((Get-Item -LiteralPath $Root -Force))\n$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\nforeach ($item in $items) {\n    $acl = Get-Acl -LiteralPath $item.FullName\n    foreach ($rule in @($acl.Access)) {\n        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n            [void]$acl.RemoveAccessRuleSpecific($rule)\n        }\n    }\n    if ($item.PSIsContainer) {\n        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n    } else {\n        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n    }\n    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n        $identity,\n        $rights,\n        $inherit,\n        $propagate,\n        [System.Security.AccessControl.AccessControlType]::Allow\n    )\n    $acl.SetAccessRule($allow)\n    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n}\n\"\"\"\n        completed = subprocess.run(\n            [\n                shell,\n                \"-NoProfile\",\n                \"-NonInteractive\",\n                \"-Command\",\n                script,\n            ],\n            check=False,\n            capture_output=True,\n            env={\n                **os.environ,\n                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n                \"CODEX_PERMISSION_GROUP\": group,\n            },\n            text=True,\n        )\n        if completed.returncode != 0:\n            print(\n                \"Warning: could not grant sandbox write permissions for \"\n                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n                file=sys.stderr,\n            )\n\n    def conflict_context(\n        self, integration_worktree: Path, names: Names, plan_path: Path\n    ) -> str:\n        status = self.runner.run(\n            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n        ).stdout\n        conflicted = self.runner.run(\n            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n            integration_worktree,\n            check=False,\n        ).stdout\n        merge_base = self.runner.run(\n            [\"git\", \"merge-base\", self.config.base, names.branch],\n            integration_worktree,\n            check=False,\n        ).stdout.strip()\n        base_log = \"\"\n        feature_log = \"\"\n        if merge_base:\n            base_log = self.runner.run(\n                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n                integration_worktree,\n                check=False,\n            ).stdout\n            feature_log = self.runner.run(\n                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n                integration_worktree,\n                check=False,\n            ).stdout\n\n        return f\"\"\"# Merge Conflict Context\n\n## Branches\n- Base branch: {self.config.base}\n- Feature branch: {names.branch}\n\n## Plan\n- Path: {self.rel(integration_worktree, plan_path)}\n\n## Merge base\n{merge_base or \"unknown\"}\n\n## Conflicted files\n{conflicted.strip() or \"unknown\"}\n\n## Status\n```text\n{status.strip()}\n```\n\n## Base commits since merge base\n```text\n{base_log.strip()}\n```\n\n## Feature commits since merge base\n```text\n{feature_log.strip()}\n```\n\n## Resolution rules\n1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n2. Feature intent comes from the approved plan and implementation summary.\n3. Preserve audited feature behavior when compatible with latest base.\n4. Prefer the smallest conflict-only edit.\n5. Remove all conflict markers.\n\"\"\"\n\n    def cleanup(\n        self,\n        repo: Path,\n        integration_worktree: Path,\n        integration_branch: str,\n        names: Names,\n    ) -> None:\n        for worktree in (integration_worktree, names.worktree):\n            if worktree.exists():\n                self.runner.run(\n                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n                )\n        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n        # Squash merges do not mark the feature branch as merged, so force-delete\n        # only after the integration branch has fast-forwarded successfully.\n        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n\n    def start_log(self, repo: Path, run_id: str) -> None:\n        if self.runner.dry_run:\n            return\n        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n        ensure_dir(self.log_file.parent)\n        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n\n    def log_event(self, event: str, **fields: object) -> None:\n        if self.log_file is None:\n            return\n        record = {\n            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n            \"event\": event,\n            **fields,\n        }\n        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n\n    def require_file(self, path: Path) -> None:\n        if not path.exists() and not self.runner.dry_run:\n            raise FlowError(f\"Required output file was not created: {path}\")\n\n    @staticmethod\n    def rel(root: Path, path: Path) -> str:\n        try:\n            return path.resolve().relative_to(root.resolve()).as_posix()\n        except ValueError:\n            return str(path)\n\n\ndef build_parser() -> argparse.ArgumentParser:\n    parser = argparse.ArgumentParser(\n        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n    )\n    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n    parser.add_argument(\n        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n    )\n    parser.add_argument(\n        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n    )\n    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n    parser.add_argument(\n        \"--harness\",\n        default=DEFAULT_HARNESS,\n        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n    )\n    parser.add_argument(\n        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n    )\n    parser.add_argument(\n        \"--keep-worktrees\",\n        action=\"store_true\",\n        help=\"Do not remove feature/integration worktrees.\",\n    )\n    parser.add_argument(\n        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n    )\n    return parser\n\n\ndef main(argv: list[str] | None = None) -> int:\n    parser = build_parser()\n    args = parser.parse_args(argv)\n    config = FlowConfig(\n        repo=Path(args.repo).expanduser().resolve(),\n        plan=Path(args.plan).expanduser().resolve(),\n        base=args.base,\n        harness=args.harness,\n        model=args.model,\n        merge_mode=args.merge_mode,\n        keep_worktrees=args.keep_worktrees,\n    )\n    try:\n        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n    except FlowError as exc:\n        print(str(exc), file=sys.stderr)\n        return 1\n    return 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n\n\n succeeded in 1354ms:\n#!/usr/bin/env python3\n\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport os\nimport re\nimport shutil\nimport subprocess\nimport sys\nfrom dataclasses import dataclass\nfrom datetime import datetime\nfrom pathlib import Path\nfrom typing import Sequence\n\nHARNESS_DIR = Path(\".omp\")\nDEFAULT_HARNESS = \"omp\"\nHANDOFF_DIR = HARNESS_DIR / \"handoff\"\nEMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n\n\nclass FlowError(RuntimeError):\n    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n\n\n@dataclass(frozen=True)\nclass CommandResult:\n    args: tuple[str, ...]\n    cwd: Path\n    returncode: int\n    stdout: str = \"\"\n    stderr: str = \"\"\n\n\nclass CommandRunner:\n    def __init__(self, dry_run: bool = False) -> None:\n        self.dry_run = dry_run\n\n    def run(\n        self,\n        args: Sequence[str],\n        cwd: Path,\n        *,\n        check: bool = True,\n        capture: bool = True,\n        input_text: str | None = None,\n    ) -> CommandResult:\n        display = \" \".join(args)\n        print(f\"+ ({cwd}) {display}\")\n        if self.dry_run:\n            return CommandResult(tuple(args), cwd, 0)\n\n        executable = shutil.which(args[0])\n        if executable is None:\n            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n        resolved_args = [executable, *args[1:]]\n        try:\n            completed = subprocess.run(\n                resolved_args,\n                cwd=cwd,\n                check=False,\n                capture_output=capture,\n                text=True,\n                input=input_text,\n            )\n        except OSError as exc:\n            raise FlowError(\n                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n            ) from exc\n        result = CommandResult(\n            tuple(args),\n            cwd,\n            completed.returncode,\n            completed.stdout or \"\",\n            completed.stderr or \"\",\n        )\n        if check and result.returncode != 0:\n            raise FlowError(format_command_failure(result))\n        return result\n\n\ndef format_command_failure(result: CommandResult) -> str:\n    parts = [\n        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n        f\"cwd: {result.cwd}\",\n    ]\n    if result.stdout.strip():\n        parts.append(\"stdout:\\n\" + result.stdout.strip())\n    if result.stderr.strip():\n        parts.append(\"stderr:\\n\" + result.stderr.strip())\n    return \"\\n\".join(parts)\n\n\ndef slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n    words = [part for part in cleaned.split(\"-\") if part]\n    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n\n\ndef plan_title(plan_path: Path) -> str:\n    text = plan_path.read_text(encoding=\"utf-8\")\n    for line in text.splitlines():\n        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n        if match:\n            return match.group(1).strip()\n    return plan_path.stem\n\n\ndef derive_slug(plan_path: Path) -> str:\n    return slugify(plan_title(plan_path))\n\n\ndef is_relative_to(path: Path, parent: Path) -> bool:\n    try:\n        path.resolve().relative_to(parent.resolve())\n        return True\n    except ValueError:\n        return False\n\n\ndef ensure_dir(path: Path) -> None:\n    path.mkdir(parents=True, exist_ok=True)\n\n\ndef write_text(path: Path, text: str) -> None:\n    ensure_dir(path.parent)\n    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n\n\n@dataclass(frozen=True)\nclass Names:\n    slug: str\n    branch: str\n    worktree: Path\n    run_id: str\n\n\n@dataclass(frozen=True)\nclass FlowConfig:\n    repo: Path\n    plan: Path\n    base: str\n    model: str | None\n    harness: str\n    merge_mode: str\n    keep_worktrees: bool\n\n\nclass HarnessWorktreeFlow:\n    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n        self.config = config\n        self.runner = runner\n        self.log_file: Path | None = None\n\n    def run(self) -> None:\n        repo = self.git_root(self.config.repo.resolve())\n        plan = self.config.plan.resolve()\n        self.validate(repo, plan)\n\n        names = self.unique_feature_names(repo, derive_slug(plan))\n        self.prepare_harness_permissions(repo / HARNESS_DIR)\n        self.prepare_git_permissions(repo)\n        self.start_log(repo, names.run_id)\n        print(f\"Feature branch: {names.branch}\")\n        print(f\"Feature worktree: {names.worktree}\")\n\n        self.create_feature_worktree(repo, names)\n        plan_in_worktree = self.ensure_plan_in_worktree(\n            repo, plan, names.worktree, names.slug\n        )\n        self.snapshot_skill_usage_baseline(names.worktree, repo)\n        self.run_implementation(names.worktree, plan_in_worktree)\n        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n        self.run_audit(names.worktree, plan_in_worktree)\n        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n        archive_dir = self.archive_handoff(\n            repo, names.worktree, names.run_id, \"feature\"\n        )\n        print(f\"Handoff archive: {archive_dir}\")\n\n        if self.config.merge_mode == \"stop\":\n            print(\"Stopped before merge by request.\")\n            print(f\"Plan: {plan_in_worktree}\")\n            print(f\"Worktree: {names.worktree}\")\n            print(f\"Branch: {names.branch}\")\n            return\n\n        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n\n    def validate(self, repo: Path, plan: Path) -> None:\n        if not plan.exists():\n            raise FlowError(f\"Plan file does not exist: {plan}\")\n        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n\n    def git_root(self, start: Path) -> Path:\n        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n        root = result.stdout.strip()\n        return Path(root).resolve() if root else start\n\n    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n        repo_name = repo.name\n        parent = repo.parent\n        suffix = 1\n        while True:\n            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n            branch = f\"feature/{candidate_slug}\"\n            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n            branch_exists = self.runner.run(\n                [\"git\", \"branch\", \"--list\", branch], repo\n            ).stdout.strip()\n            if not branch_exists and not worktree.exists():\n                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n                return Names(candidate_slug, branch, worktree, run_id)\n            suffix += 1\n\n    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n        self.runner.run(\n            [\n                \"git\",\n                \"worktree\",\n                \"add\",\n                str(names.worktree),\n                \"-b\",\n                names.branch,\n                self.config.base,\n            ],\n            repo,\n        )\n        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n        self.ensure_dir(names.worktree / HANDOFF_DIR)\n        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n        self.prepare_git_permissions(names.worktree)\n\n    def ensure_plan_in_worktree(\n        self, repo: Path, plan: Path, worktree: Path, slug: str\n    ) -> Path:\n        if is_relative_to(plan, repo):\n            rel = plan.relative_to(repo)\n            target = worktree / rel\n            if target.exists():\n                return target\n        else:\n            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n            target = worktree / rel\n\n        self.ensure_dir(target.parent)\n        self.copy_file(plan, target)\n        return target\n\n    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n        prompt = f\"\"\"Use the implement-worktree skill.\n\nImplement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n\nRequirements:\n- Do not create, switch, merge, delete, or rebase worktrees.\n- Keep edits scoped to the plan.\n- Run focused tests or checks appropriate to the change.\n- Commit the completed implementation.\n- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n\"\"\"\n        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n        self.harness_exec(worktree, prompt, output)\n\n    def run_audit(\n        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n    ) -> None:\n        summary = HANDOFF_DIR / (\n            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n        )\n        audit_finish_instruction = (\n            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n            if post_conflict\n            else \"Commit audit fixes if changes are made.\"\n        )\n        prompt = f\"\"\"Use the audit-worktree skill.\n\nFresh audit pass in this worktree.\n\nRead:\n- `{self.rel(worktree, plan_path)}`\n- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n\nAudit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n{audit_finish_instruction}\nDo not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\nWrite `{summary.as_posix()}` before finishing.\n\"\"\"\n        output = (\n            worktree\n            / HANDOFF_DIR\n            / (\n                \"post-conflict-audit-final-response.md\"\n                if post_conflict\n                else \"audit-final-response.md\"\n            )\n        )\n        self.harness_exec(worktree, prompt, output)\n\n    def harness_sandbox_mode(self) -> str:\n        if os.name == \"nt\":\n            return \"danger-full-access\"\n        return \"workspace-write\"\n\n    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n        self.ensure_dir(output_file.parent)\n        args = [\n            self.config.harness,\n            \"exec\",\n            \"--cd\",\n            str(cwd),\n            \"--sandbox\",\n            self.harness_sandbox_mode(),\n        ]\n        for writable_root in self.extra_writable_roots(cwd):\n            args.extend([\"--add-dir\", str(writable_root)])\n        if self.config.model:\n            args.extend([\"--model\", self.config.model])\n        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n        self.log_event(\n            \"harness_exec_start\",\n            cwd=str(cwd),\n            output_file=str(output_file),\n            command=args[:-1],\n        )\n        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n        self.log_event(\n            \"harness_exec_finish\",\n            cwd=str(cwd),\n            output_file=str(output_file),\n            output_file_exists=output_file.exists(),\n            returncode=result.returncode,\n            stdout=result.stdout,\n            stderr=result.stderr,\n        )\n        if result.returncode != 0:\n            raise FlowError(format_command_failure(result))\n\n    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n        integration_branch = f\"integration/{names.slug}-{stamp}\"\n        integration_worktree = (\n            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n        )\n\n        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n        self.runner.run(\n            [\n                \"git\",\n                \"worktree\",\n                \"add\",\n                str(integration_worktree),\n                \"-b\",\n                integration_branch,\n                self.config.base,\n            ],\n            repo,\n        )\n        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n        self.prepare_git_permissions(integration_worktree)\n        integration_plan = self.copy_integration_context(\n            names.worktree, integration_worktree, plan_path\n        )\n        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n\n        integrated = False\n        try:\n            skill_usage_restored = False\n            if self.config.merge_mode == \"squash\":\n                merge = self.runner.run(\n                    [\"git\", \"merge\", \"--squash\", names.branch],\n                    integration_worktree,\n                    check=False,\n                )\n                if merge.returncode != 0:\n                    unmerged = self.unmerged_paths(integration_worktree)\n                    if unmerged:\n                        self.restore_integration_skill_usage_to_head(\n                            integration_worktree, repo\n                        )\n                        skill_usage_restored = True\n                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n                        self.resolve_conflict(\n                            integration_worktree, names, integration_plan\n                        )\n                    elif not unmerged:\n                        raise FlowError(format_command_failure(merge))\n            else:\n                merge = self.runner.run(\n                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n                    integration_worktree,\n                    check=False,\n                )\n                if merge.returncode != 0:\n                    unmerged = self.unmerged_paths(integration_worktree)\n                    if unmerged:\n                        self.restore_integration_skill_usage_to_head(\n                            integration_worktree, repo\n                        )\n                        skill_usage_restored = True\n                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n                        self.resolve_conflict(\n                            integration_worktree, names, integration_plan\n                        )\n                    elif not unmerged:\n                        raise FlowError(format_command_failure(merge))\n\n            if not skill_usage_restored:\n                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n            self.consolidate_skill_usage(\n                names.worktree, integration_worktree, repo, feature_baseline\n            )\n            self.stage_integration_changes(integration_worktree)\n            self.runner.run(\n                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n            )\n\n            self.archive_handoff(\n                repo, integration_worktree, names.run_id, \"integration\"\n            )\n            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n            integrated = True\n        finally:\n            if integrated and not self.config.keep_worktrees:\n                self.cleanup(repo, integration_worktree, integration_branch, names)\n\n    def resolve_conflict(\n        self, integration_worktree: Path, names: Names, plan_path: Path\n    ) -> None:\n        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n        self.write_text(\n            context_path, self.conflict_context(integration_worktree, names, plan_path)\n        )\n        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n\nResolve merge conflicts in this integration worktree.\n\nRead:\n- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n- `{self.rel(integration_worktree, plan_path)}`\n- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n\nPreserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\nDo not commit.\n\"\"\"\n        self.harness_exec(\n            integration_worktree,\n            prompt,\n            integration_worktree\n            / HANDOFF_DIR\n            / \"conflict-resolution-final-response.md\",\n        )\n        self.require_file(\n            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n        )\n        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n        self.require_file(\n            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n        )\n\n    def copy_integration_context(\n        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n    ) -> Path:\n        source_handoff = feature_worktree / HANDOFF_DIR\n        dest_handoff = integration_worktree / HANDOFF_DIR\n        self.ensure_dir(dest_handoff)\n        if source_handoff.exists():\n            for item in source_handoff.iterdir():\n                if item.is_file():\n                    self.copy_file(item, dest_handoff / item.name)\n\n        try:\n            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n        except ValueError:\n            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n        dest_plan = integration_worktree / rel_plan\n        self.ensure_dir(dest_plan.parent)\n        if plan_path.exists():\n            self.copy_file(plan_path, dest_plan)\n        return dest_plan\n\n    def archive_handoff(\n        self, repo: Path, worktree: Path, run_id: str, stage: str\n    ) -> Path:\n        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n        self.ensure_dir(archive_dir)\n        source = worktree / HANDOFF_DIR\n        if source.exists():\n            for item in source.iterdir():\n                if item.is_file():\n                    self.copy_file(item, archive_dir / item.name)\n        return archive_dir\n\n    def stage_integration_changes(self, worktree: Path) -> None:\n        self.runner.run(\n            [\n                \"git\",\n                \"add\",\n                \"-A\",\n                \"--\",\n                \".\",\n                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n            ],\n            worktree,\n        )\n\n    def skill_usage_script(self, worktree: Path) -> Path:\n        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n\n    def skill_usage_ledger(self, repo_root: Path) -> Path:\n        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n            candidate = repo_root / harness_dir\n            if candidate.exists():\n                return candidate / \"skill-usage.json\"\n        return repo_root / \".skill-usage.json\"\n\n    def skill_usage_ledger_in_worktree(\n        self, worktree: Path, reference_repo: Path\n    ) -> Path:\n        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n        return worktree / rel\n\n    def snapshot_skill_usage_baseline(\n        self, worktree: Path, reference_repo: Path | None = None\n    ) -> Path:\n        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n        ledger = (\n            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n            if reference_repo is not None\n            else self.skill_usage_ledger(worktree)\n        )\n        if self.runner.dry_run:\n            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n            return baseline\n        self.ensure_dir(baseline.parent)\n        if ledger.exists():\n            self.copy_file(ledger, baseline)\n        else:\n            baseline.write_text(\n                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n                encoding=\"utf-8\",\n                newline=\"\\n\",\n            )\n        return baseline\n\n    def restore_integration_skill_usage_to_head(\n        self, integration_worktree: Path, reference_repo: Path | None = None\n    ) -> None:\n        ledger = (\n            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n            if reference_repo is not None\n            else self.skill_usage_ledger(integration_worktree)\n        )\n        rel = ledger.relative_to(integration_worktree).as_posix()\n        exists_at_head = (\n            self.runner.run(\n                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n                integration_worktree,\n                check=False,\n            ).returncode\n            == 0\n        )\n        if exists_at_head:\n            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n        else:\n            self.runner.run(\n                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n                integration_worktree,\n                check=False,\n            )\n            if not self.runner.dry_run and ledger.exists():\n                ledger.unlink()\n\n    def consolidate_skill_usage(\n        self,\n        source_worktree: Path,\n        integration_worktree: Path,\n        target_repo: Path,\n        baseline_path: Path,\n    ) -> None:\n        self.runner.run(\n            [\n                sys.executable,\n                str(self.skill_usage_script(integration_worktree)),\n                \"consolidate\",\n                \"--source-ledger\",\n                str(\n                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n                ),\n                \"--base-ledger\",\n                str(baseline_path),\n                \"--target-ledger\",\n                str(\n                    self.skill_usage_ledger_in_worktree(\n                        integration_worktree, target_repo\n                    )\n                ),\n                \"--source-repo\",\n                str(source_worktree),\n                \"--target-repo\",\n                str(target_repo),\n                \"--target-worktree\",\n                str(integration_worktree),\n            ],\n            integration_worktree,\n        )\n\n    def has_unmerged_paths(self, worktree: Path) -> bool:\n        return bool(self.unmerged_paths(worktree))\n\n    def unmerged_paths(self, worktree: Path) -> list[str]:\n        result = self.runner.run(\n            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n            worktree,\n            check=False,\n        )\n        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n\n    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n        expected = {\n            f\"{harness_dir}/skill-usage.json\"\n            for harness_dir in (\n                HARNESS_DIR.as_posix(),\n                \".harness\",\n                \".codex\",\n                \".opencode\",\n                \".claude\",\n                \".omp\",\n                \".agents\",\n            )\n        }\n        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n\n    def require_no_tracked_handoff_artifacts(\n        self, worktree: Path, treeish: str\n    ) -> None:\n        result = self.runner.run(\n            [\n                \"git\",\n                \"ls-tree\",\n                \"-r\",\n                \"--name-only\",\n                treeish,\n                \"--\",\n                HANDOFF_DIR.as_posix(),\n            ],\n            worktree,\n            check=False,\n        )\n        tracked = result.stdout.strip()\n        if result.returncode != 0 or not tracked:\n            return\n        raise FlowError(\n            \"Workflow handoff artifacts were committed, but they must remain \"\n            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n        )\n\n    def git_common_dir(self, worktree: Path) -> Path | None:\n        result = self.runner.run(\n            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n            worktree,\n            check=False,\n        )\n        raw = result.stdout.strip()\n        if result.returncode != 0 or not raw:\n            return None\n        path = Path(raw)\n        if not path.is_absolute():\n            path = worktree / path\n        return path.resolve()\n\n    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n        roots: list[Path] = []\n        harness_dir = worktree / HARNESS_DIR\n        if harness_dir.exists():\n            roots.append(harness_dir.resolve())\n\n        common_dir = self.git_common_dir(worktree)\n        if common_dir is not None and not is_relative_to(common_dir, worktree):\n            roots.append(common_dir)\n        return roots\n\n    def prepare_git_permissions(self, worktree: Path) -> None:\n        common_dir = self.git_common_dir(worktree)\n        if common_dir is not None and not is_relative_to(common_dir, worktree):\n            self.prepare_harness_permissions(common_dir)\n\n    def ensure_dir(self, path: Path) -> None:\n        if self.runner.dry_run:\n            print(f\"+ mkdir -p {path}\")\n            return\n        ensure_dir(path)\n\n    def copy_file(self, source: Path, dest: Path) -> None:\n        if self.runner.dry_run:\n            print(f\"+ copy {source} {dest}\")\n            return\n        shutil.copy2(source, dest)\n\n    def write_text(self, path: Path, text: str) -> None:\n        if self.runner.dry_run:\n            print(f\"+ write {path}\")\n            return\n        write_text(path, text)\n\n    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n            return\n        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n        if shell is None:\n            print(\n                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n                \"PowerShell was not found.\",\n                file=sys.stderr,\n            )\n            return\n        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n        script = r\"\"\"\n$Root = $env:CODEX_PERMISSION_ROOT\n$Group = $env:CODEX_PERMISSION_GROUP\n$ErrorActionPreference = 'Stop'\n$identity = New-Object System.Security.Principal.NTAccount($Group)\n$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n$propagate = [System.Security.AccessControl.PropagationFlags]::None\n$items = @((Get-Item -LiteralPath $Root -Force))\n$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\nforeach ($item in $items) {\n    $acl = Get-Acl -LiteralPath $item.FullName\n    foreach ($rule in @($acl.Access)) {\n        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n            [void]$acl.RemoveAccessRuleSpecific($rule)\n        }\n    }\n    if ($item.PSIsContainer) {\n        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n    } else {\n        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n    }\n    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n        $identity,\n        $rights,\n        $inherit,\n        $propagate,\n        [System.Security.AccessControl.AccessControlType]::Allow\n    )\n    $acl.SetAccessRule($allow)\n    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n}\n\"\"\"\n        completed = subprocess.run(\n            [\n                shell,\n                \"-NoProfile\",\n                \"-NonInteractive\",\n                \"-Command\",\n                script,\n            ],\n            check=False,\n            capture_output=True,\n            env={\n                **os.environ,\n                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n                \"CODEX_PERMISSION_GROUP\": group,\n            },\n            text=True,\n        )\n        if completed.returncode != 0:\n            print(\n                \"Warning: could not grant sandbox write permissions for \"\n                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n                file=sys.stderr,\n            )\n\n    def conflict_context(\n        self, integration_worktree: Path, names: Names, plan_path: Path\n    ) -> str:\n        status = self.runner.run(\n            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n        ).stdout\n        conflicted = self.runner.run(\n            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n            integration_worktree,\n            check=False,\n        ).stdout\n        merge_base = self.runner.run(\n            [\"git\", \"merge-base\", self.config.base, names.branch],\n            integration_worktree,\n            check=False,\n        ).stdout.strip()\n        base_log = \"\"\n        feature_log = \"\"\n        if merge_base:\n            base_log = self.runner.run(\n                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n                integration_worktree,\n                check=False,\n            ).stdout\n            feature_log = self.runner.run(\n                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n                integration_worktree,\n                check=False,\n            ).stdout\n\n        return f\"\"\"# Merge Conflict Context\n\n## Branches\n- Base branch: {self.config.base}\n- Feature branch: {names.branch}\n\n## Plan\n- Path: {self.rel(integration_worktree, plan_path)}\n\n## Merge base\n{merge_base or \"unknown\"}\n\n## Conflicted files\n{conflicted.strip() or \"unknown\"}\n\n## Status\n```text\n{status.strip()}\n```\n\n## Base commits since merge base\n```text\n{base_log.strip()}\n```\n\n## Feature commits since merge base\n```text\n{feature_log.strip()}\n```\n\n## Resolution rules\n1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n2. Feature intent comes from the approved plan and implementation summary.\n3. Preserve audited feature behavior when compatible with latest base.\n4. Prefer the smallest conflict-only edit.\n5. Remove all conflict markers.\n\"\"\"\n\n    def cleanup(\n        self,\n        repo: Path,\n        integration_worktree: Path,\n        integration_branch: str,\n        names: Names,\n    ) -> None:\n        for worktree in (integration_worktree, names.worktree):\n            if worktree.exists():\n                self.runner.run(\n                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n                )\n        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n        # Squash merges do not mark the feature branch as merged, so force-delete\n        # only after the integration branch has fast-forwarded successfully.\n        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n\n    def start_log(self, repo: Path, run_id: str) -> None:\n        if self.runner.dry_run:\n            return\n        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n        ensure_dir(self.log_file.parent)\n        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n\n    def log_event(self, event: str, **fields: object) -> None:\n        if self.log_file is None:\n            return\n        record = {\n            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n            \"event\": event,\n            **fields,\n        }\n        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n\n    def require_file(self, path: Path) -> None:\n        if not path.exists() and not self.runner.dry_run:\n            raise FlowError(f\"Required output file was not created: {path}\")\n\n    @staticmethod\n    def rel(root: Path, path: Path) -> str:\n        try:\n            return path.resolve().relative_to(root.resolve()).as_posix()\n        except ValueError:\n            return str(path)\n\n\ndef build_parser() -> argparse.ArgumentParser:\n    parser = argparse.ArgumentParser(\n        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n    )\n    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n    parser.add_argument(\n        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n    )\n    parser.add_argument(\n        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n    )\n    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n    parser.add_argument(\n        \"--harness\",\n        default=DEFAULT_HARNESS,\n        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n    )\n    parser.add_argument(\n        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n    )\n    parser.add_argument(\n        \"--keep-worktrees\",\n        action=\"store_true\",\n        help=\"Do not remove feature/integration worktrees.\",\n    )\n    parser.add_argument(\n        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n    )\n    return parser\n\n\ndef main(argv: list[str] | None = None) -> int:\n    parser = build_parser()\n    args = parser.parse_args(argv)\n    config = FlowConfig(\n        repo=Path(args.repo).expanduser().resolve(),\n        plan=Path(args.plan).expanduser().resolve(),\n        base=args.base,\n        harness=args.harness,\n        model=args.model,\n        merge_mode=args.merge_mode,\n        keep_worktrees=args.keep_worktrees,\n    )\n    try:\n        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n    except FlowError as exc:\n        print(str(exc), file=sys.stderr)\n        return 1\n    return 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n\n\n succeeded in 1739ms:\nfrom __future__ import annotations\n\nimport importlib.util\nimport json\nimport shutil\nimport subprocess\nimport sys\nimport tempfile\nimport unittest\nfrom unittest import mock\nfrom pathlib import Path\n\n\nSCRIPT_DIR = Path(__file__).resolve().parents[1] / \".codex\" / \"scripts\"\n\n\ndef load_flow_module(name: str, script_name: str):\n    spec = importlib.util.spec_from_file_location(name, SCRIPT_DIR / script_name)\n    module = importlib.util.module_from_spec(spec)\n    assert spec and spec.loader\n    sys.modules[spec.name] = module\n    spec.loader.exec_module(module)\n    return module\n\n\nflow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\nomp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n\n\nclass FakeRunner:\n    def __init__(\n        self,\n        outputs: dict[\n            tuple[str, ...], flow.CommandResult | list[str] | str\n        ] | None = None,\n        *,\n        dry_run: bool = False,\n    ) -> None:\n        self.outputs = outputs or {}\n        self.calls: list[tuple[tuple[str, ...], Path, bool]] = []\n        self.inputs: list[str | None] = []\n        self.dry_run = dry_run\n\n    def run(self, args, cwd, *, check=True, capture=True, input_text=None):\n        key = tuple(args)\n        self.calls.append((key, Path(cwd), check))\n        self.inputs.append(input_text)\n        value = self.outputs.get(key, \"\")\n        if isinstance(value, flow.CommandResult):\n            return value\n        if isinstance(value, list):\n            stdout = value.pop(0) if value else \"\"\n        else:\n            stdout = value\n        return flow.CommandResult(key, Path(cwd), 0, stdout, \"\")\n\n\nclass FailingFastForwardRunner(FakeRunner):\n    def run(self, args, cwd, *, check=True, capture=True, input_text=None):\n        if tuple(args[:3]) == (\"git\", \"merge\", \"--ff-only\"):\n            result = flow.CommandResult(tuple(args), Path(cwd), 1, \"\", \"not a fast-forward\")\n            if check:\n                raise flow.FlowError(flow.format_command_failure(result))\n            return result\n        return super().run(args, cwd, check=check, capture=capture, input_text=input_text)\n\n\nclass FailingSquashMergeRunner(FakeRunner):\n    def __init__(self, *, unmerged_paths: str) -> None:\n        super().__init__()\n        self.unmerged_paths = unmerged_paths\n\n    def run(self, args, cwd, *, check=True, capture=True, input_text=None):\n        key = tuple(args)\n        if key[:3] == (\"git\", \"merge\", \"--squash\"):\n            self.calls.append((key, Path(cwd), check))\n            return flow.CommandResult(key, Path(cwd), 1, \"\", \"merge failed\")\n        if key == (\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"):\n            self.calls.append((key, Path(cwd), check))\n            return flow.CommandResult(key, Path(cwd), 0, self.unmerged_paths, \"\")\n        return super().run(args, cwd, check=check, capture=capture, input_text=input_text)\n\n\nclass CommandRunnerTests(unittest.TestCase):\n    def test_run_resolves_executable_before_invoking_subprocess(self) -> None:\n        completed = subprocess.CompletedProcess(\n            [\"C:/bin/codex.CMD\", \"exec\", \"--help\"],\n            0,\n            \"ok\",\n            \"\",\n        )\n        with (\n            tempfile.TemporaryDirectory() as temp,\n            mock.patch.object(flow.shutil, \"which\", return_value=\"C:/bin/codex.CMD\") as which,\n            mock.patch.object(flow.subprocess, \"run\", return_value=completed) as run,\n        ):\n            result = flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n\n        which.assert_called_once_with(\"codex\")\n        run.assert_called_once()\n        self.assertEqual(\n            run.call_args.args[0],\n            [\"C:/bin/codex.CMD\", \"exec\", \"--help\"],\n        )\n        self.assertEqual(result.args, (\"codex\", \"exec\", \"--help\"))\n        self.assertEqual(result.stdout, \"ok\")\n\n    def test_run_reports_missing_executable_as_flow_error(self) -> None:\n        with (\n            tempfile.TemporaryDirectory() as temp,\n            mock.patch.object(flow.shutil, \"which\", return_value=None),\n            mock.patch.object(flow.subprocess, \"run\") as run,\n            self.assertRaisesRegex(flow.FlowError, \"Executable not found on PATH: codex\"),\n        ):\n            flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n\n        run.assert_not_called()\n\n    def test_run_reports_launch_oserror_as_flow_error(self) -> None:\n        with (\n            tempfile.TemporaryDirectory() as temp,\n            mock.patch.object(flow.shutil, \"which\", return_value=\"C:/bin/codex.CMD\"),\n            mock.patch.object(\n                flow.subprocess,\n                \"run\",\n                side_effect=FileNotFoundError(\"missing shim target\"),\n            ),\n            self.assertRaisesRegex(flow.FlowError, \"Failed to run command: codex exec --help\"),\n        ):\n            flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n\n\nclass OmpVariantTests(unittest.TestCase):\n    def test_omp_defaults_are_explicit(self) -> None:\n        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n\n    def test_omp_parser_defaults_to_omp_harness(self) -> None:\n        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n\n        self.assertEqual(args.harness, \"omp\")\n\n    def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            (repo / \".omp\").mkdir()\n            runner = FakeRunner()\n            config = omp_flow.FlowConfig(\n                repo=repo,\n                plan=repo / \"plan.md\",\n                base=\"main\",\n                model=None,\n                harness=omp_flow.DEFAULT_HARNESS,\n                merge_mode=\"squash\",\n                keep_worktrees=False,\n            )\n            subject = omp_flow.HarnessWorktreeFlow(config, runner)\n\n            output_file = repo / \".omp\" / \"handoff\" / \"implementation-final-response.md\"\n            subject.harness_exec(repo, \"Prompt\", output_file)\n\n            args = runner.calls[-1][0]\n            self.assertEqual(args[:2], (\"omp\", \"exec\"))\n            self.assertIn(str(output_file), args)\n            self.assertIn(str((repo / \".omp\").resolve()), args)\n\n    def test_omp_consolidates_existing_repo_ledger_not_created_omp_ledger(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            root = Path(temp)\n            repo = root / \"repo\"\n            feature = root / \"repo-feature\"\n            integration = root / \"repo-integration\"\n            (repo / \".codex\").mkdir(parents=True)\n            (integration / \".omp\").mkdir(parents=True)\n            baseline = feature / \".omp\" / \"handoff\" / \"skill-usage-baseline.json\"\n            runner = FakeRunner()\n            config = omp_flow.FlowConfig(\n                repo=repo,\n                plan=repo / \"plan.md\",\n                base=\"main\",\n                model=None,\n                harness=omp_flow.DEFAULT_HARNESS,\n                merge_mode=\"squash\",\n                keep_worktrees=False,\n            )\n\n            omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n                feature,\n                integration,\n                repo,\n                baseline,\n            )\n\n            args = runner.calls[-1][0]\n            self.assertEqual(\n                args[args.index(\"--source-ledger\") + 1],\n                str(feature / \".codex\" / \"skill-usage.json\"),\n            )\n            self.assertEqual(\n                args[args.index(\"--target-ledger\") + 1],\n                str(integration / \".codex\" / \"skill-usage.json\"),\n            )\n\n    def test_omp_treats_codex_skill_usage_conflict_as_usage_only(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            config = omp_flow.FlowConfig(\n                repo=repo,\n                plan=repo / \"plan.md\",\n                base=\"main\",\n                model=None,\n                harness=omp_flow.DEFAULT_HARNESS,\n                merge_mode=\"squash\",\n                keep_worktrees=False,\n            )\n\n            subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n\n            self.assertTrue(subject.only_skill_usage_unmerged([\".codex/skill-usage.json\"]))\n\n\nclass HarnessWorktreeFlowTests(unittest.TestCase):\n    def test_slug_uses_first_h1(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            plan = Path(temp) / \"plan.md\"\n            plan.write_text(\"# Add Better Audit Flow!\\n\\nBody\", encoding=\"utf-8\")\n            self.assertEqual(flow.derive_slug(plan), \"add-better-audit-flow\")\n\n    def test_slug_falls_back_to_filename(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            plan = Path(temp) / \"My Plan File.md\"\n            plan.write_text(\"No heading\", encoding=\"utf-8\")\n            self.assertEqual(flow.derive_slug(plan), \"my-plan-file\")\n\n    def test_unique_names_skip_existing_branch_and_worktree(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            (Path(temp) / \"repo-example\").mkdir()\n            runner = FakeRunner(\n                {\n                    (\"git\", \"branch\", \"--list\", \"feature/example\"): \"feature/example\\n\",\n                    (\"git\", \"branch\", \"--list\", \"feature/example-2\"): \"\",\n                }\n            )\n            config = self.config(repo, repo / \"plan.md\")\n            names = flow.HarnessWorktreeFlow(config, runner).unique_feature_names(repo, \"example\")\n            self.assertEqual(names.branch, \"feature/example-2\")\n            self.assertEqual(names.worktree.name, \"repo-example-2\")\n\n    def test_plan_inside_repo_uses_same_relative_path(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            plan = repo / \"docs\" / \"plans\" / \"p.md\"\n            target = worktree / \"docs\" / \"plans\" / \"p.md\"\n            plan.parent.mkdir(parents=True)\n            target.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            target.write_text(\"# Plan\", encoding=\"utf-8\")\n            actual = flow.HarnessWorktreeFlow(self.config(repo, plan), FakeRunner()).ensure_plan_in_worktree(repo, plan, worktree, \"plan\")\n            self.assertEqual(actual, target)\n\n    def test_dry_run_plan_copy_does_not_mutate_filesystem(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            plan = Path(temp) / \"external.md\"\n            repo.mkdir()\n            worktree.mkdir()\n            plan.write_text(\"# External\", encoding=\"utf-8\")\n\n            actual = flow.HarnessWorktreeFlow(\n                self.config(repo, plan), FakeRunner(dry_run=True)\n            ).ensure_plan_in_worktree(repo, plan, worktree, \"external\")\n\n            self.assertEqual(actual, worktree / \"docs\" / \"plans\" / \"external.md\")\n            self.assertFalse(actual.exists())\n\n    def test_dry_run_archive_handoff_does_not_mutate_filesystem(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            source = worktree / \".codex\" / \"handoff\"\n            repo.mkdir()\n            source.mkdir(parents=True)\n            (source / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n\n            archive = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"), FakeRunner(dry_run=True)\n            ).archive_handoff(repo, worktree, \"plan-run\", \"feature\")\n\n            self.assertFalse(archive.exists())\n\n    def test_prepare_harness_permissions_grants_sandbox_group_on_windows(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            harness_dir = Path(temp) / \".codex\"\n            harness_dir.mkdir()\n            completed = subprocess.CompletedProcess([\"pwsh\"], 0, \"\", \"\")\n            subject = flow.HarnessWorktreeFlow(\n                self.config(Path(temp), Path(temp) / \"plan.md\"), FakeRunner()\n            )\n\n            with (\n                mock.patch.object(flow.os, \"name\", \"nt\"),\n                mock.patch.object(flow.shutil, \"which\", return_value=\"C:/PowerShell/pwsh.exe\"),\n                mock.patch.object(flow.subprocess, \"run\", return_value=completed) as run,\n            ):\n                subject.prepare_harness_permissions(harness_dir)\n\n            args = run.call_args.args[0]\n            kwargs = run.call_args.kwargs\n            self.assertEqual(args[0], \"C:/PowerShell/pwsh.exe\")\n            self.assertEqual(kwargs[\"env\"][\"CODEX_PERMISSION_ROOT\"], str(harness_dir))\n            self.assertEqual(kwargs[\"env\"][\"CODEX_PERMISSION_GROUP\"], \"CodexSandboxUsers\")\n            self.assertIn(\"RemoveAccessRuleSpecific\", args[4])\n\n    def test_prepare_git_permissions_grants_external_common_git_dir(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            common_git = repo / \".git\"\n            repo.mkdir()\n            worktree.mkdir()\n            common_git.mkdir()\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"),\n                FakeRunner(\n                    {\n                        (\"git\", \"rev-parse\", \"--git-common-dir\"): str(common_git),\n                    }\n                ),\n            )\n            prepared: list[Path] = []\n            subject.prepare_harness_permissions = prepared.append\n\n            subject.prepare_git_permissions(worktree)\n\n            self.assertEqual(prepared, [common_git.resolve()])\n\n    def test_extra_writable_roots_include_harness_and_external_git_dir(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            common_git = repo / \".git\"\n            harness_dir = worktree / \".codex\"\n            repo.mkdir()\n            worktree.mkdir()\n            common_git.mkdir()\n            harness_dir.mkdir()\n            runner = FakeRunner(\n                {\n                    (\"git\", \"rev-parse\", \"--git-common-dir\"): str(common_git),\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            roots = subject.extra_writable_roots(worktree)\n\n            self.assertEqual(roots, [harness_dir.resolve(), common_git.resolve()])\n\n\n    def test_harness_exec_adds_harness_and_common_git_dirs_as_writable_roots(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            common_git = repo / \".git\"\n            harness_dir = worktree / \".codex\"\n            repo.mkdir()\n            worktree.mkdir()\n            common_git.mkdir()\n            harness_dir.mkdir()\n            runner = FakeRunner(\n                {\n                    (\"git\", \"rev-parse\", \"--git-common-dir\"): str(common_git),\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.harness_exec(\n                worktree,\n                \"Prompt\",\n                worktree / \".codex\" / \"handoff\" / \"implementation-final-response.md\",\n            )\n\n            args = runner.calls[-1][0]\n            writable_roots = [\n                args[index + 1]\n                for index, value in enumerate(args)\n                if value == \"--add-dir\"\n            ]\n            self.assertEqual(\n                writable_roots,\n                [str(harness_dir.resolve()), str(common_git.resolve())],\n            )\n\n    def test_harness_exec_uses_full_access_sandbox_on_windows(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            runner = FakeRunner()\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            with mock.patch.object(flow.os, \"name\", \"nt\"):\n                subject.harness_exec(repo, \"Prompt\", repo / \"out.md\")\n\n            args = runner.calls[-1][0]\n            sandbox_index = args.index(\"--sandbox\")\n            self.assertEqual(args[sandbox_index + 1], \"danger-full-access\")\n\n    def test_harness_exec_uses_workspace_write_sandbox_off_windows(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            runner = FakeRunner()\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            with mock.patch.object(flow.os, \"name\", \"posix\"):\n                subject.harness_exec(repo, \"Prompt\", repo / \"out.md\")\n\n            args = runner.calls[-1][0]\n            sandbox_index = args.index(\"--sandbox\")\n            self.assertEqual(args[sandbox_index + 1], \"workspace-write\")\n\n    def test_run_prepares_primary_repo_permissions_before_logging(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-plan\"\n            plan = repo / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            (repo / \".codex\").mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (worktree / \"docs\" / \"plans\").mkdir(parents=True)\n            (worktree / \"docs\" / \"plans\" / \"plan.md\").write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = worktree / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n            runner = FakeRunner(\n                {\n                    (\"git\", \"branch\", \"--list\", \"feature/plan\"): \"\",\n                    (\"git\", \"rev-parse\", \"--git-common-dir\"): \".git\",\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan, merge_mode=\"stop\"), runner\n            )\n            prepared: list[Path] = []\n            subject.prepare_harness_permissions = prepared.append\n            subject.create_feature_worktree = lambda _repo, _names: None\n            subject.run_implementation = lambda _worktree, _plan: None\n            subject.run_audit = lambda _worktree, _plan: None\n            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n            )\n            subject.validate = lambda _repo, _plan: None\n            subject.finish = lambda *_args: (_ for _ in ()).throw(\n                AssertionError(\"finish should not run\")\n            )\n\n            subject.run()\n\n            self.assertEqual(prepared[0], repo / \".codex\")\n\n    def test_stage_integration_changes_excludes_handoff_artifacts(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            runner = FakeRunner()\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.stage_integration_changes(repo)\n\n            self.assertEqual(\n                runner.calls[0][0],\n                (\n                    \"git\",\n                    \"add\",\n                    \"-A\",\n                    \"--\",\n                    \".\",\n                    \":(exclude).codex/handoff/**\",\n                ),\n            )\n\n\n    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            runner = FakeRunner(\n                {\n                    (\n                        \"git\",\n                        \"ls-tree\",\n                        \"-r\",\n                        \"--name-only\",\n                        \"feature/plan\",\n                        \"--\",\n                        \".codex/handoff\",\n                    ): \".codex/handoff/audit-summary.md\\n\",\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n\n    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            runner = FakeRunner()\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.run_implementation(repo, repo / \"plan.md\")\n            subject.run_audit(repo, repo / \"plan.md\")\n\n            prompts = [value for value in runner.inputs if value is not None]\n            self.assertIn(\"Do not commit files under `.codex/handoff/`\", prompts[-2])\n            self.assertIn(\"Do not commit files under `.codex/handoff/`\", prompts[-1])\n\n    def test_snapshot_skill_usage_baseline_writes_empty_ledger_when_absent(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            worktree = Path(temp) / \"repo-feature\"\n            (worktree / \".codex\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(worktree, worktree / \"plan.md\"), FakeRunner()\n            )\n\n            baseline = subject.snapshot_skill_usage_baseline(worktree)\n\n            self.assertEqual(baseline, worktree / \".codex\" / \"handoff\" / \"skill-usage-baseline.json\")\n            self.assertEqual(\n                json.loads(baseline.read_text(encoding=\"utf-8\")),\n                {\"version\": 1, \"scopes\": {}},\n            )\n\n    def test_finish_restores_and_consolidates_before_staging_in_squash_mode(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"skill-usage-baseline.json\").write_text(\"{}\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FakeRunner())\n            subject.prepare_harness_permissions = lambda _path: None\n            events: list[str] = []\n            subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(events, [\"restore\", \"consolidate\", \"stage\"])\n\n\n    def test_consolidate_skill_usage_targets_primary_repo_paths(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            root = Path(temp)\n            feature = root / \"repo-plan\"\n            integration = root / \"repo-integrate\"\n            repo = root / \"repo\"\n            baseline = feature / \".codex\" / \"handoff\" / \"skill-usage-baseline.json\"\n            runner = FakeRunner()\n\n            flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner).consolidate_skill_usage(\n                feature,\n                integration,\n                repo,\n                baseline,\n            )\n\n            args = runner.calls[-1][0]\n            self.assertIn(\"--target-repo\", args)\n            self.assertEqual(args[args.index(\"--target-repo\") + 1], str(repo))\n            self.assertIn(\"--target-worktree\", args)\n            self.assertEqual(args[args.index(\"--target-worktree\") + 1], str(integration))\n\n    def test_no_ff_merge_uses_no_commit_and_workflow_commit(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan, merge_mode=\"no-ff\"), FakeRunner()\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.restore_integration_skill_usage_to_head = lambda *_args: None\n            subject.consolidate_skill_usage = lambda *_args: None\n            subject.stage_integration_changes = lambda _worktree: None\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            calls = [call[0] for call in subject.runner.calls]\n            self.assertIn((\"git\", \"merge\", \"--no-ff\", \"--no-commit\", \"feature/plan\"), calls)\n            self.assertIn((\"git\", \"commit\", \"-m\", \"Harness: Plan\"), calls)\n            self.assertNotIn((\"git\", \"merge\", \"--continue\"), calls)\n\n    def test_restore_skill_usage_to_head_restores_tracked_ledger(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            (repo / \".codex\").mkdir(parents=True)\n            runner = FakeRunner(\n                {\n                    (\"git\", \"cat-file\", \"-e\", \"HEAD:.codex/skill-usage.json\"): flow.CommandResult(\n                        (\"git\", \"cat-file\", \"-e\", \"HEAD:.codex/skill-usage.json\"),\n                        repo,\n                        0,\n                    )\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.restore_integration_skill_usage_to_head(repo)\n\n            calls = [call[0] for call in runner.calls]\n            self.assertIn((\"git\", \"checkout\", \"HEAD\", \"--\", \".codex/skill-usage.json\"), calls)\n\n    def test_only_skill_usage_conflict_skips_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan),\n                FailingSquashMergeRunner(unmerged_paths=\".codex/skill-usage.json\\n\"),\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n                AssertionError(\"resolver should not run for usage-only conflicts\")\n            )\n            subject.restore_integration_skill_usage_to_head = lambda *_args: None\n            subject.consolidate_skill_usage = lambda *_args: None\n            subject.stage_integration_changes = lambda _worktree: None\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n    def test_conflict_restores_skill_usage_before_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan),\n                FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\"),\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            events: list[str] = []\n            subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n            subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(events, [\"restore\", \"resolve\", \"consolidate\", \"stage\"])\n\n    def test_plan_outside_repo_is_copied(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            plan = Path(temp) / \"external.md\"\n            repo.mkdir()\n            worktree.mkdir()\n            plan.write_text(\"# External\", encoding=\"utf-8\")\n            actual = flow.HarnessWorktreeFlow(self.config(repo, plan), FakeRunner()).ensure_plan_in_worktree(repo, plan, worktree, \"external\")\n            self.assertEqual(actual, worktree / \"docs\" / \"plans\" / \"external.md\")\n            self.assertEqual(actual.read_text(encoding=\"utf-8\"), \"# External\")\n\n    def test_harness_command_includes_model_and_output_file(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            plan = repo / \"plan.md\"\n            config = self.config(repo, plan, model=\"gpt-5\")\n            runner = FakeRunner()\n            flow.HarnessWorktreeFlow(config, runner).harness_exec(repo, \"Prompt\", repo / \"out.md\")\n            args = runner.calls[-1][0]\n            self.assertEqual(args[:2], (\"codex\", \"exec\"))\n            self.assertIn(\"--model\", args)\n            self.assertIn(\"gpt-5\", args)\n            self.assertEqual(args[-1], \"-\")\n            self.assertEqual(runner.inputs[-1], \"Prompt\")\n            self.assertIn(\"--output-last-message\", args)\n\n    def test_harness_exec_logs_jsonl_to_main_repo_archive(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            repo.mkdir()\n            worktree.mkdir()\n            output_file = worktree / \".codex\" / \"handoff\" / \"implementation-final-response.md\"\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), FakeRunner())\n\n            subject.start_log(repo, \"plan-run\")\n            subject.harness_exec(worktree, \"Secret prompt\", output_file)\n\n            log_file = repo / \".codex\" / \"worktree-flow\" / \"plan-run\" / \"workflow.jsonl\"\n            records = [\n                json.loads(line)\n                for line in log_file.read_text(encoding=\"utf-8\").splitlines()\n            ]\n            self.assertEqual(log_file, subject.log_file)\n            self.assertEqual(\n                [record[\"event\"] for record in records],\n                [\n                    \"workflow_log_started\",\n                    \"harness_exec_start\",\n                    \"harness_exec_finish\",\n                ],\n            )\n            self.assertEqual(records[1][\"cwd\"], str(worktree))\n            self.assertNotIn(\"Secret prompt\", records[1][\"command\"])\n            self.assertFalse(records[2][\"output_file_exists\"])\n            self.assertEqual(records[2][\"returncode\"], 0)\n\n    def test_stop_merge_mode_does_not_finish(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-plan\"\n            plan = repo / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            (worktree / \"docs\" / \"plans\").mkdir(parents=True)\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (worktree / \"docs\" / \"plans\" / \"plan.md\").write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = worktree / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n\n            runner = FakeRunner(\n                {\n                    (\"git\", \"branch\", \"--list\", \"feature/plan\"): \"\",\n                }\n            )\n            config = self.config(repo, plan, merge_mode=\"stop\")\n            subject = flow.HarnessWorktreeFlow(config, runner)\n            subject.create_feature_worktree = lambda _repo, _names: None\n            subject.run_implementation = lambda _worktree, _plan: None\n            subject.run_audit = lambda _worktree, _plan: None\n            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n            subject.validate = lambda _repo, _plan: None\n            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n\n            subject.run()\n\n    def test_conflict_context_contains_files_and_rules(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            plan = repo / \"plan.md\"\n            runner = FakeRunner(\n                {\n                    (\"git\", \"status\", \"--short\"): \"UU app.py\\n\",\n                    (\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"): \"app.py\\n\",\n                    (\"git\", \"merge-base\", \"main\", \"feature/plan\"): \"abc123\\n\",\n                    (\"git\", \"log\", \"--oneline\", \"abc123..main\"): \"base commit\\n\",\n                    (\"git\", \"log\", \"--oneline\", \"abc123..feature/plan\"): \"feature commit\\n\",\n                }\n            )\n            text = flow.HarnessWorktreeFlow(self.config(repo, plan), runner).conflict_context(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", repo, \"plan-run\"),\n                plan,\n            )\n            self.assertIn(\"app.py\", text)\n            self.assertIn(\"Latest base behavior is presumed correct\", text)\n            self.assertIn(\"feature commit\", text)\n\n    def test_post_conflict_audit_prompt_does_not_allow_commit(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            runner = FakeRunner()\n            flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner).run_audit(\n                repo,\n                repo / \"plan.md\",\n                post_conflict=True,\n            )\n            prompt = runner.inputs[-1]\n            self.assertIn(\"Do not commit\", prompt)\n            self.assertNotIn(\"commit audit fixes if changes are made\", prompt.lower())\n\n    def test_finish_does_not_cleanup_if_primary_merge_fails(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n\n            subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n            subject.prepare_harness_permissions = lambda _path: None\n\n            with self.assertRaises(flow.FlowError):\n                subject.finish(\n                    repo,\n                    flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                    plan,\n                    \"Plan\",\n                )\n\n    def test_non_conflict_squash_merge_failure_does_not_run_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n                AssertionError(\"resolver should not run\")\n            )\n\n            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                subject.finish(\n                    repo,\n                    flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                    plan,\n                    \"Plan\",\n                )\n\n    def test_conflict_squash_merge_failure_runs_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n            )\n            resolved = []\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.resolve_conflict = lambda *_args: resolved.append(True)\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(resolved, [True])\n\n    def test_parser_rejects_removed_yes_option(self) -> None:\n        with self.assertRaises(SystemExit):\n            flow.build_parser().parse_args([\"--plan\", \"plan.md\", \"--yes\"])\n\n    def test_local_git_worktree_and_squash_merge(self) -> None:\n        if not shutil.which(\"git\"):\n            self.skipTest(\"git is not installed\")\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            subprocess.run([\"git\", \"init\", \"-b\", \"main\", str(repo)], check=True, capture_output=True)\n            subprocess.run([\"git\", \"config\", \"user.email\", \"test@example.com\"], cwd=repo, check=True)\n            subprocess.run([\"git\", \"config\", \"user.name\", \"Test\"], cwd=repo, check=True)\n            (repo / \"file.txt\").write_text(\"base\\n\", encoding=\"utf-8\")\n            subprocess.run([\"git\", \"add\", \"file.txt\"], cwd=repo, check=True)\n            subprocess.run([\"git\", \"commit\", \"-m\", \"base\"], cwd=repo, check=True, capture_output=True)\n\n            feature = Path(temp) / \"repo-feature\"\n            subprocess.run([\"git\", \"worktree\", \"add\", str(feature), \"-b\", \"feature/test\", \"main\"], cwd=repo, check=True, capture_output=True)\n            (feature / \"file.txt\").write_text(\"feature\\n\", encoding=\"utf-8\")\n            subprocess.run([\"git\", \"add\", \"file.txt\"], cwd=feature, check=True)\n            subprocess.run([\"git\", \"commit\", \"-m\", \"feature\"], cwd=feature, check=True, capture_output=True)\n\n            integration = Path(temp) / \"repo-integration\"\n            subprocess.run([\"git\", \"worktree\", \"add\", str(integration), \"-b\", \"integration/test\", \"main\"], cwd=repo, check=True, capture_output=True)\n            subprocess.run([\"git\", \"merge\", \"--squash\", \"feature/test\"], cwd=integration, check=True, capture_output=True)\n            (integration / \".codex\" / \"handoff\").mkdir(parents=True)\n            (integration / \".codex\" / \"handoff\" / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"), flow.CommandRunner()\n            )\n            subject.stage_integration_changes(integration)\n            subprocess.run([\"git\", \"commit\", \"-m\", \"Harness: test\"], cwd=integration, check=True, capture_output=True)\n            subprocess.run([\"git\", \"switch\", \"main\"], cwd=repo, check=True, capture_output=True)\n            subprocess.run([\"git\", \"merge\", \"--ff-only\", \"integration/test\"], cwd=repo, check=True, capture_output=True)\n\n            committed = subprocess.run(\n                [\"git\", \"ls-tree\", \"-r\", \"--name-only\", \"HEAD\"],\n                cwd=repo,\n                check=True,\n                capture_output=True,\n                text=True,\n            ).stdout.splitlines()\n            self.assertEqual((repo / \"file.txt\").read_text(encoding=\"utf-8\"), \"feature\\n\")\n            self.assertIn(\"file.txt\", committed)\n            self.assertNotIn(\".codex/handoff/implementation-summary.md\", committed)\n\n    def config(\n        self,\n        repo: Path,\n        plan: Path,\n        *,\n        model: str | None = None,\n        merge_mode: str = \"squash\",\n    ):\n        return flow.FlowConfig(\n            repo=repo,\n            plan=plan,\n            base=\"main\",\n            model=model,\n            harness=\"codex\",\n            merge_mode=merge_mode,\n            keep_worktrees=False,\n        )\n\nif __name__ == \"__main__\":\n    unittest.main()\n\n\n exited 1 in 2170ms:\n\n\u001b[32;1mFullName\u001b[0m\n\u001b[32;1m--------\u001b[0m\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loaâ€¦\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loaâ€¦\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loaâ€¦\n\n\n2026-06-04T19:42:21.202602Z ERROR codex_core::tools::router: error=Exit code: 1\nWall time: 2.2 seconds\nOutput:\n\n\u001b[32;1mFullName\u001b[0m\n\u001b[32;1m--------\u001b[0m\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loaâ€¦\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loaâ€¦\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loaâ€¦\n\n\ncodex\nThe two current entrypoints are full copies that only differ in harness constants, which matches the plan. Iâ€™m going to add a shared sibling script and make both entrypoints thin loaders, then adjust tests to import the shared module for behavior checks.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Raw loadouts/worktrees/.harness/docs/codex-worktree-flow.md' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"rg -n \\\"HARNESS_DIR|DEFAULT_HARNESS|HANDOFF_DIR|worktree-flow|require_no_tracked|run_implementation|run_audit|finish\\\\(\\\" tests/test_codex_worktree_flow.py\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-ChildItem -Force loadouts/worktrees/.harness/scripts | Select-Object Name,Length' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-ChildItem -Force loadouts/worktrees/.opencode/scripts -ErrorAction SilentlyContinue | Select-Object Name,Length' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 935ms:\n# <harness> Worktree Flow\n\n`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n\n- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n\n1. Creates a feature branch and Git worktree from `main`.\n2. Runs <harness> implementation in that worktree.\n3. Requires an implementation commit and handoff summary.\n4. Runs a fresh <harness> audit pass.\n5. Requires an audit summary.\n6. Finishes through a temporary integration worktree.\n7. Squash-merges by default and cleans up only after a successful merge.\n\n## Quick Start\n\nCreate and approve a plan, save it as Markdown, then run the matching harness copy.\n\nCodex:\n\n```powershell\npython .\\.codex\\scripts\\worktree-flow-codex.py --plan docs\\plans\\my-plan.md\n```\n\nOh My Pi:\n\n```powershell\npython .\\.omp\\scripts\\worktree-flow-omp.py --plan docs\\plans\\my-plan.md\n```\n\nCommon options:\n\n```powershell\n# Show Git/<harness> commands without running them\npython .\\.codex\\scripts\\worktree-flow-codex.py --plan docs\\plans\\my-plan.md --dry-run\n\n# Implement and audit, but stop before merging\npython .\\.codex\\scripts\\worktree-flow-codex.py --plan docs\\plans\\my-plan.md --merge-mode stop\n\n# Keep feature/integration worktrees after completion\npython .\\.codex\\scripts\\worktree-flow-codex.py --plan docs\\plans\\my-plan.md --keep-worktrees\n\n# Use a non-main base branch\npython .\\.codex\\scripts\\worktree-flow-codex.py --plan docs\\plans\\my-plan.md --base develop\n```\n\n## Required Skills\n\nThe script expects these skills to be installed in the target repo for the active harness:\n\n- `implement-worktree`: implements the approved plan inside the script-created worktree.\n- `audit-worktree`: audits the implementation in a fresh <harness> run and fixes confirmed issues.\n- `merge-conflict-resolver`: resolves merge conflicts in the temporary integration worktree.\n\nFor this loadout, the source templates live under:\n\n```text\nloadouts/worktrees/.harness/skills/\n```\n\nWhen applying this loadout, `harness-init.ps1 -Harness <harness>` copies skills into:\n\n```text\n.<harness>/skills/\n```\n\n## Files Produced\n\nFeature worktree:\n\n```text\n.<harness>/handoff/implementation-summary.md\n.<harness>/handoff/implementation-final-response.md\n.<harness>/handoff/audit-summary.md\n.<harness>/handoff/audit-final-response.md\n```\n\nConflict path only, inside the integration worktree:\n\n```text\n.<harness>/handoff/merge-conflict-context.md\n.<harness>/handoff/conflict-resolution-summary.md\n.<harness>/handoff/conflict-resolution-final-response.md\n.<harness>/handoff/post-conflict-audit-summary.md\n.<harness>/handoff/post-conflict-audit-final-response.md\n```\n\nIf the plan is outside the repo, the script copies it into the feature worktree at:\n\n```text\ndocs/plans/<slug>.md\n```\n\nBefore cleanup, the script archives handoff files back into the primary checkout:\n\n```text\n.<harness>/worktree-flow/<slug>-<timestamp>/feature/\n.<harness>/worktree-flow/<slug>-<timestamp>/integration/\n```\n\nThese archive directories are the durable record after the feature and integration worktrees are removed.\n\n## Safety Notes\n\n- The script does not push branches or open PRs.\n- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n\n\n succeeded in 1269ms:\n26:flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n27:omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n135:        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n136:        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n137:        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n155:                harness=omp_flow.DEFAULT_HARNESS,\n184:                harness=omp_flow.DEFAULT_HARNESS,\n215:                harness=omp_flow.DEFAULT_HARNESS,\n455:            subject.run_implementation = lambda _worktree, _plan: None\n456:            subject.run_audit = lambda _worktree, _plan: None\n509:                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n518:            subject.run_implementation(repo, repo / \"plan.md\")\n519:            subject.run_audit(repo, repo / \"plan.md\")\n560:            subject.finish(\n610:            subject.finish(\n664:            subject.finish(\n692:            subject.finish(\n740:            log_file = repo / \".codex\" / \"worktree-flow\" / \"plan-run\" / \"workflow.jsonl\"\n759:    def test_stop_merge_mode_does_not_finish(self) -> None:\n782:            subject.run_implementation = lambda _worktree, _plan: None\n783:            subject.run_audit = lambda _worktree, _plan: None\n817:            flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner).run_audit(\n845:                subject.finish(\n873:                subject.finish(\n900:            subject.finish(\n\n succeeded in 1752ms:\n\n\u001b[32;1mName                  \u001b[0m\u001b[32;1m Length\u001b[0m\n\u001b[32;1m----                  \u001b[0m \u001b[32;1m------\u001b[0m\nskill-usage-manager.py  30399\nworktree-flow-codex.py  34967\nworktree-flow-omp.py    34963\n\n\n exited 1 in 2183ms:\n2026-06-04T19:42:37.989666Z ERROR codex_core::tools::router: error=Exit code: 1\nWall time: 2.2 seconds\nOutput:\n\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command '$lines=Get-Content tests/test_codex_worktree_flow.py; $lines[575..720]' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command '$lines=Get-Content tests/test_codex_worktree_flow.py; $lines[1..120]' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command '$lines=Get-Content tests/test_codex_worktree_flow.py; $lines[420..575]' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Compare-Object (Get-Content .codex/scripts/skill-usage-manager.py) (Get-Content loadouts/worktrees/.harness/scripts/skill-usage-manager.py) | Select-Object -First 5' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 1040ms:\n succeeded in 1345ms:\n\nimport importlib.util\nimport json\nimport shutil\nimport subprocess\nimport sys\nimport tempfile\nimport unittest\nfrom unittest import mock\nfrom pathlib import Path\n\n\nSCRIPT_DIR = Path(__file__).resolve().parents[1] / \".codex\" / \"scripts\"\n\n\ndef load_flow_module(name: str, script_name: str):\n    spec = importlib.util.spec_from_file_location(name, SCRIPT_DIR / script_name)\n    module = importlib.util.module_from_spec(spec)\n    assert spec and spec.loader\n    sys.modules[spec.name] = module\n    spec.loader.exec_module(module)\n    return module\n\n\nflow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\nomp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n\n\nclass FakeRunner:\n    def __init__(\n        self,\n        outputs: dict[\n            tuple[str, ...], flow.CommandResult | list[str] | str\n        ] | None = None,\n        *,\n        dry_run: bool = False,\n    ) -> None:\n        self.outputs = outputs or {}\n        self.calls: list[tuple[tuple[str, ...], Path, bool]] = []\n        self.inputs: list[str | None] = []\n        self.dry_run = dry_run\n\n    def run(self, args, cwd, *, check=True, capture=True, input_text=None):\n        key = tuple(args)\n        self.calls.append((key, Path(cwd), check))\n        self.inputs.append(input_text)\n        value = self.outputs.get(key, \"\")\n        if isinstance(value, flow.CommandResult):\n            return value\n        if isinstance(value, list):\n            stdout = value.pop(0) if value else \"\"\n        else:\n            stdout = value\n        return flow.CommandResult(key, Path(cwd), 0, stdout, \"\")\n\n\nclass FailingFastForwardRunner(FakeRunner):\n    def run(self, args, cwd, *, check=True, capture=True, input_text=None):\n        if tuple(args[:3]) == (\"git\", \"merge\", \"--ff-only\"):\n            result = flow.CommandResult(tuple(args), Path(cwd), 1, \"\", \"not a fast-forward\")\n            if check:\n                raise flow.FlowError(flow.format_command_failure(result))\n            return result\n        return super().run(args, cwd, check=check, capture=capture, input_text=input_text)\n\n\nclass FailingSquashMergeRunner(FakeRunner):\n    def __init__(self, *, unmerged_paths: str) -> None:\n        super().__init__()\n        self.unmerged_paths = unmerged_paths\n\n    def run(self, args, cwd, *, check=True, capture=True, input_text=None):\n        key = tuple(args)\n        if key[:3] == (\"git\", \"merge\", \"--squash\"):\n            self.calls.append((key, Path(cwd), check))\n            return flow.CommandResult(key, Path(cwd), 1, \"\", \"merge failed\")\n        if key == (\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"):\n            self.calls.append((key, Path(cwd), check))\n            return flow.CommandResult(key, Path(cwd), 0, self.unmerged_paths, \"\")\n        return super().run(args, cwd, check=check, capture=capture, input_text=input_text)\n\n\nclass CommandRunnerTests(unittest.TestCase):\n    def test_run_resolves_executable_before_invoking_subprocess(self) -> None:\n        completed = subprocess.CompletedProcess(\n            [\"C:/bin/codex.CMD\", \"exec\", \"--help\"],\n            0,\n            \"ok\",\n            \"\",\n        )\n        with (\n            tempfile.TemporaryDirectory() as temp,\n            mock.patch.object(flow.shutil, \"which\", return_value=\"C:/bin/codex.CMD\") as which,\n            mock.patch.object(flow.subprocess, \"run\", return_value=completed) as run,\n        ):\n            result = flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n\n        which.assert_called_once_with(\"codex\")\n        run.assert_called_once()\n        self.assertEqual(\n            run.call_args.args[0],\n            [\"C:/bin/codex.CMD\", \"exec\", \"--help\"],\n        )\n        self.assertEqual(result.args, (\"codex\", \"exec\", \"--help\"))\n        self.assertEqual(result.stdout, \"ok\")\n\n    def test_run_reports_missing_executable_as_flow_error(self) -> None:\n        with (\n            tempfile.TemporaryDirectory() as temp,\n            mock.patch.object(flow.shutil, \"which\", return_value=None),\n            mock.patch.object(flow.subprocess, \"run\") as run,\n            self.assertRaisesRegex(flow.FlowError, \"Executable not found on PATH: codex\"),\n        ):\n            flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n\n        run.assert_not_called()\n\n    def test_run_reports_launch_oserror_as_flow_error(self) -> None:\n        with (\n            tempfile.TemporaryDirectory() as temp,\n\n succeeded in 1730ms:\n            with mock.patch.object(flow.os, \"name\", \"posix\"):\n                subject.harness_exec(repo, \"Prompt\", repo / \"out.md\")\n\n            args = runner.calls[-1][0]\n            sandbox_index = args.index(\"--sandbox\")\n            self.assertEqual(args[sandbox_index + 1], \"workspace-write\")\n\n    def test_run_prepares_primary_repo_permissions_before_logging(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-plan\"\n            plan = repo / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            (repo / \".codex\").mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (worktree / \"docs\" / \"plans\").mkdir(parents=True)\n            (worktree / \"docs\" / \"plans\" / \"plan.md\").write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = worktree / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n            runner = FakeRunner(\n                {\n                    (\"git\", \"branch\", \"--list\", \"feature/plan\"): \"\",\n                    (\"git\", \"rev-parse\", \"--git-common-dir\"): \".git\",\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan, merge_mode=\"stop\"), runner\n            )\n            prepared: list[Path] = []\n            subject.prepare_harness_permissions = prepared.append\n            subject.create_feature_worktree = lambda _repo, _names: None\n            subject.run_implementation = lambda _worktree, _plan: None\n            subject.run_audit = lambda _worktree, _plan: None\n            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n            )\n            subject.validate = lambda _repo, _plan: None\n            subject.finish = lambda *_args: (_ for _ in ()).throw(\n                AssertionError(\"finish should not run\")\n            )\n\n            subject.run()\n\n            self.assertEqual(prepared[0], repo / \".codex\")\n\n    def test_stage_integration_changes_excludes_handoff_artifacts(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            runner = FakeRunner()\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.stage_integration_changes(repo)\n\n            self.assertEqual(\n                runner.calls[0][0],\n                (\n                    \"git\",\n                    \"add\",\n                    \"-A\",\n                    \"--\",\n                    \".\",\n                    \":(exclude).codex/handoff/**\",\n                ),\n            )\n\n\n    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            runner = FakeRunner(\n                {\n                    (\n                        \"git\",\n                        \"ls-tree\",\n                        \"-r\",\n                        \"--name-only\",\n                        \"feature/plan\",\n                        \"--\",\n                        \".codex/handoff\",\n                    ): \".codex/handoff/audit-summary.md\\n\",\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n\n    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            runner = FakeRunner()\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.run_implementation(repo, repo / \"plan.md\")\n            subject.run_audit(repo, repo / \"plan.md\")\n\n            prompts = [value for value in runner.inputs if value is not None]\n            self.assertIn(\"Do not commit files under `.codex/handoff/`\", prompts[-2])\n            self.assertIn(\"Do not commit files under `.codex/handoff/`\", prompts[-1])\n\n    def test_snapshot_skill_usage_baseline_writes_empty_ledger_when_absent(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            worktree = Path(temp) / \"repo-feature\"\n            (worktree / \".codex\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(worktree, worktree / \"plan.md\"), FakeRunner()\n            )\n\n            baseline = subject.snapshot_skill_usage_baseline(worktree)\n\n            self.assertEqual(baseline, worktree / \".codex\" / \"handoff\" / \"skill-usage-baseline.json\")\n            self.assertEqual(\n                json.loads(baseline.read_text(encoding=\"utf-8\")),\n                {\"version\": 1, \"scopes\": {}},\n            )\n\n    def test_finish_restores_and_consolidates_before_staging_in_squash_mode(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"skill-usage-baseline.json\").write_text(\"{}\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FakeRunner())\n            subject.prepare_harness_permissions = lambda _path: None\n            events: list[str] = []\n            subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(events, [\"restore\", \"consolidate\", \"stage\"])\n\n\n    def test_consolidate_skill_usage_targets_primary_repo_paths(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            root = Path(temp)\n            feature = root / \"repo-plan\"\n            integration = root / \"repo-integrate\"\n            repo = root / \"repo\"\n            baseline = feature / \".codex\" / \"handoff\" / \"skill-usage-baseline.json\"\n\n succeeded in 2194ms:\n            baseline = feature / \".codex\" / \"handoff\" / \"skill-usage-baseline.json\"\n            runner = FakeRunner()\n\n            flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner).consolidate_skill_usage(\n                feature,\n                integration,\n                repo,\n                baseline,\n            )\n\n            args = runner.calls[-1][0]\n            self.assertIn(\"--target-repo\", args)\n            self.assertEqual(args[args.index(\"--target-repo\") + 1], str(repo))\n            self.assertIn(\"--target-worktree\", args)\n            self.assertEqual(args[args.index(\"--target-worktree\") + 1], str(integration))\n\n    def test_no_ff_merge_uses_no_commit_and_workflow_commit(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan, merge_mode=\"no-ff\"), FakeRunner()\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.restore_integration_skill_usage_to_head = lambda *_args: None\n            subject.consolidate_skill_usage = lambda *_args: None\n            subject.stage_integration_changes = lambda _worktree: None\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            calls = [call[0] for call in subject.runner.calls]\n            self.assertIn((\"git\", \"merge\", \"--no-ff\", \"--no-commit\", \"feature/plan\"), calls)\n            self.assertIn((\"git\", \"commit\", \"-m\", \"Harness: Plan\"), calls)\n            self.assertNotIn((\"git\", \"merge\", \"--continue\"), calls)\n\n    def test_restore_skill_usage_to_head_restores_tracked_ledger(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            (repo / \".codex\").mkdir(parents=True)\n            runner = FakeRunner(\n                {\n                    (\"git\", \"cat-file\", \"-e\", \"HEAD:.codex/skill-usage.json\"): flow.CommandResult(\n                        (\"git\", \"cat-file\", \"-e\", \"HEAD:.codex/skill-usage.json\"),\n                        repo,\n                        0,\n                    )\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.restore_integration_skill_usage_to_head(repo)\n\n            calls = [call[0] for call in runner.calls]\n            self.assertIn((\"git\", \"checkout\", \"HEAD\", \"--\", \".codex/skill-usage.json\"), calls)\n\n    def test_only_skill_usage_conflict_skips_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan),\n                FailingSquashMergeRunner(unmerged_paths=\".codex/skill-usage.json\\n\"),\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n                AssertionError(\"resolver should not run for usage-only conflicts\")\n            )\n            subject.restore_integration_skill_usage_to_head = lambda *_args: None\n            subject.consolidate_skill_usage = lambda *_args: None\n            subject.stage_integration_changes = lambda _worktree: None\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n    def test_conflict_restores_skill_usage_before_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan),\n                FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\"),\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            events: list[str] = []\n            subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n            subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(events, [\"restore\", \"resolve\", \"consolidate\", \"stage\"])\n\n    def test_plan_outside_repo_is_copied(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            plan = Path(temp) / \"external.md\"\n            repo.mkdir()\n            worktree.mkdir()\n            plan.write_text(\"# External\", encoding=\"utf-8\")\n            actual = flow.HarnessWorktreeFlow(self.config(repo, plan), FakeRunner()).ensure_plan_in_worktree(repo, plan, worktree, \"external\")\n            self.assertEqual(actual, worktree / \"docs\" / \"plans\" / \"external.md\")\n            self.assertEqual(actual.read_text(encoding=\"utf-8\"), \"# External\")\n\n    def test_harness_command_includes_model_and_output_file(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            plan = repo / \"plan.md\"\n            config = self.config(repo, plan, model=\"gpt-5\")\n            runner = FakeRunner()\n            flow.HarnessWorktreeFlow(config, runner).harness_exec(repo, \"Prompt\", repo / \"out.md\")\n            args = runner.calls[-1][0]\n            self.assertEqual(args[:2], (\"codex\", \"exec\"))\n\ncodex\nIâ€™m making the script changes now: first copying the existing Codex implementation into a shared `worktree-flow.py`, then converting harness-specific paths into `FlowConfig` fields so one imported module can drive either Codex or OMP without global-state collisions.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Copy-Item -LiteralPath .codex/scripts/worktree-flow-codex.py -Destination .codex/scripts/worktree-flow.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 966ms:\ncodex\nThe shared file is in place. Iâ€™m patching it manually now so harness directory and handoff paths are instance configuration instead of fixed module constants.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\scripts\\worktree-flow.py\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..bc665566aafcf993c171ef99556228f04ff156e3\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,14 +154,15 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n class HarnessWorktreeFlow:\n\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..bc665566aafcf993c171ef99556228f04ff156e3\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,14 +154,15 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n class HarnessWorktreeFlow:\n\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'rg -n \"HARNESS_DIR|HANDOFF_DIR\" .codex/scripts/worktree-flow.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 915ms:\n34:HARNESS_DIR = infer_default_harness_dir()\n35:DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n36:HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n180:        self.prepare_harness_permissions(repo / HARNESS_DIR)\n192:        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n195:        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n252:        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n253:        self.ensure_dir(names.worktree / HANDOFF_DIR)\n254:        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n283:- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n284:- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n286:        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n292:        summary = HANDOFF_DIR / (\n306:- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n307:{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n311:Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n316:            / HANDOFF_DIR\n385:        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n386:        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n387:        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n392:        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n459:        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n468:- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n470:- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n471:- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n473:Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n480:            / HANDOFF_DIR\n484:            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n488:            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n494:        source_handoff = feature_worktree / HANDOFF_DIR\n495:        dest_handoff = integration_worktree / HANDOFF_DIR\n515:        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n517:        source = worktree / HANDOFF_DIR\n532:                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n538:        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n541:        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n556:        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n653:                HARNESS_DIR.as_posix(),\n675:                HANDOFF_DIR.as_posix(),\n686:            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n705:        harness_dir = worktree / HARNESS_DIR\n893:        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..bc665566aafcf993c171ef99556228f04ff156e3\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,14 +154,15 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n class HarnessWorktreeFlow:\n\ncodex\nIâ€™m adding small accessors for the active harness directory and handoff directory, then replacing the old fixed references through the flow lifecycle. This keeps the existing wrapper constants available while making behavior configurable.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\scripts\\worktree-flow.py\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..8389ff8728304f8cadefae4e69ffd2967f352d0f\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -173,10 +197,10 @@\n         )\n         self.snapshot_skill_usage_baseline(names.worktree, repo)\n         self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n         self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n         archive_dir = self.archive_handoff(\n             repo, names.worktree, names.run_id, \"feature\"\n@@ -233,9 +257,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +288,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +311,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -366,14 +390,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +464,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +473,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +520,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +537,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +561,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +658,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -656,7 +680,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,7 +691,7 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n         )\n \n     def git_common_dir(self, worktree: Path) -> Path | None:\n@@ -686,7 +710,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +898,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..8389ff8728304f8cadefae4e69ffd2967f352d0f\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -173,10 +197,10 @@\n         )\n         self.snapshot_skill_usage_baseline(names.worktree, repo)\n         self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n         self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n         archive_dir = self.archive_handoff(\n             repo, names.worktree, names.run_id, \"feature\"\n@@ -233,9 +257,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +288,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +311,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -366,14 +390,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +464,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +473,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +520,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +537,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +561,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +658,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -656,7 +680,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,7 +691,7 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n         )\n \n     def git_common_dir(self, worktree: Path) -> Path | None:\n@@ -686,7 +710,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +898,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"rg -n \\\"HARNESS_DIR|HANDOFF_DIR|FlowConfig\\\\(\\\" .codex/scripts/worktree-flow.py\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 884ms:\n34:HARNESS_DIR = infer_default_harness_dir()\n35:DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n36:HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n962:    config = FlowConfig(\n\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..8389ff8728304f8cadefae4e69ffd2967f352d0f\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -173,10 +197,10 @@\n         )\n         self.snapshot_skill_usage_baseline(names.worktree, repo)\n         self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n         self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n         archive_dir = self.archive_handoff(\n             repo, names.worktree, names.run_id, \"feature\"\n@@ -233,9 +257,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +288,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +311,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -366,14 +390,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +464,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +473,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +520,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +537,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +561,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +658,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -656,7 +680,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,7 +691,7 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n         )\n \n     def git_common_dir(self, worktree: Path) -> Path | None:\n@@ -686,7 +710,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +898,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n\ncodex\nThe shared flow now has per-instance harness paths. Iâ€™m updating the CLI builder so wrappers can pass explicit defaults, and direct execution can still infer a useful default from the installed script directory.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\scripts\\worktree-flow.py\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..fb56a1257f5eca2398290aaf3d22d4526491d9b4\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -173,10 +197,10 @@\n         )\n         self.snapshot_skill_usage_baseline(names.worktree, repo)\n         self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n         self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n         archive_dir = self.archive_handoff(\n             repo, names.worktree, names.run_id, \"feature\"\n@@ -233,9 +257,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +288,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +311,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -366,14 +390,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +464,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +473,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +520,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +537,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +561,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +658,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -656,7 +680,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,7 +691,7 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n         )\n \n     def git_common_dir(self, worktree: Path) -> Path | None:\n@@ -686,7 +710,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +898,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +925,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +941,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +965,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..fb56a1257f5eca2398290aaf3d22d4526491d9b4\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -173,10 +197,10 @@\n         )\n         self.snapshot_skill_usage_baseline(names.worktree, repo)\n         self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n         self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n         archive_dir = self.archive_handoff(\n             repo, names.worktree, names.run_id, \"feature\"\n@@ -233,9 +257,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +288,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +311,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -366,14 +390,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +464,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +473,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +520,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +537,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +561,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +658,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -656,7 +680,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,7 +691,7 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n         )\n \n     def git_common_dir(self, worktree: Path) -> Path | None:\n@@ -686,7 +710,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +898,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +925,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +941,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +965,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\ncodex\nIâ€™m adding explicit workflow guard helpers now. The checks will look for commits and diffs against the base branch, and theyâ€™ll permit only untracked/staged files under the active handoff directory.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\scripts\\worktree-flow.py\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..a8d75752b74895061072239b9575a2b36c34264e\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,7 +696,7 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n         )\n \n     def git_common_dir(self, worktree: Path) -> Path | None:\n@@ -686,7 +715,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +903,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +930,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +946,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +970,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..a8d75752b74895061072239b9575a2b36c34264e\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,7 +696,7 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n         )\n \n     def git_common_dir(self, worktree: Path) -> Path | None:\n@@ -686,7 +715,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +903,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +930,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +946,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +970,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\scripts\\worktree-flow.py\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\ncodex\nThe invariants are wired into the main flow after implementation, after audit, and again before integration. Iâ€™m replacing the duplicated Codex and OMP files with compatibility wrappers that delegate to the shared script with explicit defaults.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\scripts\\worktree-flow-codex.py\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\ndeleted file mode 100644\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..0000000000000000000000000000000000000000\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ /dev/null\n@@ -1,956 +0,0 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\ndeleted file mode 100644\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..0000000000000000000000000000000000000000\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ /dev/null\n@@ -1,956 +0,0 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\scripts\\worktree-flow-codex.py\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\scripts\\worktree-flow-omp.py\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\ndeleted file mode 100644\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..0000000000000000000000000000000000000000\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ /dev/null\n@@ -1,956 +0,0 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\ndeleted file mode 100644\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..0000000000000000000000000000000000000000\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ /dev/null\n@@ -1,956 +0,0 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\scripts\\worktree-flow-omp.py\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"rg -n \\\"FlowConfig\\\\(\\\" tests/test_codex_worktree_flow.py\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 886ms:\n150:            config = omp_flow.FlowConfig(\n179:            config = omp_flow.FlowConfig(\n210:            config = omp_flow.FlowConfig(\n963:        return flow.FlowConfig(\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n\ncodex\nIâ€™m updating the test harness to load the shared module for behavior coverage while still checking the Codex and OMP wrappers expose the expected defaults. The config helper now supplies the active harness directory explicitly.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\tests\\test_codex_worktree_flow.py\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..6718063929c4e7813e8fa72ea4e5e2af73ab846f\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,28 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +163,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +193,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +225,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -963,12 +979,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..6718063929c4e7813e8fa72ea4e5e2af73ab846f\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,28 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +163,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +193,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +225,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -963,12 +979,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\n\ncodex\nSome existing lifecycle tests stub out implementation and audit, so Iâ€™m isolating them from the new invariant checks and adding focused tests for the checks themselves. That keeps old intent intact and covers the new behavior directly.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\tests\\test_codex_worktree_flow.py\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..e89e379ae8e0b2aec7b01448943ec2a7dcd710d1\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -779,11 +805,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -963,12 +993,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..e89e379ae8e0b2aec7b01448943ec2a7dcd710d1\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -779,11 +805,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -963,12 +993,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\n\ncodex\nIâ€™m adding unit tests for the new invariants using the existing fake command runner. These test the command-level failure modes without creating extra worktrees.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\tests\\test_codex_worktree_flow.py\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f5a7cdbe21372fd91ab835bc4a9b0b4a91ec5566\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -779,11 +1037,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -963,12 +1225,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f5a7cdbe21372fd91ab835bc4a9b0b4a91ec5566\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -779,11 +1037,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -963,12 +1225,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\n\ncodex\nThe new final guard is intentionally stricter, so Iâ€™m adjusting older finish-path tests to bypass that guard unless the test is specifically about guard behavior. That keeps those tests focused on merge/staging/conflict behavior.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"rg -n \\\"subject\\\\.finish\\\\(\\\" tests/test_codex_worktree_flow.py\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 875ms:\n818:            subject.finish(\n868:            subject.finish(\n922:            subject.finish(\n950:            subject.finish(\n1107:                subject.finish(\n1135:                subject.finish(\n1162:            subject.finish(\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f5a7cdbe21372fd91ab835bc4a9b0b4a91ec5566\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -779,11 +1037,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -963,12 +1225,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\n\ncodex\nI found seven finish-path tests that exercise integration mechanics. Iâ€™m adding a small stub in each so they donâ€™t need to synthesize unrelated git status/diff responses.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command '$lines=Get-Content tests/test_codex_worktree_flow.py; $lines[850..970]' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command '$lines=Get-Content tests/test_codex_worktree_flow.py; $lines[1085..1170]' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command '$lines=Get-Content tests/test_codex_worktree_flow.py; $lines[790..830]' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 1034ms:\n            self.assertNotIn(\"commit audit fixes if changes are made\", prompt.lower())\n\n    def test_finish_does_not_cleanup_if_primary_merge_fails(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n\n            subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n            subject.prepare_harness_permissions = lambda _path: None\n\n            with self.assertRaises(flow.FlowError):\n                subject.finish(\n                    repo,\n                    flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                    plan,\n                    \"Plan\",\n                )\n\n    def test_non_conflict_squash_merge_failure_does_not_run_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n                AssertionError(\"resolver should not run\")\n            )\n\n            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                subject.finish(\n                    repo,\n                    flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                    plan,\n                    \"Plan\",\n                )\n\n    def test_conflict_squash_merge_failure_runs_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n            )\n            resolved = []\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.resolve_conflict = lambda *_args: resolved.append(True)\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(resolved, [True])\n\n    def test_parser_rejects_removed_yes_option(self) -> None:\n\n succeeded in 1518ms:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan, merge_mode=\"no-ff\"), FakeRunner()\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.restore_integration_skill_usage_to_head = lambda *_args: None\n            subject.consolidate_skill_usage = lambda *_args: None\n            subject.stage_integration_changes = lambda _worktree: None\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            calls = [call[0] for call in subject.runner.calls]\n            self.assertIn((\"git\", \"merge\", \"--no-ff\", \"--no-commit\", \"feature/plan\"), calls)\n            self.assertIn((\"git\", \"commit\", \"-m\", \"Harness: Plan\"), calls)\n            self.assertNotIn((\"git\", \"merge\", \"--continue\"), calls)\n\n    def test_restore_skill_usage_to_head_restores_tracked_ledger(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            (repo / \".codex\").mkdir(parents=True)\n            runner = FakeRunner(\n                {\n                    (\"git\", \"cat-file\", \"-e\", \"HEAD:.codex/skill-usage.json\"): flow.CommandResult(\n                        (\"git\", \"cat-file\", \"-e\", \"HEAD:.codex/skill-usage.json\"),\n                        repo,\n                        0,\n                    )\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.restore_integration_skill_usage_to_head(repo)\n\n            calls = [call[0] for call in runner.calls]\n            self.assertIn((\"git\", \"checkout\", \"HEAD\", \"--\", \".codex/skill-usage.json\"), calls)\n\n    def test_only_skill_usage_conflict_skips_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan),\n                FailingSquashMergeRunner(unmerged_paths=\".codex/skill-usage.json\\n\"),\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n                AssertionError(\"resolver should not run for usage-only conflicts\")\n            )\n            subject.restore_integration_skill_usage_to_head = lambda *_args: None\n            subject.consolidate_skill_usage = lambda *_args: None\n            subject.stage_integration_changes = lambda _worktree: None\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n    def test_conflict_restores_skill_usage_before_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan),\n                FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\"),\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            events: list[str] = []\n            subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n            subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(events, [\"restore\", \"resolve\", \"consolidate\", \"stage\"])\n\n    def test_plan_outside_repo_is_copied(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            plan = Path(temp) / \"external.md\"\n            repo.mkdir()\n            worktree.mkdir()\n            plan.write_text(\"# External\", encoding=\"utf-8\")\n            actual = flow.HarnessWorktreeFlow(self.config(repo, plan), FakeRunner()).ensure_plan_in_worktree(repo, plan, worktree, \"external\")\n            self.assertEqual(actual, worktree / \"docs\" / \"plans\" / \"external.md\")\n            self.assertEqual(actual.read_text(encoding=\"utf-8\"), \"# External\")\n\n    def test_harness_command_includes_model_and_output_file(self) -> None:\n\n succeeded in 1749ms:\n            baseline = subject.snapshot_skill_usage_baseline(worktree)\n\n            self.assertEqual(baseline, worktree / \".codex\" / \"handoff\" / \"skill-usage-baseline.json\")\n            self.assertEqual(\n                json.loads(baseline.read_text(encoding=\"utf-8\")),\n                {\"version\": 1, \"scopes\": {}},\n            )\n\n    def test_finish_restores_and_consolidates_before_staging_in_squash_mode(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"skill-usage-baseline.json\").write_text(\"{}\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FakeRunner())\n            subject.prepare_harness_permissions = lambda _path: None\n            events: list[str] = []\n            subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(events, [\"restore\", \"consolidate\", \"stage\"])\n\n\n    def test_consolidate_skill_usage_targets_primary_repo_paths(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            root = Path(temp)\n            feature = root / \"repo-plan\"\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f5a7cdbe21372fd91ab835bc4a9b0b4a91ec5566\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -779,11 +1037,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -963,12 +1225,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\n\ncodex\nIâ€™m applying the test stubs now. After that Iâ€™ll sync the loadout scripts and add the drift-protection test.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\tests\\test_codex_worktree_flow.py\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\n\ncodex\nIâ€™m adding a dedicated sync test that compares active `.codex/scripts` files to the worktrees loadout templates after normalizing line endings. This turns future drift into a focused test failure.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\tests\\test_worktrees_loadout_sync.py\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nThe sync test is in place. Since there is no `loadouts/worktrees/.opencode/scripts/` directory in this worktree, Iâ€™m syncing the canonical active scripts to the `.harness` loadout template copies called out by the plan.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Copy-Item -LiteralPath .codex/scripts/worktree-flow.py -Destination loadouts/worktrees/.harness/scripts/worktree-flow.py; Copy-Item -LiteralPath .codex/scripts/worktree-flow-codex.py -Destination loadouts/worktrees/.harness/scripts/worktree-flow-codex.py; Copy-Item -LiteralPath .codex/scripts/worktree-flow-omp.py -Destination loadouts/worktrees/.harness/scripts/worktree-flow-omp.py; Copy-Item -LiteralPath .codex/scripts/skill-usage-manager.py -Destination loadouts/worktrees/.harness/scripts/skill-usage-manager.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 978ms:\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nIâ€™m updating the worktree-flow documentation to describe the shared implementation and compatibility wrappers, while leaving the existing quick-start wrapper commands intact.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\loadouts\\worktrees\\.harness\\docs\\codex-worktree-flow.md\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nThe implementation, wrappers, sync test, loadout copies, and docs are now updated. Iâ€™m running the focused unittest targets from the approved plan to catch regressions before I prepare the commit.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'python -m unittest tests.test_codex_worktree_flow' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'python -m unittest tests.test_worktrees_loadout_sync' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 1118ms:\n.\n----------------------------------------------------------------------\nRan 1 test in 0.090s\n\nOK\n\n succeeded in 3405ms:\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpgr1jk6mw) codex exec --help\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpnk28uu6m) codex exec --help\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpucetfiut) codex exec --help\n+ mkdir -p C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpvegfjh1o\\repo\\.codex\\worktree-flow\\plan-run\\feature\n+ copy C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpvegfjh1o\\repo-feature\\.codex\\handoff\\implementation-summary.md C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpvegfjh1o\\repo\\.codex\\worktree-flow\\plan-run\\feature\\implementation-summary.md\n+ mkdir -p C:\\Users\\joesa\\AppData\\Local\\Temp\\tmppyqmamdt\\repo-feature\\docs\\plans\n+ copy C:\\Users\\joesa\\AppData\\Local\\Temp\\tmppyqmamdt\\external.md C:\\Users\\joesa\\AppData\\Local\\Temp\\tmppyqmamdt\\repo-feature\\docs\\plans\\external.md\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpq_7anisr\\repo-integration) git add -A -- . :(exclude).codex/handoff/**\nFeature branch: feature/plan\nFeature worktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpygdqhdrm\\repo-plan\nHandoff archive: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpygdqhdrm\\repo\\.codex\\worktree-flow\\plan-run\\feature\nStopped before merge by request.\nPlan: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpygdqhdrm\\repo-plan\\docs\\plans\\plan.md\nWorktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpygdqhdrm\\repo-plan\nBranch: feature/plan\nFeature branch: feature/plan\nFeature worktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpcniysr0v\\repo-plan\nHandoff archive: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpcniysr0v\\repo\\.codex\\worktree-flow\\plan-run\\feature\nStopped before merge by request.\nPlan: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpcniysr0v\\repo-plan\\docs\\plans\\plan.md\nWorktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpcniysr0v\\repo-plan\nBranch: feature/plan\n..............................usage: python.exe -m unittest [-h] --plan PLAN [--repo REPO] [--base BASE]\n                              [--model MODEL] [--harness HARNESS]\n                              [--harness-dir HARNESS_DIR]\n                              [--merge-mode {squash,no-ff,stop}]\n                              [--keep-worktrees] [--dry-run]\npython.exe -m unittest: error: unrecognized arguments: --yes\n.......................\n----------------------------------------------------------------------\nRan 53 tests in 1.788s\n\nOK\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nBoth focused test suites passed. Iâ€™m checking the final diff/status now so I can stage only intended implementation files and leave `.codex/handoff/` untracked.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"git diff --name-only -- . ':\"'!scratchpad.md'\"' ':\"'!.codex/handoff/**'\"'\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"git diff --stat -- . ':\"'!scratchpad.md'\"' ':\"'!.codex/handoff/**'\"'\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git status --short' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 967ms:\n .codex/scripts/worktree-flow-codex.py              | 956 +--------------------\n .codex/scripts/worktree-flow-omp.py                | 956 +--------------------\n .codex/skill-usage.json                            |  17 +\n .../worktrees/.harness/docs/codex-worktree-flow.md |   9 +-\n .../.harness/scripts/worktree-flow-codex.py        | 956 +--------------------\n .../.harness/scripts/worktree-flow-omp.py          | 956 +--------------------\n tests/test_codex_worktree_flow.py                  | 274 +++++-\n 7 files changed, 399 insertions(+), 3725 deletions(-)\nwarning: in the working copy of '.codex/scripts/worktree-flow-codex.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of '.codex/scripts/worktree-flow-omp.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/docs/codex-worktree-flow.md', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/scripts/worktree-flow-codex.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/scripts/worktree-flow-omp.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'tests/test_codex_worktree_flow.py', LF will be replaced by CRLF the next time Git touches it\n\n succeeded in 1297ms:\n.codex/scripts/worktree-flow-codex.py\n.codex/scripts/worktree-flow-omp.py\n.codex/skill-usage.json\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md\nloadouts/worktrees/.harness/scripts/worktree-flow-codex.py\nloadouts/worktrees/.harness/scripts/worktree-flow-omp.py\ntests/test_codex_worktree_flow.py\nwarning: in the working copy of '.codex/scripts/worktree-flow-codex.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of '.codex/scripts/worktree-flow-omp.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/docs/codex-worktree-flow.md', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/scripts/worktree-flow-codex.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/scripts/worktree-flow-omp.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'tests/test_codex_worktree_flow.py', LF will be replaced by CRLF the next time Git touches it\n\n succeeded in 1304ms:\n M .codex/scripts/worktree-flow-codex.py\n M .codex/scripts/worktree-flow-omp.py\n M .codex/skill-usage.json\n M loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n M loadouts/worktrees/.harness/scripts/worktree-flow-codex.py\n M loadouts/worktrees/.harness/scripts/worktree-flow-omp.py\n M tests/test_codex_worktree_flow.py\n?? .codex/handoff/\n?? .codex/scripts/worktree-flow.py\n?? loadouts/worktrees/.harness/scripts/worktree-flow.py\n?? tests/test_worktrees_loadout_sync.py\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nThe changed files match the plan plus the required skill-usage log. Iâ€™m running a final whitespace check and rerunning the focused tests after the last documentation edit before staging.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"git diff --check -- . ':\"'!scratchpad.md'\"' ':\"'!.codex/handoff/**'\"'\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 950ms:\nwarning: in the working copy of '.codex/scripts/worktree-flow-codex.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of '.codex/scripts/worktree-flow-omp.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/docs/codex-worktree-flow.md', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/scripts/worktree-flow-codex.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/scripts/worktree-flow-omp.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'tests/test_codex_worktree_flow.py', LF will be replaced by CRLF the next time Git touches it\n\n succeeded in 3255ms:\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmp69aiy9cs) codex exec --help\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmp0djz63h6) codex exec --help\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmp8a8zjv78) codex exec --help\n+ mkdir -p C:\\Users\\joesa\\AppData\\Local\\Temp\\tmppeyjwpil\\repo\\.codex\\worktree-flow\\plan-run\\feature\n+ copy C:\\Users\\joesa\\AppData\\Local\\Temp\\tmppeyjwpil\\repo-feature\\.codex\\handoff\\implementation-summary.md C:\\Users\\joesa\\AppData\\Local\\Temp\\tmppeyjwpil\\repo\\.codex\\worktree-flow\\plan-run\\feature\\implementation-summary.md\n+ mkdir -p C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpi89o_mp5\\repo-feature\\docs\\plans\n+ copy C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpi89o_mp5\\external.md C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpi89o_mp5\\repo-feature\\docs\\plans\\external.md\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpppimb0lk\\repo-integration) git add -A -- . :(exclude).codex/handoff/**\nFeature branch: feature/plan\nFeature worktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpwlbl04ic\\repo-plan\nHandoff archive: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpwlbl04ic\\repo\\.codex\\worktree-flow\\plan-run\\feature\nStopped before merge by request.\nPlan: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpwlbl04ic\\repo-plan\\docs\\plans\\plan.md\nWorktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpwlbl04ic\\repo-plan\nBranch: feature/plan\nFeature branch: feature/plan\nFeature worktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpqivxa1lt\\repo-plan\nHandoff archive: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpqivxa1lt\\repo\\.codex\\worktree-flow\\plan-run\\feature\nStopped before merge by request.\nPlan: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpqivxa1lt\\repo-plan\\docs\\plans\\plan.md\nWorktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpqivxa1lt\\repo-plan\nBranch: feature/plan\n..............................usage: python.exe -m unittest [-h] --plan PLAN [--repo REPO] [--base BASE]\n                              [--model MODEL] [--harness HARNESS]\n                              [--harness-dir HARNESS_DIR]\n                              [--merge-mode {squash,no-ff,stop}]\n                              [--keep-worktrees] [--dry-run]\npython.exe -m unittest: error: unrecognized arguments: --yes\n........................\n----------------------------------------------------------------------\nRan 54 tests in 1.689s\n\nOK\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nThe final checks passed. Iâ€™m staging the intended implementation files only, excluding `.codex/handoff/`, and then Iâ€™ll commit with the required message.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git add .codex/scripts/worktree-flow.py .codex/scripts/worktree-flow-codex.py .codex/scripts/worktree-flow-omp.py .codex/skill-usage.json loadouts/worktrees/.harness/docs/codex-worktree-flow.md loadouts/worktrees/.harness/scripts/worktree-flow.py loadouts/worktrees/.harness/scripts/worktree-flow-codex.py loadouts/worktrees/.harness/scripts/worktree-flow-omp.py loadouts/worktrees/.harness/scripts/skill-usage-manager.py tests/test_codex_worktree_flow.py tests/test_worktrees_loadout_sync.py; git status --short' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 1077ms:\nM  .codex/scripts/worktree-flow-codex.py\nM  .codex/scripts/worktree-flow-omp.py\nA  .codex/scripts/worktree-flow.py\nM  .codex/skill-usage.json\nM  loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nM  loadouts/worktrees/.harness/scripts/worktree-flow-codex.py\nM  loadouts/worktrees/.harness/scripts/worktree-flow-omp.py\nA  loadouts/worktrees/.harness/scripts/worktree-flow.py\nM  tests/test_codex_worktree_flow.py\nA  tests/test_worktrees_loadout_sync.py\n?? .codex/handoff/\nwarning: in the working copy of '.codex/scripts/worktree-flow-codex.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of '.codex/scripts/worktree-flow-omp.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/docs/codex-worktree-flow.md', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/scripts/worktree-flow-codex.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/scripts/worktree-flow-omp.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'tests/test_codex_worktree_flow.py', LF will be replaced by CRLF the next time Git touches it\nThe following paths are ignored by one of your .gitignore files:\nloadouts/worktrees/.harness/docs\nhint: Use -f if you really want to add them.\nhint: Disable this message with \"git config set advice.addIgnoredFile false\"\nwarning: in the working copy of '.codex/scripts/worktree-flow.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'loadouts/worktrees/.harness/scripts/worktree-flow.py', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'tests/test_worktrees_loadout_sync.py', LF will be replaced by CRLF the next time Git touches it\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nThe staging set contains the implementation files and the skill-usage log; `.codex/handoff/` remains untracked. The docs path is ignored by pattern but already tracked, so the modification is staged normally.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git diff --cached --stat' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git diff --cached --name-only' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 934ms:\n .codex/scripts/worktree-flow-codex.py              |  956 +----------------\n .codex/scripts/worktree-flow-omp.py                |  956 +----------------\n .codex/scripts/worktree-flow.py                    | 1090 ++++++++++++++++++++\n .codex/skill-usage.json                            |   17 +\n .../worktrees/.harness/docs/codex-worktree-flow.md |    9 +-\n .../.harness/scripts/worktree-flow-codex.py        |  956 +----------------\n .../.harness/scripts/worktree-flow-omp.py          |  956 +----------------\n .../worktrees/.harness/scripts/worktree-flow.py    | 1090 ++++++++++++++++++++\n tests/test_codex_worktree_flow.py                  |  274 ++++-\n tests/test_worktrees_loadout_sync.py               |   43 +\n 10 files changed, 2622 insertions(+), 3725 deletions(-)\n\n succeeded in 1310ms:\n.codex/scripts/worktree-flow-codex.py\n.codex/scripts/worktree-flow-omp.py\n.codex/scripts/worktree-flow.py\n.codex/skill-usage.json\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md\nloadouts/worktrees/.harness/scripts/worktree-flow-codex.py\nloadouts/worktrees/.harness/scripts/worktree-flow-omp.py\nloadouts/worktrees/.harness/scripts/worktree-flow.py\ntests/test_codex_worktree_flow.py\ntests/test_worktrees_loadout_sync.py\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nThe staged diff is the intended scope. Iâ€™m committing it now, then Iâ€™ll write the untracked handoff summary with the required details.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git commit -m \"Implement plan\"' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 1081ms:\n[feature/worktree-flow-features-1-3-plan-2 cb2f747] Implement plan\n 10 files changed, 2622 insertions(+), 3725 deletions(-)\n create mode 100644 .codex/scripts/worktree-flow.py\n create mode 100644 loadouts/worktrees/.harness/scripts/worktree-flow.py\n create mode 100644 tests/test_worktrees_loadout_sync.py\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nThe implementation commit was created. Iâ€™m collecting the commit SHA and changed file list, then Iâ€™ll write `.codex/handoff/implementation-summary.md` as an untracked workflow artifact.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Location | Select-Object -ExpandProperty Path' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git show --name-only --format= --no-renames HEAD' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git branch --show-current' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git rev-parse HEAD' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 976ms:\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n\n succeeded in 1288ms:\n.codex/scripts/worktree-flow-codex.py\n.codex/scripts/worktree-flow-omp.py\n.codex/scripts/worktree-flow.py\n.codex/skill-usage.json\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md\nloadouts/worktrees/.harness/scripts/worktree-flow-codex.py\nloadouts/worktrees/.harness/scripts/worktree-flow-omp.py\nloadouts/worktrees/.harness/scripts/worktree-flow.py\ntests/test_codex_worktree_flow.py\ntests/test_worktrees_loadout_sync.py\n\n succeeded in 1322ms:\nfeature/worktree-flow-features-1-3-plan-2\n\n succeeded in 1713ms:\ncb2f747a54f78bc31e1fa9bd79762a454bac9679\n\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nI have the commit metadata. Iâ€™m writing the handoff summary with the implementation scope, verification, assumptions, and residual risks; this file will stay untracked.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\handoff\\implementation-summary.md\ndiff --git a/.codex/handoff/implementation-summary.md b/.codex/handoff/implementation-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..8fefd621410c16ca401c002c3a9261b9fbb843e3\n--- /dev/null\n+++ b/.codex/handoff/implementation-summary.md\n@@ -0,0 +1,59 @@\n+# Implementation Summary\n+\n+## Plan\n+- Path: `docs/plans/worktree-flow-features-1-3.md`\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Commit: `cb2f747a54f78bc31e1fa9bd79762a454bac9679`\n+\n+## Changed Files\n+- `.codex/scripts/worktree-flow.py`\n+- `.codex/scripts/worktree-flow-codex.py`\n+- `.codex/scripts/worktree-flow-omp.py`\n+- `.codex/skill-usage.json`\n+- `loadouts/worktrees/.harness/docs/codex-worktree-flow.md`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow.py`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow-codex.py`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow-omp.py`\n+- `tests/test_codex_worktree_flow.py`\n+- `tests/test_worktrees_loadout_sync.py`\n+\n+## Behavior Changes\n+- Collapsed duplicated Codex and OMP worktree-flow implementations into shared `worktree-flow.py`.\n+- Replaced `worktree-flow-codex.py` and `worktree-flow-omp.py` with compatibility wrappers that pass explicit harness defaults.\n+- Added `--harness-dir` to the shared parser so direct execution can use or override the active artifact directory.\n+- Moved harness directory and handoff path behavior into `FlowConfig`, avoiding mutable global harness defaults when using multiple wrappers in one process.\n+- Added implementation guards requiring a commit after base, an actual diff from base, and a clean worktree outside handoff artifacts.\n+- Added audit guards that require a clean worktree outside handoff artifacts while allowing a no-op audit with unchanged `HEAD`.\n+- Added final pre-integration guards for no committed handoff artifacts, no pending non-handoff changes, and a branch diff from base.\n+- Added drift protection test comparing active `.codex/scripts/` files to the shipped `loadouts/worktrees/.harness/scripts/` copies after line-ending normalization.\n+- Updated worktrees loadout docs to describe the shared implementation and stricter invariants.\n+\n+## Tests And Checks\n+- `python -m unittest tests.test_codex_worktree_flow`\n+  - Result: passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync`\n+  - Result: passed, 1 test.\n+- `git diff --check -- . ':!scratchpad.md' ':!.codex/handoff/**'`\n+  - Result: passed.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync`\n+  - Result: passed, 54 tests.\n+\n+## Skipped Checks\n+- Full repository test suite was not run; the approved plan requested focused worktree-flow verification, and the changed surface is covered by the targeted unittest modules.\n+\n+## Decisions And Tradeoffs\n+- Kept wrapper filenames and quick-start commands stable to preserve existing user workflows.\n+- Kept the loadout sync check focused on flow scripts and `skill-usage-manager.py`, matching the approved plan.\n+- Did not add `loadouts/worktrees/.opencode/scripts/` updates because that directory does not exist in this worktree.\n+- Committed `.codex/skill-usage.json` because loading the required `implement-worktree` skill was logged per repo instructions.\n+\n+## Assumptions\n+- The `.harness` loadout template is the exported worktrees script copy to keep synchronized in this context.\n+- Audit no-op behavior is valid when `HEAD` is unchanged and `git status --porcelain --untracked-files=all` shows only handoff artifacts.\n+\n+## Known Risks\n+- The new invariant checks are stricter and may stop flows that previously continued with uncommitted non-handoff work or no-op implementation commits.\n+- `git diff --quiet base...branch -- .` semantics are used for branch-diff detection; unusual repository pathspec behavior could affect edge cases.\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/handoff/implementation-summary.md b/.codex/handoff/implementation-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..8fefd621410c16ca401c002c3a9261b9fbb843e3\n--- /dev/null\n+++ b/.codex/handoff/implementation-summary.md\n@@ -0,0 +1,59 @@\n+# Implementation Summary\n+\n+## Plan\n+- Path: `docs/plans/worktree-flow-features-1-3.md`\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Commit: `cb2f747a54f78bc31e1fa9bd79762a454bac9679`\n+\n+## Changed Files\n+- `.codex/scripts/worktree-flow.py`\n+- `.codex/scripts/worktree-flow-codex.py`\n+- `.codex/scripts/worktree-flow-omp.py`\n+- `.codex/skill-usage.json`\n+- `loadouts/worktrees/.harness/docs/codex-worktree-flow.md`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow.py`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow-codex.py`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow-omp.py`\n+- `tests/test_codex_worktree_flow.py`\n+- `tests/test_worktrees_loadout_sync.py`\n+\n+## Behavior Changes\n+- Collapsed duplicated Codex and OMP worktree-flow implementations into shared `worktree-flow.py`.\n+- Replaced `worktree-flow-codex.py` and `worktree-flow-omp.py` with compatibility wrappers that pass explicit harness defaults.\n+- Added `--harness-dir` to the shared parser so direct execution can use or override the active artifact directory.\n+- Moved harness directory and handoff path behavior into `FlowConfig`, avoiding mutable global harness defaults when using multiple wrappers in one process.\n+- Added implementation guards requiring a commit after base, an actual diff from base, and a clean worktree outside handoff artifacts.\n+- Added audit guards that require a clean worktree outside handoff artifacts while allowing a no-op audit with unchanged `HEAD`.\n+- Added final pre-integration guards for no committed handoff artifacts, no pending non-handoff changes, and a branch diff from base.\n+- Added drift protection test comparing active `.codex/scripts/` files to the shipped `loadouts/worktrees/.harness/scripts/` copies after line-ending normalization.\n+- Updated worktrees loadout docs to describe the shared implementation and stricter invariants.\n+\n+## Tests And Checks\n+- `python -m unittest tests.test_codex_worktree_flow`\n+  - Result: passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync`\n+  - Result: passed, 1 test.\n+- `git diff --check -- . ':!scratchpad.md' ':!.codex/handoff/**'`\n+  - Result: passed.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync`\n+  - Result: passed, 54 tests.\n+\n+## Skipped Checks\n+- Full repository test suite was not run; the approved plan requested focused worktree-flow verification, and the changed surface is covered by the targeted unittest modules.\n+\n+## Decisions And Tradeoffs\n+- Kept wrapper filenames and quick-start commands stable to preserve existing user workflows.\n+- Kept the loadout sync check focused on flow scripts and `skill-usage-manager.py`, matching the approved plan.\n+- Did not add `loadouts/worktrees/.opencode/scripts/` updates because that directory does not exist in this worktree.\n+- Committed `.codex/skill-usage.json` because loading the required `implement-worktree` skill was logged per repo instructions.\n+\n+## Assumptions\n+- The `.harness` loadout template is the exported worktrees script copy to keep synchronized in this context.\n+- Audit no-op behavior is valid when `HEAD` is unchanged and `git status --porcelain --untracked-files=all` shows only handoff artifacts.\n+\n+## Known Risks\n+- The new invariant checks are stricter and may stop flows that previously continued with uncommitted non-handoff work or no-op implementation commits.\n+- `git diff --quiet base...branch -- .` semantics are used for branch-diff detection; unusual repository pathspec behavior could affect edge cases.\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nThe handoff summary is written. Iâ€™m doing the final status check to confirm the commit is clean apart from untracked workflow artifacts.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git log -1 --oneline' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git status --short' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 864ms:\ncb2f747 Implement plan\n\n succeeded in 914ms:\n?? .codex/handoff/\n\ndiff --git a/.codex/handoff/implementation-summary.md b/.codex/handoff/implementation-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..8fefd621410c16ca401c002c3a9261b9fbb843e3\n--- /dev/null\n+++ b/.codex/handoff/implementation-summary.md\n@@ -0,0 +1,59 @@\n+# Implementation Summary\n+\n+## Plan\n+- Path: `docs/plans/worktree-flow-features-1-3.md`\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Commit: `cb2f747a54f78bc31e1fa9bd79762a454bac9679`\n+\n+## Changed Files\n+- `.codex/scripts/worktree-flow.py`\n+- `.codex/scripts/worktree-flow-codex.py`\n+- `.codex/scripts/worktree-flow-omp.py`\n+- `.codex/skill-usage.json`\n+- `loadouts/worktrees/.harness/docs/codex-worktree-flow.md`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow.py`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow-codex.py`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow-omp.py`\n+- `tests/test_codex_worktree_flow.py`\n+- `tests/test_worktrees_loadout_sync.py`\n+\n+## Behavior Changes\n+- Collapsed duplicated Codex and OMP worktree-flow implementations into shared `worktree-flow.py`.\n+- Replaced `worktree-flow-codex.py` and `worktree-flow-omp.py` with compatibility wrappers that pass explicit harness defaults.\n+- Added `--harness-dir` to the shared parser so direct execution can use or override the active artifact directory.\n+- Moved harness directory and handoff path behavior into `FlowConfig`, avoiding mutable global harness defaults when using multiple wrappers in one process.\n+- Added implementation guards requiring a commit after base, an actual diff from base, and a clean worktree outside handoff artifacts.\n+- Added audit guards that require a clean worktree outside handoff artifacts while allowing a no-op audit with unchanged `HEAD`.\n+- Added final pre-integration guards for no committed handoff artifacts, no pending non-handoff changes, and a branch diff from base.\n+- Added drift protection test comparing active `.codex/scripts/` files to the shipped `loadouts/worktrees/.harness/scripts/` copies after line-ending normalization.\n+- Updated worktrees loadout docs to describe the shared implementation and stricter invariants.\n+\n+## Tests And Checks\n+- `python -m unittest tests.test_codex_worktree_flow`\n+  - Result: passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync`\n+  - Result: passed, 1 test.\n+- `git diff --check -- . ':!scratchpad.md' ':!.codex/handoff/**'`\n+  - Result: passed.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync`\n+  - Result: passed, 54 tests.\n+\n+## Skipped Checks\n+- Full repository test suite was not run; the approved plan requested focused worktree-flow verification, and the changed surface is covered by the targeted unittest modules.\n+\n+## Decisions And Tradeoffs\n+- Kept wrapper filenames and quick-start commands stable to preserve existing user workflows.\n+- Kept the loadout sync check focused on flow scripts and `skill-usage-manager.py`, matching the approved plan.\n+- Did not add `loadouts/worktrees/.opencode/scripts/` updates because that directory does not exist in this worktree.\n+- Committed `.codex/skill-usage.json` because loading the required `implement-worktree` skill was logged per repo instructions.\n+\n+## Assumptions\n+- The `.harness` loadout template is the exported worktrees script copy to keep synchronized in this context.\n+- Audit no-op behavior is valid when `HEAD` is unchanged and `git status --porcelain --untracked-files=all` shows only handoff artifacts.\n+\n+## Known Risks\n+- The new invariant checks are stricter and may stop flows that previously continued with uncommitted non-handoff work or no-op implementation commits.\n+- `git diff --quiet base...branch -- .` semantics are used for branch-diff detection; unusual repository pathspec behavior could affect edge cases.\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n  âœ“ Inspect current scripts and tests\n  âœ“ Create shared flow implementation and wrappers\n  âœ“ Add invariant guards and tests\n  âœ“ Sync loadout copies and docs\n  âœ“ Run focused tests, commit, write handoff\ndiff --git a/.codex/handoff/implementation-summary.md b/.codex/handoff/implementation-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..8fefd621410c16ca401c002c3a9261b9fbb843e3\n--- /dev/null\n+++ b/.codex/handoff/implementation-summary.md\n@@ -0,0 +1,59 @@\n+# Implementation Summary\n+\n+## Plan\n+- Path: `docs/plans/worktree-flow-features-1-3.md`\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Commit: `cb2f747a54f78bc31e1fa9bd79762a454bac9679`\n+\n+## Changed Files\n+- `.codex/scripts/worktree-flow.py`\n+- `.codex/scripts/worktree-flow-codex.py`\n+- `.codex/scripts/worktree-flow-omp.py`\n+- `.codex/skill-usage.json`\n+- `loadouts/worktrees/.harness/docs/codex-worktree-flow.md`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow.py`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow-codex.py`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow-omp.py`\n+- `tests/test_codex_worktree_flow.py`\n+- `tests/test_worktrees_loadout_sync.py`\n+\n+## Behavior Changes\n+- Collapsed duplicated Codex and OMP worktree-flow implementations into shared `worktree-flow.py`.\n+- Replaced `worktree-flow-codex.py` and `worktree-flow-omp.py` with compatibility wrappers that pass explicit harness defaults.\n+- Added `--harness-dir` to the shared parser so direct execution can use or override the active artifact directory.\n+- Moved harness directory and handoff path behavior into `FlowConfig`, avoiding mutable global harness defaults when using multiple wrappers in one process.\n+- Added implementation guards requiring a commit after base, an actual diff from base, and a clean worktree outside handoff artifacts.\n+- Added audit guards that require a clean worktree outside handoff artifacts while allowing a no-op audit with unchanged `HEAD`.\n+- Added final pre-integration guards for no committed handoff artifacts, no pending non-handoff changes, and a branch diff from base.\n+- Added drift protection test comparing active `.codex/scripts/` files to the shipped `loadouts/worktrees/.harness/scripts/` copies after line-ending normalization.\n+- Updated worktrees loadout docs to describe the shared implementation and stricter invariants.\n+\n+## Tests And Checks\n+- `python -m unittest tests.test_codex_worktree_flow`\n+  - Result: passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync`\n+  - Result: passed, 1 test.\n+- `git diff --check -- . ':!scratchpad.md' ':!.codex/handoff/**'`\n+  - Result: passed.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync`\n+  - Result: passed, 54 tests.\n+\n+## Skipped Checks\n+- Full repository test suite was not run; the approved plan requested focused worktree-flow verification, and the changed surface is covered by the targeted unittest modules.\n+\n+## Decisions And Tradeoffs\n+- Kept wrapper filenames and quick-start commands stable to preserve existing user workflows.\n+- Kept the loadout sync check focused on flow scripts and `skill-usage-manager.py`, matching the approved plan.\n+- Did not add `loadouts/worktrees/.opencode/scripts/` updates because that directory does not exist in this worktree.\n+- Committed `.codex/skill-usage.json` because loading the required `implement-worktree` skill was logged per repo instructions.\n+\n+## Assumptions\n+- The `.harness` loadout template is the exported worktrees script copy to keep synchronized in this context.\n+- Audit no-op behavior is valid when `HEAD` is unchanged and `git status --porcelain --untracked-files=all` shows only handoff artifacts.\n+\n+## Known Risks\n+- The new invariant checks are stricter and may stop flows that previously continued with uncommitted non-handoff work or no-op implementation commits.\n+- `git diff --quiet base...branch -- .` semantics are used for branch-diff detection; unusual repository pathspec behavior could affect edge cases.\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nImplemented and committed the approved plan.\n\nCommit: `cb2f747 Implement plan`\n\nWhat changed:\n- Added shared `.codex/scripts/worktree-flow.py`.\n- Replaced Codex/OMP flow files with compatibility wrappers.\n- Added workflow invariant checks for implementation, audit, and pre-integration.\n- Synced loadout `.harness` script copies.\n- Added drift-protection test.\n- Updated worktree-flow docs.\n- Wrote `.codex/handoff/implementation-summary.md` and left it untracked as required.\n\nVerification passed:\n- `python -m unittest tests.test_codex_worktree_flow`\n- `python -m unittest tests.test_worktrees_loadout_sync`\n- `git diff --check -- . ':!scratchpad.md' ':!.codex/handoff/**'`\n- Combined focused unittest rerun: 54 tests passed\n\nFinal status is clean except for untracked `.codex/handoff/`.\ndiff --git a/.codex/handoff/implementation-summary.md b/.codex/handoff/implementation-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..8fefd621410c16ca401c002c3a9261b9fbb843e3\n--- /dev/null\n+++ b/.codex/handoff/implementation-summary.md\n@@ -0,0 +1,59 @@\n+# Implementation Summary\n+\n+## Plan\n+- Path: `docs/plans/worktree-flow-features-1-3.md`\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Commit: `cb2f747a54f78bc31e1fa9bd79762a454bac9679`\n+\n+## Changed Files\n+- `.codex/scripts/worktree-flow.py`\n+- `.codex/scripts/worktree-flow-codex.py`\n+- `.codex/scripts/worktree-flow-omp.py`\n+- `.codex/skill-usage.json`\n+- `loadouts/worktrees/.harness/docs/codex-worktree-flow.md`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow.py`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow-codex.py`\n+- `loadouts/worktrees/.harness/scripts/worktree-flow-omp.py`\n+- `tests/test_codex_worktree_flow.py`\n+- `tests/test_worktrees_loadout_sync.py`\n+\n+## Behavior Changes\n+- Collapsed duplicated Codex and OMP worktree-flow implementations into shared `worktree-flow.py`.\n+- Replaced `worktree-flow-codex.py` and `worktree-flow-omp.py` with compatibility wrappers that pass explicit harness defaults.\n+- Added `--harness-dir` to the shared parser so direct execution can use or override the active artifact directory.\n+- Moved harness directory and handoff path behavior into `FlowConfig`, avoiding mutable global harness defaults when using multiple wrappers in one process.\n+- Added implementation guards requiring a commit after base, an actual diff from base, and a clean worktree outside handoff artifacts.\n+- Added audit guards that require a clean worktree outside handoff artifacts while allowing a no-op audit with unchanged `HEAD`.\n+- Added final pre-integration guards for no committed handoff artifacts, no pending non-handoff changes, and a branch diff from base.\n+- Added drift protection test comparing active `.codex/scripts/` files to the shipped `loadouts/worktrees/.harness/scripts/` copies after line-ending normalization.\n+- Updated worktrees loadout docs to describe the shared implementation and stricter invariants.\n+\n+## Tests And Checks\n+- `python -m unittest tests.test_codex_worktree_flow`\n+  - Result: passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync`\n+  - Result: passed, 1 test.\n+- `git diff --check -- . ':!scratchpad.md' ':!.codex/handoff/**'`\n+  - Result: passed.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync`\n+  - Result: passed, 54 tests.\n+\n+## Skipped Checks\n+- Full repository test suite was not run; the approved plan requested focused worktree-flow verification, and the changed surface is covered by the targeted unittest modules.\n+\n+## Decisions And Tradeoffs\n+- Kept wrapper filenames and quick-start commands stable to preserve existing user workflows.\n+- Kept the loadout sync check focused on flow scripts and `skill-usage-manager.py`, matching the approved plan.\n+- Did not add `loadouts/worktrees/.opencode/scripts/` updates because that directory does not exist in this worktree.\n+- Committed `.codex/skill-usage.json` because loading the required `implement-worktree` skill was logged per repo instructions.\n+\n+## Assumptions\n+- The `.harness` loadout template is the exported worktrees script copy to keep synchronized in this context.\n+- Audit no-op behavior is valid when `HEAD` is unchanged and `git status --porcelain --untracked-files=all` shows only handoff artifacts.\n+\n+## Known Risks\n+- The new invariant checks are stricter and may stop flows that previously continued with uncommitted non-handoff work or no-op implementation commits.\n+- `git diff --quiet base...branch -- .` semantics are used for branch-diff detection; unusual repository pathspec behavior could affect edge cases.\ndiff --git a/.codex/scripts/worktree-flow-codex.py b/.codex/scripts/worktree-flow-codex.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..630e822162542fce2eaccf66f34a90c8d8b57f60\n--- a/.codex/scripts/worktree-flow-codex.py\n+++ b/.codex/scripts/worktree-flow-codex.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".codex\")\n+DEFAULT_HARNESS = \"codex\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow-omp.py b/.codex/scripts/worktree-flow-omp.py\nindex 3068e882cd197287ecf89e0f1d43f16f50c90d73..79a56f652b2384a6aa5d835a54805cf9300c80d8\n--- a/.codex/scripts/worktree-flow-omp.py\n+++ b/.codex/scripts/worktree-flow-omp.py\n@@ -1,956 +1,52 @@\n-#!/usr/bin/env python3\n-\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n-\n-from __future__ import annotations\n-\n-import argparse\n-import json\n-import os\n-import re\n-import shutil\n-import subprocess\n-import sys\n-from dataclasses import dataclass\n-from datetime import datetime\n-from pathlib import Path\n-from typing import Sequence\n-\n-HARNESS_DIR = Path(\".omp\")\n-DEFAULT_HARNESS = \"omp\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n-\n-\n-class FlowError(RuntimeError):\n-    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n-\n-\n-@dataclass(frozen=True)\n-class CommandResult:\n-    args: tuple[str, ...]\n-    cwd: Path\n-    returncode: int\n-    stdout: str = \"\"\n-    stderr: str = \"\"\n-\n-\n-class CommandRunner:\n-    def __init__(self, dry_run: bool = False) -> None:\n-        self.dry_run = dry_run\n-\n-    def run(\n-        self,\n-        args: Sequence[str],\n-        cwd: Path,\n-        *,\n-        check: bool = True,\n-        capture: bool = True,\n-        input_text: str | None = None,\n-    ) -> CommandResult:\n-        display = \" \".join(args)\n-        print(f\"+ ({cwd}) {display}\")\n-        if self.dry_run:\n-            return CommandResult(tuple(args), cwd, 0)\n-\n-        executable = shutil.which(args[0])\n-        if executable is None:\n-            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n-        resolved_args = [executable, *args[1:]]\n-        try:\n-            completed = subprocess.run(\n-                resolved_args,\n-                cwd=cwd,\n-                check=False,\n-                capture_output=capture,\n-                text=True,\n-                input=input_text,\n-            )\n-        except OSError as exc:\n-            raise FlowError(\n-                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n-            ) from exc\n-        result = CommandResult(\n-            tuple(args),\n-            cwd,\n-            completed.returncode,\n-            completed.stdout or \"\",\n-            completed.stderr or \"\",\n-        )\n-        if check and result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-        return result\n-\n-\n-def format_command_failure(result: CommandResult) -> str:\n-    parts = [\n-        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n-        f\"cwd: {result.cwd}\",\n-    ]\n-    if result.stdout.strip():\n-        parts.append(\"stdout:\\n\" + result.stdout.strip())\n-    if result.stderr.strip():\n-        parts.append(\"stderr:\\n\" + result.stderr.strip())\n-    return \"\\n\".join(parts)\n-\n-\n-def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n-    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n-    words = [part for part in cleaned.split(\"-\") if part]\n-    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n-    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n-\n-\n-def plan_title(plan_path: Path) -> str:\n-    text = plan_path.read_text(encoding=\"utf-8\")\n-    for line in text.splitlines():\n-        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n-        if match:\n-            return match.group(1).strip()\n-    return plan_path.stem\n-\n-\n-def derive_slug(plan_path: Path) -> str:\n-    return slugify(plan_title(plan_path))\n-\n-\n-def is_relative_to(path: Path, parent: Path) -> bool:\n-    try:\n-        path.resolve().relative_to(parent.resolve())\n-        return True\n-    except ValueError:\n-        return False\n-\n-\n-def ensure_dir(path: Path) -> None:\n-    path.mkdir(parents=True, exist_ok=True)\n-\n-\n-def write_text(path: Path, text: str) -> None:\n-    ensure_dir(path.parent)\n-    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n-\n-\n-@dataclass(frozen=True)\n-class Names:\n-    slug: str\n-    branch: str\n-    worktree: Path\n-    run_id: str\n-\n-\n-@dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n-\n-\n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n-\n-    def run(self) -> None:\n-        repo = self.git_root(self.config.repo.resolve())\n-        plan = self.config.plan.resolve()\n-        self.validate(repo, plan)\n-\n-        names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n-        self.prepare_git_permissions(repo)\n-        self.start_log(repo, names.run_id)\n-        print(f\"Feature branch: {names.branch}\")\n-        print(f\"Feature worktree: {names.worktree}\")\n-\n-        self.create_feature_worktree(repo, names)\n-        plan_in_worktree = self.ensure_plan_in_worktree(\n-            repo, plan, names.worktree, names.slug\n-        )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n-        print(f\"Handoff archive: {archive_dir}\")\n-\n-        if self.config.merge_mode == \"stop\":\n-            print(\"Stopped before merge by request.\")\n-            print(f\"Plan: {plan_in_worktree}\")\n-            print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n-\n-    def validate(self, repo: Path, plan: Path) -> None:\n-        if not plan.exists():\n-            raise FlowError(f\"Plan file does not exist: {plan}\")\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n-        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n-\n-    def git_root(self, start: Path) -> Path:\n-        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n-        root = result.stdout.strip()\n-        return Path(root).resolve() if root else start\n-\n-    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n-        repo_name = repo.name\n-        parent = repo.parent\n-        suffix = 1\n-        while True:\n-            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n-            branch = f\"feature/{candidate_slug}\"\n-            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n-            branch_exists = self.runner.run(\n-                [\"git\", \"branch\", \"--list\", branch], repo\n-            ).stdout.strip()\n-            if not branch_exists and not worktree.exists():\n-                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n-                return Names(candidate_slug, branch, worktree, run_id)\n-            suffix += 1\n-\n-    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(names.worktree),\n-                \"-b\",\n-                names.branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(names.worktree)\n-\n-    def ensure_plan_in_worktree(\n-        self, repo: Path, plan: Path, worktree: Path, slug: str\n-    ) -> Path:\n-        if is_relative_to(plan, repo):\n-            rel = plan.relative_to(repo)\n-            target = worktree / rel\n-            if target.exists():\n-                return target\n-        else:\n-            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n-            target = worktree / rel\n-\n-        self.ensure_dir(target.parent)\n-        self.copy_file(plan, target)\n-        return target\n-\n-    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n-        prompt = f\"\"\"Use the implement-worktree skill.\n-\n-Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n-\n-Requirements:\n-- Do not create, switch, merge, delete, or rebase worktrees.\n-- Keep edits scoped to the plan.\n-- Run focused tests or checks appropriate to the change.\n-- Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def run_audit(\n-        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n-    ) -> None:\n-        summary = HANDOFF_DIR / (\n-            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n-        )\n-        audit_finish_instruction = (\n-            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n-            if post_conflict\n-            else \"Commit audit fixes if changes are made.\"\n-        )\n-        prompt = f\"\"\"Use the audit-worktree skill.\n-\n-Fresh audit pass in this worktree.\n-\n-Read:\n-- `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n-        output = (\n-            worktree\n-            / HANDOFF_DIR\n-            / (\n-                \"post-conflict-audit-final-response.md\"\n-                if post_conflict\n-                else \"audit-final-response.md\"\n-            )\n-        )\n-        self.harness_exec(worktree, prompt, output)\n-\n-    def harness_sandbox_mode(self) -> str:\n-        if os.name == \"nt\":\n-            return \"danger-full-access\"\n-        return \"workspace-write\"\n-\n-    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n-        self.ensure_dir(output_file.parent)\n-        args = [\n-            self.config.harness,\n-            \"exec\",\n-            \"--cd\",\n-            str(cwd),\n-            \"--sandbox\",\n-            self.harness_sandbox_mode(),\n-        ]\n-        for writable_root in self.extra_writable_roots(cwd):\n-            args.extend([\"--add-dir\", str(writable_root)])\n-        if self.config.model:\n-            args.extend([\"--model\", self.config.model])\n-        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n-        self.log_event(\n-            \"harness_exec_start\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            command=args[:-1],\n-        )\n-        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n-        self.log_event(\n-            \"harness_exec_finish\",\n-            cwd=str(cwd),\n-            output_file=str(output_file),\n-            output_file_exists=output_file.exists(),\n-            returncode=result.returncode,\n-            stdout=result.stdout,\n-            stderr=result.stderr,\n-        )\n-        if result.returncode != 0:\n-            raise FlowError(format_command_failure(result))\n-\n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"worktree\",\n-                \"add\",\n-                str(integration_worktree),\n-                \"-b\",\n-                integration_branch,\n-                self.config.base,\n-            ],\n-            repo,\n-        )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.prepare_git_permissions(integration_worktree)\n-        integration_plan = self.copy_integration_context(\n-            names.worktree, integration_worktree, plan_path\n-        )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-\n-        integrated = False\n-        try:\n-            skill_usage_restored = False\n-            if self.config.merge_mode == \"squash\":\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--squash\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-            else:\n-                merge = self.runner.run(\n-                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n-                    integration_worktree,\n-                    check=False,\n-                )\n-                if merge.returncode != 0:\n-                    unmerged = self.unmerged_paths(integration_worktree)\n-                    if unmerged:\n-                        self.restore_integration_skill_usage_to_head(\n-                            integration_worktree, repo\n-                        )\n-                        skill_usage_restored = True\n-                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n-                        self.resolve_conflict(\n-                            integration_worktree, names, integration_plan\n-                        )\n-                    elif not unmerged:\n-                        raise FlowError(format_command_failure(merge))\n-\n-            if not skill_usage_restored:\n-                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n-            self.consolidate_skill_usage(\n-                names.worktree, integration_worktree, repo, feature_baseline\n-            )\n-            self.stage_integration_changes(integration_worktree)\n-            self.runner.run(\n-                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n-            )\n-\n-            self.archive_handoff(\n-                repo, integration_worktree, names.run_id, \"integration\"\n-            )\n-            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n-            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n-            integrated = True\n-        finally:\n-            if integrated and not self.config.keep_worktrees:\n-                self.cleanup(repo, integration_worktree, integration_branch, names)\n-\n-    def resolve_conflict(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n-        self.write_text(\n-            context_path, self.conflict_context(integration_worktree, names, plan_path)\n-        )\n-        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n-\n-Resolve merge conflicts in this integration worktree.\n-\n-Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n-        self.harness_exec(\n-            integration_worktree,\n-            prompt,\n-            integration_worktree\n-            / HANDOFF_DIR\n-            / \"conflict-resolution-final-response.md\",\n-        )\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n-        )\n-        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n-        self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n-        )\n-\n-    def copy_integration_context(\n-        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n-    ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n-        self.ensure_dir(dest_handoff)\n-        if source_handoff.exists():\n-            for item in source_handoff.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, dest_handoff / item.name)\n-\n-        try:\n-            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n-        except ValueError:\n-            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n-        dest_plan = integration_worktree / rel_plan\n-        self.ensure_dir(dest_plan.parent)\n-        if plan_path.exists():\n-            self.copy_file(plan_path, dest_plan)\n-        return dest_plan\n-\n-    def archive_handoff(\n-        self, repo: Path, worktree: Path, run_id: str, stage: str\n-    ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n-        self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n-        if source.exists():\n-            for item in source.iterdir():\n-                if item.is_file():\n-                    self.copy_file(item, archive_dir / item.name)\n-        return archive_dir\n-\n-    def stage_integration_changes(self, worktree: Path) -> None:\n-        self.runner.run(\n-            [\n-                \"git\",\n-                \"add\",\n-                \"-A\",\n-                \"--\",\n-                \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n-            ],\n-            worktree,\n-        )\n-\n-    def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n-\n-    def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n-            candidate = repo_root / harness_dir\n-            if candidate.exists():\n-                return candidate / \"skill-usage.json\"\n-        return repo_root / \".skill-usage.json\"\n-\n-    def skill_usage_ledger_in_worktree(\n-        self, worktree: Path, reference_repo: Path\n-    ) -> Path:\n-        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n-        return worktree / rel\n-\n-    def snapshot_skill_usage_baseline(\n-        self, worktree: Path, reference_repo: Path | None = None\n-    ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(worktree)\n-        )\n-        if self.runner.dry_run:\n-            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n-            return baseline\n-        self.ensure_dir(baseline.parent)\n-        if ledger.exists():\n-            self.copy_file(ledger, baseline)\n-        else:\n-            baseline.write_text(\n-                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n-                encoding=\"utf-8\",\n-                newline=\"\\n\",\n-            )\n-        return baseline\n-\n-    def restore_integration_skill_usage_to_head(\n-        self, integration_worktree: Path, reference_repo: Path | None = None\n-    ) -> None:\n-        ledger = (\n-            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n-            if reference_repo is not None\n-            else self.skill_usage_ledger(integration_worktree)\n-        )\n-        rel = ledger.relative_to(integration_worktree).as_posix()\n-        exists_at_head = (\n-            self.runner.run(\n-                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n-                integration_worktree,\n-                check=False,\n-            ).returncode\n-            == 0\n-        )\n-        if exists_at_head:\n-            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n-        else:\n-            self.runner.run(\n-                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n-                integration_worktree,\n-                check=False,\n-            )\n-            if not self.runner.dry_run and ledger.exists():\n-                ledger.unlink()\n-\n-    def consolidate_skill_usage(\n-        self,\n-        source_worktree: Path,\n-        integration_worktree: Path,\n-        target_repo: Path,\n-        baseline_path: Path,\n-    ) -> None:\n-        self.runner.run(\n-            [\n-                sys.executable,\n-                str(self.skill_usage_script(integration_worktree)),\n-                \"consolidate\",\n-                \"--source-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n-                ),\n-                \"--base-ledger\",\n-                str(baseline_path),\n-                \"--target-ledger\",\n-                str(\n-                    self.skill_usage_ledger_in_worktree(\n-                        integration_worktree, target_repo\n-                    )\n-                ),\n-                \"--source-repo\",\n-                str(source_worktree),\n-                \"--target-repo\",\n-                str(target_repo),\n-                \"--target-worktree\",\n-                str(integration_worktree),\n-            ],\n-            integration_worktree,\n-        )\n-\n-    def has_unmerged_paths(self, worktree: Path) -> bool:\n-        return bool(self.unmerged_paths(worktree))\n-\n-    def unmerged_paths(self, worktree: Path) -> list[str]:\n-        result = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            worktree,\n-            check=False,\n-        )\n-        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n-\n-    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n-        expected = {\n-            f\"{harness_dir}/skill-usage.json\"\n-            for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n-                \".harness\",\n-                \".codex\",\n-                \".opencode\",\n-                \".claude\",\n-                \".omp\",\n-                \".agents\",\n-            )\n-        }\n-        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n-\n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n-            [\n-                \"git\",\n-                \"ls-tree\",\n-                \"-r\",\n-                \"--name-only\",\n-                treeish,\n-                \"--\",\n-                HANDOFF_DIR.as_posix(),\n-            ],\n-            worktree,\n-            check=False,\n-        )\n-        tracked = result.stdout.strip()\n-        if result.returncode != 0 or not tracked:\n-            return\n-        raise FlowError(\n-            \"Workflow handoff artifacts were committed, but they must remain \"\n-            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n-            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n-            worktree,\n-            check=False,\n-        )\n-        raw = result.stdout.strip()\n-        if result.returncode != 0 or not raw:\n-            return None\n-        path = Path(raw)\n-        if not path.is_absolute():\n-            path = worktree / path\n-        return path.resolve()\n-\n-    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n-        roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n-        if harness_dir.exists():\n-            roots.append(harness_dir.resolve())\n-\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            roots.append(common_dir)\n-        return roots\n-\n-    def prepare_git_permissions(self, worktree: Path) -> None:\n-        common_dir = self.git_common_dir(worktree)\n-        if common_dir is not None and not is_relative_to(common_dir, worktree):\n-            self.prepare_harness_permissions(common_dir)\n-\n-    def ensure_dir(self, path: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ mkdir -p {path}\")\n-            return\n-        ensure_dir(path)\n-\n-    def copy_file(self, source: Path, dest: Path) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ copy {source} {dest}\")\n-            return\n-        shutil.copy2(source, dest)\n-\n-    def write_text(self, path: Path, text: str) -> None:\n-        if self.runner.dry_run:\n-            print(f\"+ write {path}\")\n-            return\n-        write_text(path, text)\n-\n-    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n-        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n-            return\n-        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n-        if shell is None:\n-            print(\n-                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n-                \"PowerShell was not found.\",\n-                file=sys.stderr,\n-            )\n-            return\n-        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n-        script = r\"\"\"\n-$Root = $env:CODEX_PERMISSION_ROOT\n-$Group = $env:CODEX_PERMISSION_GROUP\n-$ErrorActionPreference = 'Stop'\n-$identity = New-Object System.Security.Principal.NTAccount($Group)\n-$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n-$propagate = [System.Security.AccessControl.PropagationFlags]::None\n-$items = @((Get-Item -LiteralPath $Root -Force))\n-$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n-foreach ($item in $items) {\n-    $acl = Get-Acl -LiteralPath $item.FullName\n-    foreach ($rule in @($acl.Access)) {\n-        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n-            [void]$acl.RemoveAccessRuleSpecific($rule)\n-        }\n-    }\n-    if ($item.PSIsContainer) {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n-    } else {\n-        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n-    }\n-    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n-        $identity,\n-        $rights,\n-        $inherit,\n-        $propagate,\n-        [System.Security.AccessControl.AccessControlType]::Allow\n-    )\n-    $acl.SetAccessRule($allow)\n-    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n-}\n-\"\"\"\n-        completed = subprocess.run(\n-            [\n-                shell,\n-                \"-NoProfile\",\n-                \"-NonInteractive\",\n-                \"-Command\",\n-                script,\n-            ],\n-            check=False,\n-            capture_output=True,\n-            env={\n-                **os.environ,\n-                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n-                \"CODEX_PERMISSION_GROUP\": group,\n-            },\n-            text=True,\n-        )\n-        if completed.returncode != 0:\n-            print(\n-                \"Warning: could not grant sandbox write permissions for \"\n-                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n-                file=sys.stderr,\n-            )\n-\n-    def conflict_context(\n-        self, integration_worktree: Path, names: Names, plan_path: Path\n-    ) -> str:\n-        status = self.runner.run(\n-            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n-        ).stdout\n-        conflicted = self.runner.run(\n-            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n-            integration_worktree,\n-            check=False,\n-        ).stdout\n-        merge_base = self.runner.run(\n-            [\"git\", \"merge-base\", self.config.base, names.branch],\n-            integration_worktree,\n-            check=False,\n-        ).stdout.strip()\n-        base_log = \"\"\n-        feature_log = \"\"\n-        if merge_base:\n-            base_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-            feature_log = self.runner.run(\n-                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n-                integration_worktree,\n-                check=False,\n-            ).stdout\n-\n-        return f\"\"\"# Merge Conflict Context\n-\n-## Branches\n-- Base branch: {self.config.base}\n-- Feature branch: {names.branch}\n-\n-## Plan\n-- Path: {self.rel(integration_worktree, plan_path)}\n-\n-## Merge base\n-{merge_base or \"unknown\"}\n-\n-## Conflicted files\n-{conflicted.strip() or \"unknown\"}\n-\n-## Status\n-```text\n-{status.strip()}\n-```\n-\n-## Base commits since merge base\n-```text\n-{base_log.strip()}\n-```\n-\n-## Feature commits since merge base\n-```text\n-{feature_log.strip()}\n-```\n-\n-## Resolution rules\n-1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n-2. Feature intent comes from the approved plan and implementation summary.\n-3. Preserve audited feature behavior when compatible with latest base.\n-4. Prefer the smallest conflict-only edit.\n-5. Remove all conflict markers.\n-\"\"\"\n-\n-    def cleanup(\n-        self,\n-        repo: Path,\n-        integration_worktree: Path,\n-        integration_branch: str,\n-        names: Names,\n-    ) -> None:\n-        for worktree in (integration_worktree, names.worktree):\n-            if worktree.exists():\n-                self.runner.run(\n-                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n-                )\n-        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n-        # Squash merges do not mark the feature branch as merged, so force-delete\n-        # only after the integration branch has fast-forwarded successfully.\n-        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n-\n-    def start_log(self, repo: Path, run_id: str) -> None:\n-        if self.runner.dry_run:\n-            return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n-        ensure_dir(self.log_file.parent)\n-        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n-\n-    def log_event(self, event: str, **fields: object) -> None:\n-        if self.log_file is None:\n-            return\n-        record = {\n-            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n-            \"event\": event,\n-            **fields,\n-        }\n-        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n-            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n-\n-    def require_file(self, path: Path) -> None:\n-        if not path.exists() and not self.runner.dry_run:\n-            raise FlowError(f\"Required output file was not created: {path}\")\n-\n-    @staticmethod\n-    def rel(root: Path, path: Path) -> str:\n-        try:\n-            return path.resolve().relative_to(root.resolve()).as_posix()\n-        except ValueError:\n-            return str(path)\n-\n-\n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n-    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n-    parser.add_argument(\n-        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n-    )\n-    parser.add_argument(\n-        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n-    )\n-    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n-    parser.add_argument(\n-        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n-    )\n-    parser.add_argument(\n-        \"--keep-worktrees\",\n-        action=\"store_true\",\n-        help=\"Do not remove feature/integration worktrees.\",\n-    )\n-    parser.add_argument(\n-        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n-    )\n-    return parser\n-\n-\n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n-    )\n-    try:\n-        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n-    except FlowError as exc:\n-        print(str(exc), file=sys.stderr)\n-        return 1\n-    return 0\n-\n-\n-if __name__ == \"__main__\":\n-    raise SystemExit(main())\n+#!/usr/bin/env python3\n+\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import importlib.util\n+import sys\n+from pathlib import Path\n+\n+HARNESS_DIR = Path(\".omp\")\n+DEFAULT_HARNESS = \"omp\"\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+def load_shared_module():\n+    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n+    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n+    module = importlib.util.module_from_spec(spec)\n+    assert spec and spec.loader\n+    sys.modules[spec.name] = module\n+    spec.loader.exec_module(module)\n+    return module\n+\n+\n+_shared = load_shared_module()\n+\n+FlowError = _shared.FlowError\n+CommandResult = _shared.CommandResult\n+CommandRunner = _shared.CommandRunner\n+FlowConfig = _shared.FlowConfig\n+HarnessWorktreeFlow = _shared.HarnessWorktreeFlow\n+Names = _shared.Names\n+format_command_failure = _shared.format_command_failure\n+\n+\n+def build_parser():\n+    return _shared.build_parser(\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+def main(argv: list[str] | None = None) -> int:\n+    return _shared.main(\n+        argv,\n+        default_harness=DEFAULT_HARNESS,\n+        default_harness_dir=HARNESS_DIR,\n+    )\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nindex 4062b37dc6e7ded5e5a198faf862243b21e61a55..d8b0c2d44c3a95915e22641de9969abf59fee37c\n--- a/.codex/scripts/worktree-flow.py\n+++ b/.codex/scripts/worktree-flow.py\n@@ -15,10 +15,25 @@\n from pathlib import Path\n from typing import Sequence\n \n-HARNESS_DIR = Path(\".codex\")\n-DEFAULT_HARNESS = \"codex\"\n-HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n-EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n \n \n class FlowError(RuntimeError):\n@@ -139,21 +154,30 @@\n \n \n @dataclass(frozen=True)\n-class FlowConfig:\n-    repo: Path\n-    plan: Path\n-    base: str\n-    model: str | None\n-    harness: str\n-    merge_mode: str\n-    keep_worktrees: bool\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n \n \n-class HarnessWorktreeFlow:\n-    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n-        self.config = config\n-        self.runner = runner\n-        self.log_file: Path | None = None\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n \n     def run(self) -> None:\n         repo = self.git_root(self.config.repo.resolve())\n@@ -161,7 +185,7 @@\n         self.validate(repo, plan)\n \n         names = self.unique_feature_names(repo, derive_slug(plan))\n-        self.prepare_harness_permissions(repo / HARNESS_DIR)\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n         self.prepare_git_permissions(repo)\n         self.start_log(repo, names.run_id)\n         print(f\"Feature branch: {names.branch}\")\n@@ -171,26 +195,30 @@\n         plan_in_worktree = self.ensure_plan_in_worktree(\n             repo, plan, names.worktree, names.slug\n         )\n-        self.snapshot_skill_usage_baseline(names.worktree, repo)\n-        self.run_implementation(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"implementation-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        self.run_audit(names.worktree, plan_in_worktree)\n-        self.require_file(names.worktree / HANDOFF_DIR / \"audit-summary.md\")\n-        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n-        archive_dir = self.archive_handoff(\n-            repo, names.worktree, names.run_id, \"feature\"\n-        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n         print(f\"Handoff archive: {archive_dir}\")\n \n         if self.config.merge_mode == \"stop\":\n             print(\"Stopped before merge by request.\")\n             print(f\"Plan: {plan_in_worktree}\")\n             print(f\"Worktree: {names.worktree}\")\n-            print(f\"Branch: {names.branch}\")\n-            return\n-\n-        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n \n     def validate(self, repo: Path, plan: Path) -> None:\n         if not plan.exists():\n@@ -233,9 +261,9 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n-        self.ensure_dir(names.worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(names.worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n         self.prepare_git_permissions(names.worktree)\n \n     def ensure_plan_in_worktree(\n@@ -264,16 +292,16 @@\n - Keep edits scoped to the plan.\n - Run focused tests or checks appropriate to the change.\n - Commit the completed implementation.\n-- Write `{HANDOFF_DIR.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n-- Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-\"\"\"\n-        output = worktree / HANDOFF_DIR / \"implementation-final-response.md\"\n-        self.harness_exec(worktree, prompt, output)\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n \n     def run_audit(\n         self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n     ) -> None:\n-        summary = HANDOFF_DIR / (\n+        summary = self.handoff_dir / (\n             \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n         )\n         audit_finish_instruction = (\n@@ -287,17 +315,17 @@\n \n Read:\n - `{self.rel(worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-{f\"- `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n-\n-Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n-{audit_finish_instruction}\n-Do not commit files under `{HANDOFF_DIR.as_posix()}/`; they are workflow artifacts and must remain untracked.\n-Write `{summary.as_posix()}` before finishing.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n         output = (\n             worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / (\n                 \"post-conflict-audit-final-response.md\"\n                 if post_conflict\n@@ -345,15 +373,16 @@\n         if result.returncode != 0:\n             raise FlowError(format_command_failure(result))\n \n-    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n-        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n-        integration_branch = f\"integration/{names.slug}-{stamp}\"\n-        integration_worktree = (\n-            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n-        )\n-\n-        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n-        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n         self.runner.run(\n             [\n                 \"git\",\n@@ -366,14 +395,14 @@\n             ],\n             repo,\n         )\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n-        self.ensure_dir(integration_worktree / HANDOFF_DIR)\n-        self.prepare_harness_permissions(integration_worktree / HARNESS_DIR)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n         self.prepare_git_permissions(integration_worktree)\n         integration_plan = self.copy_integration_context(\n             names.worktree, integration_worktree, plan_path\n         )\n-        feature_baseline = integration_worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n \n         integrated = False\n         try:\n@@ -440,7 +469,7 @@\n     def resolve_conflict(\n         self, integration_worktree: Path, names: Names, plan_path: Path\n     ) -> None:\n-        context_path = integration_worktree / HANDOFF_DIR / \"merge-conflict-context.md\"\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n         self.write_text(\n             context_path, self.conflict_context(integration_worktree, names, plan_path)\n         )\n@@ -449,34 +478,34 @@\n Resolve merge conflicts in this integration worktree.\n \n Read:\n-- `{HANDOFF_DIR.as_posix()}/merge-conflict-context.md`\n-- `{self.rel(integration_worktree, plan_path)}`\n-- `{HANDOFF_DIR.as_posix()}/implementation-summary.md`\n-- `{HANDOFF_DIR.as_posix()}/audit-summary.md`\n-\n-Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{HANDOFF_DIR.as_posix()}/conflict-resolution-summary.md`.\n-Do not commit.\n-\"\"\"\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n         self.harness_exec(\n             integration_worktree,\n             prompt,\n             integration_worktree\n-            / HANDOFF_DIR\n+            / self.handoff_dir\n             / \"conflict-resolution-final-response.md\",\n         )\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"conflict-resolution-summary.md\"\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n         )\n         self.run_audit(integration_worktree, plan_path, post_conflict=True)\n         self.require_file(\n-            integration_worktree / HANDOFF_DIR / \"post-conflict-audit-summary.md\"\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n         )\n \n     def copy_integration_context(\n         self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n     ) -> Path:\n-        source_handoff = feature_worktree / HANDOFF_DIR\n-        dest_handoff = integration_worktree / HANDOFF_DIR\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n         self.ensure_dir(dest_handoff)\n         if source_handoff.exists():\n             for item in source_handoff.iterdir():\n@@ -496,9 +525,9 @@\n     def archive_handoff(\n         self, repo: Path, worktree: Path, run_id: str, stage: str\n     ) -> Path:\n-        archive_dir = repo / HARNESS_DIR / \"worktree-flow\" / run_id / stage\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n         self.ensure_dir(archive_dir)\n-        source = worktree / HANDOFF_DIR\n+        source = worktree / self.handoff_dir\n         if source.exists():\n             for item in source.iterdir():\n                 if item.is_file():\n@@ -513,16 +542,16 @@\n                 \"-A\",\n                 \"--\",\n                 \".\",\n-                f\":(exclude){HANDOFF_DIR.as_posix()}/**\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n             ],\n             worktree,\n         )\n \n     def skill_usage_script(self, worktree: Path) -> Path:\n-        return worktree / HARNESS_DIR / \"scripts\" / \"skill-usage-manager.py\"\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n \n     def skill_usage_ledger(self, repo_root: Path) -> Path:\n-        for harness_dir in (HARNESS_DIR.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n             candidate = repo_root / harness_dir\n             if candidate.exists():\n                 return candidate / \"skill-usage.json\"\n@@ -537,7 +566,7 @@\n     def snapshot_skill_usage_baseline(\n         self, worktree: Path, reference_repo: Path | None = None\n     ) -> Path:\n-        baseline = worktree / HANDOFF_DIR / \"skill-usage-baseline.json\"\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n         ledger = (\n             self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n             if reference_repo is not None\n@@ -634,7 +663,7 @@\n         expected = {\n             f\"{harness_dir}/skill-usage.json\"\n             for harness_dir in (\n-                HARNESS_DIR.as_posix(),\n+                self.harness_dir.as_posix(),\n                 \".harness\",\n                 \".codex\",\n                 \".opencode\",\n@@ -645,10 +674,10 @@\n         }\n         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n \n-    def require_no_tracked_handoff_artifacts(\n-        self, worktree: Path, treeish: str\n-    ) -> None:\n-        result = self.runner.run(\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n             [\n                 \"git\",\n                 \"ls-tree\",\n@@ -656,7 +685,7 @@\n                 \"--name-only\",\n                 treeish,\n                 \"--\",\n-                HANDOFF_DIR.as_posix(),\n+                self.handoff_dir.as_posix(),\n             ],\n             worktree,\n             check=False,\n@@ -667,11 +696,97 @@\n         raise FlowError(\n             \"Workflow handoff artifacts were committed, but they must remain \"\n             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n-            f\"merging, for example: git rm --cached -- {HANDOFF_DIR.as_posix()}/*\"\n-        )\n-\n-    def git_common_dir(self, worktree: Path) -> Path | None:\n-        result = self.runner.run(\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n             worktree,\n             check=False,\n@@ -686,7 +801,7 @@\n \n     def extra_writable_roots(self, worktree: Path) -> list[Path]:\n         roots: list[Path] = []\n-        harness_dir = worktree / HARNESS_DIR\n+        harness_dir = worktree / self.harness_dir\n         if harness_dir.exists():\n             roots.append(harness_dir.resolve())\n \n@@ -874,7 +989,7 @@\n     def start_log(self, repo: Path, run_id: str) -> None:\n         if self.runner.dry_run:\n             return\n-        self.log_file = repo / HARNESS_DIR / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n         ensure_dir(self.log_file.parent)\n         self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n \n@@ -901,10 +1016,14 @@\n             return str(path)\n \n \n-def build_parser() -> argparse.ArgumentParser:\n-    parser = argparse.ArgumentParser(\n-        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n-    )\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n     parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n     parser.add_argument(\n         \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n@@ -913,11 +1032,16 @@\n         \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n     )\n     parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n-    parser.add_argument(\n-        \"--harness\",\n-        default=DEFAULT_HARNESS,\n-        help=f\"Harness CLI executable. Defaults to {DEFAULT_HARNESS}.\",\n-    )\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n     parser.add_argument(\n         \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n     )\n@@ -932,17 +1056,27 @@\n     return parser\n \n \n-def main(argv: list[str] | None = None) -> int:\n-    parser = build_parser()\n-    args = parser.parse_args(argv)\n-    config = FlowConfig(\n-        repo=Path(args.repo).expanduser().resolve(),\n-        plan=Path(args.plan).expanduser().resolve(),\n-        base=args.base,\n-        harness=args.harness,\n-        model=args.model,\n-        merge_mode=args.merge_mode,\n-        keep_worktrees=args.keep_worktrees,\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n     )\n     try:\n         HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\ndiff --git a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\nindex c2dc2290816f150101b3fd6912afc39c5dc5acdc..f1ce72b6059e0a217a93358b4a33fd55289fe89c\n--- a/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n+++ b/loadouts/worktrees/.harness/docs/codex-worktree-flow.md\n@@ -1,17 +1,18 @@\n # <harness> Worktree Flow\n \n-`worktree-flow-codex.py` and `worktree-flow-omp.py` run the repeatable part of the workflow after you have an approved plan. They use the same workflow and differ only in their default harness CLI and artifact directory:\n-\n-- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n-- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n+`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\n+\n+- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\n+- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\n \n 1. Creates a feature branch and Git worktree from `main`.\n 2. Runs <harness> implementation in that worktree.\n-3. Requires an implementation commit and handoff summary.\n-4. Runs a fresh <harness> audit pass.\n-5. Requires an audit summary.\n-6. Finishes through a temporary integration worktree.\n-7. Squash-merges by default and cleans up only after a successful merge.\n+3. Requires an implementation commit and handoff summary.\n+4. Runs a fresh <harness> audit pass.\n+5. Requires an audit summary.\n+6. Verifies no non-handoff changes are left pending.\n+7. Finishes through a temporary integration worktree.\n+8. Squash-merges by default and cleans up only after a successful merge.\n \n ## Quick Start\n \n@@ -103,8 +104,10 @@\n \n ## Safety Notes\n \n-- The script does not push branches or open PRs.\n-- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n-- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n-- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n-- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The script does not push branches or open PRs.\n+- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\n+- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\n+- The primary checkout is updated only after a merge succeeds in a temporary integration worktree.\n+- Worktrees and branches are deleted only after successful integration unless `--keep-worktrees` is set.\n+- The implementation phase must create at least one commit and produce a diff from the base branch.\n+- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex cdce69f8f5afecc57e8c93f5b155c80279cd7ed6..f69f976adaf59754f971107e5f7459072a6b7d54\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,8 +23,9 @@\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n-omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n class FakeRunner:\n@@ -130,16 +131,35 @@\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n-    def test_omp_defaults_are_explicit(self) -> None:\n-        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n-        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n-        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n+    def test_omp_defaults_are_explicit(self) -> None:\n+        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n+        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n+        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n \n     def test_omp_parser_defaults_to_omp_harness(self) -> None:\n-        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n-\n-        self.assertEqual(args.harness, \"omp\")\n+        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -150,11 +170,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n             subject = omp_flow.HarnessWorktreeFlow(config, runner)\n \n@@ -179,11 +200,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n@@ -210,11 +232,12 @@\n             config = omp_flow.FlowConfig(\n                 repo=repo,\n                 plan=repo / \"plan.md\",\n-                base=\"main\",\n-                model=None,\n-                harness=omp_flow.DEFAULT_HARNESS,\n-                merge_mode=\"squash\",\n-                keep_worktrees=False,\n+                base=\"main\",\n+                model=None,\n+                harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n+                merge_mode=\"squash\",\n+                keep_worktrees=False,\n             )\n \n             subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n@@ -452,11 +475,14 @@\n             prepared: list[Path] = []\n             subject.prepare_harness_permissions = prepared.append\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n-                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n-            )\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n+                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n+            )\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"finish should not run\")\n@@ -487,10 +513,10 @@\n             )\n \n \n-    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n-        with tempfile.TemporaryDirectory() as temp:\n-            repo = Path(temp) / \"repo\"\n-            runner = FakeRunner(\n+    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            runner = FakeRunner(\n                 {\n                     (\n                         \"git\",\n@@ -505,10 +531,242 @@\n             )\n             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n \n-            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n-                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n-\n-    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n+            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n+                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n+    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n             repo.mkdir()\n@@ -553,11 +811,12 @@\n             subject.prepare_harness_permissions = lambda _path: None\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -603,11 +862,12 @@\n             )\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -657,11 +917,12 @@\n                 AssertionError(\"resolver should not run for usage-only conflicts\")\n             )\n             subject.restore_integration_skill_usage_to_head = lambda *_args: None\n-            subject.consolidate_skill_usage = lambda *_args: None\n-            subject.stage_integration_changes = lambda _worktree: None\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: None\n+            subject.stage_integration_changes = lambda _worktree: None\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -685,11 +946,12 @@\n             events: list[str] = []\n             subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n             subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n-            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n-            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n+            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -779,11 +1041,15 @@\n             config = self.config(repo, plan, merge_mode=\"stop\")\n             subject = flow.HarnessWorktreeFlow(config, runner)\n             subject.create_feature_worktree = lambda _repo, _names: None\n-            subject.run_implementation = lambda _worktree, _plan: None\n-            subject.run_audit = lambda _worktree, _plan: None\n-            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n-            subject.validate = lambda _repo, _plan: None\n-            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n+            subject.run_implementation = lambda _worktree, _plan: None\n+            subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n+            subject.validate = lambda _repo, _plan: None\n+            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n \n             subject.run()\n \n@@ -837,11 +1103,12 @@\n             (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n \n             subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n-            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-            subject.prepare_harness_permissions = lambda _path: None\n-\n-            with self.assertRaises(flow.FlowError):\n+            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaises(flow.FlowError):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -864,12 +1131,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n             )\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n-                AssertionError(\"resolver should not run\")\n-            )\n-\n-            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n+                AssertionError(\"resolver should not run\")\n+            )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n                     repo,\n                     flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n@@ -892,12 +1160,13 @@\n             subject = flow.HarnessWorktreeFlow(\n                 self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n             )\n-            resolved = []\n-            subject.prepare_harness_permissions = lambda _path: None\n-            subject.resolve_conflict = lambda *_args: resolved.append(True)\n-            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n-\n-            subject.finish(\n+            resolved = []\n+            subject.prepare_harness_permissions = lambda _path: None\n+            subject.resolve_conflict = lambda *_args: resolved.append(True)\n+            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n+\n+            subject.finish(\n                 repo,\n                 flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                 plan,\n@@ -963,12 +1232,13 @@\n         return flow.FlowConfig(\n             repo=repo,\n             plan=plan,\n-            base=\"main\",\n-            model=model,\n-            harness=\"codex\",\n-            merge_mode=merge_mode,\n-            keep_worktrees=False,\n-        )\n+            base=\"main\",\n+            model=model,\n+            harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n+            merge_mode=merge_mode,\n+            keep_worktrees=False,\n+        )\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..dd84cabdab59f973032708fd28eb652054892492\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ntokens used\n103,947\n"}
{"timestamp": "2026-06-04T12:52:13", "event": "harness_exec_start", "cwd": "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2", "output_file": "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\handoff\\audit-final-response.md", "command": ["codex", "exec", "--cd", "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2", "--sandbox", "danger-full-access", "--add-dir", "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex", "--add-dir", "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts\\.git", "--output-last-message", "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\handoff\\audit-final-response.md"]}
{"timestamp": "2026-06-04T12:56:48", "event": "harness_exec_finish", "cwd": "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2", "output_file": "C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\handoff\\audit-final-response.md", "output_file_exists": true, "returncode": 0, "stdout": "Audit complete.\n\n**Worktree**\n`C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n\n**Branch/Base**\n`feature/worktree-flow-features-1-3-plan-2` against `main` at `b1703981b0d7ee4d65a7ec97e0e6c6e7b5761d80`\n\n**Prior Summary**\nThe implementation collapsed duplicated Codex/OMP worktree flow scripts into shared `worktree-flow.py`, added loadout drift protection, and enforced implementation/audit/pre-integration clean-worktree invariants.\n\n**Skills Loaded**\n`audit-worktree`, `python-pro`, `code-reviewer`\n\n**Findings**\nNo confirmed code issues found. No implementation fixes were needed.\n\n**Audit Changes**\nCommitted required skill usage logging only:\n`ab38df448b8ccab843440a8afbdb346b7fbe5e8c` (`Record audit skill usage`)\n\nWrote `.codex/handoff/audit-summary.md` and left it untracked as requested. Final status shows only `.codex/handoff/` untracked.\n\n**Verification**\nPassed:\n`python -m unittest tests.test_codex_worktree_flow`  \n`python -m unittest tests.test_worktrees_loadout_sync`  \n`python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync`  \n`git diff --check -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md' ':(exclude).codex/handoff/**'`\n\nResidual risk: full repo test suite was not run; focused worktree-flow tests cover the changed surface.\n", "stderr": "2026-06-04T19:52:14.516022Z ERROR codex_core::session::session: failed to load skill C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\skills\\barebones-code-refactor\\SKILL.md: invalid YAML: mapping values are not allowed in this context at line 2 column 83\nOpenAI Codex v0.137.0\n--------\nworkdir: C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nmodel: gpt-5.5\nprovider: openai\napproval: never\nsandbox: danger-full-access\nreasoning effort: medium\nreasoning summaries: none\nsession id: 019e9431-84d0-76f3-9a04-784a2f0048f9\n--------\nuser\nUse the audit-worktree skill.\n\nFresh audit pass in this worktree.\n\nRead:\n- `docs/plans/worktree-flow-features-1-3.md`\n- `.codex/handoff/implementation-summary.md`\n\n\nAudit the actual diff against `main`. Fix confirmed issues and run relevant tests.\nCommit audit fixes if changes are made.\nDo not commit files under `.codex/handoff/`; they are workflow artifacts and must remain untracked.\nWrite `.codex/handoff/audit-summary.md` before finishing.\n\ncodex\nIâ€™ll use the `audit-worktree` skill for this pass and log that skill load first, then Iâ€™ll inspect the plan, handoff summary, and the diff against `main`.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"Get-Content -Path 'C:\\\\Users\\\\joesa\\\\Documents\\\\law-school-python\\\\harness-loadouts-worktree-flow-features-1-3-plan-2\\\\.codex\\\\skills\\\\audit-worktree\\\\SKILL.md'\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'python .codex/scripts/skill-usage-manager.py record audit-worktree --scope repo --path .codex/skills --repo .' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 959ms:\n---\nname: audit-worktree\ndescription: Audits an implementation worktree branch against main using a provided implementation summary, loads relevant project skills as needed, fixes all identified issues in the current worktree only, verifies the result, and reports the audit outcome. Use after script-driven worktree implementation when <harness> context has been cleared and the user passes the prior implementation summary.\n---\n\n# Audit Worktree\n\nUse this skill inside an implementation worktree after another <harness> session has completed a plan and returned a full implementation summary. The goal is to audit the current branch against `main`, use the provided summary as intent/context, load relevant project skills as necessary, identify issues, fix them in this worktree, and verify the final state.\n\n## Required Input\n\n- The implementation summary from the prior script-driven worktree run.\n- The current shell location should be the implementation worktree, not the primary `main` checkout.\n\nIf the summary is missing, continue with the git diff but state that intent coverage is limited. If the current checkout is `main`, stop before editing and tell the user to run the skill from the implementation worktree.\n\n## Safety Rules\n\n- Do not edit the primary/main worktree.\n- Do not switch to or edit `main`.\n- Do not merge, rebase, reset, or force-push unless the user explicitly asks.\n- Do not read, write, or diff `scratchpad.md`.\n- Preserve user changes you did not make.\n- Make all fixes only in the current implementation worktree.\n\n## Workflow\n\n1. Confirm the current worktree and branch:\n\n```powershell\ngit worktree list\ngit branch --show-current\ngit status --short\n```\n\nStop before editing if the current branch is `main` or if `git worktree list` shows the current path is the primary `main` checkout.\n\n2. Establish the comparison base:\n\n```powershell\ngit fetch --all --prune\ngit merge-base main HEAD\ngit diff --stat main...HEAD -- . \":(exclude)scratchpad.md\" \":(exclude)docs/scratchpad.md\"\ngit diff --name-only main...HEAD -- . \":(exclude)scratchpad.md\" \":(exclude)docs/scratchpad.md\"\n```\n\nIf local `main` is unavailable, inspect branches and use the repository default branch. Do not check out that branch.\n\n3. Read the provided implementation summary and map it to the changed files. Treat the summary as intent, not proof. Verify the actual diff.\n\n4. Audit the full diff against `main`:\n   - Check correctness, regressions, missing edge cases, missing tests, and incomplete plan items.\n   - Check this repo's architecture rules from `AGENTS.md`, especially CLI/web behavioral alignment, `EssayPipeline.run_step()`, index-to-text replacement, `_metadata.input_messages`, Supabase RLS/storage rules, prompt/schema pairing, and callback/usage tracking for LLM calls.\n   - Check that new behavior is represented in focused tests or explain why tests are not feasible.\n   - Inspect relevant existing code paths, not only changed lines, when needed to validate behavior.\n\n5. Load project skills as necessary based on the changed files and risk areas. Read the relevant `.agent-harness/skills/<skill-name>/SKILL.md` files directly, then follow their audit guidance. Common mappings:\n   - FastAPI routes, middleware, ARQ worker APIs: `api-review`\n   - SSE or HITL streaming: `sse-audit`\n   - Supabase migrations, RLS, storage, auth, SQL: `supabase`, `supabase-schema`, `supabase-postgres-best-practices`\n   - Python async, typing, pipeline code: `python-pro`\n   - Next.js, React, TypeScript, shadcn, frontend hooks: `ts-pro`, `audit-ts`, `vercel-react-best-practices`, `nextjs-shadcn`\n   - Pydantic schemas or generated frontend types: `cross-stack-types`\n   - Prompt templates or structured-output schemas: `prompt-schema-conventions`\n   - Docker or deployment files: `docker-review`\n   - Broad code quality/security review: `code-reviewer`\n\n6. Create a concise issue list before editing. Prioritize bugs, security issues, data loss, broken contracts, missing migrations/RLS, missing callback/usage tracking, CLI/web drift, and test gaps.\n\n7. Fix all confirmed issues in the current worktree. Keep edits scoped to the audit findings. Do not make unrelated refactors.\n\n8. Re-run focused verification appropriate to the final diff. If the implementation worktree does not have its own `venv`, run Python/test commands from the worktree but activate the original checkout's virtualenv path:\n\n```powershell\n& <primary-checkout>\\venv\\Scripts\\Activate.ps1\n```\n\nKeep all file operations in the implementation worktree.\n\nFor frontend changes, prefer the existing package scripts from `frontend/package.json`. If generated frontend types are affected, run the documented generation flow only when the API server requirement can be satisfied; otherwise report the skipped generation clearly.\n\n9. Commit all intended audit fixes on the current branch if changes were made:\n\n```powershell\ngit status --short\ngit add <fixed-files>\ngit commit -m \"Fix audit findings\"\ngit status --short\n```\n\nDo not include `scratchpad.md` or workflow handoff artifacts. If no fixes were needed, do not create an empty commit. The workflow merge only receives committed branch changes; uncommitted or unstaged files left in the worktree will not be included in the squash/no-ff integration merge.\n\n10. Final response must include:\n\n- Worktree path\n- Branch name\n- Base branch/ref used for diff\n- Summary received from the prior implementation, briefly restated\n- Skills loaded and why\n- Issues found and fixes applied\n- Files changed by the audit\n- Commit SHA for audit fixes, or note that no commit was created\n- Tests/checks run and any skipped checks\n- Residual risks or follow-up work\n\n## Output Standard\n\nBe explicit about both findings and fixes. If an issue is suspected but not confirmed, label it as a residual risk instead of silently editing around it. The final report should let a reviewer understand what changed in the audit pass without reopening the entire diff.\n\n succeeded in 1508ms:\ncodex\nThe skill is loaded and recorded. Iâ€™m checking the worktree identity and branch state before reading the implementation context, because the audit must not run on `main` or the primary checkout.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git branch --show-current' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git worktree list' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git status --short' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Path .gitignore' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 921ms:\nfeature/worktree-flow-features-1-3-plan-2\n\n succeeded in 1310ms:\nC:/Users/joesa/Documents/law-school-python/harness-loadouts                                                              b170398 [main]\n/mnt/c/Users/joesa/Documents/law-school-python/harness-loadouts-worktree-flow-features-1-3-plan                          b170398 [feature/worktree-flow-features-1-3-plan] prunable\nC:/Users/joesa/Documents/law-school-python/harness-loadouts-codex-and-omp-worktree-flow-split                            47a1977 [feature/codex-and-omp-worktree-flow-split]\nC:/Users/joesa/Documents/law-school-python/harness-loadouts-integrate-codex-and-omp-worktree-flow-split-20260602-173358  f6179d0 [integration/codex-and-omp-worktree-flow-split-20260602-173358]\nC:/Users/joesa/Documents/law-school-python/harness-loadouts-worktree-flow-features-1-3-plan-2                            cb2f747 [feature/worktree-flow-features-1-3-plan-2]\n\n succeeded in 1700ms:\n M .codex/skill-usage.json\n?? .codex/handoff/\n\n succeeded in 2097ms:\n# Planning docs \ndocs/\n\n# OS\nThumbs.db\n.DS_Store\n\n# Python\n__pycache__/\n.pytest_cache/\n*.py[cod]\n\n# Editor\n*.swp\n*.swo\n*~\n.vscode/\n.idea/\n\n# Blank files \nblank.*\n\ncodex\nThis is the implementation worktree on `feature/worktree-flow-features-1-3-plan-2`, not `main`. The only current changes are the required skill usage log and untracked handoff artifacts, so Iâ€™ll keep those separate from any audit fix commit unless the repoâ€™s workflow expects otherwise.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Path docs/plans/worktree-flow-features-1-3.md' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git fetch --all --prune' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Path .codex/handoff/implementation-summary.md' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 968ms:\n# Worktree Flow Features 1-3 Plan\n\n## Requirements\n\nImplement the first three proposed improvements for the worktrees loadout scripts:\n\n1. Collapse the duplicated `worktree-flow-codex.py` and `worktree-flow-omp.py` implementations into one harness-neutral implementation.\n2. Add drift protection so active installed scripts in this repo stay synchronized with the shipped worktrees loadout scripts.\n3. Enforce post-implementation and post-audit workflow invariants so the script does not silently drop uncommitted work, while still allowing a no-op audit that creates no commit.\n\nConstraints and repo rules:\n\n- The worktrees loadout is installed in this repo; update both active scripts under `.codex/scripts/` and loadout templates under `loadouts/worktrees/.harness/scripts/`.\n- Do not read, write, or diff `scratchpad.md`.\n- When changing scripts in `.codex/scripts/`, keep the exported worktrees loadout script copy in sync. If `loadouts/worktrees/.opencode/scripts/` exists in the implementation context, update it as required by repo guidance; otherwise update the current `.harness` template copy used by `harness-init.ps1`.\n- Preserve existing behavior for Codex and OMP wrappers unless the shared implementation makes a behavior intentionally stricter.\n- For audit enforcement, do not require a new audit commit when the audit finds nothing to change.\n\n## Key Findings From Read-Only Review\n\n- `.codex/scripts/worktree-flow-codex.py` and `.codex/scripts/worktree-flow-omp.py` are full duplicated implementations. They differ only in:\n  - `HARNESS_DIR = Path(\".codex\")` vs `Path(\".omp\")`\n  - `DEFAULT_HARNESS = \"codex\"` vs `\"omp\"`\n- Matching copies exist under `loadouts/worktrees/.harness/scripts/`.\n- `.codex/scripts/worktree-flow-codex.py` currently performs the main phase flow around lines 158-193:\n  - create feature worktree\n  - run implementation\n  - require `implementation-summary.md`\n  - reject tracked handoff artifacts\n  - run audit\n  - require `audit-summary.md`\n  - reject tracked handoff artifacts\n  - finish or stop\n- The script already excludes handoff artifacts from the integration commit in `stage_integration_changes()` with `:(exclude)<handoff>/**`.\n- Existing tests are concentrated in `tests/test_codex_worktree_flow.py` and already cover wrappers/defaults, prompts, merge modes, handoff exclusions, conflict handling, skill usage consolidation, and a local git squash merge smoke path.\n- Test helpers use `FakeRunner`, so new invariant tests can be mostly command-level unit tests with targeted real-git tests only where command output semantics matter.\n\n## Design Decisions\n\n### Shared implementation plus compatibility wrappers\n\nUse one shared implementation file and keep wrapper entrypoints:\n\n- `.codex/scripts/worktree-flow.py`\n- `.codex/scripts/worktree-flow-codex.py`\n- `.codex/scripts/worktree-flow-omp.py`\n- `loadouts/worktrees/.harness/scripts/worktree-flow.py`\n- `loadouts/worktrees/.harness/scripts/worktree-flow-codex.py`\n- `loadouts/worktrees/.harness/scripts/worktree-flow-omp.py`\n\nThe shared file should contain the current flow implementation. Wrappers should be tiny and should invoke the shared module with explicit harness defaults:\n\n- Codex wrapper: default harness dir `.codex`, default CLI `codex`.\n- OMP wrapper: default harness dir `.omp`, default CLI `omp`.\n\nThis preserves current command names and tests while removing the duplicated 900-line implementation.\n\nThe shared script should also support direct execution. Recommended direct behavior:\n\n- Infer default harness directory from the script grandparent directory when installed under `.<harness>/scripts/`.\n- Derive default CLI from that directory name when possible.\n- Keep `--harness` for explicit CLI override.\n- Add `--harness-dir` only if needed to make direct execution from `.harness/scripts/` unambiguous; otherwise wrappers can supply explicit defaults and direct execution can default from parent directory.\n\n### Drift protection by normalized content comparison\n\nAdd a focused test, preferably a new `tests/test_worktrees_loadout_sync.py`, that compares active `.codex/scripts/` files to `loadouts/worktrees/.harness/scripts/` files after normalizing line endings.\n\nCompare these files at minimum:\n\n- `worktree-flow.py`\n- `worktree-flow-codex.py`\n- `worktree-flow-omp.py`\n- `skill-usage-manager.py`\n\nThe failure message should identify the stale pair and explain that active and loadout copies must be updated together.\n\nLine-ending-only differences must not fail the test.\n\n### Workflow invariant enforcement\n\nAdd explicit checks after implementation, after audit, and immediately before integration.\n\nThe implementation pass must fail unless:\n\n1. `.<harness>/handoff/implementation-summary.md` exists.\n2. No handoff artifacts are committed.\n3. The feature branch has at least one commit after the base ref.\n4. The feature branch differs from the base ref.\n5. The feature worktree has no pending tracked or untracked non-handoff files.\n\nThe audit pass must allow no-op audits:\n\n1. Capture `HEAD` before audit.\n2. Run audit.\n3. Require `.<harness>/handoff/audit-summary.md`.\n4. Reject committed handoff artifacts.\n5. Require no pending tracked or untracked non-handoff files.\n6. Capture `HEAD` after audit.\n7. Accept either:\n   - `HEAD` unchanged and clean outside handoff, meaning no findings/no fixes; or\n   - `HEAD` changed and clean outside handoff, meaning audit fixes were committed.\n\nDo not require a new audit commit when `HEAD` is unchanged and the worktree is clean outside handoff.\n\nBefore calling `finish()`, repeat the guards that protect the integration merge:\n\n- no committed handoff artifacts on the feature branch\n- no pending non-handoff files in the feature worktree\n- feature branch still differs from base\n\n## Critical Files To Modify\n\n- `.codex/scripts/worktree-flow.py` â€” new shared implementation.\n- `.codex/scripts/worktree-flow-codex.py` â€” replace duplicated body with Codex wrapper.\n- `.codex/scripts/worktree-flow-omp.py` â€” replace duplicated body with OMP wrapper.\n- `loadouts/worktrees/.harness/scripts/worktree-flow.py` â€” loadout copy of shared implementation.\n- `loadouts/worktrees/.harness/scripts/worktree-flow-codex.py` â€” loadout Codex wrapper.\n- `loadouts/worktrees/.harness/scripts/worktree-flow-omp.py` â€” loadout OMP wrapper.\n- `tests/test_codex_worktree_flow.py` â€” update imports and existing wrapper/default tests; add invariant tests.\n- `tests/test_worktrees_loadout_sync.py` â€” add script drift protection test, or add equivalent tests to `test_codex_worktree_flow.py` if keeping one file is simpler.\n- `loadouts/worktrees/.harness/docs/codex-worktree-flow.md` â€” document the shared script and compatibility wrappers.\n\n## Implementation Steps\n\n1. Create the shared flow script.\n   - Move the current implementation from `worktree-flow-codex.py` into `worktree-flow.py`.\n   - Replace module constants with values supplied through a small defaults object or `main()` parameters.\n   - Keep `HANDOFF_DIR` behavior derived from the active harness directory.\n   - Avoid global mutable harness defaults that make importing Codex and OMP variants in one test process unreliable.\n\n2. Replace the two variant scripts with wrappers.\n   - Each wrapper imports/loads the sibling shared script by file path because the filename contains hyphens and is not a normal Python module name.\n   - Each wrapper delegates to shared `main()` with explicit defaults.\n   - Ensure `if __name__ == \"__main__\"` still exits with the delegated return code.\n\n3. Update tests for the shared implementation.\n   - Load shared module from `.codex/scripts/worktree-flow.py`.\n   - Load wrapper modules for Codex and OMP wrapper default tests.\n   - Keep existing behavioral tests pointed at the shared implementation configured with Codex defaults.\n   - Add tests proving wrappers still default to the right harness CLI and harness dir.\n\n4. Add drift protection.\n   - Add normalized script comparison from `.codex/scripts/` to `loadouts/worktrees/.harness/scripts/`.\n   - Normalize CRLF/LF before comparing.\n   - Include all flow files and `skill-usage-manager.py`.\n\n5. Add invariant helpers to the shared implementation.\n   - `head_rev(worktree: Path) -> str`\n   - `commits_since_base(worktree: Path, base: str, branch: str) -> list[str]` or a count via `git rev-list --count base..branch`\n   - `require_commits_since_base(worktree, base, branch, phase_name)` for implementation only.\n   - `require_branch_changed_since_base(worktree, base, branch)` using `git diff --quiet base...branch -- .` or equivalent command semantics.\n   - `non_handoff_status(worktree) -> list[str]` based on porcelain status.\n   - `require_clean_except_handoff(worktree)` that permits only files under the active handoff directory and fails for any other tracked, staged, or untracked change.\n\n6. Insert checks in the phase flow.\n   - After implementation summary and tracked-handoff checks:\n     - require implementation commit(s) since base\n     - require branch changed since base\n     - require clean except handoff\n   - Capture audit `HEAD` before `run_audit()`.\n   - After audit summary and tracked-handoff checks:\n     - require clean except handoff\n     - capture audit `HEAD` after audit\n     - allow same or different `HEAD`; no extra audit commit requirement\n   - Before `finish()`, repeat no tracked handoff artifacts, clean except handoff, and branch changed checks.\n\n7. Copy synchronized scripts to loadout template.\n   - Ensure `.codex/scripts/` and `loadouts/worktrees/.harness/scripts/` are identical after normalizing line endings for shared script, wrappers, and skill usage manager.\n\n8. Update docs.\n   - Document the shared script as the implementation and wrappers as stable compatibility entrypoints.\n   - Keep quick start examples using existing wrapper names unless choosing to promote direct `worktree-flow.py` usage.\n\n## Tests To Add Or Update\n\nAdd focused tests for:\n\n1. Shared script default inference/direct parser behavior.\n2. Codex wrapper passes `.codex` and `codex` defaults.\n3. OMP wrapper passes `.omp` and `omp` defaults.\n4. Active scripts match loadout template scripts after line-ending normalization.\n5. Implementation fails when no commit exists after base.\n6. Implementation fails when branch has no diff from base.\n7. Implementation fails when untracked non-handoff files remain.\n8. Implementation allows untracked handoff files.\n9. Audit succeeds with no new commit when the worktree is clean outside handoff.\n10. Audit succeeds with a new commit when the worktree is clean outside handoff.\n11. Audit fails when non-handoff changes are left uncommitted.\n12. Final pre-integration guard fails if non-handoff changes appear after audit.\n\n## Verification\n\nRun focused verification after implementation:\n\n```powershell\npython -m unittest tests.test_codex_worktree_flow\npython -m unittest tests.test_worktrees_loadout_sync\n```\n\nIf the sync test is added to `tests/test_codex_worktree_flow.py` instead of a new file, run:\n\n```powershell\npython -m unittest tests.test_codex_worktree_flow\n```\n\nAlso run the existing local git smoke test as part of `tests.test_codex_worktree_flow`; it already exercises squash staging and handoff exclusion.\n\n## Remaining Todos For Execution\n\n- Decide during implementation whether direct shared-script execution needs a public `--harness-dir` option or whether wrapper-supplied defaults are sufficient. Prefer the smallest public API that keeps direct execution useful.\n- Check whether `loadouts/worktrees/.opencode/scripts/` exists in the execution context; if it does, update it per repo guidance.\n- Preserve all existing test coverage while moving imports to the shared implementation.\n\n succeeded in 1672ms:\n# Implementation Summary\n\n## Plan\n- Path: `docs/plans/worktree-flow-features-1-3.md`\n\n## Worktree\n- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n- Branch: `feature/worktree-flow-features-1-3-plan-2`\n- Commit: `cb2f747a54f78bc31e1fa9bd79762a454bac9679`\n\n## Changed Files\n- `.codex/scripts/worktree-flow.py`\n- `.codex/scripts/worktree-flow-codex.py`\n- `.codex/scripts/worktree-flow-omp.py`\n- `.codex/skill-usage.json`\n- `loadouts/worktrees/.harness/docs/codex-worktree-flow.md`\n- `loadouts/worktrees/.harness/scripts/worktree-flow.py`\n- `loadouts/worktrees/.harness/scripts/worktree-flow-codex.py`\n- `loadouts/worktrees/.harness/scripts/worktree-flow-omp.py`\n- `tests/test_codex_worktree_flow.py`\n- `tests/test_worktrees_loadout_sync.py`\n\n## Behavior Changes\n- Collapsed duplicated Codex and OMP worktree-flow implementations into shared `worktree-flow.py`.\n- Replaced `worktree-flow-codex.py` and `worktree-flow-omp.py` with compatibility wrappers that pass explicit harness defaults.\n- Added `--harness-dir` to the shared parser so direct execution can use or override the active artifact directory.\n- Moved harness directory and handoff path behavior into `FlowConfig`, avoiding mutable global harness defaults when using multiple wrappers in one process.\n- Added implementation guards requiring a commit after base, an actual diff from base, and a clean worktree outside handoff artifacts.\n- Added audit guards that require a clean worktree outside handoff artifacts while allowing a no-op audit with unchanged `HEAD`.\n- Added final pre-integration guards for no committed handoff artifacts, no pending non-handoff changes, and a branch diff from base.\n- Added drift protection test comparing active `.codex/scripts/` files to the shipped `loadouts/worktrees/.harness/scripts/` copies after line-ending normalization.\n- Updated worktrees loadout docs to describe the shared implementation and stricter invariants.\n\n## Tests And Checks\n- `python -m unittest tests.test_codex_worktree_flow`\n  - Result: passed, 53 tests.\n- `python -m unittest tests.test_worktrees_loadout_sync`\n  - Result: passed, 1 test.\n- `git diff --check -- . ':!scratchpad.md' ':!.codex/handoff/**'`\n  - Result: passed.\n- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync`\n  - Result: passed, 54 tests.\n\n## Skipped Checks\n- Full repository test suite was not run; the approved plan requested focused worktree-flow verification, and the changed surface is covered by the targeted unittest modules.\n\n## Decisions And Tradeoffs\n- Kept wrapper filenames and quick-start commands stable to preserve existing user workflows.\n- Kept the loadout sync check focused on flow scripts and `skill-usage-manager.py`, matching the approved plan.\n- Did not add `loadouts/worktrees/.opencode/scripts/` updates because that directory does not exist in this worktree.\n- Committed `.codex/skill-usage.json` because loading the required `implement-worktree` skill was logged per repo instructions.\n\n## Assumptions\n- The `.harness` loadout template is the exported worktrees script copy to keep synchronized in this context.\n- Audit no-op behavior is valid when `HEAD` is unchanged and `git status --porcelain --untracked-files=all` shows only handoff artifacts.\n\n## Known Risks\n- The new invariant checks are stricter and may stop flows that previously continued with uncommitted non-handoff work or no-op implementation commits.\n- `git diff --quiet base...branch -- .` semantics are used for branch-diff detection; unusual repository pathspec behavior could affect edge cases.\n\n succeeded in 1969ms:\ncodex\nThe implementation summary matches the plan: a shared worktree flow script, thin wrappers, drift tests, and stricter workflow guards. Iâ€™m loading the Python/code-review skills now because the changed surface is mostly Python orchestration plus tests.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'python .codex/scripts/skill-usage-manager.py record python-pro --scope repo --path .codex/skills --repo .' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Path .codex/skills/code-reviewer/SKILL.md' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Path .codex/skills/python-pro/SKILL.md' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'python .codex/scripts/skill-usage-manager.py record code-reviewer --scope repo --path .codex/skills --repo .' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 1092ms:\n succeeded in 1389ms:\n---\nname: code-reviewer\ndescription: Analyzes code diffs and files to identify bugs, security vulnerabilities (SQL injection, XSS, insecure deserialization), code smells, N+1 queries, naming issues, and architectural concerns, then produces a structured review report with prioritized, actionable feedback. Use when reviewing pull requests, conducting code quality audits, identifying refactoring opportunities, or checking for security issues. Invoke for PR reviews, code quality checks, refactoring suggestions, review code, code quality. Complements specialized skills (security-reviewer, test-master) by providing broad-scope review across correctness, performance, maintainability, and test coverage in a single pass.\nlicense: MIT\nmetadata:\n  author: https://github.com/Jeffallan\n  version: \"1.1.0\"\n  domain: quality\n  triggers: code review, PR review, pull request, review code, code quality\n  role: specialist\n  scope: review\n  output-format: report\n  related-skills: security-reviewer, test-master, architecture-designer\n---\n\n# Code Reviewer\n\nSenior engineer conducting thorough, constructive code reviews that improve quality and share knowledge.\n\n## When to Use This Skill\n\n- Reviewing pull requests\n- Conducting code quality audits\n- Identifying refactoring opportunities\n- Checking for security vulnerabilities\n- Validating architectural decisions\n\n## Core Workflow\n\n1. **Context** â€” Read PR description, understand the problem being solved. **Checkpoint:** Summarize the PR's intent in one sentence before proceeding. If you cannot, ask the author to clarify.\n2. **Structure** â€” Review architecture and design decisions. Ask: Does this follow existing patterns in the codebase? Are new abstractions justified?\n3. **Details** â€” Check code quality, security, and performance. Apply the checks in the Reference Guide below. Ask: Are there N+1 queries, hardcoded secrets, or injection risks?\n4. **Tests** â€” Validate test coverage and quality. Ask: Are edge cases covered? Do tests assert behavior, not implementation?\n5. **Feedback** â€” Produce a categorized report using the Output Template. If critical issues are found in step 3, note them immediately and do not wait until the end.\n\n> **Disagreement handling:** If the author has left comments explaining a non-obvious choice, acknowledge their reasoning before suggesting an alternative. Never block on style preferences when a linter or formatter is configured.\n\n## Reference Guide\n\nLoad detailed guidance based on context:\n\n<!-- Spec Compliance and Receiving Feedback rows adapted from obra/superpowers by Jesse Vincent (@obra), MIT License -->\n\n| Topic              | Reference                              | Load When                                               |\n| ------------------ | -------------------------------------- | ------------------------------------------------------- |\n| Review Checklist   | `references/review-checklist.md`       | Starting a review, categories                           |\n| Common Issues      | `references/common-issues.md`          | N+1 queries, magic numbers, patterns                    |\n| Feedback Examples  | `references/feedback-examples.md`      | Writing good feedback                                   |\n| Report Template    | `references/report-template.md`        | Writing final review report                             |\n| Spec Compliance    | `references/spec-compliance-review.md` | Reviewing implementations, PR review, spec verification |\n| Receiving Feedback | `references/receiving-feedback.md`     | Responding to review comments, handling feedback        |\n\n## Review Patterns (Quick Reference)\n\n### N+1 Query â€” Bad vs Good\n\n```python\n# BAD: query inside loop\nfor user in users:\n    orders = Order.objects.filter(user=user)  # N+1\n\n# GOOD: prefetch in bulk\nusers = User.objects.prefetch_related('orders').all()\n```\n\n### Magic Number â€” Bad vs Good\n\n```python\n# BAD\nif status == 3:\n    ...\n\n# GOOD\nORDER_STATUS_SHIPPED = 3\nif status == ORDER_STATUS_SHIPPED:\n    ...\n```\n\n### Security: SQL Injection â€” Bad vs Good\n\n```python\n# BAD: string interpolation in query\ncursor.execute(f\"SELECT * FROM users WHERE id = {user_id}\")\n\n# GOOD: parameterized query\ncursor.execute(\"SELECT * FROM users WHERE id = %s\", [user_id])\n```\n\n## Constraints\n\n### MUST DO\n\n- Summarize PR intent before reviewing (see Workflow step 1)\n- Provide specific, actionable feedback\n- Include code examples in suggestions\n- Praise good patterns\n- Prioritize feedback (critical â†’ minor)\n- Review tests as thoroughly as code\n- Check for security issues (OWASP Top 10 as baseline)\n\n### MUST NOT DO\n\n- Be condescending or rude\n- Nitpick style when linters exist\n- Block on personal preferences\n- Demand perfection\n- Review without understanding the why\n- Skip praising good work\n\n## Output Template\n\nCode review report must include:\n\n1. **Summary** â€” One-sentence intent recap + overall assessment\n2. **Critical issues** â€” Must fix before merge (bugs, security, data loss)\n3. **Major issues** â€” Should fix (performance, design, maintainability)\n4. **Minor issues** â€” Nice to have (naming, readability)\n5. **Positive feedback** â€” Specific patterns done well\n6. **Questions for author** â€” Clarifications needed\n7. **Verdict** â€” Approve / Request Changes / Comment\n\n## Knowledge Reference\n\nSOLID, DRY, KISS, YAGNI, design patterns, OWASP Top 10, language idioms, testing patterns\n\n succeeded in 1808ms:\n---\nname: python-pro\ndescription: Use when building Python 3.11+ applications requiring type safety, async programming, or robust error handling. Generates type-annotated Python code, configures mypy in strict mode, writes pytest test suites with fixtures and mocking, and validates code with black and ruff. Invoke for type hints, async/await patterns, dataclasses, dependency injection, logging configuration, and structured error handling.\nlicense: MIT\nmetadata:\n  author: https://github.com/Jeffallan\n  version: \"1.1.0\"\n  domain: language\n  triggers: Python development, type hints, async Python, pytest, mypy, dataclasses, Python best practices, Pythonic code\n  role: specialist\n  scope: implementation\n  output-format: code\n  related-skills: fastapi-expert, devops-engineer\n---\n\n# Python Pro\n\nModern Python 3.11+ specialist focused on type-safe, async-first, production-ready code.\n\n## When to Use This Skill\n\n- Writing type-safe Python with complete type coverage\n- Implementing async/await patterns for I/O operations\n- Setting up pytest test suites with fixtures and mocking\n- Creating Pythonic code with comprehensions, generators, context managers\n- Building packages with Poetry and proper project structure\n- Performance optimization and profiling\n\n## Core Workflow\n\n1. **Analyze codebase** â€” Review structure, dependencies, type coverage, test suite\n2. **Design interfaces** â€” Define protocols, dataclasses, type aliases\n3. **Implement** â€” Write Pythonic code with full type hints and error handling\n4. **Test** â€” Create comprehensive pytest suite with >90% coverage\n5. **Validate** â€” Run `mypy --strict`, `black`, `ruff`\n   - If mypy fails: fix type errors reported and re-run before proceeding\n   - If tests fail: debug assertions, update fixtures, and iterate until green\n   - If ruff/black reports issues: apply auto-fixes, then re-validate\n\n## Reference Guide\n\nLoad detailed guidance based on context:\n\n| Topic            | Reference                        | Load When                                  |\n| ---------------- | -------------------------------- | ------------------------------------------ |\n| Type System      | `references/type-system.md`      | Type hints, mypy, generics, Protocol       |\n| Async Patterns   | `references/async-patterns.md`   | async/await, asyncio, task groups          |\n| Standard Library | `references/standard-library.md` | pathlib, dataclasses, functools, itertools |\n| Testing          | `references/testing.md`          | pytest, fixtures, mocking, parametrize     |\n| Packaging        | `references/packaging.md`        | poetry, pip, pyproject.toml, distribution  |\n\n## Constraints\n\n### MUST DO\n\n- Type hints for all function signatures and class attributes\n- PEP 8 compliance with black formatting\n- Comprehensive docstrings (Google style)\n- Test coverage exceeding 90% with pytest\n- Use `X | None` instead of `Optional[X]` (Python 3.10+)\n- Async/await for I/O-bound operations\n- Dataclasses over manual **init** methods\n- Context managers for resource handling\n\n### MUST NOT DO\n\n- Skip type annotations on public APIs\n- Use mutable default arguments\n- Mix sync and async code improperly\n- Ignore mypy errors in strict mode\n- Use bare except clauses\n- Hardcode secrets or configuration\n- Use deprecated stdlib modules (use pathlib not os.path)\n\n## Code Examples\n\n### Type-annotated function with error handling\n\n```python\nfrom pathlib import Path\n\ndef read_config(path: Path) -> dict[str, str]:\n    \"\"\"Read configuration from a file.\n\n    Args:\n        path: Path to the configuration file.\n\n    Returns:\n        Parsed key-value configuration entries.\n\n    Raises:\n        FileNotFoundError: If the config file does not exist.\n        ValueError: If a line cannot be parsed.\n    \"\"\"\n    config: dict[str, str] = {}\n    with path.open() as f:\n        for line in f:\n            key, _, value = line.partition(\"=\")\n            if not key.strip():\n                raise ValueError(f\"Invalid config line: {line!r}\")\n            config[key.strip()] = value.strip()\n    return config\n```\n\n### Dataclass with validation\n\n```python\nfrom dataclasses import dataclass, field\n\n@dataclass\nclass AppConfig:\n    host: str\n    port: int\n    debug: bool = False\n    allowed_origins: list[str] = field(default_factory=list)\n\n    def __post_init__(self) -> None:\n        if not (1 <= self.port <= 65535):\n            raise ValueError(f\"Invalid port: {self.port}\")\n```\n\n### Async pattern\n\n```python\nimport asyncio\nimport httpx\n\nasync def fetch_all(urls: list[str]) -> list[bytes]:\n    \"\"\"Fetch multiple URLs concurrently.\"\"\"\n    async with httpx.AsyncClient() as client:\n        tasks = [client.get(url) for url in urls]\n        responses = await asyncio.gather(*tasks)\n        return [r.content for r in responses]\n```\n\n### pytest fixture and parametrize\n\n```python\nimport pytest\nfrom pathlib import Path\n\n@pytest.fixture\ndef config_file(tmp_path: Path) -> Path:\n    cfg = tmp_path / \"config.txt\"\n    cfg.write_text(\"host=localhost\\nport=8080\\n\")\n    return cfg\n\n@pytest.mark.parametrize(\"port,valid\", [(8080, True), (0, False), (99999, False)])\ndef test_app_config_port_validation(port: int, valid: bool) -> None:\n    if valid:\n        AppConfig(host=\"localhost\", port=port)\n    else:\n        with pytest.raises(ValueError):\n            AppConfig(host=\"localhost\", port=port)\n```\n\n### mypy strict configuration (pyproject.toml)\n\n```toml\n[tool.mypy]\npython_version = \"3.11\"\nstrict = true\nwarn_return_any = true\nwarn_unused_configs = true\ndisallow_untyped_defs = true\n```\n\nClean `mypy --strict` output looks like:\n\n```\nSuccess: no issues found in 12 source files\n```\n\nAny reported error (e.g., `error: Function is missing a return type annotation`) must be resolved before the implementation is considered complete.\n\n## Output Templates\n\nWhen implementing Python features, provide:\n\n1. Module file with complete type hints\n2. Test file with pytest fixtures\n3. Type checking confirmation (mypy --strict passes)\n4. Brief explanation of Pythonic patterns used\n\n## Knowledge Reference\n\nPython 3.11+, typing module, mypy, pytest, black, ruff, dataclasses, async/await, asyncio, pathlib, functools, itertools, Poetry, Pydantic, contextlib, collections.abc, Protocol\n\n succeeded in 2368ms:\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"git diff --stat main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"git diff --name-only main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git merge-base main HEAD' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git status --short' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 944ms:\n .codex/scripts/worktree-flow-codex.py              |  956 +----------------\n .codex/scripts/worktree-flow-omp.py                |  956 +----------------\n .codex/scripts/worktree-flow.py                    | 1090 ++++++++++++++++++++\n .codex/skill-usage.json                            |   17 +\n .../worktrees/.harness/docs/codex-worktree-flow.md |    9 +-\n .../.harness/scripts/worktree-flow-codex.py        |  956 +----------------\n .../.harness/scripts/worktree-flow-omp.py          |  956 +----------------\n .../worktrees/.harness/scripts/worktree-flow.py    | 1090 ++++++++++++++++++++\n tests/test_codex_worktree_flow.py                  |  274 ++++-\n tests/test_worktrees_loadout_sync.py               |   43 +\n 10 files changed, 2622 insertions(+), 3725 deletions(-)\n\n succeeded in 1317ms:\n.codex/scripts/worktree-flow-codex.py\n.codex/scripts/worktree-flow-omp.py\n.codex/scripts/worktree-flow.py\n.codex/skill-usage.json\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md\nloadouts/worktrees/.harness/scripts/worktree-flow-codex.py\nloadouts/worktrees/.harness/scripts/worktree-flow-omp.py\nloadouts/worktrees/.harness/scripts/worktree-flow.py\ntests/test_codex_worktree_flow.py\ntests/test_worktrees_loadout_sync.py\n\n succeeded in 1649ms:\nb1703981b0d7ee4d65a7ec97e0e6c6e7b5761d80\n\n succeeded in 1696ms:\n M .codex/skill-usage.json\n?? .codex/handoff/\n\ncodex\nThe diff is limited to the shared flow script, wrappers, tests, docs, and skill usage. Iâ€™m now reading the changed Python files around the new invariant logic and wrapper delegation, then Iâ€™ll run the focused tests before deciding whether fixes are needed.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Path .codex/scripts/worktree-flow-codex.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Path .codex/scripts/worktree-flow.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Path tests/test_worktrees_loadout_sync.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Path .codex/scripts/worktree-flow-omp.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-Content -Path tests/test_codex_worktree_flow.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 951ms:\n#!/usr/bin/env python3\n\"\"\"Codex compatibility wrapper for the shared worktree flow.\"\"\"\n\nfrom __future__ import annotations\n\nimport importlib.util\nimport sys\nfrom pathlib import Path\n\nHARNESS_DIR = Path(\".codex\")\nDEFAULT_HARNESS = \"codex\"\nHANDOFF_DIR = HARNESS_DIR / \"handoff\"\n\n\ndef load_shared_module():\n    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n    module = importlib.util.module_from_spec(spec)\n    assert spec and spec.loader\n    sys.modules[spec.name] = module\n    spec.loader.exec_module(module)\n    return module\n\n\n_shared = load_shared_module()\n\nFlowError = _shared.FlowError\nCommandResult = _shared.CommandResult\nCommandRunner = _shared.CommandRunner\nFlowConfig = _shared.FlowConfig\nHarnessWorktreeFlow = _shared.HarnessWorktreeFlow\nNames = _shared.Names\nformat_command_failure = _shared.format_command_failure\n\n\ndef build_parser():\n    return _shared.build_parser(\n        default_harness=DEFAULT_HARNESS,\n        default_harness_dir=HARNESS_DIR,\n    )\n\n\ndef main(argv: list[str] | None = None) -> int:\n    return _shared.main(\n        argv,\n        default_harness=DEFAULT_HARNESS,\n        default_harness_dir=HARNESS_DIR,\n    )\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n\n succeeded in 1512ms:\n#!/usr/bin/env python3\n\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport os\nimport re\nimport shutil\nimport subprocess\nimport sys\nfrom dataclasses import dataclass\nfrom datetime import datetime\nfrom pathlib import Path\nfrom typing import Sequence\n\nEMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n\n\ndef infer_default_harness_dir(script_path: Path | None = None) -> Path:\n    script = script_path or Path(__file__)\n    parent = script.resolve().parent.parent.name\n    if parent.startswith(\".\"):\n        return Path(parent)\n    return Path(\".harness\")\n\n\ndef infer_default_harness(harness_dir: Path) -> str:\n    name = harness_dir.name\n    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n\n\nHARNESS_DIR = infer_default_harness_dir()\nDEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\nHANDOFF_DIR = HARNESS_DIR / \"handoff\"\n\n\nclass FlowError(RuntimeError):\n    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n\n\n@dataclass(frozen=True)\nclass CommandResult:\n    args: tuple[str, ...]\n    cwd: Path\n    returncode: int\n    stdout: str = \"\"\n    stderr: str = \"\"\n\n\nclass CommandRunner:\n    def __init__(self, dry_run: bool = False) -> None:\n        self.dry_run = dry_run\n\n    def run(\n        self,\n        args: Sequence[str],\n        cwd: Path,\n        *,\n        check: bool = True,\n        capture: bool = True,\n        input_text: str | None = None,\n    ) -> CommandResult:\n        display = \" \".join(args)\n        print(f\"+ ({cwd}) {display}\")\n        if self.dry_run:\n            return CommandResult(tuple(args), cwd, 0)\n\n        executable = shutil.which(args[0])\n        if executable is None:\n            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n        resolved_args = [executable, *args[1:]]\n        try:\n            completed = subprocess.run(\n                resolved_args,\n                cwd=cwd,\n                check=False,\n                capture_output=capture,\n                text=True,\n                input=input_text,\n            )\n        except OSError as exc:\n            raise FlowError(\n                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n            ) from exc\n        result = CommandResult(\n            tuple(args),\n            cwd,\n            completed.returncode,\n            completed.stdout or \"\",\n            completed.stderr or \"\",\n        )\n        if check and result.returncode != 0:\n            raise FlowError(format_command_failure(result))\n        return result\n\n\ndef format_command_failure(result: CommandResult) -> str:\n    parts = [\n        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n        f\"cwd: {result.cwd}\",\n    ]\n    if result.stdout.strip():\n        parts.append(\"stdout:\\n\" + result.stdout.strip())\n    if result.stderr.strip():\n        parts.append(\"stderr:\\n\" + result.stderr.strip())\n    return \"\\n\".join(parts)\n\n\ndef slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n    words = [part for part in cleaned.split(\"-\") if part]\n    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n\n\ndef plan_title(plan_path: Path) -> str:\n    text = plan_path.read_text(encoding=\"utf-8\")\n    for line in text.splitlines():\n        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n        if match:\n            return match.group(1).strip()\n    return plan_path.stem\n\n\ndef derive_slug(plan_path: Path) -> str:\n    return slugify(plan_title(plan_path))\n\n\ndef is_relative_to(path: Path, parent: Path) -> bool:\n    try:\n        path.resolve().relative_to(parent.resolve())\n        return True\n    except ValueError:\n        return False\n\n\ndef ensure_dir(path: Path) -> None:\n    path.mkdir(parents=True, exist_ok=True)\n\n\ndef write_text(path: Path, text: str) -> None:\n    ensure_dir(path.parent)\n    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n\n\n@dataclass(frozen=True)\nclass Names:\n    slug: str\n    branch: str\n    worktree: Path\n    run_id: str\n\n\n@dataclass(frozen=True)\nclass FlowConfig:\n    repo: Path\n    plan: Path\n    base: str\n    model: str | None\n    harness: str\n    harness_dir: Path\n    merge_mode: str\n    keep_worktrees: bool\n\n\nclass HarnessWorktreeFlow:\n    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n        self.config = config\n        self.runner = runner\n        self.log_file: Path | None = None\n\n    @property\n    def harness_dir(self) -> Path:\n        return self.config.harness_dir\n\n    @property\n    def handoff_dir(self) -> Path:\n        return self.config.harness_dir / \"handoff\"\n\n    def run(self) -> None:\n        repo = self.git_root(self.config.repo.resolve())\n        plan = self.config.plan.resolve()\n        self.validate(repo, plan)\n\n        names = self.unique_feature_names(repo, derive_slug(plan))\n        self.prepare_harness_permissions(repo / self.harness_dir)\n        self.prepare_git_permissions(repo)\n        self.start_log(repo, names.run_id)\n        print(f\"Feature branch: {names.branch}\")\n        print(f\"Feature worktree: {names.worktree}\")\n\n        self.create_feature_worktree(repo, names)\n        plan_in_worktree = self.ensure_plan_in_worktree(\n            repo, plan, names.worktree, names.slug\n        )\n        self.snapshot_skill_usage_baseline(names.worktree, repo)\n        self.run_implementation(names.worktree, plan_in_worktree)\n        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n        self.require_implementation_invariants(names.worktree, names.branch)\n        audit_head_before = self.head_rev(names.worktree)\n        self.run_audit(names.worktree, plan_in_worktree)\n        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n        archive_dir = self.archive_handoff(\n            repo, names.worktree, names.run_id, \"feature\"\n        )\n        print(f\"Handoff archive: {archive_dir}\")\n\n        if self.config.merge_mode == \"stop\":\n            print(\"Stopped before merge by request.\")\n            print(f\"Plan: {plan_in_worktree}\")\n            print(f\"Worktree: {names.worktree}\")\n            print(f\"Branch: {names.branch}\")\n            return\n\n        self.require_ready_for_integration(names.worktree, names.branch)\n        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n\n    def validate(self, repo: Path, plan: Path) -> None:\n        if not plan.exists():\n            raise FlowError(f\"Plan file does not exist: {plan}\")\n        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n\n    def git_root(self, start: Path) -> Path:\n        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n        root = result.stdout.strip()\n        return Path(root).resolve() if root else start\n\n    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n        repo_name = repo.name\n        parent = repo.parent\n        suffix = 1\n        while True:\n            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n            branch = f\"feature/{candidate_slug}\"\n            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n            branch_exists = self.runner.run(\n                [\"git\", \"branch\", \"--list\", branch], repo\n            ).stdout.strip()\n            if not branch_exists and not worktree.exists():\n                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n                return Names(candidate_slug, branch, worktree, run_id)\n            suffix += 1\n\n    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n        self.runner.run(\n            [\n                \"git\",\n                \"worktree\",\n                \"add\",\n                str(names.worktree),\n                \"-b\",\n                names.branch,\n                self.config.base,\n            ],\n            repo,\n        )\n        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n        self.ensure_dir(names.worktree / self.handoff_dir)\n        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n        self.prepare_git_permissions(names.worktree)\n\n    def ensure_plan_in_worktree(\n        self, repo: Path, plan: Path, worktree: Path, slug: str\n    ) -> Path:\n        if is_relative_to(plan, repo):\n            rel = plan.relative_to(repo)\n            target = worktree / rel\n            if target.exists():\n                return target\n        else:\n            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n            target = worktree / rel\n\n        self.ensure_dir(target.parent)\n        self.copy_file(plan, target)\n        return target\n\n    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n        prompt = f\"\"\"Use the implement-worktree skill.\n\nImplement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n\nRequirements:\n- Do not create, switch, merge, delete, or rebase worktrees.\n- Keep edits scoped to the plan.\n- Run focused tests or checks appropriate to the change.\n- Commit the completed implementation.\n- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n\"\"\"\n        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n        self.harness_exec(worktree, prompt, output)\n\n    def run_audit(\n        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n    ) -> None:\n        summary = self.handoff_dir / (\n            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n        )\n        audit_finish_instruction = (\n            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n            if post_conflict\n            else \"Commit audit fixes if changes are made.\"\n        )\n        prompt = f\"\"\"Use the audit-worktree skill.\n\nFresh audit pass in this worktree.\n\nRead:\n- `{self.rel(worktree, plan_path)}`\n- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n\nAudit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n{audit_finish_instruction}\nDo not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\nWrite `{summary.as_posix()}` before finishing.\n\"\"\"\n        output = (\n            worktree\n            / self.handoff_dir\n            / (\n                \"post-conflict-audit-final-response.md\"\n                if post_conflict\n                else \"audit-final-response.md\"\n            )\n        )\n        self.harness_exec(worktree, prompt, output)\n\n    def harness_sandbox_mode(self) -> str:\n        if os.name == \"nt\":\n            return \"danger-full-access\"\n        return \"workspace-write\"\n\n    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n        self.ensure_dir(output_file.parent)\n        args = [\n            self.config.harness,\n            \"exec\",\n            \"--cd\",\n            str(cwd),\n            \"--sandbox\",\n            self.harness_sandbox_mode(),\n        ]\n        for writable_root in self.extra_writable_roots(cwd):\n            args.extend([\"--add-dir\", str(writable_root)])\n        if self.config.model:\n            args.extend([\"--model\", self.config.model])\n        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n        self.log_event(\n            \"harness_exec_start\",\n            cwd=str(cwd),\n            output_file=str(output_file),\n            command=args[:-1],\n        )\n        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n        self.log_event(\n            \"harness_exec_finish\",\n            cwd=str(cwd),\n            output_file=str(output_file),\n            output_file_exists=output_file.exists(),\n            returncode=result.returncode,\n            stdout=result.stdout,\n            stderr=result.stderr,\n        )\n        if result.returncode != 0:\n            raise FlowError(format_command_failure(result))\n\n    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n        integration_branch = f\"integration/{names.slug}-{stamp}\"\n        integration_worktree = (\n            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n        )\n\n        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n        self.require_ready_for_integration(names.worktree, names.branch)\n        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n        self.runner.run(\n            [\n                \"git\",\n                \"worktree\",\n                \"add\",\n                str(integration_worktree),\n                \"-b\",\n                integration_branch,\n                self.config.base,\n            ],\n            repo,\n        )\n        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n        self.ensure_dir(integration_worktree / self.handoff_dir)\n        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n        self.prepare_git_permissions(integration_worktree)\n        integration_plan = self.copy_integration_context(\n            names.worktree, integration_worktree, plan_path\n        )\n        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n\n        integrated = False\n        try:\n            skill_usage_restored = False\n            if self.config.merge_mode == \"squash\":\n                merge = self.runner.run(\n                    [\"git\", \"merge\", \"--squash\", names.branch],\n                    integration_worktree,\n                    check=False,\n                )\n                if merge.returncode != 0:\n                    unmerged = self.unmerged_paths(integration_worktree)\n                    if unmerged:\n                        self.restore_integration_skill_usage_to_head(\n                            integration_worktree, repo\n                        )\n                        skill_usage_restored = True\n                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n                        self.resolve_conflict(\n                            integration_worktree, names, integration_plan\n                        )\n                    elif not unmerged:\n                        raise FlowError(format_command_failure(merge))\n            else:\n                merge = self.runner.run(\n                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n                    integration_worktree,\n                    check=False,\n                )\n                if merge.returncode != 0:\n                    unmerged = self.unmerged_paths(integration_worktree)\n                    if unmerged:\n                        self.restore_integration_skill_usage_to_head(\n                            integration_worktree, repo\n                        )\n                        skill_usage_restored = True\n                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n                        self.resolve_conflict(\n                            integration_worktree, names, integration_plan\n                        )\n                    elif not unmerged:\n                        raise FlowError(format_command_failure(merge))\n\n            if not skill_usage_restored:\n                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n            self.consolidate_skill_usage(\n                names.worktree, integration_worktree, repo, feature_baseline\n            )\n            self.stage_integration_changes(integration_worktree)\n            self.runner.run(\n                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n            )\n\n            self.archive_handoff(\n                repo, integration_worktree, names.run_id, \"integration\"\n            )\n            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n            integrated = True\n        finally:\n            if integrated and not self.config.keep_worktrees:\n                self.cleanup(repo, integration_worktree, integration_branch, names)\n\n    def resolve_conflict(\n        self, integration_worktree: Path, names: Names, plan_path: Path\n    ) -> None:\n        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n        self.write_text(\n            context_path, self.conflict_context(integration_worktree, names, plan_path)\n        )\n        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n\nResolve merge conflicts in this integration worktree.\n\nRead:\n- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n- `{self.rel(integration_worktree, plan_path)}`\n- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n- `{self.handoff_dir.as_posix()}/audit-summary.md`\n\nPreserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\nDo not commit.\n\"\"\"\n        self.harness_exec(\n            integration_worktree,\n            prompt,\n            integration_worktree\n            / self.handoff_dir\n            / \"conflict-resolution-final-response.md\",\n        )\n        self.require_file(\n            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n        )\n        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n        self.require_file(\n            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n        )\n\n    def copy_integration_context(\n        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n    ) -> Path:\n        source_handoff = feature_worktree / self.handoff_dir\n        dest_handoff = integration_worktree / self.handoff_dir\n        self.ensure_dir(dest_handoff)\n        if source_handoff.exists():\n            for item in source_handoff.iterdir():\n                if item.is_file():\n                    self.copy_file(item, dest_handoff / item.name)\n\n        try:\n            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n        except ValueError:\n            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n        dest_plan = integration_worktree / rel_plan\n        self.ensure_dir(dest_plan.parent)\n        if plan_path.exists():\n            self.copy_file(plan_path, dest_plan)\n        return dest_plan\n\n    def archive_handoff(\n        self, repo: Path, worktree: Path, run_id: str, stage: str\n    ) -> Path:\n        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n        self.ensure_dir(archive_dir)\n        source = worktree / self.handoff_dir\n        if source.exists():\n            for item in source.iterdir():\n                if item.is_file():\n                    self.copy_file(item, archive_dir / item.name)\n        return archive_dir\n\n    def stage_integration_changes(self, worktree: Path) -> None:\n        self.runner.run(\n            [\n                \"git\",\n                \"add\",\n                \"-A\",\n                \"--\",\n                \".\",\n                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n            ],\n            worktree,\n        )\n\n    def skill_usage_script(self, worktree: Path) -> Path:\n        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n\n    def skill_usage_ledger(self, repo_root: Path) -> Path:\n        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n            candidate = repo_root / harness_dir\n            if candidate.exists():\n                return candidate / \"skill-usage.json\"\n        return repo_root / \".skill-usage.json\"\n\n    def skill_usage_ledger_in_worktree(\n        self, worktree: Path, reference_repo: Path\n    ) -> Path:\n        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n        return worktree / rel\n\n    def snapshot_skill_usage_baseline(\n        self, worktree: Path, reference_repo: Path | None = None\n    ) -> Path:\n        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n        ledger = (\n            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n            if reference_repo is not None\n            else self.skill_usage_ledger(worktree)\n        )\n        if self.runner.dry_run:\n            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n            return baseline\n        self.ensure_dir(baseline.parent)\n        if ledger.exists():\n            self.copy_file(ledger, baseline)\n        else:\n            baseline.write_text(\n                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n                encoding=\"utf-8\",\n                newline=\"\\n\",\n            )\n        return baseline\n\n    def restore_integration_skill_usage_to_head(\n        self, integration_worktree: Path, reference_repo: Path | None = None\n    ) -> None:\n        ledger = (\n            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n            if reference_repo is not None\n            else self.skill_usage_ledger(integration_worktree)\n        )\n        rel = ledger.relative_to(integration_worktree).as_posix()\n        exists_at_head = (\n            self.runner.run(\n                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n                integration_worktree,\n                check=False,\n            ).returncode\n            == 0\n        )\n        if exists_at_head:\n            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n        else:\n            self.runner.run(\n                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n                integration_worktree,\n                check=False,\n            )\n            if not self.runner.dry_run and ledger.exists():\n                ledger.unlink()\n\n    def consolidate_skill_usage(\n        self,\n        source_worktree: Path,\n        integration_worktree: Path,\n        target_repo: Path,\n        baseline_path: Path,\n    ) -> None:\n        self.runner.run(\n            [\n                sys.executable,\n                str(self.skill_usage_script(integration_worktree)),\n                \"consolidate\",\n                \"--source-ledger\",\n                str(\n                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n                ),\n                \"--base-ledger\",\n                str(baseline_path),\n                \"--target-ledger\",\n                str(\n                    self.skill_usage_ledger_in_worktree(\n                        integration_worktree, target_repo\n                    )\n                ),\n                \"--source-repo\",\n                str(source_worktree),\n                \"--target-repo\",\n                str(target_repo),\n                \"--target-worktree\",\n                str(integration_worktree),\n            ],\n            integration_worktree,\n        )\n\n    def has_unmerged_paths(self, worktree: Path) -> bool:\n        return bool(self.unmerged_paths(worktree))\n\n    def unmerged_paths(self, worktree: Path) -> list[str]:\n        result = self.runner.run(\n            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n            worktree,\n            check=False,\n        )\n        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n\n    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n        expected = {\n            f\"{harness_dir}/skill-usage.json\"\n            for harness_dir in (\n                self.harness_dir.as_posix(),\n                \".harness\",\n                \".codex\",\n                \".opencode\",\n                \".claude\",\n                \".omp\",\n                \".agents\",\n            )\n        }\n        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n\n    def require_no_tracked_handoff_artifacts(\n        self, worktree: Path, treeish: str\n    ) -> None:\n        result = self.runner.run(\n            [\n                \"git\",\n                \"ls-tree\",\n                \"-r\",\n                \"--name-only\",\n                treeish,\n                \"--\",\n                self.handoff_dir.as_posix(),\n            ],\n            worktree,\n            check=False,\n        )\n        tracked = result.stdout.strip()\n        if result.returncode != 0 or not tracked:\n            return\n        raise FlowError(\n            \"Workflow handoff artifacts were committed, but they must remain \"\n            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n        )\n\n    def head_rev(self, worktree: Path) -> str:\n        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n        return result.stdout.strip()\n\n    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n        result = self.runner.run(\n            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n            worktree,\n        )\n        raw = result.stdout.strip()\n        return int(raw) if raw else 0\n\n    def require_commits_since_base(\n        self, worktree: Path, branch: str, phase_name: str\n    ) -> None:\n        count = self.commit_count_since_base(worktree, branch)\n        if count <= 0:\n            raise FlowError(\n                f\"{phase_name} did not create any commits on {branch} after \"\n                f\"{self.config.base}. Commit the completed implementation before \"\n                \"continuing.\"\n            )\n\n    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n        result = self.runner.run(\n            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n            worktree,\n            check=False,\n        )\n        if result.returncode == 1:\n            return\n        if result.returncode == 0:\n            raise FlowError(\n                f\"{branch} has no file changes compared with {self.config.base}. \"\n                \"The workflow cannot integrate a no-op implementation.\"\n            )\n        raise FlowError(format_command_failure(result))\n\n    def non_handoff_status(self, worktree: Path) -> list[str]:\n        result = self.runner.run(\n            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n            worktree,\n        )\n        return [\n            line\n            for line in result.stdout.splitlines()\n            if line.strip() and not self.status_line_is_handoff(line)\n        ]\n\n    def status_line_is_handoff(self, line: str) -> bool:\n        path_text = line[3:].strip()\n        paths = [part.strip() for part in path_text.split(\" -> \")]\n        return all(self.path_is_handoff(path) for path in paths if path)\n\n    def path_is_handoff(self, path: str) -> bool:\n        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n\n    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n        status = self.non_handoff_status(worktree)\n        if not status:\n            return\n        raise FlowError(\n            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n            + \"\\n\".join(status)\n        )\n\n    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n        self.require_commits_since_base(worktree, branch, \"Implementation\")\n        self.require_branch_changed_since_base(worktree, branch)\n        self.require_clean_except_handoff(worktree, \"Implementation\")\n\n    def require_audit_invariants(\n        self, worktree: Path, branch: str, head_before: str\n    ) -> None:\n        self.require_clean_except_handoff(worktree, \"Audit\")\n        head_after = self.head_rev(worktree)\n        if head_after != head_before:\n            self.require_branch_changed_since_base(worktree, branch)\n\n    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n        self.require_no_tracked_handoff_artifacts(worktree, branch)\n        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n        self.require_branch_changed_since_base(worktree, branch)\n\n    def git_common_dir(self, worktree: Path) -> Path | None:\n        result = self.runner.run(\n            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n            worktree,\n            check=False,\n        )\n        raw = result.stdout.strip()\n        if result.returncode != 0 or not raw:\n            return None\n        path = Path(raw)\n        if not path.is_absolute():\n            path = worktree / path\n        return path.resolve()\n\n    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n        roots: list[Path] = []\n        harness_dir = worktree / self.harness_dir\n        if harness_dir.exists():\n            roots.append(harness_dir.resolve())\n\n        common_dir = self.git_common_dir(worktree)\n        if common_dir is not None and not is_relative_to(common_dir, worktree):\n            roots.append(common_dir)\n        return roots\n\n    def prepare_git_permissions(self, worktree: Path) -> None:\n        common_dir = self.git_common_dir(worktree)\n        if common_dir is not None and not is_relative_to(common_dir, worktree):\n            self.prepare_harness_permissions(common_dir)\n\n    def ensure_dir(self, path: Path) -> None:\n        if self.runner.dry_run:\n            print(f\"+ mkdir -p {path}\")\n            return\n        ensure_dir(path)\n\n    def copy_file(self, source: Path, dest: Path) -> None:\n        if self.runner.dry_run:\n            print(f\"+ copy {source} {dest}\")\n            return\n        shutil.copy2(source, dest)\n\n    def write_text(self, path: Path, text: str) -> None:\n        if self.runner.dry_run:\n            print(f\"+ write {path}\")\n            return\n        write_text(path, text)\n\n    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n            return\n        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n        if shell is None:\n            print(\n                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n                \"PowerShell was not found.\",\n                file=sys.stderr,\n            )\n            return\n        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n        script = r\"\"\"\n$Root = $env:CODEX_PERMISSION_ROOT\n$Group = $env:CODEX_PERMISSION_GROUP\n$ErrorActionPreference = 'Stop'\n$identity = New-Object System.Security.Principal.NTAccount($Group)\n$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n$propagate = [System.Security.AccessControl.PropagationFlags]::None\n$items = @((Get-Item -LiteralPath $Root -Force))\n$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\nforeach ($item in $items) {\n    $acl = Get-Acl -LiteralPath $item.FullName\n    foreach ($rule in @($acl.Access)) {\n        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n            [void]$acl.RemoveAccessRuleSpecific($rule)\n        }\n    }\n    if ($item.PSIsContainer) {\n        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n    } else {\n        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n    }\n    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n        $identity,\n        $rights,\n        $inherit,\n        $propagate,\n        [System.Security.AccessControl.AccessControlType]::Allow\n    )\n    $acl.SetAccessRule($allow)\n    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n}\n\"\"\"\n        completed = subprocess.run(\n            [\n                shell,\n                \"-NoProfile\",\n                \"-NonInteractive\",\n                \"-Command\",\n                script,\n            ],\n            check=False,\n            capture_output=True,\n            env={\n                **os.environ,\n                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n                \"CODEX_PERMISSION_GROUP\": group,\n            },\n            text=True,\n        )\n        if completed.returncode != 0:\n            print(\n                \"Warning: could not grant sandbox write permissions for \"\n                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n                file=sys.stderr,\n            )\n\n    def conflict_context(\n        self, integration_worktree: Path, names: Names, plan_path: Path\n    ) -> str:\n        status = self.runner.run(\n            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n        ).stdout\n        conflicted = self.runner.run(\n            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n            integration_worktree,\n            check=False,\n        ).stdout\n        merge_base = self.runner.run(\n            [\"git\", \"merge-base\", self.config.base, names.branch],\n            integration_worktree,\n            check=False,\n        ).stdout.strip()\n        base_log = \"\"\n        feature_log = \"\"\n        if merge_base:\n            base_log = self.runner.run(\n                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n                integration_worktree,\n                check=False,\n            ).stdout\n            feature_log = self.runner.run(\n                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n                integration_worktree,\n                check=False,\n            ).stdout\n\n        return f\"\"\"# Merge Conflict Context\n\n## Branches\n- Base branch: {self.config.base}\n- Feature branch: {names.branch}\n\n## Plan\n- Path: {self.rel(integration_worktree, plan_path)}\n\n## Merge base\n{merge_base or \"unknown\"}\n\n## Conflicted files\n{conflicted.strip() or \"unknown\"}\n\n## Status\n```text\n{status.strip()}\n```\n\n## Base commits since merge base\n```text\n{base_log.strip()}\n```\n\n## Feature commits since merge base\n```text\n{feature_log.strip()}\n```\n\n## Resolution rules\n1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n2. Feature intent comes from the approved plan and implementation summary.\n3. Preserve audited feature behavior when compatible with latest base.\n4. Prefer the smallest conflict-only edit.\n5. Remove all conflict markers.\n\"\"\"\n\n    def cleanup(\n        self,\n        repo: Path,\n        integration_worktree: Path,\n        integration_branch: str,\n        names: Names,\n    ) -> None:\n        for worktree in (integration_worktree, names.worktree):\n            if worktree.exists():\n                self.runner.run(\n                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n                )\n        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n        # Squash merges do not mark the feature branch as merged, so force-delete\n        # only after the integration branch has fast-forwarded successfully.\n        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n\n    def start_log(self, repo: Path, run_id: str) -> None:\n        if self.runner.dry_run:\n            return\n        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n        ensure_dir(self.log_file.parent)\n        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n\n    def log_event(self, event: str, **fields: object) -> None:\n        if self.log_file is None:\n            return\n        record = {\n            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n            \"event\": event,\n            **fields,\n        }\n        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n\n    def require_file(self, path: Path) -> None:\n        if not path.exists() and not self.runner.dry_run:\n            raise FlowError(f\"Required output file was not created: {path}\")\n\n    @staticmethod\n    def rel(root: Path, path: Path) -> str:\n        try:\n            return path.resolve().relative_to(root.resolve()).as_posix()\n        except ValueError:\n            return str(path)\n\n\ndef build_parser(\n    *,\n    default_harness: str = DEFAULT_HARNESS,\n    default_harness_dir: Path = HARNESS_DIR,\n) -> argparse.ArgumentParser:\n    parser = argparse.ArgumentParser(\n        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n    )\n    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n    parser.add_argument(\n        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n    )\n    parser.add_argument(\n        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n    )\n    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n    parser.add_argument(\n        \"--harness\",\n        default=default_harness,\n        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n    )\n    parser.add_argument(\n        \"--harness-dir\",\n        default=default_harness_dir.as_posix(),\n        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n    )\n    parser.add_argument(\n        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n    )\n    parser.add_argument(\n        \"--keep-worktrees\",\n        action=\"store_true\",\n        help=\"Do not remove feature/integration worktrees.\",\n    )\n    parser.add_argument(\n        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n    )\n    return parser\n\n\ndef main(\n    argv: list[str] | None = None,\n    *,\n    default_harness: str = DEFAULT_HARNESS,\n    default_harness_dir: Path = HARNESS_DIR,\n) -> int:\n    parser = build_parser(\n        default_harness=default_harness,\n        default_harness_dir=default_harness_dir,\n    )\n    args = parser.parse_args(argv)\n    harness_dir = Path(args.harness_dir)\n    config = FlowConfig(\n        repo=Path(args.repo).expanduser().resolve(),\n        plan=Path(args.plan).expanduser().resolve(),\n        base=args.base,\n        harness=args.harness,\n        harness_dir=harness_dir,\n        model=args.model,\n        merge_mode=args.merge_mode,\n        keep_worktrees=args.keep_worktrees,\n    )\n    try:\n        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n    except FlowError as exc:\n        print(str(exc), file=sys.stderr)\n        return 1\n    return 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n\n succeeded in 1683ms:\n#!/usr/bin/env python3\n\"\"\"OMP compatibility wrapper for the shared worktree flow.\"\"\"\n\nfrom __future__ import annotations\n\nimport importlib.util\nimport sys\nfrom pathlib import Path\n\nHARNESS_DIR = Path(\".omp\")\nDEFAULT_HARNESS = \"omp\"\nHANDOFF_DIR = HARNESS_DIR / \"handoff\"\n\n\ndef load_shared_module():\n    script = Path(__file__).resolve().with_name(\"worktree-flow.py\")\n    spec = importlib.util.spec_from_file_location(\"worktree_flow_shared\", script)\n    module = importlib.util.module_from_spec(spec)\n    assert spec and spec.loader\n    sys.modules[spec.name] = module\n    spec.loader.exec_module(module)\n    return module\n\n\n_shared = load_shared_module()\n\nFlowError = _shared.FlowError\nCommandResult = _shared.CommandResult\nCommandRunner = _shared.CommandRunner\nFlowConfig = _shared.FlowConfig\nHarnessWorktreeFlow = _shared.HarnessWorktreeFlow\nNames = _shared.Names\nformat_command_failure = _shared.format_command_failure\n\n\ndef build_parser():\n    return _shared.build_parser(\n        default_harness=DEFAULT_HARNESS,\n        default_harness_dir=HARNESS_DIR,\n    )\n\n\ndef main(argv: list[str] | None = None) -> int:\n    return _shared.main(\n        argv,\n        default_harness=DEFAULT_HARNESS,\n        default_harness_dir=HARNESS_DIR,\n    )\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n\n succeeded in 2058ms:\nfrom __future__ import annotations\n\nimport unittest\nfrom pathlib import Path\n\n\nROOT = Path(__file__).resolve().parents[1]\nACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\nLOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n\n\nSCRIPT_PAIRS = (\n    \"worktree-flow.py\",\n    \"worktree-flow-codex.py\",\n    \"worktree-flow-omp.py\",\n    \"skill-usage-manager.py\",\n)\n\n\ndef normalized_text(path: Path) -> str:\n    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n\n\nclass WorktreesLoadoutSyncTests(unittest.TestCase):\n    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n        for script_name in SCRIPT_PAIRS:\n            active = ACTIVE_SCRIPTS / script_name\n            loadout = LOADOUT_SCRIPTS / script_name\n            with self.subTest(script=script_name):\n                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n                self.assertEqual(\n                    normalized_text(active),\n                    normalized_text(loadout),\n                    (\n                        f\"{active} and {loadout} are out of sync. Update the \"\n                        \"active script and shipped worktrees loadout copy together.\"\n                    ),\n                )\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n\n succeeded in 2641ms:\nfrom __future__ import annotations\n\nimport importlib.util\nimport json\nimport shutil\nimport subprocess\nimport sys\nimport tempfile\nimport unittest\nfrom unittest import mock\nfrom pathlib import Path\n\n\nSCRIPT_DIR = Path(__file__).resolve().parents[1] / \".codex\" / \"scripts\"\n\n\ndef load_flow_module(name: str, script_name: str):\n    spec = importlib.util.spec_from_file_location(name, SCRIPT_DIR / script_name)\n    module = importlib.util.module_from_spec(spec)\n    assert spec and spec.loader\n    sys.modules[spec.name] = module\n    spec.loader.exec_module(module)\n    return module\n\n\nflow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\ncodex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\nomp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n\n\nclass FakeRunner:\n    def __init__(\n        self,\n        outputs: dict[\n            tuple[str, ...], flow.CommandResult | list[str] | str\n        ] | None = None,\n        *,\n        dry_run: bool = False,\n    ) -> None:\n        self.outputs = outputs or {}\n        self.calls: list[tuple[tuple[str, ...], Path, bool]] = []\n        self.inputs: list[str | None] = []\n        self.dry_run = dry_run\n\n    def run(self, args, cwd, *, check=True, capture=True, input_text=None):\n        key = tuple(args)\n        self.calls.append((key, Path(cwd), check))\n        self.inputs.append(input_text)\n        value = self.outputs.get(key, \"\")\n        if isinstance(value, flow.CommandResult):\n            return value\n        if isinstance(value, list):\n            stdout = value.pop(0) if value else \"\"\n        else:\n            stdout = value\n        return flow.CommandResult(key, Path(cwd), 0, stdout, \"\")\n\n\nclass FailingFastForwardRunner(FakeRunner):\n    def run(self, args, cwd, *, check=True, capture=True, input_text=None):\n        if tuple(args[:3]) == (\"git\", \"merge\", \"--ff-only\"):\n            result = flow.CommandResult(tuple(args), Path(cwd), 1, \"\", \"not a fast-forward\")\n            if check:\n                raise flow.FlowError(flow.format_command_failure(result))\n            return result\n        return super().run(args, cwd, check=check, capture=capture, input_text=input_text)\n\n\nclass FailingSquashMergeRunner(FakeRunner):\n    def __init__(self, *, unmerged_paths: str) -> None:\n        super().__init__()\n        self.unmerged_paths = unmerged_paths\n\n    def run(self, args, cwd, *, check=True, capture=True, input_text=None):\n        key = tuple(args)\n        if key[:3] == (\"git\", \"merge\", \"--squash\"):\n            self.calls.append((key, Path(cwd), check))\n            return flow.CommandResult(key, Path(cwd), 1, \"\", \"merge failed\")\n        if key == (\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"):\n            self.calls.append((key, Path(cwd), check))\n            return flow.CommandResult(key, Path(cwd), 0, self.unmerged_paths, \"\")\n        return super().run(args, cwd, check=check, capture=capture, input_text=input_text)\n\n\nclass CommandRunnerTests(unittest.TestCase):\n    def test_run_resolves_executable_before_invoking_subprocess(self) -> None:\n        completed = subprocess.CompletedProcess(\n            [\"C:/bin/codex.CMD\", \"exec\", \"--help\"],\n            0,\n            \"ok\",\n            \"\",\n        )\n        with (\n            tempfile.TemporaryDirectory() as temp,\n            mock.patch.object(flow.shutil, \"which\", return_value=\"C:/bin/codex.CMD\") as which,\n            mock.patch.object(flow.subprocess, \"run\", return_value=completed) as run,\n        ):\n            result = flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n\n        which.assert_called_once_with(\"codex\")\n        run.assert_called_once()\n        self.assertEqual(\n            run.call_args.args[0],\n            [\"C:/bin/codex.CMD\", \"exec\", \"--help\"],\n        )\n        self.assertEqual(result.args, (\"codex\", \"exec\", \"--help\"))\n        self.assertEqual(result.stdout, \"ok\")\n\n    def test_run_reports_missing_executable_as_flow_error(self) -> None:\n        with (\n            tempfile.TemporaryDirectory() as temp,\n            mock.patch.object(flow.shutil, \"which\", return_value=None),\n            mock.patch.object(flow.subprocess, \"run\") as run,\n            self.assertRaisesRegex(flow.FlowError, \"Executable not found on PATH: codex\"),\n        ):\n            flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n\n        run.assert_not_called()\n\n    def test_run_reports_launch_oserror_as_flow_error(self) -> None:\n        with (\n            tempfile.TemporaryDirectory() as temp,\n            mock.patch.object(flow.shutil, \"which\", return_value=\"C:/bin/codex.CMD\"),\n            mock.patch.object(\n                flow.subprocess,\n                \"run\",\n                side_effect=FileNotFoundError(\"missing shim target\"),\n            ),\n            self.assertRaisesRegex(flow.FlowError, \"Failed to run command: codex exec --help\"),\n        ):\n            flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n\n\nclass WrapperVariantTests(unittest.TestCase):\n    def test_codex_defaults_are_explicit(self) -> None:\n        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n\n    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n\n        self.assertEqual(args.harness, \"codex\")\n        self.assertEqual(args.harness_dir, \".codex\")\n\n    def test_shared_parser_can_override_harness_dir(self) -> None:\n        args = flow.build_parser().parse_args(\n            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n        )\n\n        self.assertEqual(args.harness_dir, \".custom\")\n\n    def test_omp_defaults_are_explicit(self) -> None:\n        self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n        self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\n\n    def test_omp_parser_defaults_to_omp_harness(self) -> None:\n        args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n\n        self.assertEqual(args.harness, \"omp\")\n        self.assertEqual(args.harness_dir, \".omp\")\n\n    def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            (repo / \".omp\").mkdir()\n            runner = FakeRunner()\n            config = omp_flow.FlowConfig(\n                repo=repo,\n                plan=repo / \"plan.md\",\n                base=\"main\",\n                model=None,\n                harness=omp_flow.DEFAULT_HARNESS,\n                harness_dir=omp_flow.HARNESS_DIR,\n                merge_mode=\"squash\",\n                keep_worktrees=False,\n            )\n            subject = omp_flow.HarnessWorktreeFlow(config, runner)\n\n            output_file = repo / \".omp\" / \"handoff\" / \"implementation-final-response.md\"\n            subject.harness_exec(repo, \"Prompt\", output_file)\n\n            args = runner.calls[-1][0]\n            self.assertEqual(args[:2], (\"omp\", \"exec\"))\n            self.assertIn(str(output_file), args)\n            self.assertIn(str((repo / \".omp\").resolve()), args)\n\n    def test_omp_consolidates_existing_repo_ledger_not_created_omp_ledger(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            root = Path(temp)\n            repo = root / \"repo\"\n            feature = root / \"repo-feature\"\n            integration = root / \"repo-integration\"\n            (repo / \".codex\").mkdir(parents=True)\n            (integration / \".omp\").mkdir(parents=True)\n            baseline = feature / \".omp\" / \"handoff\" / \"skill-usage-baseline.json\"\n            runner = FakeRunner()\n            config = omp_flow.FlowConfig(\n                repo=repo,\n                plan=repo / \"plan.md\",\n                base=\"main\",\n                model=None,\n                harness=omp_flow.DEFAULT_HARNESS,\n                harness_dir=omp_flow.HARNESS_DIR,\n                merge_mode=\"squash\",\n                keep_worktrees=False,\n            )\n\n            omp_flow.HarnessWorktreeFlow(config, runner).consolidate_skill_usage(\n                feature,\n                integration,\n                repo,\n                baseline,\n            )\n\n            args = runner.calls[-1][0]\n            self.assertEqual(\n                args[args.index(\"--source-ledger\") + 1],\n                str(feature / \".codex\" / \"skill-usage.json\"),\n            )\n            self.assertEqual(\n                args[args.index(\"--target-ledger\") + 1],\n                str(integration / \".codex\" / \"skill-usage.json\"),\n            )\n\n    def test_omp_treats_codex_skill_usage_conflict_as_usage_only(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            config = omp_flow.FlowConfig(\n                repo=repo,\n                plan=repo / \"plan.md\",\n                base=\"main\",\n                model=None,\n                harness=omp_flow.DEFAULT_HARNESS,\n                harness_dir=omp_flow.HARNESS_DIR,\n                merge_mode=\"squash\",\n                keep_worktrees=False,\n            )\n\n            subject = omp_flow.HarnessWorktreeFlow(config, FakeRunner())\n\n            self.assertTrue(subject.only_skill_usage_unmerged([\".codex/skill-usage.json\"]))\n\n\nclass HarnessWorktreeFlowTests(unittest.TestCase):\n    def test_slug_uses_first_h1(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            plan = Path(temp) / \"plan.md\"\n            plan.write_text(\"# Add Better Audit Flow!\\n\\nBody\", encoding=\"utf-8\")\n            self.assertEqual(flow.derive_slug(plan), \"add-better-audit-flow\")\n\n    def test_slug_falls_back_to_filename(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            plan = Path(temp) / \"My Plan File.md\"\n            plan.write_text(\"No heading\", encoding=\"utf-8\")\n            self.assertEqual(flow.derive_slug(plan), \"my-plan-file\")\n\n    def test_unique_names_skip_existing_branch_and_worktree(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            (Path(temp) / \"repo-example\").mkdir()\n            runner = FakeRunner(\n                {\n                    (\"git\", \"branch\", \"--list\", \"feature/example\"): \"feature/example\\n\",\n                    (\"git\", \"branch\", \"--list\", \"feature/example-2\"): \"\",\n                }\n            )\n            config = self.config(repo, repo / \"plan.md\")\n            names = flow.HarnessWorktreeFlow(config, runner).unique_feature_names(repo, \"example\")\n            self.assertEqual(names.branch, \"feature/example-2\")\n            self.assertEqual(names.worktree.name, \"repo-example-2\")\n\n    def test_plan_inside_repo_uses_same_relative_path(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            plan = repo / \"docs\" / \"plans\" / \"p.md\"\n            target = worktree / \"docs\" / \"plans\" / \"p.md\"\n            plan.parent.mkdir(parents=True)\n            target.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            target.write_text(\"# Plan\", encoding=\"utf-8\")\n            actual = flow.HarnessWorktreeFlow(self.config(repo, plan), FakeRunner()).ensure_plan_in_worktree(repo, plan, worktree, \"plan\")\n            self.assertEqual(actual, target)\n\n    def test_dry_run_plan_copy_does_not_mutate_filesystem(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            plan = Path(temp) / \"external.md\"\n            repo.mkdir()\n            worktree.mkdir()\n            plan.write_text(\"# External\", encoding=\"utf-8\")\n\n            actual = flow.HarnessWorktreeFlow(\n                self.config(repo, plan), FakeRunner(dry_run=True)\n            ).ensure_plan_in_worktree(repo, plan, worktree, \"external\")\n\n            self.assertEqual(actual, worktree / \"docs\" / \"plans\" / \"external.md\")\n            self.assertFalse(actual.exists())\n\n    def test_dry_run_archive_handoff_does_not_mutate_filesystem(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            source = worktree / \".codex\" / \"handoff\"\n            repo.mkdir()\n            source.mkdir(parents=True)\n            (source / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n\n            archive = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"), FakeRunner(dry_run=True)\n            ).archive_handoff(repo, worktree, \"plan-run\", \"feature\")\n\n            self.assertFalse(archive.exists())\n\n    def test_prepare_harness_permissions_grants_sandbox_group_on_windows(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            harness_dir = Path(temp) / \".codex\"\n            harness_dir.mkdir()\n            completed = subprocess.CompletedProcess([\"pwsh\"], 0, \"\", \"\")\n            subject = flow.HarnessWorktreeFlow(\n                self.config(Path(temp), Path(temp) / \"plan.md\"), FakeRunner()\n            )\n\n            with (\n                mock.patch.object(flow.os, \"name\", \"nt\"),\n                mock.patch.object(flow.shutil, \"which\", return_value=\"C:/PowerShell/pwsh.exe\"),\n                mock.patch.object(flow.subprocess, \"run\", return_value=completed) as run,\n            ):\n                subject.prepare_harness_permissions(harness_dir)\n\n            args = run.call_args.args[0]\n            kwargs = run.call_args.kwargs\n            self.assertEqual(args[0], \"C:/PowerShell/pwsh.exe\")\n            self.assertEqual(kwargs[\"env\"][\"CODEX_PERMISSION_ROOT\"], str(harness_dir))\n            self.assertEqual(kwargs[\"env\"][\"CODEX_PERMISSION_GROUP\"], \"CodexSandboxUsers\")\n            self.assertIn(\"RemoveAccessRuleSpecific\", args[4])\n\n    def test_prepare_git_permissions_grants_external_common_git_dir(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            common_git = repo / \".git\"\n            repo.mkdir()\n            worktree.mkdir()\n            common_git.mkdir()\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"),\n                FakeRunner(\n                    {\n                        (\"git\", \"rev-parse\", \"--git-common-dir\"): str(common_git),\n                    }\n                ),\n            )\n            prepared: list[Path] = []\n            subject.prepare_harness_permissions = prepared.append\n\n            subject.prepare_git_permissions(worktree)\n\n            self.assertEqual(prepared, [common_git.resolve()])\n\n    def test_extra_writable_roots_include_harness_and_external_git_dir(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            common_git = repo / \".git\"\n            harness_dir = worktree / \".codex\"\n            repo.mkdir()\n            worktree.mkdir()\n            common_git.mkdir()\n            harness_dir.mkdir()\n            runner = FakeRunner(\n                {\n                    (\"git\", \"rev-parse\", \"--git-common-dir\"): str(common_git),\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            roots = subject.extra_writable_roots(worktree)\n\n            self.assertEqual(roots, [harness_dir.resolve(), common_git.resolve()])\n\n\n    def test_harness_exec_adds_harness_and_common_git_dirs_as_writable_roots(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            common_git = repo / \".git\"\n            harness_dir = worktree / \".codex\"\n            repo.mkdir()\n            worktree.mkdir()\n            common_git.mkdir()\n            harness_dir.mkdir()\n            runner = FakeRunner(\n                {\n                    (\"git\", \"rev-parse\", \"--git-common-dir\"): str(common_git),\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.harness_exec(\n                worktree,\n                \"Prompt\",\n                worktree / \".codex\" / \"handoff\" / \"implementation-final-response.md\",\n            )\n\n            args = runner.calls[-1][0]\n            writable_roots = [\n                args[index + 1]\n                for index, value in enumerate(args)\n                if value == \"--add-dir\"\n            ]\n            self.assertEqual(\n                writable_roots,\n                [str(harness_dir.resolve()), str(common_git.resolve())],\n            )\n\n    def test_harness_exec_uses_full_access_sandbox_on_windows(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            runner = FakeRunner()\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            with mock.patch.object(flow.os, \"name\", \"nt\"):\n                subject.harness_exec(repo, \"Prompt\", repo / \"out.md\")\n\n            args = runner.calls[-1][0]\n            sandbox_index = args.index(\"--sandbox\")\n            self.assertEqual(args[sandbox_index + 1], \"danger-full-access\")\n\n    def test_harness_exec_uses_workspace_write_sandbox_off_windows(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            runner = FakeRunner()\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            with mock.patch.object(flow.os, \"name\", \"posix\"):\n                subject.harness_exec(repo, \"Prompt\", repo / \"out.md\")\n\n            args = runner.calls[-1][0]\n            sandbox_index = args.index(\"--sandbox\")\n            self.assertEqual(args[sandbox_index + 1], \"workspace-write\")\n\n    def test_run_prepares_primary_repo_permissions_before_logging(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-plan\"\n            plan = repo / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            (repo / \".codex\").mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (worktree / \"docs\" / \"plans\").mkdir(parents=True)\n            (worktree / \"docs\" / \"plans\" / \"plan.md\").write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = worktree / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n            runner = FakeRunner(\n                {\n                    (\"git\", \"branch\", \"--list\", \"feature/plan\"): \"\",\n                    (\"git\", \"rev-parse\", \"--git-common-dir\"): \".git\",\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan, merge_mode=\"stop\"), runner\n            )\n            prepared: list[Path] = []\n            subject.prepare_harness_permissions = prepared.append\n            subject.create_feature_worktree = lambda _repo, _names: None\n            subject.run_implementation = lambda _worktree, _plan: None\n            subject.run_audit = lambda _worktree, _plan: None\n            subject.require_implementation_invariants = lambda _worktree, _branch: None\n            subject.head_rev = lambda _worktree: \"before\"\n            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n                \"plan\", \"feature/plan\", worktree, \"plan-run\"\n            )\n            subject.validate = lambda _repo, _plan: None\n            subject.finish = lambda *_args: (_ for _ in ()).throw(\n                AssertionError(\"finish should not run\")\n            )\n\n            subject.run()\n\n            self.assertEqual(prepared[0], repo / \".codex\")\n\n    def test_stage_integration_changes_excludes_handoff_artifacts(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            runner = FakeRunner()\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.stage_integration_changes(repo)\n\n            self.assertEqual(\n                runner.calls[0][0],\n                (\n                    \"git\",\n                    \"add\",\n                    \"-A\",\n                    \"--\",\n                    \".\",\n                    \":(exclude).codex/handoff/**\",\n                ),\n            )\n\n\n    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            runner = FakeRunner(\n                {\n                    (\n                        \"git\",\n                        \"ls-tree\",\n                        \"-r\",\n                        \"--name-only\",\n                        \"feature/plan\",\n                        \"--\",\n                        \".codex/handoff\",\n                    ): \".codex/handoff/audit-summary.md\\n\",\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n\n    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"),\n                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n            )\n\n            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n                subject.require_implementation_invariants(repo, \"feature/plan\")\n\n    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"),\n                FakeRunner(\n                    {\n                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n                        (\n                            \"git\",\n                            \"diff\",\n                            \"--quiet\",\n                            \"main...feature/plan\",\n                            \"--\",\n                            \".\",\n                        ): flow.CommandResult(\n                            (\n                                \"git\",\n                                \"diff\",\n                                \"--quiet\",\n                                \"main...feature/plan\",\n                                \"--\",\n                                \".\",\n                            ),\n                            repo,\n                            0,\n                        ),\n                    }\n                ),\n            )\n\n            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n                subject.require_implementation_invariants(repo, \"feature/plan\")\n\n    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"),\n                FakeRunner(\n                    {\n                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n                        (\n                            \"git\",\n                            \"diff\",\n                            \"--quiet\",\n                            \"main...feature/plan\",\n                            \"--\",\n                            \".\",\n                        ): flow.CommandResult(\n                            (\n                                \"git\",\n                                \"diff\",\n                                \"--quiet\",\n                                \"main...feature/plan\",\n                                \"--\",\n                                \".\",\n                            ),\n                            repo,\n                            1,\n                        ),\n                        (\n                            \"git\",\n                            \"status\",\n                            \"--porcelain\",\n                            \"--untracked-files=all\",\n                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n                    }\n                ),\n            )\n\n            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n                subject.require_implementation_invariants(repo, \"feature/plan\")\n\n    def test_implementation_allows_untracked_handoff_files(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"),\n                FakeRunner(\n                    {\n                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n                        (\n                            \"git\",\n                            \"diff\",\n                            \"--quiet\",\n                            \"main...feature/plan\",\n                            \"--\",\n                            \".\",\n                        ): flow.CommandResult(\n                            (\n                                \"git\",\n                                \"diff\",\n                                \"--quiet\",\n                                \"main...feature/plan\",\n                                \"--\",\n                                \".\",\n                            ),\n                            repo,\n                            1,\n                        ),\n                        (\n                            \"git\",\n                            \"status\",\n                            \"--porcelain\",\n                            \"--untracked-files=all\",\n                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n                    }\n                ),\n            )\n\n            subject.require_implementation_invariants(repo, \"feature/plan\")\n\n    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"),\n                FakeRunner(\n                    {\n                        (\n                            \"git\",\n                            \"status\",\n                            \"--porcelain\",\n                            \"--untracked-files=all\",\n                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n                    }\n                ),\n            )\n\n            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n\n    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"),\n                FakeRunner(\n                    {\n                        (\n                            \"git\",\n                            \"status\",\n                            \"--porcelain\",\n                            \"--untracked-files=all\",\n                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n                        (\n                            \"git\",\n                            \"diff\",\n                            \"--quiet\",\n                            \"main...feature/plan\",\n                            \"--\",\n                            \".\",\n                        ): flow.CommandResult(\n                            (\n                                \"git\",\n                                \"diff\",\n                                \"--quiet\",\n                                \"main...feature/plan\",\n                                \"--\",\n                                \".\",\n                            ),\n                            repo,\n                            1,\n                        ),\n                    }\n                ),\n            )\n\n            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n\n    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"),\n                FakeRunner(\n                    {\n                        (\n                            \"git\",\n                            \"status\",\n                            \"--porcelain\",\n                            \"--untracked-files=all\",\n                        ): \" M app.py\\n\",\n                    }\n                ),\n            )\n\n            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n\n    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"),\n                FakeRunner(\n                    {\n                        (\n                            \"git\",\n                            \"ls-tree\",\n                            \"-r\",\n                            \"--name-only\",\n                            \"feature/plan\",\n                            \"--\",\n                            \".codex/handoff\",\n                        ): \"\",\n                        (\n                            \"git\",\n                            \"status\",\n                            \"--porcelain\",\n                            \"--untracked-files=all\",\n                        ): \"?? leaked.txt\\n\",\n                    }\n                ),\n            )\n\n            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n                subject.require_ready_for_integration(repo, \"feature/plan\")\n\n    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            runner = FakeRunner()\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.run_implementation(repo, repo / \"plan.md\")\n            subject.run_audit(repo, repo / \"plan.md\")\n\n            prompts = [value for value in runner.inputs if value is not None]\n            self.assertIn(\"Do not commit files under `.codex/handoff/`\", prompts[-2])\n            self.assertIn(\"Do not commit files under `.codex/handoff/`\", prompts[-1])\n\n    def test_snapshot_skill_usage_baseline_writes_empty_ledger_when_absent(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            worktree = Path(temp) / \"repo-feature\"\n            (worktree / \".codex\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(worktree, worktree / \"plan.md\"), FakeRunner()\n            )\n\n            baseline = subject.snapshot_skill_usage_baseline(worktree)\n\n            self.assertEqual(baseline, worktree / \".codex\" / \"handoff\" / \"skill-usage-baseline.json\")\n            self.assertEqual(\n                json.loads(baseline.read_text(encoding=\"utf-8\")),\n                {\"version\": 1, \"scopes\": {}},\n            )\n\n    def test_finish_restores_and_consolidates_before_staging_in_squash_mode(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"skill-usage-baseline.json\").write_text(\"{}\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FakeRunner())\n            subject.prepare_harness_permissions = lambda _path: None\n            events: list[str] = []\n            subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n            subject.require_ready_for_integration = lambda _worktree, _branch: None\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(events, [\"restore\", \"consolidate\", \"stage\"])\n\n\n    def test_consolidate_skill_usage_targets_primary_repo_paths(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            root = Path(temp)\n            feature = root / \"repo-plan\"\n            integration = root / \"repo-integrate\"\n            repo = root / \"repo\"\n            baseline = feature / \".codex\" / \"handoff\" / \"skill-usage-baseline.json\"\n            runner = FakeRunner()\n\n            flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner).consolidate_skill_usage(\n                feature,\n                integration,\n                repo,\n                baseline,\n            )\n\n            args = runner.calls[-1][0]\n            self.assertIn(\"--target-repo\", args)\n            self.assertEqual(args[args.index(\"--target-repo\") + 1], str(repo))\n            self.assertIn(\"--target-worktree\", args)\n            self.assertEqual(args[args.index(\"--target-worktree\") + 1], str(integration))\n\n    def test_no_ff_merge_uses_no_commit_and_workflow_commit(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan, merge_mode=\"no-ff\"), FakeRunner()\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.restore_integration_skill_usage_to_head = lambda *_args: None\n            subject.consolidate_skill_usage = lambda *_args: None\n            subject.stage_integration_changes = lambda _worktree: None\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n            subject.require_ready_for_integration = lambda _worktree, _branch: None\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            calls = [call[0] for call in subject.runner.calls]\n            self.assertIn((\"git\", \"merge\", \"--no-ff\", \"--no-commit\", \"feature/plan\"), calls)\n            self.assertIn((\"git\", \"commit\", \"-m\", \"Harness: Plan\"), calls)\n            self.assertNotIn((\"git\", \"merge\", \"--continue\"), calls)\n\n    def test_restore_skill_usage_to_head_restores_tracked_ledger(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            (repo / \".codex\").mkdir(parents=True)\n            runner = FakeRunner(\n                {\n                    (\"git\", \"cat-file\", \"-e\", \"HEAD:.codex/skill-usage.json\"): flow.CommandResult(\n                        (\"git\", \"cat-file\", \"-e\", \"HEAD:.codex/skill-usage.json\"),\n                        repo,\n                        0,\n                    )\n                }\n            )\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n\n            subject.restore_integration_skill_usage_to_head(repo)\n\n            calls = [call[0] for call in runner.calls]\n            self.assertIn((\"git\", \"checkout\", \"HEAD\", \"--\", \".codex/skill-usage.json\"), calls)\n\n    def test_only_skill_usage_conflict_skips_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan),\n                FailingSquashMergeRunner(unmerged_paths=\".codex/skill-usage.json\\n\"),\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n                AssertionError(\"resolver should not run for usage-only conflicts\")\n            )\n            subject.restore_integration_skill_usage_to_head = lambda *_args: None\n            subject.consolidate_skill_usage = lambda *_args: None\n            subject.stage_integration_changes = lambda _worktree: None\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n            subject.require_ready_for_integration = lambda _worktree, _branch: None\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n    def test_conflict_restores_skill_usage_before_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan),\n                FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\"),\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            events: list[str] = []\n            subject.restore_integration_skill_usage_to_head = lambda *_args: events.append(\"restore\")\n            subject.resolve_conflict = lambda *_args: events.append(\"resolve\")\n            subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n            subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n            subject.require_ready_for_integration = lambda _worktree, _branch: None\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(events, [\"restore\", \"resolve\", \"consolidate\", \"stage\"])\n\n    def test_plan_outside_repo_is_copied(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            plan = Path(temp) / \"external.md\"\n            repo.mkdir()\n            worktree.mkdir()\n            plan.write_text(\"# External\", encoding=\"utf-8\")\n            actual = flow.HarnessWorktreeFlow(self.config(repo, plan), FakeRunner()).ensure_plan_in_worktree(repo, plan, worktree, \"external\")\n            self.assertEqual(actual, worktree / \"docs\" / \"plans\" / \"external.md\")\n            self.assertEqual(actual.read_text(encoding=\"utf-8\"), \"# External\")\n\n    def test_harness_command_includes_model_and_output_file(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            plan = repo / \"plan.md\"\n            config = self.config(repo, plan, model=\"gpt-5\")\n            runner = FakeRunner()\n            flow.HarnessWorktreeFlow(config, runner).harness_exec(repo, \"Prompt\", repo / \"out.md\")\n            args = runner.calls[-1][0]\n            self.assertEqual(args[:2], (\"codex\", \"exec\"))\n            self.assertIn(\"--model\", args)\n            self.assertIn(\"gpt-5\", args)\n            self.assertEqual(args[-1], \"-\")\n            self.assertEqual(runner.inputs[-1], \"Prompt\")\n            self.assertIn(\"--output-last-message\", args)\n\n    def test_harness_exec_logs_jsonl_to_main_repo_archive(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-feature\"\n            repo.mkdir()\n            worktree.mkdir()\n            output_file = worktree / \".codex\" / \"handoff\" / \"implementation-final-response.md\"\n            subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), FakeRunner())\n\n            subject.start_log(repo, \"plan-run\")\n            subject.harness_exec(worktree, \"Secret prompt\", output_file)\n\n            log_file = repo / \".codex\" / \"worktree-flow\" / \"plan-run\" / \"workflow.jsonl\"\n            records = [\n                json.loads(line)\n                for line in log_file.read_text(encoding=\"utf-8\").splitlines()\n            ]\n            self.assertEqual(log_file, subject.log_file)\n            self.assertEqual(\n                [record[\"event\"] for record in records],\n                [\n                    \"workflow_log_started\",\n                    \"harness_exec_start\",\n                    \"harness_exec_finish\",\n                ],\n            )\n            self.assertEqual(records[1][\"cwd\"], str(worktree))\n            self.assertNotIn(\"Secret prompt\", records[1][\"command\"])\n            self.assertFalse(records[2][\"output_file_exists\"])\n            self.assertEqual(records[2][\"returncode\"], 0)\n\n    def test_stop_merge_mode_does_not_finish(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            worktree = Path(temp) / \"repo-plan\"\n            plan = repo / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            (worktree / \"docs\" / \"plans\").mkdir(parents=True)\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            (worktree / \"docs\" / \"plans\" / \"plan.md\").write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = worktree / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n\n            runner = FakeRunner(\n                {\n                    (\"git\", \"branch\", \"--list\", \"feature/plan\"): \"\",\n                }\n            )\n            config = self.config(repo, plan, merge_mode=\"stop\")\n            subject = flow.HarnessWorktreeFlow(config, runner)\n            subject.create_feature_worktree = lambda _repo, _names: None\n            subject.run_implementation = lambda _worktree, _plan: None\n            subject.run_audit = lambda _worktree, _plan: None\n            subject.require_implementation_invariants = lambda _worktree, _branch: None\n            subject.head_rev = lambda _worktree: \"before\"\n            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n            subject.require_ready_for_integration = lambda _worktree, _branch: None\n            subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n            subject.validate = lambda _repo, _plan: None\n            subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n\n            subject.run()\n\n    def test_conflict_context_contains_files_and_rules(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            plan = repo / \"plan.md\"\n            runner = FakeRunner(\n                {\n                    (\"git\", \"status\", \"--short\"): \"UU app.py\\n\",\n                    (\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"): \"app.py\\n\",\n                    (\"git\", \"merge-base\", \"main\", \"feature/plan\"): \"abc123\\n\",\n                    (\"git\", \"log\", \"--oneline\", \"abc123..main\"): \"base commit\\n\",\n                    (\"git\", \"log\", \"--oneline\", \"abc123..feature/plan\"): \"feature commit\\n\",\n                }\n            )\n            text = flow.HarnessWorktreeFlow(self.config(repo, plan), runner).conflict_context(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", repo, \"plan-run\"),\n                plan,\n            )\n            self.assertIn(\"app.py\", text)\n            self.assertIn(\"Latest base behavior is presumed correct\", text)\n            self.assertIn(\"feature commit\", text)\n\n    def test_post_conflict_audit_prompt_does_not_allow_commit(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            repo.mkdir()\n            runner = FakeRunner()\n            flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner).run_audit(\n                repo,\n                repo / \"plan.md\",\n                post_conflict=True,\n            )\n            prompt = runner.inputs[-1]\n            self.assertIn(\"Do not commit\", prompt)\n            self.assertNotIn(\"commit audit fixes if changes are made\", prompt.lower())\n\n    def test_finish_does_not_cleanup_if_primary_merge_fails(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n\n            subject = flow.HarnessWorktreeFlow(self.config(repo, plan), FailingFastForwardRunner())\n            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.require_ready_for_integration = lambda _worktree, _branch: None\n\n            with self.assertRaises(flow.FlowError):\n                subject.finish(\n                    repo,\n                    flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                    plan,\n                    \"Plan\",\n                )\n\n    def test_non_conflict_squash_merge_failure_does_not_run_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"\")\n            )\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n                AssertionError(\"resolver should not run\")\n            )\n            subject.require_ready_for_integration = lambda _worktree, _branch: None\n\n            with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                subject.finish(\n                    repo,\n                    flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                    plan,\n                    \"Plan\",\n                )\n\n    def test_conflict_squash_merge_failure_runs_resolver(self) -> None:\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            feature = Path(temp) / \"repo-plan\"\n            plan = feature / \"docs\" / \"plans\" / \"plan.md\"\n            repo.mkdir()\n            plan.parent.mkdir(parents=True)\n            plan.write_text(\"# Plan\", encoding=\"utf-8\")\n            handoff = feature / \".codex\" / \"handoff\"\n            handoff.mkdir(parents=True)\n            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, plan), FailingSquashMergeRunner(unmerged_paths=\"app.py\\n\")\n            )\n            resolved = []\n            subject.prepare_harness_permissions = lambda _path: None\n            subject.resolve_conflict = lambda *_args: resolved.append(True)\n            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n            subject.require_ready_for_integration = lambda _worktree, _branch: None\n\n            subject.finish(\n                repo,\n                flow.Names(\"plan\", \"feature/plan\", feature, \"plan-run\"),\n                plan,\n                \"Plan\",\n            )\n\n            self.assertEqual(resolved, [True])\n\n    def test_parser_rejects_removed_yes_option(self) -> None:\n        with self.assertRaises(SystemExit):\n            flow.build_parser().parse_args([\"--plan\", \"plan.md\", \"--yes\"])\n\n    def test_local_git_worktree_and_squash_merge(self) -> None:\n        if not shutil.which(\"git\"):\n            self.skipTest(\"git is not installed\")\n        with tempfile.TemporaryDirectory() as temp:\n            repo = Path(temp) / \"repo\"\n            subprocess.run([\"git\", \"init\", \"-b\", \"main\", str(repo)], check=True, capture_output=True)\n            subprocess.run([\"git\", \"config\", \"user.email\", \"test@example.com\"], cwd=repo, check=True)\n            subprocess.run([\"git\", \"config\", \"user.name\", \"Test\"], cwd=repo, check=True)\n            (repo / \"file.txt\").write_text(\"base\\n\", encoding=\"utf-8\")\n            subprocess.run([\"git\", \"add\", \"file.txt\"], cwd=repo, check=True)\n            subprocess.run([\"git\", \"commit\", \"-m\", \"base\"], cwd=repo, check=True, capture_output=True)\n\n            feature = Path(temp) / \"repo-feature\"\n            subprocess.run([\"git\", \"worktree\", \"add\", str(feature), \"-b\", \"feature/test\", \"main\"], cwd=repo, check=True, capture_output=True)\n            (feature / \"file.txt\").write_text(\"feature\\n\", encoding=\"utf-8\")\n            subprocess.run([\"git\", \"add\", \"file.txt\"], cwd=feature, check=True)\n            subprocess.run([\"git\", \"commit\", \"-m\", \"feature\"], cwd=feature, check=True, capture_output=True)\n\n            integration = Path(temp) / \"repo-integration\"\n            subprocess.run([\"git\", \"worktree\", \"add\", str(integration), \"-b\", \"integration/test\", \"main\"], cwd=repo, check=True, capture_output=True)\n            subprocess.run([\"git\", \"merge\", \"--squash\", \"feature/test\"], cwd=integration, check=True, capture_output=True)\n            (integration / \".codex\" / \"handoff\").mkdir(parents=True)\n            (integration / \".codex\" / \"handoff\" / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\n            subject = flow.HarnessWorktreeFlow(\n                self.config(repo, repo / \"plan.md\"), flow.CommandRunner()\n            )\n            subject.stage_integration_changes(integration)\n            subprocess.run([\"git\", \"commit\", \"-m\", \"Harness: test\"], cwd=integration, check=True, capture_output=True)\n            subprocess.run([\"git\", \"switch\", \"main\"], cwd=repo, check=True, capture_output=True)\n            subprocess.run([\"git\", \"merge\", \"--ff-only\", \"integration/test\"], cwd=repo, check=True, capture_output=True)\n\n            committed = subprocess.run(\n                [\"git\", \"ls-tree\", \"-r\", \"--name-only\", \"HEAD\"],\n                cwd=repo,\n                check=True,\n                capture_output=True,\n                text=True,\n            ).stdout.splitlines()\n            self.assertEqual((repo / \"file.txt\").read_text(encoding=\"utf-8\"), \"feature\\n\")\n            self.assertIn(\"file.txt\", committed)\n            self.assertNotIn(\".codex/handoff/implementation-summary.md\", committed)\n\n    def config(\n        self,\n        repo: Path,\n        plan: Path,\n        *,\n        model: str | None = None,\n        merge_mode: str = \"squash\",\n    ):\n        return flow.FlowConfig(\n            repo=repo,\n            plan=plan,\n            base=\"main\",\n            model=model,\n            harness=\"codex\",\n            harness_dir=Path(\".codex\"),\n            merge_mode=merge_mode,\n            keep_worktrees=False,\n        )\n\nif __name__ == \"__main__\":\n    unittest.main()\n\ncodex\nThe high-level structure looks consistent with the plan. I need a tighter pass over the invariant helpers and their tests now; thatâ€™s where subtle workflow regressions are most likely.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'rg -n \"require_|non_handoff|status_line|path_is_handoff|audit_invariants|implementation_invariants|ready_for_integration|commit_count|branch_changed|head_rev\" .codex/scripts/worktree-flow.py tests/test_codex_worktree_flow.py' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"git diff -- .codex/scripts/worktree-flow.py tests/test_codex_worktree_flow.py tests/test_worktrees_loadout_sync.py ':(exclude)scratchpad.md'\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'rg -n \"worktree-flow|wrapper|shared|invariant|clean|audit|commit|diff|handoff|loadout\" tests/test_codex_worktree_flow.py tests/test_worktrees_loadout_sync.py loadouts/worktrees/.harness/docs/codex-worktree-flow.md' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 896ms:\ntests/test_codex_worktree_flow.py:480:            subject.require_implementation_invariants = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:481:            subject.head_rev = lambda _worktree: \"before\"\ntests/test_codex_worktree_flow.py:482:            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\ntests/test_codex_worktree_flow.py:535:                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:546:                subject.require_implementation_invariants(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:580:                subject.require_implementation_invariants(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:582:    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\ntests/test_codex_worktree_flow.py:620:                subject.require_implementation_invariants(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:659:            subject.require_implementation_invariants(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:679:            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\ntests/test_codex_worktree_flow.py:718:            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\ntests/test_codex_worktree_flow.py:720:    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\ntests/test_codex_worktree_flow.py:738:                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\ntests/test_codex_worktree_flow.py:740:    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\ntests/test_codex_worktree_flow.py:767:                subject.require_ready_for_integration(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:817:            subject.require_ready_for_integration = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:868:            subject.require_ready_for_integration = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:923:            subject.require_ready_for_integration = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:952:            subject.require_ready_for_integration = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:1046:            subject.require_implementation_invariants = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:1047:            subject.head_rev = lambda _worktree: \"before\"\ntests/test_codex_worktree_flow.py:1048:            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\ntests/test_codex_worktree_flow.py:1049:            subject.require_ready_for_integration = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:1109:            subject.require_ready_for_integration = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:1138:            subject.require_ready_for_integration = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:1167:            subject.require_ready_for_integration = lambda _worktree, _branch: None\n.codex/scripts/worktree-flow.py:200:        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n.codex/scripts/worktree-flow.py:201:        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n.codex/scripts/worktree-flow.py:202:        self.require_implementation_invariants(names.worktree, names.branch)\n.codex/scripts/worktree-flow.py:203:        audit_head_before = self.head_rev(names.worktree)\n.codex/scripts/worktree-flow.py:205:        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n.codex/scripts/worktree-flow.py:206:        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n.codex/scripts/worktree-flow.py:207:        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n.codex/scripts/worktree-flow.py:220:        self.require_ready_for_integration(names.worktree, names.branch)\n.codex/scripts/worktree-flow.py:383:        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n.codex/scripts/worktree-flow.py:384:        self.require_ready_for_integration(names.worktree, names.branch)\n.codex/scripts/worktree-flow.py:496:        self.require_file(\n.codex/scripts/worktree-flow.py:500:        self.require_file(\n.codex/scripts/worktree-flow.py:677:    def require_no_tracked_handoff_artifacts(\n.codex/scripts/worktree-flow.py:702:    def head_rev(self, worktree: Path) -> str:\n.codex/scripts/worktree-flow.py:706:    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n.codex/scripts/worktree-flow.py:714:    def require_commits_since_base(\n.codex/scripts/worktree-flow.py:717:        count = self.commit_count_since_base(worktree, branch)\n.codex/scripts/worktree-flow.py:725:    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n.codex/scripts/worktree-flow.py:740:    def non_handoff_status(self, worktree: Path) -> list[str]:\n.codex/scripts/worktree-flow.py:748:            if line.strip() and not self.status_line_is_handoff(line)\n.codex/scripts/worktree-flow.py:751:    def status_line_is_handoff(self, line: str) -> bool:\n.codex/scripts/worktree-flow.py:754:        return all(self.path_is_handoff(path) for path in paths if path)\n.codex/scripts/worktree-flow.py:756:    def path_is_handoff(self, path: str) -> bool:\n.codex/scripts/worktree-flow.py:761:    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n.codex/scripts/worktree-flow.py:762:        status = self.non_handoff_status(worktree)\n.codex/scripts/worktree-flow.py:770:    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n.codex/scripts/worktree-flow.py:771:        self.require_commits_since_base(worktree, branch, \"Implementation\")\n.codex/scripts/worktree-flow.py:772:        self.require_branch_changed_since_base(worktree, branch)\n.codex/scripts/worktree-flow.py:773:        self.require_clean_except_handoff(worktree, \"Implementation\")\n.codex/scripts/worktree-flow.py:775:    def require_audit_invariants(\n.codex/scripts/worktree-flow.py:778:        self.require_clean_except_handoff(worktree, \"Audit\")\n.codex/scripts/worktree-flow.py:779:        head_after = self.head_rev(worktree)\n.codex/scripts/worktree-flow.py:781:            self.require_branch_changed_since_base(worktree, branch)\n.codex/scripts/worktree-flow.py:783:    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n.codex/scripts/worktree-flow.py:784:        self.require_no_tracked_handoff_artifacts(worktree, branch)\n.codex/scripts/worktree-flow.py:785:        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n.codex/scripts/worktree-flow.py:786:        self.require_branch_changed_since_base(worktree, branch)\n.codex/scripts/worktree-flow.py:1007:    def require_file(self, path: Path) -> None:\n\n succeeded in 1298ms:\n succeeded in 1627ms:\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:3:`worktree-flow.py` contains the shared implementation for the repeatable part of the workflow after you have an approved plan. `worktree-flow-codex.py` and `worktree-flow-omp.py` are compatibility wrappers that call the shared implementation with explicit harness defaults:\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:5:- `worktree-flow-codex.py` defaults to `codex` and `.codex/`.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:6:- `worktree-flow-omp.py` defaults to `omp` and `.omp/`.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:10:3. Requires an implementation commit and handoff summary.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:11:4. Runs a fresh <harness> audit pass.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:12:5. Requires an audit summary.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:13:6. Verifies no non-handoff changes are left pending.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:15:8. Squash-merges by default and cleans up only after a successful merge.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:24:python .\\.codex\\scripts\\worktree-flow-codex.py --plan docs\\plans\\my-plan.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:30:python .\\.omp\\scripts\\worktree-flow-omp.py --plan docs\\plans\\my-plan.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:37:python .\\.codex\\scripts\\worktree-flow-codex.py --plan docs\\plans\\my-plan.md --dry-run\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:39:# Implement and audit, but stop before merging\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:40:python .\\.codex\\scripts\\worktree-flow-codex.py --plan docs\\plans\\my-plan.md --merge-mode stop\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:43:python .\\.codex\\scripts\\worktree-flow-codex.py --plan docs\\plans\\my-plan.md --keep-worktrees\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:46:python .\\.codex\\scripts\\worktree-flow-codex.py --plan docs\\plans\\my-plan.md --base develop\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:54:- `audit-worktree`: audits the implementation in a fresh <harness> run and fixes confirmed issues.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:57:For this loadout, the source templates live under:\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:60:loadouts/worktrees/.harness/skills/\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:63:When applying this loadout, `harness-init.ps1 -Harness <harness>` copies skills into:\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:74:.<harness>/handoff/implementation-summary.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:75:.<harness>/handoff/implementation-final-response.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:76:.<harness>/handoff/audit-summary.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:77:.<harness>/handoff/audit-final-response.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:83:.<harness>/handoff/merge-conflict-context.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:84:.<harness>/handoff/conflict-resolution-summary.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:85:.<harness>/handoff/conflict-resolution-final-response.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:86:.<harness>/handoff/post-conflict-audit-summary.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:87:.<harness>/handoff/post-conflict-audit-final-response.md\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:96:Before cleanup, the script archives handoff files back into the primary checkout:\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:99:.<harness>/worktree-flow/<slug>-<timestamp>/feature/\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:100:.<harness>/worktree-flow/<slug>-<timestamp>/integration/\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:108:- `--merge-mode stop` leaves the audited feature worktree ready for manual review.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:109:- Default merge mode is `squash`, so implementation/audit commits are treated as execution detail.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:112:- The implementation phase must create at least one commit and produce a diff from the base branch.\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md:113:- The audit phase may be a no-op with no new commit when the worktree is clean outside `.<harness>/handoff/`.\ntests/test_worktrees_loadout_sync.py:9:LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\ntests/test_worktrees_loadout_sync.py:13:    \"worktree-flow.py\",\ntests/test_worktrees_loadout_sync.py:14:    \"worktree-flow-codex.py\",\ntests/test_worktrees_loadout_sync.py:15:    \"worktree-flow-omp.py\",\ntests/test_worktrees_loadout_sync.py:25:    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\ntests/test_worktrees_loadout_sync.py:28:            loadout = LOADOUT_SCRIPTS / script_name\ntests/test_worktrees_loadout_sync.py:31:                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\ntests/test_worktrees_loadout_sync.py:34:                    normalized_text(loadout),\ntests/test_worktrees_loadout_sync.py:36:                        f\"{active} and {loadout} are out of sync. Update the \"\ntests/test_worktrees_loadout_sync.py:37:                        \"active script and shipped worktrees loadout copy together.\"\ntests/test_codex_worktree_flow.py:26:flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\ntests/test_codex_worktree_flow.py:27:codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\ntests/test_codex_worktree_flow.py:28:omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\ntests/test_codex_worktree_flow.py:79:        if key == (\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"):\ntests/test_codex_worktree_flow.py:138:        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\ntests/test_codex_worktree_flow.py:146:    def test_shared_parser_can_override_harness_dir(self) -> None:\ntests/test_codex_worktree_flow.py:156:        self.assertEqual(omp_flow.HANDOFF_DIR, Path(\".omp\") / \"handoff\")\ntests/test_codex_worktree_flow.py:164:    def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\ntests/test_codex_worktree_flow.py:182:            output_file = repo / \".omp\" / \"handoff\" / \"implementation-final-response.md\"\ntests/test_codex_worktree_flow.py:198:            baseline = feature / \".omp\" / \"handoff\" / \"skill-usage-baseline.json\"\ntests/test_codex_worktree_flow.py:253:            self.assertEqual(flow.derive_slug(plan), \"add-better-audit-flow\")\ntests/test_codex_worktree_flow.py:306:    def test_dry_run_archive_handoff_does_not_mutate_filesystem(self) -> None:\ntests/test_codex_worktree_flow.py:310:            source = worktree / \".codex\" / \"handoff\"\ntests/test_codex_worktree_flow.py:317:            ).archive_handoff(repo, worktree, \"plan-run\", \"feature\")\ntests/test_codex_worktree_flow.py:409:                worktree / \".codex\" / \"handoff\" / \"implementation-final-response.md\",\ntests/test_codex_worktree_flow.py:462:            handoff = worktree / \".codex\" / \"handoff\"\ntests/test_codex_worktree_flow.py:463:            handoff.mkdir(parents=True)\ntests/test_codex_worktree_flow.py:464:            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:465:            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:479:            subject.run_audit = lambda _worktree, _plan: None\ntests/test_codex_worktree_flow.py:480:            subject.require_implementation_invariants = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:482:            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\ntests/test_codex_worktree_flow.py:495:    def test_stage_integration_changes_excludes_handoff_artifacts(self) -> None:\ntests/test_codex_worktree_flow.py:511:                    \":(exclude).codex/handoff/**\",\ntests/test_codex_worktree_flow.py:516:    def test_tracked_handoff_artifacts_are_rejected(self) -> None:\ntests/test_codex_worktree_flow.py:528:                        \".codex/handoff\",\ntests/test_codex_worktree_flow.py:529:                    ): \".codex/handoff/audit-summary.md\\n\",\ntests/test_codex_worktree_flow.py:534:            with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\ntests/test_codex_worktree_flow.py:535:                subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:537:    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\ntests/test_codex_worktree_flow.py:545:            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\ntests/test_codex_worktree_flow.py:546:                subject.require_implementation_invariants(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:548:    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\ntests/test_codex_worktree_flow.py:558:                            \"diff\",\ntests/test_codex_worktree_flow.py:566:                                \"diff\",\ntests/test_codex_worktree_flow.py:580:                subject.require_implementation_invariants(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:582:    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\ntests/test_codex_worktree_flow.py:592:                            \"diff\",\ntests/test_codex_worktree_flow.py:600:                                \"diff\",\ntests/test_codex_worktree_flow.py:614:                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\ntests/test_codex_worktree_flow.py:619:            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\ntests/test_codex_worktree_flow.py:620:                subject.require_implementation_invariants(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:622:    def test_implementation_allows_untracked_handoff_files(self) -> None:\ntests/test_codex_worktree_flow.py:632:                            \"diff\",\ntests/test_codex_worktree_flow.py:640:                                \"diff\",\ntests/test_codex_worktree_flow.py:654:                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\ntests/test_codex_worktree_flow.py:659:            subject.require_implementation_invariants(repo, \"feature/plan\")\ntests/test_codex_worktree_flow.py:661:    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\ntests/test_codex_worktree_flow.py:673:                        ): \"?? .codex/handoff/audit-summary.md\\n\",\ntests/test_codex_worktree_flow.py:679:            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\ntests/test_codex_worktree_flow.py:681:    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\ntests/test_codex_worktree_flow.py:693:                        ): \"?? .codex/handoff/audit-summary.md\\n\",\ntests/test_codex_worktree_flow.py:697:                            \"diff\",\ntests/test_codex_worktree_flow.py:705:                                \"diff\",\ntests/test_codex_worktree_flow.py:718:            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\ntests/test_codex_worktree_flow.py:720:    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\ntests/test_codex_worktree_flow.py:737:            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\ntests/test_codex_worktree_flow.py:738:                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\ntests/test_codex_worktree_flow.py:740:    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\ntests/test_codex_worktree_flow.py:754:                            \".codex/handoff\",\ntests/test_codex_worktree_flow.py:769:    def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\ntests/test_codex_worktree_flow.py:777:            subject.run_audit(repo, repo / \"plan.md\")\ntests/test_codex_worktree_flow.py:780:            self.assertIn(\"Do not commit files under `.codex/handoff/`\", prompts[-2])\ntests/test_codex_worktree_flow.py:781:            self.assertIn(\"Do not commit files under `.codex/handoff/`\", prompts[-1])\ntests/test_codex_worktree_flow.py:793:            self.assertEqual(baseline, worktree / \".codex\" / \"handoff\" / \"skill-usage-baseline.json\")\ntests/test_codex_worktree_flow.py:807:            handoff = feature / \".codex\" / \"handoff\"\ntests/test_codex_worktree_flow.py:808:            handoff.mkdir(parents=True)\ntests/test_codex_worktree_flow.py:809:            (handoff / \"skill-usage-baseline.json\").write_text(\"{}\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:816:            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\ntests/test_codex_worktree_flow.py:835:            baseline = feature / \".codex\" / \"handoff\" / \"skill-usage-baseline.json\"\ntests/test_codex_worktree_flow.py:851:    def test_no_ff_merge_uses_no_commit_and_workflow_commit(self) -> None:\ntests/test_codex_worktree_flow.py:859:            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\ntests/test_codex_worktree_flow.py:867:            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\ntests/test_codex_worktree_flow.py:878:            self.assertIn((\"git\", \"merge\", \"--no-ff\", \"--no-commit\", \"feature/plan\"), calls)\ntests/test_codex_worktree_flow.py:879:            self.assertIn((\"git\", \"commit\", \"-m\", \"Harness: Plan\"), calls)\ntests/test_codex_worktree_flow.py:910:            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\ntests/test_codex_worktree_flow.py:922:            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\ntests/test_codex_worktree_flow.py:940:            (feature / \".codex\" / \"handoff\").mkdir(parents=True)\ntests/test_codex_worktree_flow.py:951:            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\ntests/test_codex_worktree_flow.py:996:            output_file = worktree / \".codex\" / \"handoff\" / \"implementation-final-response.md\"\ntests/test_codex_worktree_flow.py:1002:            log_file = repo / \".codex\" / \"worktree-flow\" / \"plan-run\" / \"workflow.jsonl\"\ntests/test_codex_worktree_flow.py:1031:            handoff = worktree / \".codex\" / \"handoff\"\ntests/test_codex_worktree_flow.py:1032:            handoff.mkdir(parents=True)\ntests/test_codex_worktree_flow.py:1033:            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:1034:            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:1045:            subject.run_audit = lambda _worktree, _plan: None\ntests/test_codex_worktree_flow.py:1046:            subject.require_implementation_invariants = lambda _worktree, _branch: None\ntests/test_codex_worktree_flow.py:1048:            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\ntests/test_codex_worktree_flow.py:1063:                    (\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"): \"app.py\\n\",\ntests/test_codex_worktree_flow.py:1065:                    (\"git\", \"log\", \"--oneline\", \"abc123..main\"): \"base commit\\n\",\ntests/test_codex_worktree_flow.py:1066:                    (\"git\", \"log\", \"--oneline\", \"abc123..feature/plan\"): \"feature commit\\n\",\ntests/test_codex_worktree_flow.py:1076:            self.assertIn(\"feature commit\", text)\ntests/test_codex_worktree_flow.py:1078:    def test_post_conflict_audit_prompt_does_not_allow_commit(self) -> None:\ntests/test_codex_worktree_flow.py:1083:            flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner).run_audit(\ntests/test_codex_worktree_flow.py:1089:            self.assertIn(\"Do not commit\", prompt)\ntests/test_codex_worktree_flow.py:1090:            self.assertNotIn(\"commit audit fixes if changes are made\", prompt.lower())\ntests/test_codex_worktree_flow.py:1092:    def test_finish_does_not_cleanup_if_primary_merge_fails(self) -> None:\ntests/test_codex_worktree_flow.py:1100:            handoff = feature / \".codex\" / \"handoff\"\ntests/test_codex_worktree_flow.py:1101:            handoff.mkdir(parents=True)\ntests/test_codex_worktree_flow.py:1102:            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:1103:            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:1106:            subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\ntests/test_codex_worktree_flow.py:1107:            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\ntests/test_codex_worktree_flow.py:1127:            handoff = feature / \".codex\" / \"handoff\"\ntests/test_codex_worktree_flow.py:1128:            handoff.mkdir(parents=True)\ntests/test_codex_worktree_flow.py:1129:            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:1130:            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:1156:            handoff = feature / \".codex\" / \"handoff\"\ntests/test_codex_worktree_flow.py:1157:            handoff.mkdir(parents=True)\ntests/test_codex_worktree_flow.py:1158:            (handoff / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:1159:            (handoff / \"audit-summary.md\").write_text(\"audit\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:1166:            subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\ntests/test_codex_worktree_flow.py:1192:            subprocess.run([\"git\", \"commit\", \"-m\", \"base\"], cwd=repo, check=True, capture_output=True)\ntests/test_codex_worktree_flow.py:1198:            subprocess.run([\"git\", \"commit\", \"-m\", \"feature\"], cwd=feature, check=True, capture_output=True)\ntests/test_codex_worktree_flow.py:1203:            (integration / \".codex\" / \"handoff\").mkdir(parents=True)\ntests/test_codex_worktree_flow.py:1204:            (integration / \".codex\" / \"handoff\" / \"implementation-summary.md\").write_text(\"impl\", encoding=\"utf-8\")\ntests/test_codex_worktree_flow.py:1209:            subprocess.run([\"git\", \"commit\", \"-m\", \"Harness: test\"], cwd=integration, check=True, capture_output=True)\ntests/test_codex_worktree_flow.py:1213:            committed = subprocess.run(\ntests/test_codex_worktree_flow.py:1221:            self.assertIn(\"file.txt\", committed)\ntests/test_codex_worktree_flow.py:1222:            self.assertNotIn(\".codex/handoff/implementation-summary.md\", committed)\n\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"git diff main...HEAD -- .codex/scripts/worktree-flow.py tests/test_codex_worktree_flow.py tests/test_worktrees_loadout_sync.py ':(exclude)scratchpad.md'\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command '$i=1; Get-Content tests/test_codex_worktree_flow.py | ForEach-Object { if ($i -ge 516 -and $i -le 770) { '\"'{0,4}: {1}' -f \"'$i, $_ }; $i++ }' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command '$i=1; Get-Content .codex/scripts/worktree-flow.py | ForEach-Object { if ($i -ge 660 -and $i -le 790) { '\"'{0,4}: {1}' -f \"'$i, $_ }; $i++ }' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 949ms:\ndiff --git a/.codex/scripts/worktree-flow.py b/.codex/scripts/worktree-flow.py\nnew file mode 100644\nindex 0000000..1139951\n--- /dev/null\n+++ b/.codex/scripts/worktree-flow.py\n@@ -0,0 +1,1090 @@\n+#!/usr/bin/env python3\n+\"\"\"Run a plan -> implement -> audit -> finish harness worktree workflow.\"\"\"\n+\n+from __future__ import annotations\n+\n+import argparse\n+import json\n+import os\n+import re\n+import shutil\n+import subprocess\n+import sys\n+from dataclasses import dataclass\n+from datetime import datetime\n+from pathlib import Path\n+from typing import Sequence\n+\n+EMPTY_SKILL_USAGE_LEDGER = {\"version\": 1, \"scopes\": {}}\n+\n+\n+def infer_default_harness_dir(script_path: Path | None = None) -> Path:\n+    script = script_path or Path(__file__)\n+    parent = script.resolve().parent.parent.name\n+    if parent.startswith(\".\"):\n+        return Path(parent)\n+    return Path(\".harness\")\n+\n+\n+def infer_default_harness(harness_dir: Path) -> str:\n+    name = harness_dir.name\n+    return name[1:] if name.startswith(\".\") and len(name) > 1 else name\n+\n+\n+HARNESS_DIR = infer_default_harness_dir()\n+DEFAULT_HARNESS = infer_default_harness(HARNESS_DIR)\n+HANDOFF_DIR = HARNESS_DIR / \"handoff\"\n+\n+\n+class FlowError(RuntimeError):\n+    \"\"\"A recoverable workflow error with a user-facing message.\"\"\"\n+\n+\n+@dataclass(frozen=True)\n+class CommandResult:\n+    args: tuple[str, ...]\n+    cwd: Path\n+    returncode: int\n+    stdout: str = \"\"\n+    stderr: str = \"\"\n+\n+\n+class CommandRunner:\n+    def __init__(self, dry_run: bool = False) -> None:\n+        self.dry_run = dry_run\n+\n+    def run(\n+        self,\n+        args: Sequence[str],\n+        cwd: Path,\n+        *,\n+        check: bool = True,\n+        capture: bool = True,\n+        input_text: str | None = None,\n+    ) -> CommandResult:\n+        display = \" \".join(args)\n+        print(f\"+ ({cwd}) {display}\")\n+        if self.dry_run:\n+            return CommandResult(tuple(args), cwd, 0)\n+\n+        executable = shutil.which(args[0])\n+        if executable is None:\n+            raise FlowError(f\"Executable not found on PATH: {args[0]}\")\n+        resolved_args = [executable, *args[1:]]\n+        try:\n+            completed = subprocess.run(\n+                resolved_args,\n+                cwd=cwd,\n+                check=False,\n+                capture_output=capture,\n+                text=True,\n+                input=input_text,\n+            )\n+        except OSError as exc:\n+            raise FlowError(\n+                f\"Failed to run command: {display}\\ncwd: {cwd}\\n{exc}\"\n+            ) from exc\n+        result = CommandResult(\n+            tuple(args),\n+            cwd,\n+            completed.returncode,\n+            completed.stdout or \"\",\n+            completed.stderr or \"\",\n+        )\n+        if check and result.returncode != 0:\n+            raise FlowError(format_command_failure(result))\n+        return result\n+\n+\n+def format_command_failure(result: CommandResult) -> str:\n+    parts = [\n+        f\"Command failed with exit code {result.returncode}: {' '.join(result.args)}\",\n+        f\"cwd: {result.cwd}\",\n+    ]\n+    if result.stdout.strip():\n+        parts.append(\"stdout:\\n\" + result.stdout.strip())\n+    if result.stderr.strip():\n+        parts.append(\"stderr:\\n\" + result.stderr.strip())\n+    return \"\\n\".join(parts)\n+\n+\n+def slugify(value: str, *, max_words: int = 6, max_len: int = 60) -> str:\n+    cleaned = re.sub(r\"[^a-z0-9]+\", \"-\", value.lower()).strip(\"-\")\n+    words = [part for part in cleaned.split(\"-\") if part]\n+    slug = \"-\".join(words[:max_words]) or \"harness-plan\"\n+    return slug[:max_len].strip(\"-\") or \"harness-plan\"\n+\n+\n+def plan_title(plan_path: Path) -> str:\n+    text = plan_path.read_text(encoding=\"utf-8\")\n+    for line in text.splitlines():\n+        match = re.match(r\"^#\\s+(.+?)\\s*$\", line)\n+        if match:\n+            return match.group(1).strip()\n+    return plan_path.stem\n+\n+\n+def derive_slug(plan_path: Path) -> str:\n+    return slugify(plan_title(plan_path))\n+\n+\n+def is_relative_to(path: Path, parent: Path) -> bool:\n+    try:\n+        path.resolve().relative_to(parent.resolve())\n+        return True\n+    except ValueError:\n+        return False\n+\n+\n+def ensure_dir(path: Path) -> None:\n+    path.mkdir(parents=True, exist_ok=True)\n+\n+\n+def write_text(path: Path, text: str) -> None:\n+    ensure_dir(path.parent)\n+    path.write_text(text, encoding=\"utf-8\", newline=\"\\n\")\n+\n+\n+@dataclass(frozen=True)\n+class Names:\n+    slug: str\n+    branch: str\n+    worktree: Path\n+    run_id: str\n+\n+\n+@dataclass(frozen=True)\n+class FlowConfig:\n+    repo: Path\n+    plan: Path\n+    base: str\n+    model: str | None\n+    harness: str\n+    harness_dir: Path\n+    merge_mode: str\n+    keep_worktrees: bool\n+\n+\n+class HarnessWorktreeFlow:\n+    def __init__(self, config: FlowConfig, runner: CommandRunner) -> None:\n+        self.config = config\n+        self.runner = runner\n+        self.log_file: Path | None = None\n+\n+    @property\n+    def harness_dir(self) -> Path:\n+        return self.config.harness_dir\n+\n+    @property\n+    def handoff_dir(self) -> Path:\n+        return self.config.harness_dir / \"handoff\"\n+\n+    def run(self) -> None:\n+        repo = self.git_root(self.config.repo.resolve())\n+        plan = self.config.plan.resolve()\n+        self.validate(repo, plan)\n+\n+        names = self.unique_feature_names(repo, derive_slug(plan))\n+        self.prepare_harness_permissions(repo / self.harness_dir)\n+        self.prepare_git_permissions(repo)\n+        self.start_log(repo, names.run_id)\n+        print(f\"Feature branch: {names.branch}\")\n+        print(f\"Feature worktree: {names.worktree}\")\n+\n+        self.create_feature_worktree(repo, names)\n+        plan_in_worktree = self.ensure_plan_in_worktree(\n+            repo, plan, names.worktree, names.slug\n+        )\n+        self.snapshot_skill_usage_baseline(names.worktree, repo)\n+        self.run_implementation(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"implementation-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_implementation_invariants(names.worktree, names.branch)\n+        audit_head_before = self.head_rev(names.worktree)\n+        self.run_audit(names.worktree, plan_in_worktree)\n+        self.require_file(names.worktree / self.handoff_dir / \"audit-summary.md\")\n+        self.require_no_tracked_handoff_artifacts(names.worktree, names.branch)\n+        self.require_audit_invariants(names.worktree, names.branch, audit_head_before)\n+        archive_dir = self.archive_handoff(\n+            repo, names.worktree, names.run_id, \"feature\"\n+        )\n+        print(f\"Handoff archive: {archive_dir}\")\n+\n+        if self.config.merge_mode == \"stop\":\n+            print(\"Stopped before merge by request.\")\n+            print(f\"Plan: {plan_in_worktree}\")\n+            print(f\"Worktree: {names.worktree}\")\n+            print(f\"Branch: {names.branch}\")\n+            return\n+\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.finish(repo, names, plan_in_worktree, plan_title(plan))\n+\n+    def validate(self, repo: Path, plan: Path) -> None:\n+        if not plan.exists():\n+            raise FlowError(f\"Plan file does not exist: {plan}\")\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+        self.runner.run([\"git\", \"rev-parse\", \"--verify\", self.config.base], repo)\n+        self.runner.run([self.config.harness, \"exec\", \"--help\"], repo)\n+\n+    def git_root(self, start: Path) -> Path:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"--show-toplevel\"], start)\n+        root = result.stdout.strip()\n+        return Path(root).resolve() if root else start\n+\n+    def unique_feature_names(self, repo: Path, slug: str) -> Names:\n+        repo_name = repo.name\n+        parent = repo.parent\n+        suffix = 1\n+        while True:\n+            candidate_slug = slug if suffix == 1 else f\"{slug}-{suffix}\"\n+            branch = f\"feature/{candidate_slug}\"\n+            worktree = parent / f\"{repo_name}-{candidate_slug}\"\n+            branch_exists = self.runner.run(\n+                [\"git\", \"branch\", \"--list\", branch], repo\n+            ).stdout.strip()\n+            if not branch_exists and not worktree.exists():\n+                run_id = datetime.now().strftime(f\"{candidate_slug}-%Y%m%d-%H%M%S\")\n+                return Names(candidate_slug, branch, worktree, run_id)\n+            suffix += 1\n+\n+    def create_feature_worktree(self, repo: Path, names: Names) -> None:\n+        self.runner.run(\n+            [\n+                \"git\",\n+                \"worktree\",\n+                \"add\",\n+                str(names.worktree),\n+                \"-b\",\n+                names.branch,\n+                self.config.base,\n+            ],\n+            repo,\n+        )\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.ensure_dir(names.worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(names.worktree / self.harness_dir)\n+        self.prepare_git_permissions(names.worktree)\n+\n+    def ensure_plan_in_worktree(\n+        self, repo: Path, plan: Path, worktree: Path, slug: str\n+    ) -> Path:\n+        if is_relative_to(plan, repo):\n+            rel = plan.relative_to(repo)\n+            target = worktree / rel\n+            if target.exists():\n+                return target\n+        else:\n+            rel = Path(\"docs\") / \"plans\" / f\"{slug}.md\"\n+            target = worktree / rel\n+\n+        self.ensure_dir(target.parent)\n+        self.copy_file(plan, target)\n+        return target\n+\n+    def run_implementation(self, worktree: Path, plan_path: Path) -> None:\n+        prompt = f\"\"\"Use the implement-worktree skill.\n+\n+Implement the approved plan in `{self.rel(worktree, plan_path)}` inside this worktree.\n+\n+Requirements:\n+- Do not create, switch, merge, delete, or rebase worktrees.\n+- Keep edits scoped to the plan.\n+- Run focused tests or checks appropriate to the change.\n+- Commit the completed implementation.\n+- Write `{self.handoff_dir.as_posix()}/implementation-summary.md` with plan path, branch/worktree, changed files, behavior changes, tests run, skipped checks, assumptions, and known risks.\n+- Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+\"\"\"\n+        output = worktree / self.handoff_dir / \"implementation-final-response.md\"\n+        self.harness_exec(worktree, prompt, output)\n+\n+    def run_audit(\n+        self, worktree: Path, plan_path: Path, *, post_conflict: bool = False\n+    ) -> None:\n+        summary = self.handoff_dir / (\n+            \"post-conflict-audit-summary.md\" if post_conflict else \"audit-summary.md\"\n+        )\n+        audit_finish_instruction = (\n+            \"Do not commit. Leave all resolved merge state and audit fixes staged or unstaged for the workflow script to finalize.\"\n+            if post_conflict\n+            else \"Commit audit fixes if changes are made.\"\n+        )\n+        prompt = f\"\"\"Use the audit-worktree skill.\n+\n+Fresh audit pass in this worktree.\n+\n+Read:\n+- `{self.rel(worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+{f\"- `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`\" if post_conflict else \"\"}\n+\n+Audit the actual diff against `{self.config.base}`. Fix confirmed issues and run relevant tests.\n+{audit_finish_instruction}\n+Do not commit files under `{self.handoff_dir.as_posix()}/`; they are workflow artifacts and must remain untracked.\n+Write `{summary.as_posix()}` before finishing.\n+\"\"\"\n+        output = (\n+            worktree\n+            / self.handoff_dir\n+            / (\n+                \"post-conflict-audit-final-response.md\"\n+                if post_conflict\n+                else \"audit-final-response.md\"\n+            )\n+        )\n+        self.harness_exec(worktree, prompt, output)\n+\n+    def harness_sandbox_mode(self) -> str:\n+        if os.name == \"nt\":\n+            return \"danger-full-access\"\n+        return \"workspace-write\"\n+\n+    def harness_exec(self, cwd: Path, prompt: str, output_file: Path) -> None:\n+        self.ensure_dir(output_file.parent)\n+        args = [\n+            self.config.harness,\n+            \"exec\",\n+            \"--cd\",\n+            str(cwd),\n+            \"--sandbox\",\n+            self.harness_sandbox_mode(),\n+        ]\n+        for writable_root in self.extra_writable_roots(cwd):\n+            args.extend([\"--add-dir\", str(writable_root)])\n+        if self.config.model:\n+            args.extend([\"--model\", self.config.model])\n+        args.extend([\"--output-last-message\", str(output_file), \"-\"])\n+        self.log_event(\n+            \"harness_exec_start\",\n+            cwd=str(cwd),\n+            output_file=str(output_file),\n+            command=args[:-1],\n+        )\n+        result = self.runner.run(args, cwd, check=False, input_text=prompt)\n+        self.log_event(\n+            \"harness_exec_finish\",\n+            cwd=str(cwd),\n+            output_file=str(output_file),\n+            output_file_exists=output_file.exists(),\n+            returncode=result.returncode,\n+            stdout=result.stdout,\n+            stderr=result.stderr,\n+        )\n+        if result.returncode != 0:\n+            raise FlowError(format_command_failure(result))\n+\n+    def finish(self, repo: Path, names: Names, plan_path: Path, title: str) -> None:\n+        stamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n+        integration_branch = f\"integration/{names.slug}-{stamp}\"\n+        integration_worktree = (\n+            repo.parent / f\"{repo.name}-integrate-{names.slug}-{stamp}\"\n+        )\n+\n+        self.require_no_tracked_handoff_artifacts(repo, names.branch)\n+        self.require_ready_for_integration(names.worktree, names.branch)\n+        self.runner.run([\"git\", \"fetch\", \"--all\", \"--prune\"], repo)\n+        self.runner.run(\n+            [\n+                \"git\",\n+                \"worktree\",\n+                \"add\",\n+                str(integration_worktree),\n+                \"-b\",\n+                integration_branch,\n+                self.config.base,\n+            ],\n+            repo,\n+        )\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.ensure_dir(integration_worktree / self.handoff_dir)\n+        self.prepare_harness_permissions(integration_worktree / self.harness_dir)\n+        self.prepare_git_permissions(integration_worktree)\n+        integration_plan = self.copy_integration_context(\n+            names.worktree, integration_worktree, plan_path\n+        )\n+        feature_baseline = integration_worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n+\n+        integrated = False\n+        try:\n+            skill_usage_restored = False\n+            if self.config.merge_mode == \"squash\":\n+                merge = self.runner.run(\n+                    [\"git\", \"merge\", \"--squash\", names.branch],\n+                    integration_worktree,\n+                    check=False,\n+                )\n+                if merge.returncode != 0:\n+                    unmerged = self.unmerged_paths(integration_worktree)\n+                    if unmerged:\n+                        self.restore_integration_skill_usage_to_head(\n+                            integration_worktree, repo\n+                        )\n+                        skill_usage_restored = True\n+                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n+                        self.resolve_conflict(\n+                            integration_worktree, names, integration_plan\n+                        )\n+                    elif not unmerged:\n+                        raise FlowError(format_command_failure(merge))\n+            else:\n+                merge = self.runner.run(\n+                    [\"git\", \"merge\", \"--no-ff\", \"--no-commit\", names.branch],\n+                    integration_worktree,\n+                    check=False,\n+                )\n+                if merge.returncode != 0:\n+                    unmerged = self.unmerged_paths(integration_worktree)\n+                    if unmerged:\n+                        self.restore_integration_skill_usage_to_head(\n+                            integration_worktree, repo\n+                        )\n+                        skill_usage_restored = True\n+                    if unmerged and not self.only_skill_usage_unmerged(unmerged):\n+                        self.resolve_conflict(\n+                            integration_worktree, names, integration_plan\n+                        )\n+                    elif not unmerged:\n+                        raise FlowError(format_command_failure(merge))\n+\n+            if not skill_usage_restored:\n+                self.restore_integration_skill_usage_to_head(integration_worktree, repo)\n+            self.consolidate_skill_usage(\n+                names.worktree, integration_worktree, repo, feature_baseline\n+            )\n+            self.stage_integration_changes(integration_worktree)\n+            self.runner.run(\n+                [\"git\", \"commit\", \"-m\", f\"Harness: {title}\"], integration_worktree\n+            )\n+\n+            self.archive_handoff(\n+                repo, integration_worktree, names.run_id, \"integration\"\n+            )\n+            self.runner.run([\"git\", \"switch\", self.config.base], repo)\n+            self.runner.run([\"git\", \"merge\", \"--ff-only\", integration_branch], repo)\n+            integrated = True\n+        finally:\n+            if integrated and not self.config.keep_worktrees:\n+                self.cleanup(repo, integration_worktree, integration_branch, names)\n+\n+    def resolve_conflict(\n+        self, integration_worktree: Path, names: Names, plan_path: Path\n+    ) -> None:\n+        context_path = integration_worktree / self.handoff_dir / \"merge-conflict-context.md\"\n+        self.write_text(\n+            context_path, self.conflict_context(integration_worktree, names, plan_path)\n+        )\n+        prompt = f\"\"\"Use the merge-conflict-resolver skill.\n+\n+Resolve merge conflicts in this integration worktree.\n+\n+Read:\n+- `{self.handoff_dir.as_posix()}/merge-conflict-context.md`\n+- `{self.rel(integration_worktree, plan_path)}`\n+- `{self.handoff_dir.as_posix()}/implementation-summary.md`\n+- `{self.handoff_dir.as_posix()}/audit-summary.md`\n+\n+Preserve latest `{self.config.base}` behavior unless the approved plan explicitly supersedes it. Keep the resolution narrow, remove all conflict markers, run focused checks if possible, and write `{self.handoff_dir.as_posix()}/conflict-resolution-summary.md`.\n+Do not commit.\n+\"\"\"\n+        self.harness_exec(\n+            integration_worktree,\n+            prompt,\n+            integration_worktree\n+            / self.handoff_dir\n+            / \"conflict-resolution-final-response.md\",\n+        )\n+        self.require_file(\n+            integration_worktree / self.handoff_dir / \"conflict-resolution-summary.md\"\n+        )\n+        self.run_audit(integration_worktree, plan_path, post_conflict=True)\n+        self.require_file(\n+            integration_worktree / self.handoff_dir / \"post-conflict-audit-summary.md\"\n+        )\n+\n+    def copy_integration_context(\n+        self, feature_worktree: Path, integration_worktree: Path, plan_path: Path\n+    ) -> Path:\n+        source_handoff = feature_worktree / self.handoff_dir\n+        dest_handoff = integration_worktree / self.handoff_dir\n+        self.ensure_dir(dest_handoff)\n+        if source_handoff.exists():\n+            for item in source_handoff.iterdir():\n+                if item.is_file():\n+                    self.copy_file(item, dest_handoff / item.name)\n+\n+        try:\n+            rel_plan = plan_path.resolve().relative_to(feature_worktree.resolve())\n+        except ValueError:\n+            rel_plan = Path(\"docs\") / \"plans\" / plan_path.name\n+        dest_plan = integration_worktree / rel_plan\n+        self.ensure_dir(dest_plan.parent)\n+        if plan_path.exists():\n+            self.copy_file(plan_path, dest_plan)\n+        return dest_plan\n+\n+    def archive_handoff(\n+        self, repo: Path, worktree: Path, run_id: str, stage: str\n+    ) -> Path:\n+        archive_dir = repo / self.harness_dir / \"worktree-flow\" / run_id / stage\n+        self.ensure_dir(archive_dir)\n+        source = worktree / self.handoff_dir\n+        if source.exists():\n+            for item in source.iterdir():\n+                if item.is_file():\n+                    self.copy_file(item, archive_dir / item.name)\n+        return archive_dir\n+\n+    def stage_integration_changes(self, worktree: Path) -> None:\n+        self.runner.run(\n+            [\n+                \"git\",\n+                \"add\",\n+                \"-A\",\n+                \"--\",\n+                \".\",\n+                f\":(exclude){self.handoff_dir.as_posix()}/**\",\n+            ],\n+            worktree,\n+        )\n+\n+    def skill_usage_script(self, worktree: Path) -> Path:\n+        return worktree / self.harness_dir / \"scripts\" / \"skill-usage-manager.py\"\n+\n+    def skill_usage_ledger(self, repo_root: Path) -> Path:\n+        for harness_dir in (self.harness_dir.as_posix(), \".harness\", \".codex\", \".opencode\", \".claude\", \".omp\", \".agents\"):\n+            candidate = repo_root / harness_dir\n+            if candidate.exists():\n+                return candidate / \"skill-usage.json\"\n+        return repo_root / \".skill-usage.json\"\n+\n+    def skill_usage_ledger_in_worktree(\n+        self, worktree: Path, reference_repo: Path\n+    ) -> Path:\n+        rel = self.skill_usage_ledger(reference_repo).relative_to(reference_repo)\n+        return worktree / rel\n+\n+    def snapshot_skill_usage_baseline(\n+        self, worktree: Path, reference_repo: Path | None = None\n+    ) -> Path:\n+        baseline = worktree / self.handoff_dir / \"skill-usage-baseline.json\"\n+        ledger = (\n+            self.skill_usage_ledger_in_worktree(worktree, reference_repo)\n+            if reference_repo is not None\n+            else self.skill_usage_ledger(worktree)\n+        )\n+        if self.runner.dry_run:\n+            print(f\"+ snapshot skill usage {ledger} {baseline}\")\n+            return baseline\n+        self.ensure_dir(baseline.parent)\n+        if ledger.exists():\n+            self.copy_file(ledger, baseline)\n+        else:\n+            baseline.write_text(\n+                json.dumps(EMPTY_SKILL_USAGE_LEDGER, indent=2, sort_keys=True) + \"\\n\",\n+                encoding=\"utf-8\",\n+                newline=\"\\n\",\n+            )\n+        return baseline\n+\n+    def restore_integration_skill_usage_to_head(\n+        self, integration_worktree: Path, reference_repo: Path | None = None\n+    ) -> None:\n+        ledger = (\n+            self.skill_usage_ledger_in_worktree(integration_worktree, reference_repo)\n+            if reference_repo is not None\n+            else self.skill_usage_ledger(integration_worktree)\n+        )\n+        rel = ledger.relative_to(integration_worktree).as_posix()\n+        exists_at_head = (\n+            self.runner.run(\n+                [\"git\", \"cat-file\", \"-e\", f\"HEAD:{rel}\"],\n+                integration_worktree,\n+                check=False,\n+            ).returncode\n+            == 0\n+        )\n+        if exists_at_head:\n+            self.runner.run([\"git\", \"checkout\", \"HEAD\", \"--\", rel], integration_worktree)\n+        else:\n+            self.runner.run(\n+                [\"git\", \"rm\", \"-f\", \"--ignore-unmatch\", \"--\", rel],\n+                integration_worktree,\n+                check=False,\n+            )\n+            if not self.runner.dry_run and ledger.exists():\n+                ledger.unlink()\n+\n+    def consolidate_skill_usage(\n+        self,\n+        source_worktree: Path,\n+        integration_worktree: Path,\n+        target_repo: Path,\n+        baseline_path: Path,\n+    ) -> None:\n+        self.runner.run(\n+            [\n+                sys.executable,\n+                str(self.skill_usage_script(integration_worktree)),\n+                \"consolidate\",\n+                \"--source-ledger\",\n+                str(\n+                    self.skill_usage_ledger_in_worktree(source_worktree, target_repo)\n+                ),\n+                \"--base-ledger\",\n+                str(baseline_path),\n+                \"--target-ledger\",\n+                str(\n+                    self.skill_usage_ledger_in_worktree(\n+                        integration_worktree, target_repo\n+                    )\n+                ),\n+                \"--source-repo\",\n+                str(source_worktree),\n+                \"--target-repo\",\n+                str(target_repo),\n+                \"--target-worktree\",\n+                str(integration_worktree),\n+            ],\n+            integration_worktree,\n+        )\n+\n+    def has_unmerged_paths(self, worktree: Path) -> bool:\n+        return bool(self.unmerged_paths(worktree))\n+\n+    def unmerged_paths(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n+            worktree,\n+            check=False,\n+        )\n+        return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n+\n+    def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n+        expected = {\n+            f\"{harness_dir}/skill-usage.json\"\n+            for harness_dir in (\n+                self.harness_dir.as_posix(),\n+                \".harness\",\n+                \".codex\",\n+                \".opencode\",\n+                \".claude\",\n+                \".omp\",\n+                \".agents\",\n+            )\n+        }\n+        return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n+\n+    def require_no_tracked_handoff_artifacts(\n+        self, worktree: Path, treeish: str\n+    ) -> None:\n+        result = self.runner.run(\n+            [\n+                \"git\",\n+                \"ls-tree\",\n+                \"-r\",\n+                \"--name-only\",\n+                treeish,\n+                \"--\",\n+                self.handoff_dir.as_posix(),\n+            ],\n+            worktree,\n+            check=False,\n+        )\n+        tracked = result.stdout.strip()\n+        if result.returncode != 0 or not tracked:\n+            return\n+        raise FlowError(\n+            \"Workflow handoff artifacts were committed, but they must remain \"\n+            f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n+            f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n+        )\n+\n+    def head_rev(self, worktree: Path) -> str:\n+        result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n+        return result.stdout.strip()\n+\n+    def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n+        result = self.runner.run(\n+            [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n+            worktree,\n+        )\n+        raw = result.stdout.strip()\n+        return int(raw) if raw else 0\n+\n+    def require_commits_since_base(\n+        self, worktree: Path, branch: str, phase_name: str\n+    ) -> None:\n+        count = self.commit_count_since_base(worktree, branch)\n+        if count <= 0:\n+            raise FlowError(\n+                f\"{phase_name} did not create any commits on {branch} after \"\n+                f\"{self.config.base}. Commit the completed implementation before \"\n+                \"continuing.\"\n+            )\n+\n+    def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n+        result = self.runner.run(\n+            [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n+            worktree,\n+            check=False,\n+        )\n+        if result.returncode == 1:\n+            return\n+        if result.returncode == 0:\n+            raise FlowError(\n+                f\"{branch} has no file changes compared with {self.config.base}. \"\n+                \"The workflow cannot integrate a no-op implementation.\"\n+            )\n+        raise FlowError(format_command_failure(result))\n+\n+    def non_handoff_status(self, worktree: Path) -> list[str]:\n+        result = self.runner.run(\n+            [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n+            worktree,\n+        )\n+        return [\n+            line\n+            for line in result.stdout.splitlines()\n+            if line.strip() and not self.status_line_is_handoff(line)\n+        ]\n+\n+    def status_line_is_handoff(self, line: str) -> bool:\n+        path_text = line[3:].strip()\n+        paths = [part.strip() for part in path_text.split(\" -> \")]\n+        return all(self.path_is_handoff(path) for path in paths if path)\n+\n+    def path_is_handoff(self, path: str) -> bool:\n+        normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n+        handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n+        return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n+\n+    def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n+        status = self.non_handoff_status(worktree)\n+        if not status:\n+            return\n+        raise FlowError(\n+            f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n+            + \"\\n\".join(status)\n+        )\n+\n+    def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n+        self.require_commits_since_base(worktree, branch, \"Implementation\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Implementation\")\n+\n+    def require_audit_invariants(\n+        self, worktree: Path, branch: str, head_before: str\n+    ) -> None:\n+        self.require_clean_except_handoff(worktree, \"Audit\")\n+        head_after = self.head_rev(worktree)\n+        if head_after != head_before:\n+            self.require_branch_changed_since_base(worktree, branch)\n+\n+    def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n+        self.require_no_tracked_handoff_artifacts(worktree, branch)\n+        self.require_clean_except_handoff(worktree, \"Pre-integration\")\n+        self.require_branch_changed_since_base(worktree, branch)\n+\n+    def git_common_dir(self, worktree: Path) -> Path | None:\n+        result = self.runner.run(\n+            [\"git\", \"rev-parse\", \"--git-common-dir\"],\n+            worktree,\n+            check=False,\n+        )\n+        raw = result.stdout.strip()\n+        if result.returncode != 0 or not raw:\n+            return None\n+        path = Path(raw)\n+        if not path.is_absolute():\n+            path = worktree / path\n+        return path.resolve()\n+\n+    def extra_writable_roots(self, worktree: Path) -> list[Path]:\n+        roots: list[Path] = []\n+        harness_dir = worktree / self.harness_dir\n+        if harness_dir.exists():\n+            roots.append(harness_dir.resolve())\n+\n+        common_dir = self.git_common_dir(worktree)\n+        if common_dir is not None and not is_relative_to(common_dir, worktree):\n+            roots.append(common_dir)\n+        return roots\n+\n+    def prepare_git_permissions(self, worktree: Path) -> None:\n+        common_dir = self.git_common_dir(worktree)\n+        if common_dir is not None and not is_relative_to(common_dir, worktree):\n+            self.prepare_harness_permissions(common_dir)\n+\n+    def ensure_dir(self, path: Path) -> None:\n+        if self.runner.dry_run:\n+            print(f\"+ mkdir -p {path}\")\n+            return\n+        ensure_dir(path)\n+\n+    def copy_file(self, source: Path, dest: Path) -> None:\n+        if self.runner.dry_run:\n+            print(f\"+ copy {source} {dest}\")\n+            return\n+        shutil.copy2(source, dest)\n+\n+    def write_text(self, path: Path, text: str) -> None:\n+        if self.runner.dry_run:\n+            print(f\"+ write {path}\")\n+            return\n+        write_text(path, text)\n+\n+    def prepare_harness_permissions(self, harness_dir: Path) -> None:\n+        if self.runner.dry_run or os.name != \"nt\" or not harness_dir.exists():\n+            return\n+        shell = shutil.which(\"pwsh\") or shutil.which(\"powershell\")\n+        if shell is None:\n+            print(\n+                f\"Warning: could not grant sandbox write permissions for {harness_dir}: \"\n+                \"PowerShell was not found.\",\n+                file=sys.stderr,\n+            )\n+            return\n+        group = os.environ.get(\"CODEX_SANDBOX_GROUP\", \"CodexSandboxUsers\")\n+        script = r\"\"\"\n+$Root = $env:CODEX_PERMISSION_ROOT\n+$Group = $env:CODEX_PERMISSION_GROUP\n+$ErrorActionPreference = 'Stop'\n+$identity = New-Object System.Security.Principal.NTAccount($Group)\n+$rights = [System.Security.AccessControl.FileSystemRights]::Modify\n+$propagate = [System.Security.AccessControl.PropagationFlags]::None\n+$items = @((Get-Item -LiteralPath $Root -Force))\n+$items += @(Get-ChildItem -LiteralPath $Root -Force -Recurse)\n+foreach ($item in $items) {\n+    $acl = Get-Acl -LiteralPath $item.FullName\n+    foreach ($rule in @($acl.Access)) {\n+        if ($rule.AccessControlType -eq [System.Security.AccessControl.AccessControlType]::Deny) {\n+            [void]$acl.RemoveAccessRuleSpecific($rule)\n+        }\n+    }\n+    if ($item.PSIsContainer) {\n+        $inherit = [System.Security.AccessControl.InheritanceFlags]'ContainerInherit,ObjectInherit'\n+    } else {\n+        $inherit = [System.Security.AccessControl.InheritanceFlags]::None\n+    }\n+    $allow = New-Object System.Security.AccessControl.FileSystemAccessRule(\n+        $identity,\n+        $rights,\n+        $inherit,\n+        $propagate,\n+        [System.Security.AccessControl.AccessControlType]::Allow\n+    )\n+    $acl.SetAccessRule($allow)\n+    Set-Acl -LiteralPath $item.FullName -AclObject $acl\n+}\n+\"\"\"\n+        completed = subprocess.run(\n+            [\n+                shell,\n+                \"-NoProfile\",\n+                \"-NonInteractive\",\n+                \"-Command\",\n+                script,\n+            ],\n+            check=False,\n+            capture_output=True,\n+            env={\n+                **os.environ,\n+                \"CODEX_PERMISSION_ROOT\": str(harness_dir),\n+                \"CODEX_PERMISSION_GROUP\": group,\n+            },\n+            text=True,\n+        )\n+        if completed.returncode != 0:\n+            print(\n+                \"Warning: could not grant sandbox write permissions for \"\n+                f\"{harness_dir}: {(completed.stderr or completed.stdout).strip()}\",\n+                file=sys.stderr,\n+            )\n+\n+    def conflict_context(\n+        self, integration_worktree: Path, names: Names, plan_path: Path\n+    ) -> str:\n+        status = self.runner.run(\n+            [\"git\", \"status\", \"--short\"], integration_worktree, check=False\n+        ).stdout\n+        conflicted = self.runner.run(\n+            [\"git\", \"diff\", \"--name-only\", \"--diff-filter=U\"],\n+            integration_worktree,\n+            check=False,\n+        ).stdout\n+        merge_base = self.runner.run(\n+            [\"git\", \"merge-base\", self.config.base, names.branch],\n+            integration_worktree,\n+            check=False,\n+        ).stdout.strip()\n+        base_log = \"\"\n+        feature_log = \"\"\n+        if merge_base:\n+            base_log = self.runner.run(\n+                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{self.config.base}\"],\n+                integration_worktree,\n+                check=False,\n+            ).stdout\n+            feature_log = self.runner.run(\n+                [\"git\", \"log\", \"--oneline\", f\"{merge_base}..{names.branch}\"],\n+                integration_worktree,\n+                check=False,\n+            ).stdout\n+\n+        return f\"\"\"# Merge Conflict Context\n+\n+## Branches\n+- Base branch: {self.config.base}\n+- Feature branch: {names.branch}\n+\n+## Plan\n+- Path: {self.rel(integration_worktree, plan_path)}\n+\n+## Merge base\n+{merge_base or \"unknown\"}\n+\n+## Conflicted files\n+{conflicted.strip() or \"unknown\"}\n+\n+## Status\n+```text\n+{status.strip()}\n+```\n+\n+## Base commits since merge base\n+```text\n+{base_log.strip()}\n+```\n+\n+## Feature commits since merge base\n+```text\n+{feature_log.strip()}\n+```\n+\n+## Resolution rules\n+1. Latest base behavior is presumed correct unless the approved plan explicitly supersedes it.\n+2. Feature intent comes from the approved plan and implementation summary.\n+3. Preserve audited feature behavior when compatible with latest base.\n+4. Prefer the smallest conflict-only edit.\n+5. Remove all conflict markers.\n+\"\"\"\n+\n+    def cleanup(\n+        self,\n+        repo: Path,\n+        integration_worktree: Path,\n+        integration_branch: str,\n+        names: Names,\n+    ) -> None:\n+        for worktree in (integration_worktree, names.worktree):\n+            if worktree.exists():\n+                self.runner.run(\n+                    [\"git\", \"worktree\", \"remove\", str(worktree)], repo, check=False\n+                )\n+        self.runner.run([\"git\", \"branch\", \"-d\", integration_branch], repo, check=False)\n+        # Squash merges do not mark the feature branch as merged, so force-delete\n+        # only after the integration branch has fast-forwarded successfully.\n+        self.runner.run([\"git\", \"branch\", \"-D\", names.branch], repo, check=False)\n+\n+    def start_log(self, repo: Path, run_id: str) -> None:\n+        if self.runner.dry_run:\n+            return\n+        self.log_file = repo / self.harness_dir / \"worktree-flow\" / run_id / \"workflow.jsonl\"\n+        ensure_dir(self.log_file.parent)\n+        self.log_event(\"workflow_log_started\", log_file=str(self.log_file))\n+\n+    def log_event(self, event: str, **fields: object) -> None:\n+        if self.log_file is None:\n+            return\n+        record = {\n+            \"timestamp\": datetime.now().isoformat(timespec=\"seconds\"),\n+            \"event\": event,\n+            **fields,\n+        }\n+        with self.log_file.open(\"a\", encoding=\"utf-8\", newline=\"\\n\") as handle:\n+            handle.write(json.dumps(record, ensure_ascii=False) + \"\\n\")\n+\n+    def require_file(self, path: Path) -> None:\n+        if not path.exists() and not self.runner.dry_run:\n+            raise FlowError(f\"Required output file was not created: {path}\")\n+\n+    @staticmethod\n+    def rel(root: Path, path: Path) -> str:\n+        try:\n+            return path.resolve().relative_to(root.resolve()).as_posix()\n+        except ValueError:\n+            return str(path)\n+\n+\n+def build_parser(\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> argparse.ArgumentParser:\n+    parser = argparse.ArgumentParser(\n+        description=\"Run the harness plan -> worktree -> audit -> finish workflow.\"\n+    )\n+    parser.add_argument(\"--plan\", required=True, help=\"Approved Markdown plan file.\")\n+    parser.add_argument(\n+        \"--repo\", default=\".\", help=\"Repository root. Defaults to current directory.\"\n+    )\n+    parser.add_argument(\n+        \"--base\", default=\"main\", help=\"Base branch/ref. Defaults to main.\"\n+    )\n+    parser.add_argument(\"--model\", help=\"Optional harness model override.\")\n+    parser.add_argument(\n+        \"--harness\",\n+        default=default_harness,\n+        help=f\"Harness CLI executable. Defaults to {default_harness}.\",\n+    )\n+    parser.add_argument(\n+        \"--harness-dir\",\n+        default=default_harness_dir.as_posix(),\n+        help=f\"Harness artifact directory. Defaults to {default_harness_dir.as_posix()}.\",\n+    )\n+    parser.add_argument(\n+        \"--merge-mode\", choices=[\"squash\", \"no-ff\", \"stop\"], default=\"squash\"\n+    )\n+    parser.add_argument(\n+        \"--keep-worktrees\",\n+        action=\"store_true\",\n+        help=\"Do not remove feature/integration worktrees.\",\n+    )\n+    parser.add_argument(\n+        \"--dry-run\", action=\"store_true\", help=\"Print commands without running them.\"\n+    )\n+    return parser\n+\n+\n+def main(\n+    argv: list[str] | None = None,\n+    *,\n+    default_harness: str = DEFAULT_HARNESS,\n+    default_harness_dir: Path = HARNESS_DIR,\n+) -> int:\n+    parser = build_parser(\n+        default_harness=default_harness,\n+        default_harness_dir=default_harness_dir,\n+    )\n+    args = parser.parse_args(argv)\n+    harness_dir = Path(args.harness_dir)\n+    config = FlowConfig(\n+        repo=Path(args.repo).expanduser().resolve(),\n+        plan=Path(args.plan).expanduser().resolve(),\n+        base=args.base,\n+        harness=args.harness,\n+        harness_dir=harness_dir,\n+        model=args.model,\n+        merge_mode=args.merge_mode,\n+        keep_worktrees=args.keep_worktrees,\n+    )\n+    try:\n+        HarnessWorktreeFlow(config, CommandRunner(args.dry_run)).run()\n+    except FlowError as exc:\n+        print(str(exc), file=sys.stderr)\n+        return 1\n+    return 0\n+\n+\n+if __name__ == \"__main__\":\n+    raise SystemExit(main())\ndiff --git a/tests/test_codex_worktree_flow.py b/tests/test_codex_worktree_flow.py\nindex 5ae0f33..5ca952e 100644\n--- a/tests/test_codex_worktree_flow.py\n+++ b/tests/test_codex_worktree_flow.py\n@@ -23,7 +23,8 @@ def load_flow_module(name: str, script_name: str):\n     return module\n \n \n-flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n+flow = load_flow_module(\"worktree_flow\", \"worktree-flow.py\")\n+codex_flow = load_flow_module(\"codex_worktree_flow\", \"worktree-flow-codex.py\")\n omp_flow = load_flow_module(\"omp_worktree_flow\", \"worktree-flow-omp.py\")\n \n \n@@ -130,7 +131,25 @@ class CommandRunnerTests(unittest.TestCase):\n             flow.CommandRunner().run([\"codex\", \"exec\", \"--help\"], Path(temp))\n \n \n-class OmpVariantTests(unittest.TestCase):\n+class WrapperVariantTests(unittest.TestCase):\n+    def test_codex_defaults_are_explicit(self) -> None:\n+        self.assertEqual(codex_flow.DEFAULT_HARNESS, \"codex\")\n+        self.assertEqual(codex_flow.HARNESS_DIR, Path(\".codex\"))\n+        self.assertEqual(codex_flow.HANDOFF_DIR, Path(\".codex\") / \"handoff\")\n+\n+    def test_codex_parser_defaults_to_codex_harness(self) -> None:\n+        args = codex_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n+\n+        self.assertEqual(args.harness, \"codex\")\n+        self.assertEqual(args.harness_dir, \".codex\")\n+\n+    def test_shared_parser_can_override_harness_dir(self) -> None:\n+        args = flow.build_parser().parse_args(\n+            [\"--plan\", \"docs/plans/p.md\", \"--harness-dir\", \".custom\"]\n+        )\n+\n+        self.assertEqual(args.harness_dir, \".custom\")\n+\n     def test_omp_defaults_are_explicit(self) -> None:\n         self.assertEqual(omp_flow.DEFAULT_HARNESS, \"omp\")\n         self.assertEqual(omp_flow.HARNESS_DIR, Path(\".omp\"))\n@@ -140,6 +159,7 @@ class OmpVariantTests(unittest.TestCase):\n         args = omp_flow.build_parser().parse_args([\"--plan\", \"docs/plans/p.md\"])\n \n         self.assertEqual(args.harness, \"omp\")\n+        self.assertEqual(args.harness_dir, \".omp\")\n \n     def test_omp_harness_exec_uses_omp_command_and_handoff_path(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n@@ -153,6 +173,7 @@ class OmpVariantTests(unittest.TestCase):\n                 base=\"main\",\n                 model=None,\n                 harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n                 merge_mode=\"squash\",\n                 keep_worktrees=False,\n             )\n@@ -182,6 +203,7 @@ class OmpVariantTests(unittest.TestCase):\n                 base=\"main\",\n                 model=None,\n                 harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n                 merge_mode=\"squash\",\n                 keep_worktrees=False,\n             )\n@@ -213,6 +235,7 @@ class OmpVariantTests(unittest.TestCase):\n                 base=\"main\",\n                 model=None,\n                 harness=omp_flow.DEFAULT_HARNESS,\n+                harness_dir=omp_flow.HARNESS_DIR,\n                 merge_mode=\"squash\",\n                 keep_worktrees=False,\n             )\n@@ -454,6 +477,9 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             subject.create_feature_worktree = lambda _repo, _names: None\n             subject.run_implementation = lambda _worktree, _plan: None\n             subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n             subject.unique_feature_names = lambda _repo, _slug: flow.Names(\n                 \"plan\", \"feature/plan\", worktree, \"plan-run\"\n             )\n@@ -508,6 +534,238 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n                 subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n \n+    def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            0,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_implementation_allows_untracked_handoff_files(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/implementation-summary.md\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_implementation_invariants(repo, \"feature/plan\")\n+\n+    def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? .codex/handoff/audit-summary.md\\n\",\n+                        (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n+                        (\n+                            \"git\",\n+                            \"diff\",\n+                            \"--quiet\",\n+                            \"main...feature/plan\",\n+                            \"--\",\n+                            \".\",\n+                        ): flow.CommandResult(\n+                            (\n+                                \"git\",\n+                                \"diff\",\n+                                \"--quiet\",\n+                                \"main...feature/plan\",\n+                                \"--\",\n+                                \".\",\n+                            ),\n+                            repo,\n+                            1,\n+                        ),\n+                    }\n+                ),\n+            )\n+\n+            subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \" M app.py\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n+                subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n+\n+    def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n+        with tempfile.TemporaryDirectory() as temp:\n+            repo = Path(temp) / \"repo\"\n+            subject = flow.HarnessWorktreeFlow(\n+                self.config(repo, repo / \"plan.md\"),\n+                FakeRunner(\n+                    {\n+                        (\n+                            \"git\",\n+                            \"ls-tree\",\n+                            \"-r\",\n+                            \"--name-only\",\n+                            \"feature/plan\",\n+                            \"--\",\n+                            \".codex/handoff\",\n+                        ): \"\",\n+                        (\n+                            \"git\",\n+                            \"status\",\n+                            \"--porcelain\",\n+                            \"--untracked-files=all\",\n+                        ): \"?? leaked.txt\\n\",\n+                    }\n+                ),\n+            )\n+\n+            with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n+                subject.require_ready_for_integration(repo, \"feature/plan\")\n+\n     def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n         with tempfile.TemporaryDirectory() as temp:\n             repo = Path(temp) / \"repo\"\n@@ -556,6 +814,7 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n             subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n             subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n \n             subject.finish(\n                 repo,\n@@ -606,6 +865,7 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             subject.consolidate_skill_usage = lambda *_args: None\n             subject.stage_integration_changes = lambda _worktree: None\n             subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n \n             subject.finish(\n                 repo,\n@@ -660,6 +920,7 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             subject.consolidate_skill_usage = lambda *_args: None\n             subject.stage_integration_changes = lambda _worktree: None\n             subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n \n             subject.finish(\n                 repo,\n@@ -688,6 +949,7 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             subject.consolidate_skill_usage = lambda *_args: events.append(\"consolidate\")\n             subject.stage_integration_changes = lambda _worktree: events.append(\"stage\")\n             subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n \n             subject.finish(\n                 repo,\n@@ -781,6 +1043,10 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             subject.create_feature_worktree = lambda _repo, _names: None\n             subject.run_implementation = lambda _worktree, _plan: None\n             subject.run_audit = lambda _worktree, _plan: None\n+            subject.require_implementation_invariants = lambda _worktree, _branch: None\n+            subject.head_rev = lambda _worktree: \"before\"\n+            subject.require_audit_invariants = lambda _worktree, _branch, _head: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n             subject.unique_feature_names = lambda _repo, _slug: flow.Names(\"plan\", \"feature/plan\", worktree, \"plan-run\")\n             subject.validate = lambda _repo, _plan: None\n             subject.finish = lambda *_args: (_ for _ in ()).throw(AssertionError(\"finish should not run\"))\n@@ -840,6 +1106,7 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             subject.cleanup = lambda *_args: (_ for _ in ()).throw(AssertionError(\"cleanup should not run\"))\n             subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n             subject.prepare_harness_permissions = lambda _path: None\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n \n             with self.assertRaises(flow.FlowError):\n                 subject.finish(\n@@ -868,6 +1135,7 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             subject.resolve_conflict = lambda *_args: (_ for _ in ()).throw(\n                 AssertionError(\"resolver should not run\")\n             )\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n \n             with self.assertRaisesRegex(flow.FlowError, \"merge --squash\"):\n                 subject.finish(\n@@ -896,6 +1164,7 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             subject.prepare_harness_permissions = lambda _path: None\n             subject.resolve_conflict = lambda *_args: resolved.append(True)\n             subject.archive_handoff = lambda *_args: repo / \".codex\" / \"archive\"\n+            subject.require_ready_for_integration = lambda _worktree, _branch: None\n \n             subject.finish(\n                 repo,\n@@ -966,6 +1235,7 @@ class HarnessWorktreeFlowTests(unittest.TestCase):\n             base=\"main\",\n             model=model,\n             harness=\"codex\",\n+            harness_dir=Path(\".codex\"),\n             merge_mode=merge_mode,\n             keep_worktrees=False,\n         )\ndiff --git a/tests/test_worktrees_loadout_sync.py b/tests/test_worktrees_loadout_sync.py\nnew file mode 100644\nindex 0000000..dd84cab\n--- /dev/null\n+++ b/tests/test_worktrees_loadout_sync.py\n@@ -0,0 +1,43 @@\n+from __future__ import annotations\n+\n+import unittest\n+from pathlib import Path\n+\n+\n+ROOT = Path(__file__).resolve().parents[1]\n+ACTIVE_SCRIPTS = ROOT / \".codex\" / \"scripts\"\n+LOADOUT_SCRIPTS = ROOT / \"loadouts\" / \"worktrees\" / \".harness\" / \"scripts\"\n+\n+\n+SCRIPT_PAIRS = (\n+    \"worktree-flow.py\",\n+    \"worktree-flow-codex.py\",\n+    \"worktree-flow-omp.py\",\n+    \"skill-usage-manager.py\",\n+)\n+\n+\n+def normalized_text(path: Path) -> str:\n+    return path.read_text(encoding=\"utf-8\").replace(\"\\r\\n\", \"\\n\")\n+\n+\n+class WorktreesLoadoutSyncTests(unittest.TestCase):\n+    def test_active_scripts_match_worktrees_loadout_templates(self) -> None:\n+        for script_name in SCRIPT_PAIRS:\n+            active = ACTIVE_SCRIPTS / script_name\n+            loadout = LOADOUT_SCRIPTS / script_name\n+            with self.subTest(script=script_name):\n+                self.assertTrue(active.exists(), f\"Missing active script: {active}\")\n+                self.assertTrue(loadout.exists(), f\"Missing loadout script: {loadout}\")\n+                self.assertEqual(\n+                    normalized_text(active),\n+                    normalized_text(loadout),\n+                    (\n+                        f\"{active} and {loadout} are out of sync. Update the \"\n+                        \"active script and shipped worktrees loadout copy together.\"\n+                    ),\n+                )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n succeeded in 1443ms:\n 516:     def test_tracked_handoff_artifacts_are_rejected(self) -> None:\n 517:         with tempfile.TemporaryDirectory() as temp:\n 518:             repo = Path(temp) / \"repo\"\n 519:             runner = FakeRunner(\n 520:                 {\n 521:                     (\n 522:                         \"git\",\n 523:                         \"ls-tree\",\n 524:                         \"-r\",\n 525:                         \"--name-only\",\n 526:                         \"feature/plan\",\n 527:                         \"--\",\n 528:                         \".codex/handoff\",\n 529:                     ): \".codex/handoff/audit-summary.md\\n\",\n 530:                 }\n 531:             )\n 532:             subject = flow.HarnessWorktreeFlow(self.config(repo, repo / \"plan.md\"), runner)\n 533: \n 534:             with self.assertRaisesRegex(flow.FlowError, \"Workflow handoff artifacts\"):\n 535:                 subject.require_no_tracked_handoff_artifacts(repo, \"feature/plan\")\n 536: \n 537:     def test_implementation_fails_when_no_commit_exists_after_base(self) -> None:\n 538:         with tempfile.TemporaryDirectory() as temp:\n 539:             repo = Path(temp) / \"repo\"\n 540:             subject = flow.HarnessWorktreeFlow(\n 541:                 self.config(repo, repo / \"plan.md\"),\n 542:                 FakeRunner({(\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"0\\n\"}),\n 543:             )\n 544: \n 545:             with self.assertRaisesRegex(flow.FlowError, \"did not create any commits\"):\n 546:                 subject.require_implementation_invariants(repo, \"feature/plan\")\n 547: \n 548:     def test_implementation_fails_when_branch_has_no_diff_from_base(self) -> None:\n 549:         with tempfile.TemporaryDirectory() as temp:\n 550:             repo = Path(temp) / \"repo\"\n 551:             subject = flow.HarnessWorktreeFlow(\n 552:                 self.config(repo, repo / \"plan.md\"),\n 553:                 FakeRunner(\n 554:                     {\n 555:                         (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n 556:                         (\n 557:                             \"git\",\n 558:                             \"diff\",\n 559:                             \"--quiet\",\n 560:                             \"main...feature/plan\",\n 561:                             \"--\",\n 562:                             \".\",\n 563:                         ): flow.CommandResult(\n 564:                             (\n 565:                                 \"git\",\n 566:                                 \"diff\",\n 567:                                 \"--quiet\",\n 568:                                 \"main...feature/plan\",\n 569:                                 \"--\",\n 570:                                 \".\",\n 571:                             ),\n 572:                             repo,\n 573:                             0,\n 574:                         ),\n 575:                     }\n 576:                 ),\n 577:             )\n 578: \n 579:             with self.assertRaisesRegex(flow.FlowError, \"no file changes\"):\n 580:                 subject.require_implementation_invariants(repo, \"feature/plan\")\n 581: \n 582:     def test_implementation_fails_when_non_handoff_file_remains(self) -> None:\n 583:         with tempfile.TemporaryDirectory() as temp:\n 584:             repo = Path(temp) / \"repo\"\n 585:             subject = flow.HarnessWorktreeFlow(\n 586:                 self.config(repo, repo / \"plan.md\"),\n 587:                 FakeRunner(\n 588:                     {\n 589:                         (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n 590:                         (\n 591:                             \"git\",\n 592:                             \"diff\",\n 593:                             \"--quiet\",\n 594:                             \"main...feature/plan\",\n 595:                             \"--\",\n 596:                             \".\",\n 597:                         ): flow.CommandResult(\n 598:                             (\n 599:                                 \"git\",\n 600:                                 \"diff\",\n 601:                                 \"--quiet\",\n 602:                                 \"main...feature/plan\",\n 603:                                 \"--\",\n 604:                                 \".\",\n 605:                             ),\n 606:                             repo,\n 607:                             1,\n 608:                         ),\n 609:                         (\n 610:                             \"git\",\n 611:                             \"status\",\n 612:                             \"--porcelain\",\n 613:                             \"--untracked-files=all\",\n 614:                         ): \"?? app.py\\n?? .codex/handoff/implementation-summary.md\\n\",\n 615:                     }\n 616:                 ),\n 617:             )\n 618: \n 619:             with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n 620:                 subject.require_implementation_invariants(repo, \"feature/plan\")\n 621: \n 622:     def test_implementation_allows_untracked_handoff_files(self) -> None:\n 623:         with tempfile.TemporaryDirectory() as temp:\n 624:             repo = Path(temp) / \"repo\"\n 625:             subject = flow.HarnessWorktreeFlow(\n 626:                 self.config(repo, repo / \"plan.md\"),\n 627:                 FakeRunner(\n 628:                     {\n 629:                         (\"git\", \"rev-list\", \"--count\", \"main..feature/plan\"): \"1\\n\",\n 630:                         (\n 631:                             \"git\",\n 632:                             \"diff\",\n 633:                             \"--quiet\",\n 634:                             \"main...feature/plan\",\n 635:                             \"--\",\n 636:                             \".\",\n 637:                         ): flow.CommandResult(\n 638:                             (\n 639:                                 \"git\",\n 640:                                 \"diff\",\n 641:                                 \"--quiet\",\n 642:                                 \"main...feature/plan\",\n 643:                                 \"--\",\n 644:                                 \".\",\n 645:                             ),\n 646:                             repo,\n 647:                             1,\n 648:                         ),\n 649:                         (\n 650:                             \"git\",\n 651:                             \"status\",\n 652:                             \"--porcelain\",\n 653:                             \"--untracked-files=all\",\n 654:                         ): \"?? .codex/handoff/implementation-summary.md\\n\",\n 655:                     }\n 656:                 ),\n 657:             )\n 658: \n 659:             subject.require_implementation_invariants(repo, \"feature/plan\")\n 660: \n 661:     def test_audit_allows_no_new_commit_when_clean_outside_handoff(self) -> None:\n 662:         with tempfile.TemporaryDirectory() as temp:\n 663:             repo = Path(temp) / \"repo\"\n 664:             subject = flow.HarnessWorktreeFlow(\n 665:                 self.config(repo, repo / \"plan.md\"),\n 666:                 FakeRunner(\n 667:                     {\n 668:                         (\n 669:                             \"git\",\n 670:                             \"status\",\n 671:                             \"--porcelain\",\n 672:                             \"--untracked-files=all\",\n 673:                         ): \"?? .codex/handoff/audit-summary.md\\n\",\n 674:                         (\"git\", \"rev-parse\", \"HEAD\"): \"before\\n\",\n 675:                     }\n 676:                 ),\n 677:             )\n 678: \n 679:             subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n 680: \n 681:     def test_audit_allows_new_commit_when_clean_outside_handoff(self) -> None:\n 682:         with tempfile.TemporaryDirectory() as temp:\n 683:             repo = Path(temp) / \"repo\"\n 684:             subject = flow.HarnessWorktreeFlow(\n 685:                 self.config(repo, repo / \"plan.md\"),\n 686:                 FakeRunner(\n 687:                     {\n 688:                         (\n 689:                             \"git\",\n 690:                             \"status\",\n 691:                             \"--porcelain\",\n 692:                             \"--untracked-files=all\",\n 693:                         ): \"?? .codex/handoff/audit-summary.md\\n\",\n 694:                         (\"git\", \"rev-parse\", \"HEAD\"): \"after\\n\",\n 695:                         (\n 696:                             \"git\",\n 697:                             \"diff\",\n 698:                             \"--quiet\",\n 699:                             \"main...feature/plan\",\n 700:                             \"--\",\n 701:                             \".\",\n 702:                         ): flow.CommandResult(\n 703:                             (\n 704:                                 \"git\",\n 705:                                 \"diff\",\n 706:                                 \"--quiet\",\n 707:                                 \"main...feature/plan\",\n 708:                                 \"--\",\n 709:                                 \".\",\n 710:                             ),\n 711:                             repo,\n 712:                             1,\n 713:                         ),\n 714:                     }\n 715:                 ),\n 716:             )\n 717: \n 718:             subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n 719: \n 720:     def test_audit_fails_when_non_handoff_changes_remain(self) -> None:\n 721:         with tempfile.TemporaryDirectory() as temp:\n 722:             repo = Path(temp) / \"repo\"\n 723:             subject = flow.HarnessWorktreeFlow(\n 724:                 self.config(repo, repo / \"plan.md\"),\n 725:                 FakeRunner(\n 726:                     {\n 727:                         (\n 728:                             \"git\",\n 729:                             \"status\",\n 730:                             \"--porcelain\",\n 731:                             \"--untracked-files=all\",\n 732:                         ): \" M app.py\\n\",\n 733:                     }\n 734:                 ),\n 735:             )\n 736: \n 737:             with self.assertRaisesRegex(flow.FlowError, \"non-handoff changes\"):\n 738:                 subject.require_audit_invariants(repo, \"feature/plan\", \"before\")\n 739: \n 740:     def test_final_guard_fails_when_non_handoff_changes_appear_after_audit(self) -> None:\n 741:         with tempfile.TemporaryDirectory() as temp:\n 742:             repo = Path(temp) / \"repo\"\n 743:             subject = flow.HarnessWorktreeFlow(\n 744:                 self.config(repo, repo / \"plan.md\"),\n 745:                 FakeRunner(\n 746:                     {\n 747:                         (\n 748:                             \"git\",\n 749:                             \"ls-tree\",\n 750:                             \"-r\",\n 751:                             \"--name-only\",\n 752:                             \"feature/plan\",\n 753:                             \"--\",\n 754:                             \".codex/handoff\",\n 755:                         ): \"\",\n 756:                         (\n 757:                             \"git\",\n 758:                             \"status\",\n 759:                             \"--porcelain\",\n 760:                             \"--untracked-files=all\",\n 761:                         ): \"?? leaked.txt\\n\",\n 762:                     }\n 763:                 ),\n 764:             )\n 765: \n 766:             with self.assertRaisesRegex(flow.FlowError, \"Pre-integration\"):\n 767:                 subject.require_ready_for_integration(repo, \"feature/plan\")\n 768: \n 769:     def test_handoff_prompts_forbid_committing_artifacts(self) -> None:\n 770:         with tempfile.TemporaryDirectory() as temp:\n\n succeeded in 1798ms:\n 660:         return [line.strip() for line in result.stdout.splitlines() if line.strip()]\n 661: \n 662:     def only_skill_usage_unmerged(self, paths: Sequence[str]) -> bool:\n 663:         expected = {\n 664:             f\"{harness_dir}/skill-usage.json\"\n 665:             for harness_dir in (\n 666:                 self.harness_dir.as_posix(),\n 667:                 \".harness\",\n 668:                 \".codex\",\n 669:                 \".opencode\",\n 670:                 \".claude\",\n 671:                 \".omp\",\n 672:                 \".agents\",\n 673:             )\n 674:         }\n 675:         return bool(paths) and all(path.replace(\"\\\\\", \"/\") in expected for path in paths)\n 676: \n 677:     def require_no_tracked_handoff_artifacts(\n 678:         self, worktree: Path, treeish: str\n 679:     ) -> None:\n 680:         result = self.runner.run(\n 681:             [\n 682:                 \"git\",\n 683:                 \"ls-tree\",\n 684:                 \"-r\",\n 685:                 \"--name-only\",\n 686:                 treeish,\n 687:                 \"--\",\n 688:                 self.handoff_dir.as_posix(),\n 689:             ],\n 690:             worktree,\n 691:             check=False,\n 692:         )\n 693:         tracked = result.stdout.strip()\n 694:         if result.returncode != 0 or not tracked:\n 695:             return\n 696:         raise FlowError(\n 697:             \"Workflow handoff artifacts were committed, but they must remain \"\n 698:             f\"untracked:\\n{tracked}\\nRemove them from the branch index before \"\n 699:             f\"merging, for example: git rm --cached -- {self.handoff_dir.as_posix()}/*\"\n 700:         )\n 701: \n 702:     def head_rev(self, worktree: Path) -> str:\n 703:         result = self.runner.run([\"git\", \"rev-parse\", \"HEAD\"], worktree)\n 704:         return result.stdout.strip()\n 705: \n 706:     def commit_count_since_base(self, worktree: Path, branch: str) -> int:\n 707:         result = self.runner.run(\n 708:             [\"git\", \"rev-list\", \"--count\", f\"{self.config.base}..{branch}\"],\n 709:             worktree,\n 710:         )\n 711:         raw = result.stdout.strip()\n 712:         return int(raw) if raw else 0\n 713: \n 714:     def require_commits_since_base(\n 715:         self, worktree: Path, branch: str, phase_name: str\n 716:     ) -> None:\n 717:         count = self.commit_count_since_base(worktree, branch)\n 718:         if count <= 0:\n 719:             raise FlowError(\n 720:                 f\"{phase_name} did not create any commits on {branch} after \"\n 721:                 f\"{self.config.base}. Commit the completed implementation before \"\n 722:                 \"continuing.\"\n 723:             )\n 724: \n 725:     def require_branch_changed_since_base(self, worktree: Path, branch: str) -> None:\n 726:         result = self.runner.run(\n 727:             [\"git\", \"diff\", \"--quiet\", f\"{self.config.base}...{branch}\", \"--\", \".\"],\n 728:             worktree,\n 729:             check=False,\n 730:         )\n 731:         if result.returncode == 1:\n 732:             return\n 733:         if result.returncode == 0:\n 734:             raise FlowError(\n 735:                 f\"{branch} has no file changes compared with {self.config.base}. \"\n 736:                 \"The workflow cannot integrate a no-op implementation.\"\n 737:             )\n 738:         raise FlowError(format_command_failure(result))\n 739: \n 740:     def non_handoff_status(self, worktree: Path) -> list[str]:\n 741:         result = self.runner.run(\n 742:             [\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"],\n 743:             worktree,\n 744:         )\n 745:         return [\n 746:             line\n 747:             for line in result.stdout.splitlines()\n 748:             if line.strip() and not self.status_line_is_handoff(line)\n 749:         ]\n 750: \n 751:     def status_line_is_handoff(self, line: str) -> bool:\n 752:         path_text = line[3:].strip()\n 753:         paths = [part.strip() for part in path_text.split(\" -> \")]\n 754:         return all(self.path_is_handoff(path) for path in paths if path)\n 755: \n 756:     def path_is_handoff(self, path: str) -> bool:\n 757:         normalized = path.replace(\"\\\\\", \"/\").strip('\"')\n 758:         handoff = self.handoff_dir.as_posix().rstrip(\"/\")\n 759:         return normalized == handoff or normalized.startswith(f\"{handoff}/\")\n 760: \n 761:     def require_clean_except_handoff(self, worktree: Path, phase_name: str) -> None:\n 762:         status = self.non_handoff_status(worktree)\n 763:         if not status:\n 764:             return\n 765:         raise FlowError(\n 766:             f\"{phase_name} left pending non-handoff changes in {worktree}:\\n\"\n 767:             + \"\\n\".join(status)\n 768:         )\n 769: \n 770:     def require_implementation_invariants(self, worktree: Path, branch: str) -> None:\n 771:         self.require_commits_since_base(worktree, branch, \"Implementation\")\n 772:         self.require_branch_changed_since_base(worktree, branch)\n 773:         self.require_clean_except_handoff(worktree, \"Implementation\")\n 774: \n 775:     def require_audit_invariants(\n 776:         self, worktree: Path, branch: str, head_before: str\n 777:     ) -> None:\n 778:         self.require_clean_except_handoff(worktree, \"Audit\")\n 779:         head_after = self.head_rev(worktree)\n 780:         if head_after != head_before:\n 781:             self.require_branch_changed_since_base(worktree, branch)\n 782: \n 783:     def require_ready_for_integration(self, worktree: Path, branch: str) -> None:\n 784:         self.require_no_tracked_handoff_artifacts(worktree, branch)\n 785:         self.require_clean_except_handoff(worktree, \"Pre-integration\")\n 786:         self.require_branch_changed_since_base(worktree, branch)\n 787: \n 788:     def git_common_dir(self, worktree: Path) -> Path | None:\n 789:         result = self.runner.run(\n 790:             [\"git\", \"rev-parse\", \"--git-common-dir\"],\n\ncodex\nOne thing Iâ€™m checking explicitly is repo guidance about exported copies. The plan targeted `.harness`; Iâ€™m verifying whether the `.opencode` loadout script directory exists too, because AGENTS.md requires keeping that copy in sync when `.codex/scripts` changes.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Get-ChildItem -Force loadouts/worktrees' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"git diff --check -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md' ':(exclude).codex/handoff/**'\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Compare-Object (Get-Content .codex/scripts/worktree-flow.py) (Get-Content loadouts/worktrees/.harness/scripts/worktree-flow.py) | Select-Object -First 5' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Test-Path loadouts/worktrees/.opencode/scripts; if (Test-Path loadouts/worktrees/.opencode/scripts) { Get-ChildItem loadouts/worktrees/.opencode/scripts }' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 1045ms:\n\n    Directory: C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-pla\nn-2\\loadouts\\worktrees\n\n\u001b[32;1mMode   \u001b[0m\u001b[32;1m              LastWriteTime\u001b[0m \u001b[32;1;3m        Length\u001b[0m\u001b[32;1m Name\u001b[0m\n\u001b[32;1m----   \u001b[0m \u001b[32;1m             -------------\u001b[0m \u001b[32;1m        ------\u001b[0m \u001b[32;1m----\u001b[0m\nd----            6/4/2026 12:40 PM                \u001b[44;1m.harness\u001b[0m\n-a---            6/4/2026 12:40 PM            420 AGENTS.md\n\n\n succeeded in 1315ms:\n succeeded in 1781ms:\n succeeded in 2166ms:\nFalse\n\ncodex\nThe `.opencode` script directory is absent, so `.harness` is the only exported loadout copy to synchronize in this worktree. The active/loadout shared script comparison and whitespace check are clean so far; Iâ€™m running the focused test modules next.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'python -m unittest tests.test_codex_worktree_flow' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'python -m unittest tests.test_worktrees_loadout_sync' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 1376ms:\n.\n----------------------------------------------------------------------\nRan 1 test in 0.003s\n\nOK\n\n succeeded in 2852ms:\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpo3o994l2) codex exec --help\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmphq2fx3la) codex exec --help\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpzejf29_3) codex exec --help\n+ mkdir -p C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpnp7aabta\\repo\\.codex\\worktree-flow\\plan-run\\feature\n+ copy C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpnp7aabta\\repo-feature\\.codex\\handoff\\implementation-summary.md C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpnp7aabta\\repo\\.codex\\worktree-flow\\plan-run\\feature\\implementation-summary.md\n+ mkdir -p C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpxyx7r88w\\repo-feature\\docs\\plans\n+ copy C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpxyx7r88w\\external.md C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpxyx7r88w\\repo-feature\\docs\\plans\\external.md\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpvh87xo9c\\repo-integration) git add -A -- . :(exclude).codex/handoff/**\nFeature branch: feature/plan\nFeature worktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpdydc62ew\\repo-plan\nHandoff archive: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpdydc62ew\\repo\\.codex\\worktree-flow\\plan-run\\feature\nStopped before merge by request.\nPlan: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpdydc62ew\\repo-plan\\docs\\plans\\plan.md\nWorktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpdydc62ew\\repo-plan\nBranch: feature/plan\nFeature branch: feature/plan\nFeature worktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpcxvd28cb\\repo-plan\nHandoff archive: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpcxvd28cb\\repo\\.codex\\worktree-flow\\plan-run\\feature\nStopped before merge by request.\nPlan: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpcxvd28cb\\repo-plan\\docs\\plans\\plan.md\nWorktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpcxvd28cb\\repo-plan\nBranch: feature/plan\n..............................usage: python.exe -m unittest [-h] --plan PLAN [--repo REPO] [--base BASE]\n                              [--model MODEL] [--harness HARNESS]\n                              [--harness-dir HARNESS_DIR]\n                              [--merge-mode {squash,no-ff,stop}]\n                              [--keep-worktrees] [--dry-run]\npython.exe -m unittest: error: unrecognized arguments: --yes\n.......................\n----------------------------------------------------------------------\nRan 53 tests in 1.707s\n\nOK\n\ncodex\nThe focused tests pass individually. I havenâ€™t found a confirmed code issue yet, so Iâ€™m doing one more verification pass: combined test execution plus status/diff checks, including the required skill usage ledger change that this audit created.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git diff -- .codex/skill-usage.json' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git log --oneline --decorate --max-count=5' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git status --short' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 1275ms:\ndiff --git a/.codex/skill-usage.json b/.codex/skill-usage.json\nindex 9c5723b..a56d269 100644\n--- a/.codex/skill-usage.json\n+++ b/.codex/skill-usage.json\n@@ -4,6 +4,24 @@\n       \"archive_dir\": \"C:/Users/joesa/Documents/law-school-python/harness-loadouts-worktree-flow-features-1-3-plan-2/.codex/skills.archive\",\n       \"scope\": \"repo\",\n       \"skills\": {\n+        \"audit-worktree\": {\n+          \"archived_at\": null,\n+          \"first_seen\": \"2026-06-04T19:52:36Z\",\n+          \"last_load_index\": 2,\n+          \"last_seen\": \"2026-06-04T19:52:36Z\",\n+          \"load_count\": 1,\n+          \"pinned\": false,\n+          \"source_path\": \"C:/Users/joesa/Documents/law-school-python/harness-loadouts-worktree-flow-features-1-3-plan-2/.codex/skills/audit-worktree\"\n+        },\n+        \"code-reviewer\": {\n+          \"archived_at\": null,\n+          \"first_seen\": \"2026-06-04T19:53:24Z\",\n+          \"last_load_index\": 4,\n+          \"last_seen\": \"2026-06-04T19:53:24Z\",\n+          \"load_count\": 1,\n+          \"pinned\": false,\n+          \"source_path\": \"C:/Users/joesa/Documents/law-school-python/harness-loadouts-worktree-flow-features-1-3-plan-2/.codex/skills/code-reviewer\"\n+        },\n         \"implement-worktree\": {\n           \"archived_at\": null,\n           \"first_seen\": \"2026-06-04T19:41:25Z\",\n@@ -12,10 +30,19 @@\n           \"load_count\": 1,\n           \"pinned\": false,\n           \"source_path\": \"C:/Users/joesa/Documents/law-school-python/harness-loadouts-worktree-flow-features-1-3-plan-2/.codex/skills/implement-worktree\"\n+        },\n+        \"python-pro\": {\n+          \"archived_at\": null,\n+          \"first_seen\": \"2026-06-04T19:53:23Z\",\n+          \"last_load_index\": 3,\n+          \"last_seen\": \"2026-06-04T19:53:23Z\",\n+          \"load_count\": 1,\n+          \"pinned\": false,\n+          \"source_path\": \"C:/Users/joesa/Documents/law-school-python/harness-loadouts-worktree-flow-features-1-3-plan-2/.codex/skills/python-pro\"\n         }\n       },\n       \"skills_dir\": \"C:/Users/joesa/Documents/law-school-python/harness-loadouts-worktree-flow-features-1-3-plan-2/.codex/skills\",\n-      \"total_loads\": 1\n+      \"total_loads\": 4\n     },\n     \"repo:C:/Users/joesa/Documents/law-school-python/harness-loadouts/.codex/skills\": {\n       \"archive_dir\": \"C:/Users/joesa/Documents/law-school-python/harness-loadouts/.codex/skills.archive\",\n\n succeeded in 1612ms:\ncb2f747 (HEAD -> feature/worktree-flow-features-1-3-plan-2) Implement plan\nb170398 (main, feature/worktree-flow-features-1-3-plan) remove docs\n912892d Fix worktree skill usage consolidation\nd8bec9c add refactor skill\nf3109ca codex docs for add omp skill\n\n succeeded in 1641ms:\n M .codex/skill-usage.json\n?? .codex/handoff/\n\n succeeded in 2948ms:\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmptovvhkk1) codex exec --help\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmp5v11hyte) codex exec --help\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmpen0cy7q7) codex exec --help\n+ mkdir -p C:\\Users\\joesa\\AppData\\Local\\Temp\\tmp62ltsz_l\\repo\\.codex\\worktree-flow\\plan-run\\feature\n+ copy C:\\Users\\joesa\\AppData\\Local\\Temp\\tmp62ltsz_l\\repo-feature\\.codex\\handoff\\implementation-summary.md C:\\Users\\joesa\\AppData\\Local\\Temp\\tmp62ltsz_l\\repo\\.codex\\worktree-flow\\plan-run\\feature\\implementation-summary.md\n+ mkdir -p C:\\Users\\joesa\\AppData\\Local\\Temp\\tmp7a5xsdi4\\repo-feature\\docs\\plans\n+ copy C:\\Users\\joesa\\AppData\\Local\\Temp\\tmp7a5xsdi4\\external.md C:\\Users\\joesa\\AppData\\Local\\Temp\\tmp7a5xsdi4\\repo-feature\\docs\\plans\\external.md\n+ (C:\\Users\\joesa\\AppData\\Local\\Temp\\tmprkd3tis_\\repo-integration) git add -A -- . :(exclude).codex/handoff/**\nFeature branch: feature/plan\nFeature worktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmped8tc69u\\repo-plan\nHandoff archive: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmped8tc69u\\repo\\.codex\\worktree-flow\\plan-run\\feature\nStopped before merge by request.\nPlan: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmped8tc69u\\repo-plan\\docs\\plans\\plan.md\nWorktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmped8tc69u\\repo-plan\nBranch: feature/plan\nFeature branch: feature/plan\nFeature worktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmptgn43f3p\\repo-plan\nHandoff archive: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmptgn43f3p\\repo\\.codex\\worktree-flow\\plan-run\\feature\nStopped before merge by request.\nPlan: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmptgn43f3p\\repo-plan\\docs\\plans\\plan.md\nWorktree: C:\\Users\\joesa\\AppData\\Local\\Temp\\tmptgn43f3p\\repo-plan\nBranch: feature/plan\n..............................usage: python.exe -m unittest [-h] --plan PLAN [--repo REPO] [--base BASE]\n                              [--model MODEL] [--harness HARNESS]\n                              [--harness-dir HARNESS_DIR]\n                              [--merge-mode {squash,no-ff,stop}]\n                              [--keep-worktrees] [--dry-run]\npython.exe -m unittest: error: unrecognized arguments: --yes\n........................\n----------------------------------------------------------------------\nRan 54 tests in 1.789s\n\nOK\n\ncodex\nThe combined focused tests pass as well. I found no confirmed implementation bug to fix; the only tracked change from this audit is the required skill usage ledger entry, and Iâ€™m writing the untracked audit summary now before committing that ledger update only.\napply patch\npatch: completed\nC:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\\.codex\\handoff\\audit-summary.md\ndiff --git a/.codex/handoff/audit-summary.md b/.codex/handoff/audit-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..239422f40219aac1454dff81397a99159249584a\n--- /dev/null\n+++ b/.codex/handoff/audit-summary.md\n@@ -0,0 +1,39 @@\n+# Audit Summary\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Base: `main` (`b1703981b0d7ee4d65a7ec97e0e6c6e7b5761d80`)\n+\n+## Prior Implementation Intent\n+- Collapsed duplicated Codex and OMP worktree-flow scripts into shared `worktree-flow.py` with compatibility wrappers.\n+- Added active/loadout drift protection for flow scripts and `skill-usage-manager.py`.\n+- Added implementation, audit, and pre-integration invariants to prevent uncommitted non-handoff work from being silently dropped while allowing a clean no-op audit.\n+\n+## Skills Loaded\n+- `audit-worktree`: required audit workflow.\n+- `python-pro`: Python orchestration and test review.\n+- `code-reviewer`: broad diff and regression review.\n+\n+## Issues Found\n+- No confirmed code issues found.\n+\n+## Fixes Applied\n+- No implementation fixes were needed.\n+- Recorded required audit skill usage in `.codex/skill-usage.json`.\n+\n+## Verification\n+- `git fetch --all --prune`\n+- `git diff --stat main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --name-only main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --check -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md' ':(exclude).codex/handoff/**'`\n+- `python -m unittest tests.test_codex_worktree_flow` passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync` passed, 1 test.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync` passed, 54 tests.\n+\n+## Files Changed By Audit\n+- `.codex/skill-usage.json`\n+- `.codex/handoff/audit-summary.md` (workflow artifact, left untracked)\n+\n+## Residual Risks\n+- Full repository test suite was not run; the changed surface is covered by the focused worktree-flow tests from the plan.\n\ndiff --git a/.codex/handoff/audit-summary.md b/.codex/handoff/audit-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..239422f40219aac1454dff81397a99159249584a\n--- /dev/null\n+++ b/.codex/handoff/audit-summary.md\n@@ -0,0 +1,39 @@\n+# Audit Summary\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Base: `main` (`b1703981b0d7ee4d65a7ec97e0e6c6e7b5761d80`)\n+\n+## Prior Implementation Intent\n+- Collapsed duplicated Codex and OMP worktree-flow scripts into shared `worktree-flow.py` with compatibility wrappers.\n+- Added active/loadout drift protection for flow scripts and `skill-usage-manager.py`.\n+- Added implementation, audit, and pre-integration invariants to prevent uncommitted non-handoff work from being silently dropped while allowing a clean no-op audit.\n+\n+## Skills Loaded\n+- `audit-worktree`: required audit workflow.\n+- `python-pro`: Python orchestration and test review.\n+- `code-reviewer`: broad diff and regression review.\n+\n+## Issues Found\n+- No confirmed code issues found.\n+\n+## Fixes Applied\n+- No implementation fixes were needed.\n+- Recorded required audit skill usage in `.codex/skill-usage.json`.\n+\n+## Verification\n+- `git fetch --all --prune`\n+- `git diff --stat main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --name-only main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --check -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md' ':(exclude).codex/handoff/**'`\n+- `python -m unittest tests.test_codex_worktree_flow` passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync` passed, 1 test.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync` passed, 54 tests.\n+\n+## Files Changed By Audit\n+- `.codex/skill-usage.json`\n+- `.codex/handoff/audit-summary.md` (workflow artifact, left untracked)\n+\n+## Residual Risks\n+- Full repository test suite was not run; the changed surface is covered by the focused worktree-flow tests from the plan.\n\ncodex\nThe audit summary is written under `.codex/handoff/` and will remain untracked. Iâ€™m committing only the required `.codex/skill-usage.json` update so the worktree is clean outside handoff artifacts.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git status --short' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git diff --name-only' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 571ms:\n M .codex/skill-usage.json\n?? .codex/handoff/\n\n succeeded in 936ms:\n.codex/skill-usage.json\n\ndiff --git a/.codex/handoff/audit-summary.md b/.codex/handoff/audit-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..239422f40219aac1454dff81397a99159249584a\n--- /dev/null\n+++ b/.codex/handoff/audit-summary.md\n@@ -0,0 +1,39 @@\n+# Audit Summary\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Base: `main` (`b1703981b0d7ee4d65a7ec97e0e6c6e7b5761d80`)\n+\n+## Prior Implementation Intent\n+- Collapsed duplicated Codex and OMP worktree-flow scripts into shared `worktree-flow.py` with compatibility wrappers.\n+- Added active/loadout drift protection for flow scripts and `skill-usage-manager.py`.\n+- Added implementation, audit, and pre-integration invariants to prevent uncommitted non-handoff work from being silently dropped while allowing a clean no-op audit.\n+\n+## Skills Loaded\n+- `audit-worktree`: required audit workflow.\n+- `python-pro`: Python orchestration and test review.\n+- `code-reviewer`: broad diff and regression review.\n+\n+## Issues Found\n+- No confirmed code issues found.\n+\n+## Fixes Applied\n+- No implementation fixes were needed.\n+- Recorded required audit skill usage in `.codex/skill-usage.json`.\n+\n+## Verification\n+- `git fetch --all --prune`\n+- `git diff --stat main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --name-only main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --check -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md' ':(exclude).codex/handoff/**'`\n+- `python -m unittest tests.test_codex_worktree_flow` passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync` passed, 1 test.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync` passed, 54 tests.\n+\n+## Files Changed By Audit\n+- `.codex/skill-usage.json`\n+- `.codex/handoff/audit-summary.md` (workflow artifact, left untracked)\n+\n+## Residual Risks\n+- Full repository test suite was not run; the changed surface is covered by the focused worktree-flow tests from the plan.\n\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git add .codex/skill-usage.json; git commit -m \"Record audit skill usage\"' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 1074ms:\n[feature/worktree-flow-features-1-3-plan-2 ab38df4] Record audit skill usage\n 1 file changed, 28 insertions(+), 1 deletion(-)\n\ndiff --git a/.codex/handoff/audit-summary.md b/.codex/handoff/audit-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..239422f40219aac1454dff81397a99159249584a\n--- /dev/null\n+++ b/.codex/handoff/audit-summary.md\n@@ -0,0 +1,39 @@\n+# Audit Summary\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Base: `main` (`b1703981b0d7ee4d65a7ec97e0e6c6e7b5761d80`)\n+\n+## Prior Implementation Intent\n+- Collapsed duplicated Codex and OMP worktree-flow scripts into shared `worktree-flow.py` with compatibility wrappers.\n+- Added active/loadout drift protection for flow scripts and `skill-usage-manager.py`.\n+- Added implementation, audit, and pre-integration invariants to prevent uncommitted non-handoff work from being silently dropped while allowing a clean no-op audit.\n+\n+## Skills Loaded\n+- `audit-worktree`: required audit workflow.\n+- `python-pro`: Python orchestration and test review.\n+- `code-reviewer`: broad diff and regression review.\n+\n+## Issues Found\n+- No confirmed code issues found.\n+\n+## Fixes Applied\n+- No implementation fixes were needed.\n+- Recorded required audit skill usage in `.codex/skill-usage.json`.\n+\n+## Verification\n+- `git fetch --all --prune`\n+- `git diff --stat main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --name-only main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --check -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md' ':(exclude).codex/handoff/**'`\n+- `python -m unittest tests.test_codex_worktree_flow` passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync` passed, 1 test.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync` passed, 54 tests.\n+\n+## Files Changed By Audit\n+- `.codex/skill-usage.json`\n+- `.codex/handoff/audit-summary.md` (workflow artifact, left untracked)\n+\n+## Residual Risks\n+- Full repository test suite was not run; the changed surface is covered by the focused worktree-flow tests from the plan.\n\ncodex\nThe skill usage commit is created. Iâ€™m doing the final status check now to confirm only handoff artifacts remain untracked and no tracked changes are pending.\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git status --short' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'git rev-parse HEAD' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command 'Test-Path .codex/handoff/audit-summary.md; Get-Content .codex/handoff/audit-summary.md' in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\nexec\n\"C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe\" -Command \"git diff --name-only main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'\" in C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2\n succeeded in 574ms:\n?? .codex/handoff/\n\n succeeded in 919ms:\nab38df448b8ccab843440a8afbdb346b7fbe5e8c\n\n succeeded in 1273ms:\nTrue\n# Audit Summary\n\n## Worktree\n- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n- Branch: `feature/worktree-flow-features-1-3-plan-2`\n- Base: `main` (`b1703981b0d7ee4d65a7ec97e0e6c6e7b5761d80`)\n\n## Prior Implementation Intent\n- Collapsed duplicated Codex and OMP worktree-flow scripts into shared `worktree-flow.py` with compatibility wrappers.\n- Added active/loadout drift protection for flow scripts and `skill-usage-manager.py`.\n- Added implementation, audit, and pre-integration invariants to prevent uncommitted non-handoff work from being silently dropped while allowing a clean no-op audit.\n\n## Skills Loaded\n- `audit-worktree`: required audit workflow.\n- `python-pro`: Python orchestration and test review.\n- `code-reviewer`: broad diff and regression review.\n\n## Issues Found\n- No confirmed code issues found.\n\n## Fixes Applied\n- No implementation fixes were needed.\n- Recorded required audit skill usage in `.codex/skill-usage.json`.\n\n## Verification\n- `git fetch --all --prune`\n- `git diff --stat main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n- `git diff --name-only main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n- `git diff --check -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md' ':(exclude).codex/handoff/**'`\n- `python -m unittest tests.test_codex_worktree_flow` passed, 53 tests.\n- `python -m unittest tests.test_worktrees_loadout_sync` passed, 1 test.\n- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync` passed, 54 tests.\n\n## Files Changed By Audit\n- `.codex/skill-usage.json`\n- `.codex/handoff/audit-summary.md` (workflow artifact, left untracked)\n\n## Residual Risks\n- Full repository test suite was not run; the changed surface is covered by the focused worktree-flow tests from the plan.\n\n succeeded in 1283ms:\n.codex/scripts/worktree-flow-codex.py\n.codex/scripts/worktree-flow-omp.py\n.codex/scripts/worktree-flow.py\n.codex/skill-usage.json\nloadouts/worktrees/.harness/docs/codex-worktree-flow.md\nloadouts/worktrees/.harness/scripts/worktree-flow-codex.py\nloadouts/worktrees/.harness/scripts/worktree-flow-omp.py\nloadouts/worktrees/.harness/scripts/worktree-flow.py\ntests/test_codex_worktree_flow.py\ntests/test_worktrees_loadout_sync.py\n\ndiff --git a/.codex/handoff/audit-summary.md b/.codex/handoff/audit-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..239422f40219aac1454dff81397a99159249584a\n--- /dev/null\n+++ b/.codex/handoff/audit-summary.md\n@@ -0,0 +1,39 @@\n+# Audit Summary\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Base: `main` (`b1703981b0d7ee4d65a7ec97e0e6c6e7b5761d80`)\n+\n+## Prior Implementation Intent\n+- Collapsed duplicated Codex and OMP worktree-flow scripts into shared `worktree-flow.py` with compatibility wrappers.\n+- Added active/loadout drift protection for flow scripts and `skill-usage-manager.py`.\n+- Added implementation, audit, and pre-integration invariants to prevent uncommitted non-handoff work from being silently dropped while allowing a clean no-op audit.\n+\n+## Skills Loaded\n+- `audit-worktree`: required audit workflow.\n+- `python-pro`: Python orchestration and test review.\n+- `code-reviewer`: broad diff and regression review.\n+\n+## Issues Found\n+- No confirmed code issues found.\n+\n+## Fixes Applied\n+- No implementation fixes were needed.\n+- Recorded required audit skill usage in `.codex/skill-usage.json`.\n+\n+## Verification\n+- `git fetch --all --prune`\n+- `git diff --stat main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --name-only main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --check -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md' ':(exclude).codex/handoff/**'`\n+- `python -m unittest tests.test_codex_worktree_flow` passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync` passed, 1 test.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync` passed, 54 tests.\n+\n+## Files Changed By Audit\n+- `.codex/skill-usage.json`\n+- `.codex/handoff/audit-summary.md` (workflow artifact, left untracked)\n+\n+## Residual Risks\n+- Full repository test suite was not run; the changed surface is covered by the focused worktree-flow tests from the plan.\n\ncodex\nAudit complete.\n\n**Worktree**\n`C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n\n**Branch/Base**\n`feature/worktree-flow-features-1-3-plan-2` against `main` at `b1703981b0d7ee4d65a7ec97e0e6c6e7b5761d80`\n\n**Prior Summary**\nThe implementation collapsed duplicated Codex/OMP worktree flow scripts into shared `worktree-flow.py`, added loadout drift protection, and enforced implementation/audit/pre-integration clean-worktree invariants.\n\n**Skills Loaded**\n`audit-worktree`, `python-pro`, `code-reviewer`\n\n**Findings**\nNo confirmed code issues found. No implementation fixes were needed.\n\n**Audit Changes**\nCommitted required skill usage logging only:\n`ab38df448b8ccab843440a8afbdb346b7fbe5e8c` (`Record audit skill usage`)\n\nWrote `.codex/handoff/audit-summary.md` and left it untracked as requested. Final status shows only `.codex/handoff/` untracked.\n\n**Verification**\nPassed:\n`python -m unittest tests.test_codex_worktree_flow`  \n`python -m unittest tests.test_worktrees_loadout_sync`  \n`python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync`  \n`git diff --check -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md' ':(exclude).codex/handoff/**'`\n\nResidual risk: full repo test suite was not run; focused worktree-flow tests cover the changed surface.\ndiff --git a/.codex/handoff/audit-summary.md b/.codex/handoff/audit-summary.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..239422f40219aac1454dff81397a99159249584a\n--- /dev/null\n+++ b/.codex/handoff/audit-summary.md\n@@ -0,0 +1,39 @@\n+# Audit Summary\n+\n+## Worktree\n+- Path: `C:\\Users\\joesa\\Documents\\law-school-python\\harness-loadouts-worktree-flow-features-1-3-plan-2`\n+- Branch: `feature/worktree-flow-features-1-3-plan-2`\n+- Base: `main` (`b1703981b0d7ee4d65a7ec97e0e6c6e7b5761d80`)\n+\n+## Prior Implementation Intent\n+- Collapsed duplicated Codex and OMP worktree-flow scripts into shared `worktree-flow.py` with compatibility wrappers.\n+- Added active/loadout drift protection for flow scripts and `skill-usage-manager.py`.\n+- Added implementation, audit, and pre-integration invariants to prevent uncommitted non-handoff work from being silently dropped while allowing a clean no-op audit.\n+\n+## Skills Loaded\n+- `audit-worktree`: required audit workflow.\n+- `python-pro`: Python orchestration and test review.\n+- `code-reviewer`: broad diff and regression review.\n+\n+## Issues Found\n+- No confirmed code issues found.\n+\n+## Fixes Applied\n+- No implementation fixes were needed.\n+- Recorded required audit skill usage in `.codex/skill-usage.json`.\n+\n+## Verification\n+- `git fetch --all --prune`\n+- `git diff --stat main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --name-only main...HEAD -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md'`\n+- `git diff --check -- . ':(exclude)scratchpad.md' ':(exclude)docs/scratchpad.md' ':(exclude).codex/handoff/**'`\n+- `python -m unittest tests.test_codex_worktree_flow` passed, 53 tests.\n+- `python -m unittest tests.test_worktrees_loadout_sync` passed, 1 test.\n+- `python -m unittest tests.test_codex_worktree_flow tests.test_worktrees_loadout_sync` passed, 54 tests.\n+\n+## Files Changed By Audit\n+- `.codex/skill-usage.json`\n+- `.codex/handoff/audit-summary.md` (workflow artifact, left untracked)\n+\n+## Residual Risks\n+- Full repository test suite was not run; the changed surface is covered by the focused worktree-flow tests from the plan.\n\ntokens used\n82,524\n"}
